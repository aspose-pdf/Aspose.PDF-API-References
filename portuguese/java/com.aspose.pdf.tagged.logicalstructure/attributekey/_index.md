---
title: "AttributeKey"
linktitle: "AttributeKey"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa chaves de atributos padrão."
type: docs
weight: 10
url: /pt/java/com.aspose.pdf.tagged.logicalstructure/attributekey/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.logicalstructure.AttributeKey

```
public final class AttributeKey extends Object
```

Representa chaves de atributos padrão.

## Campos

| Campo | Descrição |
| --- | --- |
| [BackgroundColor](#BackgroundColor) | Atributo BackgroundColor (proprietário do atributo Layout). |
| [BaselineShift](#BaselineShift) | Atributo BaselineShift (proprietário do atributo Layout). |
| [BBox](#BBox) | Atributo BBox (proprietário do atributo Layout). |
| [BlockAlign](#BlockAlign) | Atributo BlockAlign (proprietário do atributo Layout). |
| [BorderColor](#BorderColor) | Atributo BorderColor (proprietário do atributo Layout). |
| [BorderStyle](#BorderStyle) | Atributo BorderStyle (proprietário do atributo Layout). |
| [BorderThickness](#BorderThickness) | Atributo BorderThickness (proprietário do atributo Layout). |
| [Checked](#Checked) | Atributo Checked (proprietário do atributo PrintField). |
| [Color](#Color) | Atributo Color (proprietário do atributo Layout). |
| [ColSpan](#ColSpan) | Atributo ColSpan (proprietário do atributo Table). |
| [ColumnCount](#ColumnCount) | Atributo ColumnCount (proprietário do atributo Layout). |
| [ColumnGap](#ColumnGap) | Atributo ColumnGap (proprietário do atributo Layout). |
| [ColumnWidths](#ColumnWidths) | Atributo ColumnWidths (proprietário do atributo Layout). |
| [Desc](#Desc) | Atributo Desc (proprietário do atributo PrintField). |
| [EndIndent](#EndIndent) | Atributo EndIndent (proprietário do atributo Layout). |
| [GlyphOrientationVertical](#GlyphOrientationVertical) | Atributo GlyphOrientationVertical (proprietário do atributo Layout). |
| [Headers](#Headers) | Atributo Headers (proprietário do atributo Table). |
| [Height](#Height) | Atributo Height (proprietário do atributo Layout). |
| [InlineAlign](#InlineAlign) | Atributo InlineAlign (proprietário do atributo Layout). |
| [LineHeight](#LineHeight) | Atributo LineHeight (proprietário do atributo Layout). |
| [ListNumbering](#ListNumbering) | Atributo ListNumbering (proprietário do atributo List). |
| [Padding](#Padding) | Atributo Padding (proprietário do atributo Layout). |
| [Placement](#Placement) | Atributo Placement (proprietário do atributo Layout). |
| [Role](#Role) | Atributo Role (proprietário do atributo PrintField). |
| [RowSpan](#RowSpan) | Atributo RowSpan (proprietário do atributo Table). |
| [RubyAlign](#RubyAlign) | Atributo RubyAlign (proprietário do atributo Layout). |
| [RubyPosition](#RubyPosition) | Atributo RubyPosition (proprietário do atributo Layout). |
| [Scope](#Scope) | Atributo Scope (proprietário do atributo Table). |
| [SpaceAfter](#SpaceAfter) | Atributo SpaceAfter (proprietário do atributo Layout). |
| [SpaceBefore](#SpaceBefore) | Atributo SpaceBefore (proprietário do atributo Layout). |
| [StartIndent](#StartIndent) | Atributo StartIndent (proprietário do atributo Layout). |
| [Summary](#Summary) | Atributo Summary (proprietário do atributo Table). |
| [TBorderStyle](#TBorderStyle) | Atributo TBorderStyle (proprietário do atributo Layout). |
| [TextAlign](#TextAlign) | Atributo TextAlign (proprietário do atributo Layout). |
| [TextDecorationColor](#TextDecorationColor) | Atributo TextDecorationColor (proprietário do atributo Layout). |
| [TextDecorationThickness](#TextDecorationThickness) | Atributo TextDecorationThickness (proprietário do atributo Layout). |
| [TextDecorationType](#TextDecorationType) | Atributo TextDecorationType (proprietário do atributo Layout). |
| [TextIndent](#TextIndent) | Atributo TextIndent (proprietário do atributo Layout). |
| [TPadding](#TPadding) | Atributo TPadding (proprietário do atributo Layout). |
| [Width](#Width) | Atributo Width (proprietário do atributo Layout). |
| [WritingMode](#WritingMode) | Atributo WritingMode (proprietário do atributo Layout). |

## Métodos

| Método | Descrição |
| --- | --- |
| [getKey](#getKey--) | Obter chave de atributo. Valor: chave de atributo. |
| [getOwner](#getOwner--) | Obter Proprietário do Atributo. Valor: Proprietário do Atributo. |
| [hashCode](#hashCode--) |  |
| [to_AttributeKey](#to_AttributeKey-java.lang.String-) | Executa uma conversão explícita de {@link String} para {@link AttributeKey}. |
| [toString](#toString--) | Retorna uma string que representa o objeto atual. |

### BackgroundColor {#BackgroundColor}
```
public static final AttributeKey BackgroundColor
```

Atributo BackgroundColor (proprietário do atributo Layout).

### BaselineShift {#BaselineShift}
```
public static final AttributeKey BaselineShift
```

Atributo BaselineShift (proprietário do atributo Layout).

### BBox {#BBox}
```
public static final AttributeKey BBox
```

Atributo BBox (proprietário do atributo Layout).

### BlockAlign {#BlockAlign}
```
public static final AttributeKey BlockAlign
```

Atributo BlockAlign (proprietário do atributo Layout).

### BorderColor {#BorderColor}
```
public static final AttributeKey BorderColor
```

Atributo BorderColor (proprietário do atributo Layout).

### BorderStyle {#BorderStyle}
```
public static final AttributeKey BorderStyle
```

Atributo BorderStyle (proprietário do atributo Layout).

### BorderThickness {#BorderThickness}
```
public static final AttributeKey BorderThickness
```

Atributo BorderThickness (proprietário do atributo Layout).

### Checked {#Checked}
```
public static final AttributeKey Checked
```

Atributo Checked (proprietário do atributo PrintField).

### Color {#Color}
```
public static final AttributeKey Color
```

Atributo Color (proprietário do atributo Layout).

### ColSpan {#ColSpan}
```
public static final AttributeKey ColSpan
```

Atributo ColSpan (proprietário do atributo Table).

### ColumnCount {#ColumnCount}
```
public static final AttributeKey ColumnCount
```

Atributo ColumnCount (proprietário do atributo Layout).

### ColumnGap {#ColumnGap}
```
public static final AttributeKey ColumnGap
```

Atributo ColumnGap (proprietário do atributo Layout).

### ColumnWidths {#ColumnWidths}
```
public static final AttributeKey ColumnWidths
```

Atributo ColumnWidths (proprietário do atributo Layout).

### Desc {#Desc}
```
public static final AttributeKey Desc
```

Atributo Desc (proprietário do atributo PrintField).

### EndIndent {#EndIndent}
```
public static final AttributeKey EndIndent
```

Atributo EndIndent (proprietário do atributo Layout).

### GlyphOrientationVertical {#GlyphOrientationVertical}
```
public static final AttributeKey GlyphOrientationVertical
```

Atributo GlyphOrientationVertical (proprietário do atributo Layout).

### Headers {#Headers}
```
public static final AttributeKey Headers
```

Atributo Headers (proprietário do atributo Table).

### Height {#Height}
```
public static final AttributeKey Height
```

Atributo Height (proprietário do atributo Layout).

### InlineAlign {#InlineAlign}
```
public static final AttributeKey InlineAlign
```

Atributo InlineAlign (proprietário do atributo Layout).

### LineHeight {#LineHeight}
```
public static final AttributeKey LineHeight
```

Atributo LineHeight (proprietário do atributo Layout).

### ListNumbering {#ListNumbering}
```
public static final AttributeKey ListNumbering
```

Atributo ListNumbering (proprietário do atributo List).

### Padding {#Padding}
```
public static final AttributeKey Padding
```

Atributo Padding (proprietário do atributo Layout).

### Placement {#Placement}
```
public static final AttributeKey Placement
```

Atributo Placement (proprietário do atributo Layout).

### Role {#Role}
```
public static final AttributeKey Role
```

Atributo Role (proprietário do atributo PrintField).

### RowSpan {#RowSpan}
```
public static final AttributeKey RowSpan
```

Atributo RowSpan (proprietário do atributo Table).

### RubyAlign {#RubyAlign}
```
public static final AttributeKey RubyAlign
```

Atributo RubyAlign (proprietário do atributo Layout).

### RubyPosition {#RubyPosition}
```
public static final AttributeKey RubyPosition
```

Atributo RubyPosition (proprietário do atributo Layout).

### Scope {#Scope}
```
public static final AttributeKey Scope
```

Atributo Scope (proprietário do atributo Table).

### SpaceAfter {#SpaceAfter}
```
public static final AttributeKey SpaceAfter
```

Atributo SpaceAfter (proprietário do atributo Layout).

### SpaceBefore {#SpaceBefore}
```
public static final AttributeKey SpaceBefore
```

Atributo SpaceBefore (proprietário do atributo Layout).

### StartIndent {#StartIndent}
```
public static final AttributeKey StartIndent
```

Atributo StartIndent (proprietário do atributo Layout).

### Summary {#Summary}
```
public static final AttributeKey Summary
```

Atributo Summary (proprietário do atributo Table).

### TBorderStyle {#TBorderStyle}
```
public static final AttributeKey TBorderStyle
```

Atributo TBorderStyle (proprietário do atributo Layout).

### TextAlign {#TextAlign}
```
public static final AttributeKey TextAlign
```

Atributo TextAlign (proprietário do atributo Layout).

### TextDecorationColor {#TextDecorationColor}
```
public static final AttributeKey TextDecorationColor
```

Atributo TextDecorationColor (proprietário do atributo Layout).

### TextDecorationThickness {#TextDecorationThickness}
```
public static final AttributeKey TextDecorationThickness
```

Atributo TextDecorationThickness (proprietário do atributo Layout).

### TextDecorationType {#TextDecorationType}
```
public static final AttributeKey TextDecorationType
```

Atributo TextDecorationType (proprietário do atributo Layout).

### TextIndent {#TextIndent}
```
public static final AttributeKey TextIndent
```

Atributo TextIndent (proprietário do atributo Layout).

### TPadding {#TPadding}
```
public static final AttributeKey TPadding
```

Atributo TPadding (proprietário do atributo Layout).

### Width {#Width}
```
public static final AttributeKey Width
```

Atributo Width (proprietário do atributo Layout).

### WritingMode {#WritingMode}
```
public static final AttributeKey WritingMode
```

Atributo WritingMode (proprietário do atributo Layout).

### getKey {#getKey--}
```
public final String getKey()
```

Obter chave de atributo. Valor: chave de atributo.

**Returns:**
valor String

### getOwner {#getOwner--}
```
public final AttributeOwnerStandard getOwner()
```

Obter Proprietário do Atributo. Valor: Proprietário do Atributo.

**Returns:**
Instância de AttributeKey

### hashCode {#hashCode--}
```
public int hashCode()
```



### to_AttributeKey {#to_AttributeKey-java.lang.String-}
Executa uma conversão explícita de {@link String} para {@link AttributeKey}.

### toString {#toString--}
```
public String toString()
```

Retorna uma string que representa o objeto atual.

**Returns:**
String que representa o objeto atual.
