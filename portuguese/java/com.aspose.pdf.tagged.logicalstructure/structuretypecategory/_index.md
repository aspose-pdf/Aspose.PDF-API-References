---
title: "StructureTypeCategory"
linktitle: "StructureTypeCategory"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa categorias de tipos de estrutura padrão."
type: docs
weight: 120
url: /pt/java/com.aspose.pdf.tagged.logicalstructure/structuretypecategory/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.logicalstructure.StructureTypeCategory

```
public final class StructureTypeCategory extends Object
```

Representa categorias de tipos de estrutura padrão.

## Campos

| Campo | Descrição |
| --- | --- |
| [BLSEs](#BLSEs) | Elementos de estrutura em nível de bloco (BLSEs) descrevem o layout geral do conteúdo na página, avançando na direção de progressão de bloco. |
| [GroupingElements](#GroupingElements) | Elementos de agrupamento agrupam outros elementos em sequências ou hierarquias, mas não contêm conteúdo diretamente e não têm efeito direto no layout. |
| [IllustrationElements](#IllustrationElements) | Elementos de ilustração são sequências compactas de conteúdo, na ordem de conteúdo da página, que são considerados objetos unitários em relação ao layout da página. Uma ilustração pode ser tratada como um BLSE ou um ILSE. |
| [ILSEs](#ILSEs) | Elementos de estrutura em nível inline (ILSEs) descrevem o layout do conteúdo dentro de um BLSE, avançando na direção de progressão inline. |

## Métodos

| Método | Descrição |
| --- | --- |
| [to_StructureTypeCategory](#to_StructureTypeCategory-java.lang.String-) | Realiza uma conversão explícita de {@link String} para {@link StructureTypeCategory}. |
| [toString](#toString--) | Retorna uma string que representa o objeto atual. |

### BLSEs {#BLSEs}
```
public static final StructureTypeCategory BLSEs
```

Elementos de estrutura em nível de bloco (BLSEs) descrevem o layout geral do conteúdo na página, avançando na direção de progressão de bloco.

### GroupingElements {#GroupingElements}
```
public static final StructureTypeCategory GroupingElements
```

Elementos de agrupamento agrupam outros elementos em sequências ou hierarquias, mas não contêm conteúdo diretamente e não têm efeito direto no layout.

### IllustrationElements {#IllustrationElements}
```
public static final StructureTypeCategory IllustrationElements
```

Elementos de ilustração são sequências compactas de conteúdo, na ordem de conteúdo da página, que são considerados objetos unitários em relação ao layout da página. Uma ilustração pode ser tratada como um BLSE ou um ILSE.

### ILSEs {#ILSEs}
```
public static final StructureTypeCategory ILSEs
```

Elementos de estrutura em nível inline (ILSEs) descrevem o layout do conteúdo dentro de um BLSE, avançando na direção de progressão inline.

### to_StructureTypeCategory {#to_StructureTypeCategory-java.lang.String-}
Realiza uma conversão explícita de {@link String} para {@link StructureTypeCategory}.

### toString {#toString--}
```
public String toString()
```

Retorna uma string que representa o objeto atual.

**Returns:**
String que representa o objeto atual.
