---
title: "BlendingColorSpace"
linktitle: "BlendingColorSpace"
second_title: "Referência da API Aspose.PDF para Java"
description: "Classe que representa o espaço de cor de mesclagem."
type: docs
weight: 50
url: /pt/java/com.aspose.pdf.facades/blendingcolorspace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < BlendingColorSpace > com.aspose.pdf.facades.BlendingColorSpace, java.lang.Enum < BlendingColorSpace >, com.aspose.pdf.facades.BlendingColorSpace

**All Implemented Interfaces:**
Serializable, Comparable < BlendingColorSpace >

```
public enum BlendingColorSpace extends Enum < BlendingColorSpace >
```

Classe que representa o espaço de cor de mesclagem.

## Campos

| Campo | Descrição |
| --- | --- |
| [Auto](#Auto) | O espaço de cores de mesclagem é determinado automaticamente. |
| [DeviceCMYK](#DeviceCMYK) | O espaço de cores de mesclagem está definido como DeviceCMYK. |
| [DeviceRGB](#DeviceRGB) | O espaço de cores de mesclagem está definido como DeviceRGB. |
| [DontChange](#DontChange) | O espaço de cores de mesclagem não foi alterado. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Auto {#Auto}
```
public static final BlendingColorSpace Auto
```

O espaço de cores de mesclagem é determinado automaticamente.

### DeviceCMYK {#DeviceCMYK}
```
public static final BlendingColorSpace DeviceCMYK
```

O espaço de cores de mesclagem está definido como DeviceCMYK.

### DeviceRGB {#DeviceRGB}
```
public static final BlendingColorSpace DeviceRGB
```

O espaço de cores de mesclagem está definido como DeviceRGB.

### DontChange {#DontChange}
```
public static final BlendingColorSpace DontChange
```

O espaço de cores de mesclagem não foi alterado.

### getByValue {#getByValue-int-}
```
public static BlendingColorSpace getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static BlendingColorSpace [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
