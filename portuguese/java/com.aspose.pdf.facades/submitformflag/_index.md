---
title: "SubmitFormFlag"
linktitle: "SubmitFormFlag"
second_title: "Referência da API Aspose.PDF para Java"
description: "Enumeração das possíveis bandeiras de envio de formulário."
type: docs
weight: 730
url: /pt/java/com.aspose.pdf.facades/submitformflag/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < SubmitFormFlag > com.aspose.pdf.facades.SubmitFormFlag, java.lang.Enum < SubmitFormFlag >, com.aspose.pdf.facades.SubmitFormFlag

**All Implemented Interfaces:**
Serializable, Comparable < SubmitFormFlag >

```
public enum SubmitFormFlag extends Enum < SubmitFormFlag >
```

Enumeração das possíveis bandeiras de envio de formulário.

## Campos

| Campo | Descrição |
| --- | --- |
| [Fdf](#Fdf) | Os dados serão retornados no formato FDF. |
| [FdfWithComments](#FdfWithComments) | Os dados serão retornados no formato FDF com comentários. |
| [Html](#Html) | Os dados serão retornados no formato HTML. |
| [Pdf](#Pdf) | Todo o arquivo PDF será enviado. |
| [Xfdf](#Xfdf) | Os dados serão retornados no formato XFDF. |
| [XfdfWithComments](#XfdfWithComments) | Os dados serão retornados no formato XFDF com comentários. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Fdf {#Fdf}
```
public static final SubmitFormFlag Fdf
```

Os dados serão retornados no formato FDF.

### FdfWithComments {#FdfWithComments}
```
public static final SubmitFormFlag FdfWithComments
```

Os dados serão retornados no formato FDF com comentários.

### Html {#Html}
```
public static final SubmitFormFlag Html
```

Os dados serão retornados no formato HTML.

### Pdf {#Pdf}
```
public static final SubmitFormFlag Pdf
```

Todo o arquivo PDF será enviado.

### Xfdf {#Xfdf}
```
public static final SubmitFormFlag Xfdf
```

Os dados serão retornados no formato XFDF.

### XfdfWithComments {#XfdfWithComments}
```
public static final SubmitFormFlag XfdfWithComments
```

Os dados serão retornados no formato XFDF com comentários.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static SubmitFormFlag [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
