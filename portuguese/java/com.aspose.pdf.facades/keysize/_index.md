---
title: "KeySize"
linktitle: "KeySize"
second_title: "Referência da API Aspose.PDF para Java"
description: "Define diferentes tamanhos de chave que podem ser usados para criptografar documentos PDF."
type: docs
weight: 340
url: /pt/java/com.aspose.pdf.facades/keysize/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < KeySize > com.aspose.pdf.facades.KeySize, java.lang.Enum < KeySize >, com.aspose.pdf.facades.KeySize

**All Implemented Interfaces:**
Serializable, Comparable < KeySize >

```
public enum KeySize extends Enum < KeySize >
```

Define diferentes tamanhos de chave que podem ser usados para criptografar documentos PDF.

## Campos

| Campo | Descrição |
| --- | --- |
| [x128](#x128) | Chave de 128 bits. |
| [x256](#x256) | Chave de 256 bits. |
| [x40](#x40) | Chave de 40 bits. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### x128 {#x128}
```
public static final KeySize x128
```

Chave de 128 bits.

### x256 {#x256}
```
public static final KeySize x256
```

Chave de 256 bits.

### x40 {#x40}
```
public static final KeySize x40
```

Chave de 40 bits.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static KeySize [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
