---
title: "ImageMergeMode"
linktitle: "ImageMergeMode"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa modos de mesclagem de imagens."
type: docs
weight: 270
url: /pt/java/com.aspose.pdf.facades/imagemergemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ImageMergeMode > com.aspose.pdf.facades.ImageMergeMode, java.lang.Enum < ImageMergeMode >, com.aspose.pdf.facades.ImageMergeMode

**All Implemented Interfaces:**
Serializable, Comparable < ImageMergeMode >

```
public enum ImageMergeMode extends Enum < ImageMergeMode >
```

Representa modos de mesclagem de imagens.

## Campos

| Campo | Descrição |
| --- | --- |
| [Center](#Center) | Imagens alinhadas ao centro. |
| [Horizontal](#Horizontal) | Imagens mescladas horizontalmente. |
| [Vertical](#Vertical) | Imagens mescladas verticalmente. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Center {#Center}
```
public static final ImageMergeMode Center
```

Imagens alinhadas ao centro.

### Horizontal {#Horizontal}
```
public static final ImageMergeMode Horizontal
```

Imagens mescladas horizontalmente.

### Vertical {#Vertical}
```
public static final ImageMergeMode Vertical
```

Imagens mescladas verticalmente.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static ImageMergeMode [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
