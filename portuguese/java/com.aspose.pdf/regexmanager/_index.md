---
title: "RegexManager"
linktitle: "RegexManager"
second_title: "Referência da API Aspose.PDF para Java"
description: "Fornece um wrapper para operações de expressão regular com configurações de tempo limite configuráveis."
type: docs
weight: 4130
url: /pt/java/com.aspose.pdf/regexmanager/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.RegexManager

```
public class RegexManager extends Object
```

Fornece um wrapper para operações de expressão regular com configurações de tempo limite configuráveis.

## Construtores

| Construtor | Descrição |
| --- | --- |
| [RegexManager](#RegexManager--) |  |

## Métodos

| Método | Descrição |
| --- | --- |
| [getMatchTimeout](#getMatchTimeout--) | Obtém ou define o tempo limite para operações de Regex em toda a biblioteca. O valor padrão é 1000 ms. Valor: Um {@link double} que representa a duração padrão do tempo limite. |
| [setMatchTimeout](#setMatchTimeout-int-) | Obtém ou define o tempo limite para operações de Regex em toda a biblioteca. O valor padrão é 1000 ms. Valor: |

### RegexManager {#RegexManager--}
```
public RegexManager()
```



### getMatchTimeout {#getMatchTimeout--}
```
public static int getMatchTimeout()
```

Obtém ou define o tempo limite para operações de Regex em toda a biblioteca. O valor padrão é 1000 ms. Valor: Um {@link double} que representa a duração padrão do tempo limite.

**Returns:**
valor int

### setMatchTimeout {#setMatchTimeout-int-}
```
public static void setMatchTimeout(int value)
```

Obtém ou define o tempo limite para operações de Regex em toda a biblioteca. O valor padrão é 1000 ms. Valor:

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | Um representando a duração padrão do tempo limite em milissegundos. |
