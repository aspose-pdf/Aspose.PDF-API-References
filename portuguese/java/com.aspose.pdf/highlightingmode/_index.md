---
title: "HighlightingMode"
linktitle: "HighlightingMode"
second_title: "Referência da API Aspose.PDF para Java"
description: "Enumera o modo de realce da anotação, o efeito visual a ser usado quando o botão do mouse é pressionado ou mantido dentro de sua área ativa."
type: docs
weight: 1920
url: /pt/java/com.aspose.pdf/highlightingmode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < HighlightingMode > com.aspose.pdf.HighlightingMode, java.lang.Enum < HighlightingMode >, com.aspose.pdf.HighlightingMode

**All Implemented Interfaces:**
Serializable, Comparable < HighlightingMode >

```
public enum HighlightingMode extends Enum < HighlightingMode >
```

Enumera o modo de realce da anotação, o efeito visual a ser usado quando o botão do mouse é pressionado ou mantido dentro de sua área ativa.

## Campos

| Campo | Descrição |
| --- | --- |
| [Invert](#Invert) | Inverter o conteúdo do retângulo da anotação. |
| [None](#None) | Sem realce. |
| [Outline](#Outline) | Inverter a borda da anotação. |
| [Push](#Push) | Exibir a aparência inferior da anotação, se houver. |
| [Toggle](#Toggle) | Mesmo que Push (que é preferido). |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Invert {#Invert}
```
public static final HighlightingMode Invert
```

Inverter o conteúdo do retângulo da anotação.

### None {#None}
```
public static final HighlightingMode None
```

Sem realce.

### Outline {#Outline}
```
public static final HighlightingMode Outline
```

Inverter a borda da anotação.

### Push {#Push}
```
public static final HighlightingMode Push
```

Exibir a aparência inferior da anotação, se houver.

### Toggle {#Toggle}
```
public static final HighlightingMode Toggle
```

Mesmo que Push (que é preferido).

### getByValue {#getByValue-int-}
```
public static HighlightingMode getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static HighlightingMode [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
