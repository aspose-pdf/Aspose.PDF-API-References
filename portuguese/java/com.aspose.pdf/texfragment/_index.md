---
title: "TeXFragment"
linktitle: "TeXFragment"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa fragmento LaTeX."
type: docs
weight: 4860
url: /pt/java/com.aspose.pdf/texfragment/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.BaseParagraph com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.BaseParagraph, com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.FormattedFragment, com.aspose.pdf.TeXFragment

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public class TeXFragment extends FormattedFragment
```

Representa fragmento LaTeX.

## Construtores

| Construtor | Descrição |
| --- | --- |
| [TeXFragment](#TeXFragment-java.lang.String-) | Inicializa uma nova instância da classe HtmlFragment. |
| [TeXFragment](#TeXFragment-java.lang.String-boolean-) | Inicializa uma nova instância da classe HtmlFragment. |

## Métodos

| Método | Descrição |
| --- | --- |
| [deepClone](#deepClone--) | Clona fragmento. |
| [getLatexLoadOptionsOfInstance](#getLatexLoadOptionsOfInstance--) | Obtém ou define TeXLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX. |
| [getTeXLoadOptionsOfInstance](#getTeXLoadOptionsOfInstance--) | Obtém ou define TeXLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX. |
| [setLatexLoadOptionsOfInstance](#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | Obtém ou define TeXLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX. |
| [setTeXLoadOptionsOfInstance](#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | Obtém ou define LatexLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX. |

### TeXFragment {#TeXFragment-java.lang.String-}
Inicializa uma nova instância da classe HtmlFragment.

### TeXFragment {#TeXFragment-java.lang.String-boolean-}
Inicializa uma nova instância da classe HtmlFragment.

### deepClone {#deepClone--}
```
public Object deepClone()
```

Clona fragmento.

**Returns:**
Fragmento clonado.

### getLatexLoadOptionsOfInstance {#getLatexLoadOptionsOfInstance--}
```
@Deprecated public final TeXLoadOptions getLatexLoadOptionsOfInstance()
```

Obtém ou define TeXLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX.

**Returns:**
Instância de TeXLoadOptions @deprecated Por favor, use TeXLoadOptionsOfInstance em vez disso.

### getTeXLoadOptionsOfInstance {#getTeXLoadOptionsOfInstance--}
```
public TeXLoadOptions getTeXLoadOptionsOfInstance()
```

Obtém ou define TeXLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX.

**Returns:**
Instância de TeXLoadOptions

### setLatexLoadOptionsOfInstance {#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
Obtém ou define TeXLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX.

### setTeXLoadOptionsOfInstance {#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
Obtém ou define LatexLoadOptions que serão usados para carregar (e renderizar) o LaTeX nesta instância da classe. Use-o quando for necessário usar uma configuração específica para importação do LaTeX para esta ou aquela instância (por exemplo, quando esta ou aquela instância deve usar um BasePath específico para o LaTeX importado ou deve usar um carregador específico de recursos externos). Se o parâmetro for padrão (null), então serão usadas as opções padrão de carregamento do LaTeX.
