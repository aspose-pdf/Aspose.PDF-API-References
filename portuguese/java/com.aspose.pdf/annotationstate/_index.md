---
title: "AnnotationState"
linktitle: "AnnotationState"
second_title: "Referência da API Aspose.PDF para Java"
description: "A enumeração dos estados para os quais a anotação original pode ser definida."
type: docs
weight: 110
url: /pt/java/com.aspose.pdf/annotationstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationState > com.aspose.pdf.AnnotationState, java.lang.Enum < AnnotationState >, com.aspose.pdf.AnnotationState

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationState >

```
public enum AnnotationState extends Enum < AnnotationState >
```

A enumeração dos estados para os quais a anotação original pode ser definida.

## Campos

| Campo | Descrição |
| --- | --- |
| [Accepted](#Accepted) | O usuário concorda com a alteração. |
| [Cancelled](#Cancelled) | A alteração foi cancelada. |
| [Completed](#Completed) | A alteração foi concluída. |
| [Marked](#Marked) | A anotação foi marcada pelo usuário. |
| [None](#None) | O usuário não indicou nada sobre a alteração. |
| [Rejected](#Rejected) | O usuário discorda da alteração. |
| [Undefined](#Undefined) | Estado não definido. |
| [Unmarked](#Unmarked) | A anotação não foi marcada pelo usuário. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Accepted {#Accepted}
```
public static final AnnotationState Accepted
```

O usuário concorda com a alteração.

### Cancelled {#Cancelled}
```
public static final AnnotationState Cancelled
```

A alteração foi cancelada.

### Completed {#Completed}
```
public static final AnnotationState Completed
```

A alteração foi concluída.

### Marked {#Marked}
```
public static final AnnotationState Marked
```

A anotação foi marcada pelo usuário.

### None {#None}
```
public static final AnnotationState None
```

O usuário não indicou nada sobre a alteração.

### Rejected {#Rejected}
```
public static final AnnotationState Rejected
```

O usuário discorda da alteração.

### Undefined {#Undefined}
```
public static final AnnotationState Undefined
```

Estado não definido.

### Unmarked {#Unmarked}
```
public static final AnnotationState Unmarked
```

A anotação não foi marcada pelo usuário.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static AnnotationState [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
