---
title: "FreeTextIntent"
linktitle: "FreeTextIntent"
second_title: "Referência da API Aspose.PDF para Java"
description: "Enumera as intenções da anotação de texto livre."
type: docs
weight: 1800
url: /pt/java/com.aspose.pdf/freetextintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < FreeTextIntent > com.aspose.pdf.FreeTextIntent, java.lang.Enum < FreeTextIntent >, com.aspose.pdf.FreeTextIntent

**All Implemented Interfaces:**
Serializable, Comparable < FreeTextIntent >

```
public enum FreeTextIntent extends Enum < FreeTextIntent >
```

Enumera as intenções da anotação de texto livre.

## Campos

| Campo | Descrição |
| --- | --- |
| [FreeTextCallout](#FreeTextCallout) | Significa que a anotação tem a intenção de funcionar como um balão explicativo. |
| [FreeTextTypeWriter](#FreeTextTypeWriter) | Significa que a anotação tem a intenção de funcionar como um objeto clique-para-digitar ou de máquina de escrever. |
| [Undefined](#Undefined) | Estado não definido. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### FreeTextCallout {#FreeTextCallout}
```
public static final FreeTextIntent FreeTextCallout
```

Significa que a anotação tem a intenção de funcionar como um balão explicativo.

### FreeTextTypeWriter {#FreeTextTypeWriter}
```
public static final FreeTextIntent FreeTextTypeWriter
```

Significa que a anotação tem a intenção de funcionar como um objeto clique-para-digitar ou de máquina de escrever.

### Undefined {#Undefined}
```
public static final FreeTextIntent Undefined
```

Estado não definido.

### getByValue {#getByValue-int-}
```
public static FreeTextIntent getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static FreeTextIntent [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
