---
title: "ImagePlacementCollection"
linktitle: "ImagePlacementCollection"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa uma coleção de posicionamentos de imagem"
type: docs
weight: 2350
url: /pt/java/com.aspose.pdf/imageplacementcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ImagePlacementCollection

**All Implemented Interfaces:**
Iterable < ImagePlacement >

```
public final class ImagePlacementCollection extends Object implements Iterable < ImagePlacement >
```

Representa uma coleção de posicionamentos de imagem

## Métodos

| Método | Descrição |
| --- | --- |
| [add](#add-com.aspose.pdf.ImagePlacement-) | Adiciona o elemento fragmento de texto no índice especificado. |
| [clear](#clear--) | Limpa todos os itens da coleção. |
| [contains](#contains-com.aspose.pdf.ImagePlacement-) | Determina se a coleção contém um valor específico. |
| [copyTo](#copyTo-com.aspose.pdf.ImagePlacement:A-int-) | Copia toda a coleção para um Array unidimensional compatível, começando no índice especificado do array de destino. |
| [get_Item](#get_Item-int-) | Obtém o elemento de fragmento de texto no índice especificado. |
| [getSyncRoot](#getSyncRoot--) | Obtém um objeto que pode ser usado para sincronizar o acesso à coleção. |
| [isReadOnly](#isReadOnly--) | Obtém um valor que indica se a coleção é somente leitura. |
| [isSynchronized](#isSynchronized--) | Obtém um valor que indica se o acesso à coleção está sincronizado (thread safe). |
| [iterator](#iterator--) | Retorna um enumerador para toda a coleção. |
| [remove](#remove-com.aspose.pdf.ImagePlacement-) | Exclui o item especificado da coleção |
| [size](#size--) | Obtém o número de elementos de objeto {@code ImagePlacement} realmente contidos na coleção. |

### add {#add-com.aspose.pdf.ImagePlacement-}
Adiciona o elemento fragmento de texto no índice especificado.

### clear {#clear--}
```
public void clear()
```

Limpa todos os itens da coleção.

### contains {#contains-com.aspose.pdf.ImagePlacement-}
Determina se a coleção contém um valor específico.

### copyTo {#copyTo-com.aspose.pdf.ImagePlacement:A-int-}
Copia toda a coleção para um Array unidimensional compatível, começando no índice especificado do array de destino.

### get_Item {#get_Item-int-}
```
public ImagePlacement get_Item(int index)
```

Obtém o elemento de fragmento de texto no índice especificado.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| index |  | Índice da colocação de imagem. |

**Returns:**
Objeto ImagePlacement.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Obtém um objeto que pode ser usado para sincronizar o acesso à coleção.

**Returns:**
Objeto para sincronização

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Obtém um valor que indica se a coleção é somente leitura.

**Returns:**
valor booleano

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Obtém um valor que indica se o acesso à coleção está sincronizado (thread safe).

**Returns:**
valor booleano

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator< ImagePlacement > iterator()
```

Retorna um enumerador para toda a coleção.

**Returns:**
Objeto enumerador.

### remove {#remove-com.aspose.pdf.ImagePlacement-}
Exclui o item especificado da coleção

### size {#size--}
```
public int size()
```

Obtém o número de elementos de objeto {@code ImagePlacement} realmente contidos na coleção.

**Returns:**
valor int
