---
title: "PageLayout"
linktitle: "PageLayout"
second_title: "Referência da API Aspose.PDF para Java"
description: "Descreve o layout da página."
type: docs
weight: 3410
url: /pt/java/com.aspose.pdf/pagelayout/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PageLayout > com.aspose.pdf.PageLayout, java.lang.Enum < PageLayout >, com.aspose.pdf.PageLayout

**All Implemented Interfaces:**
Serializable, Comparable < PageLayout >

```
public enum PageLayout extends Enum < PageLayout >
```

Descreve o layout da página.

## Campos

| Campo | Descrição |
| --- | --- |
| [Default](#Default) | Layout padrão. |
| [OneColumn](#OneColumn) | Exibir as páginas em uma coluna. |
| [SinglePage](#SinglePage) | Página única. |
| [TwoColumnLeft](#TwoColumnLeft) | Exibir as páginas em duas colunas, com as páginas ímpares à esquerda. |
| [TwoColumnRight](#TwoColumnRight) | Exibir as páginas em duas colunas, com as páginas ímpares à direita. |
| [TwoPageLeft](#TwoPageLeft) | Exibir duas páginas por vez, com as páginas ímpares à esquerda. |
| [TwoPageRight](#TwoPageRight) | Exibir duas páginas por vez, com as páginas ímpares à direita. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Default {#Default}
```
public static final PageLayout Default
```

Layout padrão.

### OneColumn {#OneColumn}
```
public static final PageLayout OneColumn
```

Exibir as páginas em uma coluna.

### SinglePage {#SinglePage}
```
public static final PageLayout SinglePage
```

Página única.

### TwoColumnLeft {#TwoColumnLeft}
```
public static final PageLayout TwoColumnLeft
```

Exibir as páginas em duas colunas, com as páginas ímpares à esquerda.

### TwoColumnRight {#TwoColumnRight}
```
public static final PageLayout TwoColumnRight
```

Exibir as páginas em duas colunas, com as páginas ímpares à direita.

### TwoPageLeft {#TwoPageLeft}
```
public static final PageLayout TwoPageLeft
```

Exibir duas páginas por vez, com as páginas ímpares à esquerda.

### TwoPageRight {#TwoPageRight}
```
public static final PageLayout TwoPageRight
```

Exibir duas páginas por vez, com as páginas ímpares à direita.

### getByValue {#getByValue-int-}
```
public static PageLayout getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static PageLayout [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
