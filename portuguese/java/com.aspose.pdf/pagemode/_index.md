---
title: "PageMode"
linktitle: "PageMode"
second_title: "Referência da API Aspose.PDF para Java"
description: "A classe descreve os componentes usados da página do documento."
type: docs
weight: 3430
url: /pt/java/com.aspose.pdf/pagemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PageMode > com.aspose.pdf.PageMode, java.lang.Enum < PageMode >, com.aspose.pdf.PageMode

**All Implemented Interfaces:**
Serializável, Comparável < PageMode >

```
public enum PageMode extends Enum < PageMode >
```

A classe descreve os componentes usados da página do documento.

## Campos

| Campo | Descrição |
| --- | --- |
| [FullScreen](#FullScreen) | Modo de tela cheia, sem barra de menu, controles de janela ou qualquer outra janela visível. |
| [UseAttachments](#UseAttachments) | Painel de anexos visível. |
| [UseNone](#UseNone) | Não use nenhum componente. |
| [UseOC](#UseOC) | Painel de grupo de conteúdo opcional visível. |
| [UseOutlines](#UseOutlines) | Esboço do documento visível. |
| [UseThumbs](#UseThumbs) | Miniaturas visíveis. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### FullScreen {#FullScreen}
```
public static final PageMode FullScreen
```

Modo de tela cheia, sem barra de menu, controles de janela ou qualquer outra janela visível.

### UseAttachments {#UseAttachments}
```
public static final PageMode UseAttachments
```

Painel de anexos visível.

### UseNone {#UseNone}
```
public static final PageMode UseNone
```

Não use nenhum componente.

### UseOC {#UseOC}
```
public static final PageMode UseOC
```

Painel de grupo de conteúdo opcional visível.

### UseOutlines {#UseOutlines}
```
public static final PageMode UseOutlines
```

Esboço do documento visível.

### UseThumbs {#UseThumbs}
```
public static final PageMode UseThumbs
```

Miniaturas visíveis.

### getByValue {#getByValue-int-}
```
public static PageMode getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static PageMode [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
