---
title: "TextFragmentCollection"
linktitle: "TextFragmentCollection"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa uma coleção de fragmentos de texto"
type: docs
weight: 5130
url: /pt/java/com.aspose.pdf/textfragmentcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextFragmentCollection

**All Implemented Interfaces:**
Iterable < TextFragment >

```
public final class TextFragmentCollection extends Object implements Iterable < TextFragment >
```

Representa uma coleção de fragmentos de texto

## Métodos

| Método | Descrição |
| --- | --- |
| [add](#add-com.aspose.pdf.TextFragment-) | Adiciona o elemento fragmento de texto no índice especificado. |
| [clear](#clear--) | Limpa todos os itens da coleção. |
| [contains](#contains-com.aspose.pdf.TextFragment-) | Determina se a coleção contém um valor específico. |
| [copyTo](#copyTo-com.aspose.pdf.TextFragment:A-int-) | / * / * Retorna um enumerador para toda a coleção. / * / * |
| [get_Item](#get_Item-int-) | Obtém o elemento fragmento de texto no índice especificado. O índice deve estar no intervalo [1..n] onde n equivale ao número de fragmentos de texto. |
| [getSyncRoot](#getSyncRoot--) | Obtém um objeto que pode ser usado para sincronizar o acesso à coleção. |
| [isReadOnly](#isReadOnly--) | Obtém um valor que indica se a coleção é somente leitura |
| [isSynchronized](#isSynchronized--) | Obtém um valor que indica se o acesso à coleção está sincronizado (thread safe). |
| [iterator](#iterator--) | Retorna um enumerador para toda a coleção. |
| [remove](#remove-com.aspose.pdf.TextFragment-) | Exclui o item especificado da coleção. |
| [size](#size--) | Obtém o número de elementos de objeto {@code TextFragment} realmente contidos na coleção. |

### add {#add-com.aspose.pdf.TextFragment-}
Adiciona o elemento fragmento de texto no índice especificado.

### clear {#clear--}
```
public void clear()
```

Limpa todos os itens da coleção.

### contains {#contains-com.aspose.pdf.TextFragment-}
Determina se a coleção contém um valor específico.

### copyTo {#copyTo-com.aspose.pdf.TextFragment:A-int-}
/ * / * Retorna um enumerador para toda a coleção. / * / *

### get_Item {#get_Item-int-}
```
public TextFragment get_Item(int index)
```

Obtém o elemento fragmento de texto no índice especificado. O índice deve estar no intervalo [1..n] onde n equivale ao número de fragmentos de texto.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| index |  | Índice dentro da coleção. |

**Returns:**
Objeto TextFragment.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Obtém um objeto que pode ser usado para sincronizar o acesso à coleção.

**Returns:**
Elemento Object

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Obtém um valor que indica se a coleção é somente leitura

**Returns:**
valor booleano

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Obtém um valor que indica se o acesso à coleção está sincronizado (thread safe).

**Returns:**
valor booleano

### iterator {#iterator--}
```
public Iterator < TextFragment > iterator()
```

Retorna um enumerador para toda a coleção.

**Returns:**
Objeto enumerador.

### remove {#remove-com.aspose.pdf.TextFragment-}
Exclui o item especificado da coleção.

### size {#size--}
```
public int size()
```

Obtém o número de elementos de objeto {@code TextFragment} realmente contidos na coleção.

**Returns:**
valor int
