---
title: "LineIntent"
linktitle: "LineIntent"
second_title: "Referência da API Aspose.PDF para Java"
description: "Enumera as intenções da anotação de linha."
type: docs
weight: 2750
url: /pt/java/com.aspose.pdf/lineintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LineIntent > com.aspose.pdf.LineIntent, java.lang.Enum < LineIntent >, com.aspose.pdf.LineIntent

**All Implemented Interfaces:**
Serializable, Comparable < LineIntent >

```
public enum LineIntent extends Enum < LineIntent >
```

Enumera as intenções da anotação de linha.

## Campos

| Campo | Descrição |
| --- | --- |
| [LineArrow](#LineArrow) | Significa que a anotação tem a intenção de funcionar como uma seta. |
| [LineDimension](#LineDimension) | Significa que a anotação tem a intenção de funcionar como uma linha de dimensão. |
| [Undefined](#Undefined) | Estado indefinido. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### LineArrow {#LineArrow}
```
public static final LineIntent LineArrow
```

Significa que a anotação tem a intenção de funcionar como uma seta.

### LineDimension {#LineDimension}
```
public static final LineIntent LineDimension
```

Significa que a anotação tem a intenção de funcionar como uma linha de dimensão.

### Undefined {#Undefined}
```
public static final LineIntent Undefined
```

Estado indefinido.

### getByValue {#getByValue-int-}
```
public static LineIntent getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static LineIntent [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
