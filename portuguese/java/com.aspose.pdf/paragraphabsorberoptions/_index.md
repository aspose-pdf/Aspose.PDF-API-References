---
title: "ParagraphAbsorberOptions"
linktitle: "ParagraphAbsorberOptions"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa opções para o {@link ParagraphAbsorber}."
type: docs
weight: 3480
url: /pt/java/com.aspose.pdf/paragraphabsorberoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ParagraphAbsorberOptions

```
public final class ParagraphAbsorberOptions extends Object
```

Representa opções para o {@link ParagraphAbsorber}.

## Construtores

| Construtor | Descrição |
| --- | --- |
| [ParagraphAbsorberOptions](#ParagraphAbsorberOptions--) | Inicializa uma nova instância do objeto {@link ParagraphAbsorberOptions}. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getSearchRectangle](#getSearchRectangle--) | Obtém ou define o retângulo de pesquisa de parágrafo. |
| [getSectionUnbreakingHorizontalOverride](#getSectionUnbreakingHorizontalOverride--) | Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção horizontal real (como fração da largura/altura de uma página). O valor padrão é 0.005. |
| [getSectionUnbreakingVerticalOverride](#getSectionUnbreakingVerticalOverride--) | Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção vertical real (como fração da largura/altura de uma página). O valor padrão é 0.005. |
| [setSearchRectangle](#setSearchRectangle-com.aspose.pdf.Rectangle-) | Obtém ou define o retângulo de pesquisa de parágrafo. |
| [setSectionUnbreakingHorizontalOverride](#setSectionUnbreakingHorizontalOverride-double-) | Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção horizontal real (como fração da largura/altura de uma página). O valor padrão é 0.005. |
| [setSectionUnbreakingVerticalOverride](#setSectionUnbreakingVerticalOverride-double-) | Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção vertical real (como fração da largura/altura de uma página). O valor padrão é 0.005. |

### ParagraphAbsorberOptions {#ParagraphAbsorberOptions--}
```
public ParagraphAbsorberOptions()
```

Inicializa uma nova instância do objeto {@link ParagraphAbsorberOptions}.

### getSearchRectangle {#getSearchRectangle--}
```
public final Rectangle getSearchRectangle()
```

Obtém ou define o retângulo de pesquisa de parágrafo.

**Returns:**
Instância de Rectangle

### getSectionUnbreakingHorizontalOverride {#getSectionUnbreakingHorizontalOverride--}
```
public final double getSectionUnbreakingHorizontalOverride()
```

Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção horizontal real (como fração da largura/altura de uma página). O valor padrão é 0.005.

**Returns:**
valor double

### getSectionUnbreakingVerticalOverride {#getSectionUnbreakingVerticalOverride--}
```
public final double getSectionUnbreakingVerticalOverride()
```

Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção vertical real (como fração da largura/altura de uma página). O valor padrão é 0.005.

**Returns:**
valor double

### setSearchRectangle {#setSearchRectangle-com.aspose.pdf.Rectangle-}
Obtém ou define o retângulo de pesquisa de parágrafo.

### setSectionUnbreakingHorizontalOverride {#setSectionUnbreakingHorizontalOverride-double-}
```
public final void setSectionUnbreakingHorizontalOverride(double value)
```

Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção horizontal real (como fração da largura/altura de uma página). O valor padrão é 0.005.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | valor double |

### setSectionUnbreakingVerticalOverride {#setSectionUnbreakingVerticalOverride-double-}
```
public final void setSectionUnbreakingVerticalOverride(double value)
```

Obtém ou define o comprimento máximo da zona com 'nível de preenchimento zero' que não será tratado como quebra de seção vertical real (como fração da largura/altura de uma página). O valor padrão é 0.005.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | valor double |
