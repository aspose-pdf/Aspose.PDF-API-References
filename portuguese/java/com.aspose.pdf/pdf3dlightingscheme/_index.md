---
title: "PDF3DLightingScheme"
linktitle: "PDF3DLightingScheme"
second_title: "Referência da API Aspose.PDF para Java"
description: "Classe PDF3DLightingScheme."
type: docs
weight: 3620
url: /pt/java/com.aspose.pdf/pdf3dlightingscheme/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DLightingScheme

```
public class PDF3DLightingScheme extends Object
```

Classe PDF3DLightingScheme.

## Campos

| Campo | Descrição |
| --- | --- |
| [Artwork](#Artwork) | O esquema de iluminação "Artwork". |
| [Blue](#Blue) | O esquema de iluminação "Blue". |
| [CAD](#CAD) | O esquema de iluminação "Cad". |
| [Cube](#Cube) | O esquema de iluminação "Cube". |
| [Day](#Day) | O esquema de iluminação "Day". |
| [Hard](#Hard) | O esquema de iluminação "Hard". |
| [Headlamp](#Headlamp) | O esquema de iluminação "Headlamp". |
| [Night](#Night) | O esquema de iluminação "Night". |
| [None](#None) | O esquema de iluminação "None". |
| [Primary](#Primary) | O esquema de iluminação "Primary". |
| [Red](#Red) | O esquema de iluminação "Red". |
| [White](#White) | O esquema de iluminação "White". |

## Construtores

| Construtor | Descrição |
| --- | --- |
| [PDF3DLightingScheme](#PDF3DLightingScheme-com.aspose.pdf.LightingSchemeType-) | Inicializa uma nova instância da classe {@code PDF3DLightingScheme}. |
| [PDF3DLightingScheme](#PDF3DLightingScheme-java.lang.String-) | Inicializa uma nova instância da classe {@code PDF3DLightingScheme}. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getType](#getType--) | Obtém o tipo de esquema de iluminação. |

### Artwork {#Artwork}
```
public static final PDF3DLightingScheme Artwork
```

O esquema de iluminação "Artwork".

### Blue {#Blue}
```
public static final PDF3DLightingScheme Blue
```

O esquema de iluminação "Blue".

### CAD {#CAD}
```
public static final PDF3DLightingScheme CAD
```

O esquema de iluminação "Cad".

### Cube {#Cube}
```
public static final PDF3DLightingScheme Cube
```

O esquema de iluminação "Cube".

### Day {#Day}
```
public static final PDF3DLightingScheme Day
```

O esquema de iluminação "Day".

### Hard {#Hard}
```
public static final PDF3DLightingScheme Hard
```

O esquema de iluminação "Hard".

### Headlamp {#Headlamp}
```
public static final PDF3DLightingScheme Headlamp
```

O esquema de iluminação "Headlamp".

### Night {#Night}
```
public static final PDF3DLightingScheme Night
```

O esquema de iluminação "Night".

### None {#None}
```
public static final PDF3DLightingScheme None
```

O esquema de iluminação "None".

### Primary {#Primary}
```
public static final PDF3DLightingScheme Primary
```

O esquema de iluminação "Primary".

### Red {#Red}
```
public static final PDF3DLightingScheme Red
```

O esquema de iluminação "Red".

### White {#White}
```
public static final PDF3DLightingScheme White
```

O esquema de iluminação "White".

### PDF3DLightingScheme {#PDF3DLightingScheme-com.aspose.pdf.LightingSchemeType-}
Inicializa uma nova instância da classe {@code PDF3DLightingScheme}.

### PDF3DLightingScheme {#PDF3DLightingScheme-java.lang.String-}
Inicializa uma nova instância da classe {@code PDF3DLightingScheme}.

### getType {#getType--}
```
public LightingSchemeType getType()
```

Obtém o tipo de esquema de iluminação.

**Returns:**
valor int do elemento LightingSchemeType: O tipo de esquema de iluminação. @see LightingSchemeType
