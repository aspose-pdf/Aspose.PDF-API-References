---
title: "PolyIntent"
linktitle: "PolyIntent"
second_title: "Referência da API Aspose.PDF para Java"
description: "Enumera as intenções da anotação de polígono ou polilinha."
type: docs
weight: 3910
url: /pt/java/com.aspose.pdf/polyintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PolyIntent > com.aspose.pdf.PolyIntent, java.lang.Enum < PolyIntent >, com.aspose.pdf.PolyIntent

**All Implemented Interfaces:**
Serializable, Comparable < PolyIntent >

```
public enum PolyIntent extends Enum < PolyIntent >
```

Enumera as intenções da anotação de polígono ou polilinha.

## Campos

| Campo | Descrição |
| --- | --- |
| [PolygonCloud](#PolygonCloud) | Significa que a anotação tem a intenção de funcionar como um objeto de nuvem. |
| [PolygonDimension](#PolygonDimension) | Indica que a anotação de polígono tem a intenção de funcionar como uma dimensão. |
| [PolyLineDimension](#PolyLineDimension) | Indica que a anotação de polilinha destina‑se a funcionar como uma dimensão. |
| [Undefined](#Undefined) | Estado indefinido. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### PolygonCloud {#PolygonCloud}
```
public static final PolyIntent PolygonCloud
```

Significa que a anotação tem a intenção de funcionar como um objeto de nuvem.

### PolygonDimension {#PolygonDimension}
```
public static final PolyIntent PolygonDimension
```

Indica que a anotação de polígono tem a intenção de funcionar como uma dimensão.

### PolyLineDimension {#PolyLineDimension}
```
public static final PolyIntent PolyLineDimension
```

Indica que a anotação de polilinha destina‑se a funcionar como uma dimensão.

### Undefined {#Undefined}
```
public static final PolyIntent Undefined
```

Estado indefinido.

### getByValue {#getByValue-int-}
```
public static PolyIntent getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static PolyIntent [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
