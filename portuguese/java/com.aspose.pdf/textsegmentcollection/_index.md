---
title: "TextSegmentCollection"
linktitle: "TextSegmentCollection"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa uma coleção de segmentos de texto"
type: docs
weight: 5310
url: /pt/java/com.aspose.pdf/textsegmentcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextSegmentCollection

**All Implemented Interfaces:**
Iterable < TextSegment >

```
public final class TextSegmentCollection extends Object implements Iterable < TextSegment >
```

Representa uma coleção de segmentos de texto

## Métodos

| Método | Descrição |
| --- | --- |
| [add](#add-com.aspose.pdf.TextSegment-) | Adiciona o elemento de segmento de texto no índice especificado. |
| [clear](#clear--) | Limpa todos os itens da coleção. |
| [contains](#contains-com.aspose.pdf.TextSegment-) | Determina se a coleção contém um valor específico. |
| [copyTo](#copyTo-com.aspose.pdf.TextSegment:A-int-) | Copia toda a coleção para um Array unidimensional compatível, começando no índice especificado do array de destino. |
| [delete](#delete-int-) | Exclui o elemento de segmento de texto no índice especificado. |
| [get_Item](#get_Item-int-) | Obtém o elemento de segmento de texto no índice especificado. |
| [getSyncRoot](#getSyncRoot--) | Obtém um objeto que pode ser usado para sincronizar o acesso à coleção. |
| [isReadOnly](#isReadOnly--) | Obtém um valor que indica se a coleção é somente leitura |
| [isSynchronized](#isSynchronized--) | Obtém um valor que indica se o acesso à coleção está sincronizado (thread safe). |
| [iterator](#iterator--) | Retorna um enumerador para toda a coleção. |
| [remove](#remove-com.aspose.pdf.TextSegment-) | Exclui o item especificado da coleção. |
| [size](#size--) | Obtém o número de elementos de objeto {@code TextSegment} realmente contidos na coleção. |

### add {#add-com.aspose.pdf.TextSegment-}
Adiciona o elemento de segmento de texto no índice especificado.

### clear {#clear--}
```
public void clear()
```

Limpa todos os itens da coleção.

### contains {#contains-com.aspose.pdf.TextSegment-}
Determina se a coleção contém um valor específico.

### copyTo {#copyTo-com.aspose.pdf.TextSegment:A-int-}
Copia toda a coleção para um Array unidimensional compatível, começando no índice especificado do array de destino.

### delete {#delete-int-}
```
public void delete(int index)
```

Exclui o elemento de segmento de texto no índice especificado.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| index |  | valor int |

### get_Item {#get_Item-int-}
```
public TextSegment get_Item(int index)
```

Obtém o elemento de segmento de texto no índice especificado.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| index |  | Índice dentro da coleção. |

**Returns:**
Objeto TextSegment.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Obtém um objeto que pode ser usado para sincronizar o acesso à coleção.

**Returns:**
Elemento Object

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Obtém um valor que indica se a coleção é somente leitura

**Returns:**
valor booleano

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Obtém um valor que indica se o acesso à coleção está sincronizado (thread safe).

**Returns:**
valor booleano

### iterator {#iterator--}
```
public Iterator < TextSegment > iterator()
```

Retorna um enumerador para toda a coleção.

**Returns:**
Objeto enumerador.

### remove {#remove-com.aspose.pdf.TextSegment-}
Exclui o item especificado da coleção.

### size {#size--}
```
public int size()
```

Obtém o número de elementos de objeto {@code TextSegment} realmente contidos na coleção.

**Returns:**
valor int
