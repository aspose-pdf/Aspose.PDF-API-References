---
title: "TabStops"
linktitle: "TabStops"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa uma coleção de objetos {@code TabStop}."
type: docs
weight: 4850
url: /pt/java/com.aspose.pdf/tabstops/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TabStops

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public class TabStops extends Object implements com.aspose.ms.System.ICloneable
```

Representa uma coleção de objetos {@code TabStop}.

## Construtores

| Construtor | Descrição |
| --- | --- |
| [TabStops](#TabStops--) | Inicializa uma nova instância da classe {@code TabStops}. |

## Métodos

| Método | Descrição |
| --- | --- |
| [add](#add--) | Inicializa uma nova instância da classe {@code TabStop} e a adiciona à coleção TabStops. |
| [add](#add-float-) | Inicializa uma nova instância da classe {@code TabStop} com a posição especificada e a adiciona à coleção TabStops. |
| [add](#add-float-int-) | Inicializa uma nova instância da classe {@code TabStop} com a posição e o tipo de líder especificados e a adiciona à coleção TabStops. |
| [add](#add-com.aspose.pdf.TabStop-) | Inicializa uma nova instância da classe {@code TabStop} e a adiciona à coleção TabStops. |
| [deepClone](#deepClone--) | Clona novos objetos {@code TabStops}. |
| [get_Item](#get_Item-int-) | Obtém um objeto {@code TabStop} da coleção de acordo com o índice TabStop. |
| [getCount](#getCount--) | Retorna a contagem de tabStops |
| [isReadOnly](#isReadOnly--) | Obtém o valor que indica que esta instância {@code TabStops} já está anexada a {@code TextFragment} e tornou‑se somente leitura. |
| [set_Item](#set_Item-int-com.aspose.pdf.TabStop-) | Define um objeto {@code TabStop} da coleção de acordo com o índice do TabStop. |

### TabStops {#TabStops--}
```
public TabStops()
```

Inicializa uma nova instância da classe {@code TabStops}.

### add {#add--}
```
public TabStop add()
```

Inicializa uma nova instância da classe {@code TabStop} e a adiciona à coleção TabStops.

**Returns:**
O novo objeto {@code TabStop}.

### add {#add-float-}
```
public TabStop add(float position)
```

Inicializa uma nova instância da classe {@code TabStop} com a posição especificada e a adiciona à coleção TabStops.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| posição |  | A posição do tab stop. |

**Returns:**
O novo objeto {@code TabStop}.

### add {#add-float-int-}
```
public TabStop add(float position, int leaderType)
```

Inicializa uma nova instância da classe {@code TabStop} com a posição e o tipo de líder especificados e a adiciona à coleção TabStops.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| posição |  | A posição do tab stop. |
| leaderType |  | O tipo de líder do tab stop. |

**Returns:**
O novo objeto {@code TabStop}.

### add {#add-com.aspose.pdf.TabStop-}
Inicializa uma nova instância da classe {@code TabStop} e a adiciona à coleção TabStops.

**Returns:**
O novo objeto {@code TabStop}.

### deepClone {#deepClone--}
```
public Object deepClone()
```

Clona novos objetos {@code TabStops}.

**Returns:**
O novo objeto {@code TabStops}.

### get_Item {#get_Item-int-}
```
public TabStop get_Item(int index)
```

Obtém um objeto {@code TabStop} da coleção de acordo com o índice TabStop.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| index |  | Índice baseado em zero do elemento na coleção {@code TabStops}. |

**Returns:**
Objeto {@code TabStop}.

### getCount {#getCount--}
```
public int getCount()
```

Retorna a contagem de tabStops

**Returns:**
valor int

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Obtém o valor que indica que esta instância {@code TabStops} já está anexada a {@code TextFragment} e tornou‑se somente leitura.

**Returns:**
valor booleano

### set_Item {#set_Item-int-com.aspose.pdf.TabStop-}
Define um objeto {@code TabStop} da coleção de acordo com o índice do TabStop.
