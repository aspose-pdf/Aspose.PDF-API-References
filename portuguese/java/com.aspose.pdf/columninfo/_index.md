---
title: "ColumnInfo"
linktitle: "ColumnInfo"
second_title: "Referência da API Aspose.PDF para Java"
description: "Esta classe representa as informações de uma coluna."
type: docs
weight: 730
url: /pt/java/com.aspose.pdf/columninfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ColumnInfo

```
public final class ColumnInfo extends Object
```

Esta classe representa as informações de uma coluna.

## Construtores

| Construtor | Descrição |
| --- | --- |
| [ColumnInfo](#ColumnInfo--) | Inicializa uma nova instância da classe ColumnInfo. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getColumnCount](#getColumnCount--) | Obtém o valor int que indica o número de colunas. |
| [getColumnSpacing](#getColumnSpacing--) | <p> Obtém ou define uma string que contém o espaçamento entre colunas. O valor de cada espaçamento deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm e polegada também são suportados. Por exemplo,"120 2.5cm 1.5inch". </p><hr> <p> Se esta propriedade não for definida, o valor padrão 0 será usado para cada espaçamento. </p> |
| [getColumnWidths](#getColumnWidths--) | Obtém ou define uma string que contém a largura das colunas. O valor de cada coluna deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm, polegada e porcentagem da largura disponível também são suportados. Por exemplo,"120 2.5cm 1.5inch" |
| [setColumnCount](#setColumnCount-int-) | Define o valor int que indica o número de colunas. |
| [setColumnSpacing](#setColumnSpacing-java.lang.String-) | <p> Obtém ou define uma string que contém o espaçamento entre colunas. O valor de cada espaçamento deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm e polegada também são suportados. Por exemplo,"120 2.5cm 1.5inch". </p><hr> <p> Se esta propriedade não for definida, o valor padrão 0 será usado para cada espaçamento. </p> |
| [setColumnWidths](#setColumnWidths-java.lang.String-) | Obtém ou define uma string que contém a largura das colunas. O valor de cada coluna deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm, polegada e porcentagem da largura disponível também são suportados. Por exemplo,"120 2.5cm 1.5inch" |

### ColumnInfo {#ColumnInfo--}
```
public ColumnInfo()
```

Inicializa uma nova instância da classe ColumnInfo.

### getColumnCount {#getColumnCount--}
```
public int getColumnCount()
```

Obtém o valor int que indica o número de colunas.

**Returns:**
contagem de colunas

### getColumnSpacing {#getColumnSpacing--}
```
public String getColumnSpacing()
```

<p> Obtém ou define uma string que contém o espaçamento entre colunas. O valor de cada espaçamento deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm e polegada também são suportados. Por exemplo,"120 2.5cm 1.5inch". </p><hr> <p> Se esta propriedade não for definida, o valor padrão 0 será usado para cada espaçamento. </p>

**Returns:**
valor String

### getColumnWidths {#getColumnWidths--}
```
public String getColumnWidths()
```

Obtém ou define uma string que contém a largura das colunas. O valor de cada coluna deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm, polegada e porcentagem da largura disponível também são suportados. Por exemplo,"120 2.5cm 1.5inch"

**Returns:**
valor String

### setColumnCount {#setColumnCount-int-}
```
public void setColumnCount(int value)
```

Define o valor int que indica o número de colunas.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | contagem de colunas |

### setColumnSpacing {#setColumnSpacing-java.lang.String-}
<p> Obtém ou define uma string que contém o espaçamento entre colunas. O valor de cada espaçamento deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm e polegada também são suportados. Por exemplo,"120 2.5cm 1.5inch". </p><hr> <p> Se esta propriedade não for definida, o valor padrão 0 será usado para cada espaçamento. </p>

### setColumnWidths {#setColumnWidths-java.lang.String-}
Obtém ou define uma string que contém a largura das colunas. O valor de cada coluna deve ser separado por espaço em branco. A unidade padrão é ponto, mas cm, polegada e porcentagem da largura disponível também são suportados. Por exemplo,"120 2.5cm 1.5inch"
