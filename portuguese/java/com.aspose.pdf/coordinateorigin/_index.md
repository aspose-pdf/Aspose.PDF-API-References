---
title: "CoordinateOrigin"
linktitle: "CoordinateOrigin"
second_title: "Referência da API Aspose.PDF para Java"
description:
type: docs
weight: 840
url: /pt/java/com.aspose.pdf/coordinateorigin/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < CoordinateOrigin > com.aspose.pdf.CoordinateOrigin, java.lang.Enum < CoordinateOrigin >, com.aspose.pdf.CoordinateOrigin

**All Implemented Interfaces:**
Serializable, Comparable < CoordinateOrigin >

```
public enum CoordinateOrigin extends Enum < CoordinateOrigin >
```



## Campos

| Campo | Descrição |
| --- | --- |
| [BaseLine](#BaseLine) | A coordenada mais baixa do texto deve ser tratada como a linha de base. |
| [Descender](#Descender) | A coordenada mais baixa do texto deve ser tratada como a linha descendente. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### BaseLine {#BaseLine}
```
public static final CoordinateOrigin BaseLine
```

A coordenada mais baixa do texto deve ser tratada como a linha de base.

### Descender {#Descender}
```
public static final CoordinateOrigin Descender
```

A coordenada mais baixa do texto deve ser tratada como a linha descendente.

### getByValue {#getByValue-int-}
```
public static CoordinateOrigin getByValue(int value)
```



**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static CoordinateOrigin [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
