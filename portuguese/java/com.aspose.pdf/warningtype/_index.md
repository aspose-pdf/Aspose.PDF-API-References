---
title: "WarningType"
linktitle: "WarningType"
second_title: "Referência da API Aspose.PDF para Java"
description: "/* Enum representado tipo de aviso. */"
type: docs
weight: 5490
url: /pt/java/com.aspose.pdf/warningtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.Enum, com.aspose.pdf.WarningType

```
public final class WarningType extends com.aspose.ms.System.Enum
```

/* Enum representado tipo de aviso. */

## Campos

| Campo | Descrição |
| --- | --- |
| [CompatibilityIssue](#CompatibilityIssue) | Problema conhecido que impedirá a abertura do documento por determinados agentes de usuário ou versões anteriores desses agentes. |
| [DataLoss](#DataLoss) | Texto/gráfico/imagem ou outros dados estão completamente ausentes tanto da árvore do documento após o carregamento quanto do documento criado após a gravação. |
| [InvalidInputStreamType](#InvalidInputStreamType) | Tipo de fluxo de entrada inválido |
| [MajorFormattingLoss](#MajorFormattingLoss) | Perdas de formatação significativas em comparação ao documento original. Isto se aplica a situações em que a perda de formatação é substancial, mas os dados ainda permanecem. |
| [MinorFormattingLoss](#MinorFormattingLoss) | Pequenas perdas de formatação em comparação ao documento original. Isto se aplica a perdas menores de fidelidade. |
| [SourceFileCorruption](#SourceFileCorruption) | O arquivo está corrompido. |
| [UnexpectedContent](#UnexpectedContent) | O arquivo contém conteúdo inesperado. |

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

Problema conhecido que impedirá a abertura do documento por determinados agentes de usuário ou versões anteriores desses agentes.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

Texto/gráfico/imagem ou outros dados estão completamente ausentes tanto da árvore do documento após o carregamento quanto do documento criado após a gravação.

### InvalidInputStreamType {#InvalidInputStreamType}
```
public static final int InvalidInputStreamType
```

Tipo de fluxo de entrada inválido

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

Perdas de formatação significativas em comparação ao documento original. Isto se aplica a situações em que a perda de formatação é substancial, mas os dados ainda permanecem.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

Pequenas perdas de formatação em comparação ao documento original. Isto se aplica a perdas menores de fidelidade.

### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

O arquivo está corrompido.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

O arquivo contém conteúdo inesperado.
