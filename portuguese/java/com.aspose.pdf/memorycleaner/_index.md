---
title: "MemoryCleaner"
linktitle: "MemoryCleaner"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa a classe MemoryCleaner."
type: docs
weight: 3010
url: /pt/java/com.aspose.pdf/memorycleaner/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MemoryCleaner

```
public class MemoryCleaner extends Object
```

Representa a classe MemoryCleaner.

## Construtores

| Construtor | Descrição |
| --- | --- |
| [MemoryCleaner](#MemoryCleaner--) |  |

## Métodos

| Método | Descrição |
| --- | --- |
| [clear](#clear--) | Limpa a memória Heap de instâncias estáticas do Aspose.PDF (estáticas e threadLocal) e exclui todos os arquivos temporários. |
| [clearAllTempFiles](#clearAllTempFiles--) | Limpa arquivos temporários criados pelo Aspose.PDF no diretório temporário do sistema. |
| [clearCurrentThreadLocals](#clearCurrentThreadLocals--) | Limpa com segurança a memória Heap de instâncias ThreadLocal para a thread atual |
| [clearFontRegistry](#clearFontRegistry--) | Limpa informações do FontRegistry da memória Heap. |
| [clearKeepingFontSettings](#clearKeepingFontSettings--) | Limpa a memória Heap de instâncias estáticas do Aspose.PDF (estáticas e threadLocal) e exclui todos os arquivos temporários. |
| [clearLicense](#clearLicense--) | Limpa informações de licença da memória Heap. |
| [clearStaticInstances](#clearStaticInstances--) | Limpa a memória Heap de instâncias estáticas do Aspose.PDF. |

### MemoryCleaner {#MemoryCleaner--}
```
public MemoryCleaner()
```



### clear {#clear--}
```
public static void clear()
```

Limpa a memória Heap de instâncias estáticas do Aspose.PDF (estáticas e threadLocal) e exclui todos os arquivos temporários.

### clearAllTempFiles {#clearAllTempFiles--}
```
public static void clearAllTempFiles()
```

Limpa arquivos temporários criados pelo Aspose.PDF no diretório temporário do sistema.

### clearCurrentThreadLocals {#clearCurrentThreadLocals--}
```
public static void clearCurrentThreadLocals()
```

Limpa com segurança a memória Heap de instâncias ThreadLocal para a thread atual

### clearFontRegistry {#clearFontRegistry--}
```
public static void clearFontRegistry()
```

Limpa informações do FontRegistry da memória Heap.

### clearKeepingFontSettings {#clearKeepingFontSettings--}
```
public static void clearKeepingFontSettings()
```

Limpa a memória Heap de instâncias estáticas do Aspose.PDF (estáticas e threadLocal) e exclui todos os arquivos temporários.

### clearLicense {#clearLicense--}
```
public static void clearLicense()
```

Limpa informações de licença da memória Heap.

### clearStaticInstances {#clearStaticInstances--}
```
public static void clearStaticInstances()
```

Limpa a memória Heap de instâncias estáticas do Aspose.PDF.
