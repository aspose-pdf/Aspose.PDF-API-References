---
title: "TextReplaceOptions.Scope"
linktitle: "TextReplaceOptions.Scope"
second_title: "Referência da API Aspose.PDF para Java"
description: "Escopo onde a operação de substituição de texto é aplicada REPLACE_FIRST por padrão. Esta opção obsoleta foi mantida por compatibilidade. Ela afeta o PdfContentEditor e não tem efeito em."
type: docs
weight: 5280
url: /pt/java/com.aspose.pdf/textreplaceoptions.scope/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextReplaceOptions.Scope > com.aspose.pdf.TextReplaceOptions.Scope, java.lang.Enum < TextReplaceOptions.Scope >, com.aspose.pdf.TextReplaceOptions.Scope

**All Implemented Interfaces:**
Serializável, Comparável < TextReplaceOptions.Scope >

```
public static enum TextReplaceOptions.Scope extends Enum < TextReplaceOptions.Scope >
```

Escopo onde a operação de substituir texto é aplicada REPLACE_FIRST por padrão. Esta opção obsoleta foi mantida por compatibilidade. Ela afeta o PdfContentEditor e não tem efeito no TextFragmentAbsorber.

## Campos

| Campo | Descrição |
| --- | --- |
| [REPLACE_ALL](#REPLACE_ALL) | Substitua todas as ocorrências de texto em todas as páginas afetadas |
| [REPLACE_FIRST](#REPLACE_FIRST) | Substitua apenas a primeira ocorrência do texto em cada página afetada |

## Métodos

| Método | Descrição |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### REPLACE_ALL {#REPLACE_ALL}
```
public static final TextReplaceOptions.Scope REPLACE_ALL
```

Substitua todas as ocorrências de texto em todas as páginas afetadas

### REPLACE_FIRST {#REPLACE_FIRST}
```
public static final TextReplaceOptions.Scope REPLACE_FIRST
```

Substitua apenas a primeira ocorrência do texto em cada página afetada

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static TextReplaceOptions.Scope [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
