---
title: "Artifact.ArtifactType"
linktitle: "Artifact.ArtifactType"
second_title: "Referência da API Aspose.PDF para Java"
description: "Enumeração de tipos de artefato possíveis."
type: docs
weight: 210
url: /pt/java/com.aspose.pdf/artifact.artifacttype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < Artifact.ArtifactType > com.aspose.pdf.Artifact.ArtifactType, java.lang.Enum < Artifact.ArtifactType >, com.aspose.pdf.Artifact.ArtifactType

**All Implemented Interfaces:**
Serializable, Comparable < Artifact.ArtifactType >

```
public static enum Artifact.ArtifactType extends Enum < Artifact.ArtifactType >
```

Enumeração de tipos de artefato possíveis.

## Campos

| Campo | Descrição |
| --- | --- |
| [Background](#Background) | Artefatos de fundo. |
| [Layout](#Layout) | Artefatos de layout. |
| [Page](#Page) | Artefatos de página. |
| [Pagination](#Pagination) | Artefatos de paginação. |
| [Undefined](#Undefined) | Tipo de artefato não está definido ou é desconhecido. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Background {#Background}
```
public static final Artifact.ArtifactType Background
```

Artefatos de fundo.

### Layout {#Layout}
```
public static final Artifact.ArtifactType Layout
```

Artefatos de layout.

### Page {#Page}
```
public static final Artifact.ArtifactType Page
```

Artefatos de página.

### Pagination {#Pagination}
```
public static final Artifact.ArtifactType Pagination
```

Artefatos de paginação.

### Undefined {#Undefined}
```
public static final Artifact.ArtifactType Undefined
```

Tipo de artefato não está definido ou é desconhecido.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static Artifact.ArtifactType [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
