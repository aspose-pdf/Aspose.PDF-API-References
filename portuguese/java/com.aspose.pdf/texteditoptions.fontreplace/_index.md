---
title: "TextEditOptions.FontReplace"
linktitle: "TextEditOptions.FontReplace"
second_title: "Referência da API Aspose.PDF para Java"
description: "Comportamento de substituição de fonte."
type: docs
weight: 4990
url: /pt/java/com.aspose.pdf/texteditoptions.fontreplace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextEditOptions.FontReplace > com.aspose.pdf.TextEditOptions.FontReplace, java.lang.Enum < TextEditOptions.FontReplace >, com.aspose.pdf.TextEditOptions.FontReplace

**All Implemented Interfaces:**
Serializable, Comparable < TextEditOptions.FontReplace >

```
public static enum TextEditOptions.FontReplace extends Enum < TextEditOptions.FontReplace >
```

Comportamento de substituição de fonte.

## Campos

| Campo | Descrição |
| --- | --- |
| [Default](#Default) | Nenhuma alteração adicional foi realizada durante a substituição de fontes. |
| [RemoveUnusedFonts](#RemoveUnusedFonts) | Fontes que se tornam inutilizadas durante a substituição de fontes serão removidas do documento resultante. |

## Métodos

| Método | Descrição |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Retorna a constante enum deste tipo com o nome especificado. |
| [values](#values--) | Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas. |

### Default {#Default}
```
public static final TextEditOptions.FontReplace Default
```

Nenhuma alteração adicional foi realizada durante a substituição de fontes.

### RemoveUnusedFonts {#RemoveUnusedFonts}
```
public static final TextEditOptions.FontReplace RemoveUnusedFonts
```

Fontes que se tornam inutilizadas durante a substituição de fontes serão removidas do documento resultante.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Retorna a constante enum deste tipo com o nome especificado.

### values {#values--}
```
public static TextEditOptions.FontReplace [] values()
```

Retorna um array contendo as constantes deste tipo enum, na ordem em que são declaradas.

**Returns:**
um array contendo as constantes deste tipo enum, na ordem em que são declaradas
