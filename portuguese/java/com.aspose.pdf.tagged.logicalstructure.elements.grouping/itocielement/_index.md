---
title: "ITociElement"
linktitle: "ITociElement"
second_title: "Referência da API Aspose.PDF para Java"
description: "Representa um elemento que pode funcionar como uma entrada de índice (TOCI) em um documento PDF marcado. Esta interface abstrai tanto os elementos TOCI padrão quanto o Item de Lista (LI)."
type: docs
weight: 80
url: /pt/java/com.aspose.pdf.tagged.logicalstructure.elements.grouping/itocielement/
---
```
public interface ITociElement
```

Representa um elemento que pode funcionar como uma entrada de índice (TOCI) em um documento PDF marcado. Esta interface abstrai tanto os elementos TOCI padrão quanto os elementos Item de Lista (LI) usados em estruturas de índice aninhadas.

## Métodos

| Método | Descrição |
| --- | --- |
| [getGetElement](#getGetElement--) | Obtém o elemento PDF subjacente que representa esta estrutura TOCI. |

### getGetElement {#getGetElement--}
```
StructureElement getGetElement()
```

Obtém o elemento PDF subjacente que representa esta estrutura TOCI.

**Returns:**
O Element que forma a representação estrutural desta entrada de índice.
