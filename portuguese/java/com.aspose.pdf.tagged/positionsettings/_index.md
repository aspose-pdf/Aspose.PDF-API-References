---
title: "PositionSettings"
linktitle: "PositionSettings"
second_title: "Referência da API Aspose.PDF para Java"
description: "Configurações de posição."
type: docs
weight: 40
url: /pt/java/com.aspose.pdf.tagged/positionsettings/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.PositionSettings

```
public class PositionSettings extends Object
```

Configurações de posição.

## Construtores

| Construtor | Descrição |
| --- | --- |
| [PositionSettings](#PositionSettings--) |  |

## Métodos

| Método | Descrição |
| --- | --- |
| [getHorizontalAlignment](#getHorizontalAlignment--) | Obtém ou define um alinhamento horizontal do parágrafo. |
| [getMargin](#getMargin--) | Obtém ou define uma margem externa para o parágrafo. |
| [getVerticalAlignment](#getVerticalAlignment--) | Obtém ou define um alinhamento vertical do parágrafo. |
| [isFirstParagraphInColumn](#isFirstParagraphInColumn--) | Obtém ou define um valor booleano que indica se este parágrafo ficará na próxima coluna. O padrão é falso. |
| [isInLineParagraph](#isInLineParagraph--) | Obtém ou define se o parágrafo está em linha. O padrão é falso. |
| [isInNewPage](#isInNewPage--) | Obtém ou define um valor booleano que força este parágrafo a ser gerado em uma nova página. O padrão é falso. |
| [isKeptWithNext](#isKeptWithNext--) | Obtém ou define um valor booleano que indica se o parágrafo atual permanece na mesma página junto com o próximo parágrafo. O padrão é falso. |
| [setFirstParagraphInColumn](#setFirstParagraphInColumn-boolean-) | Obtém ou define um valor booleano que indica se este parágrafo ficará na próxima coluna. O padrão é falso. |
| [setHorizontalAlignment](#setHorizontalAlignment-com.aspose.pdf.HorizontalAlignment-) | Obtém ou define um alinhamento horizontal do parágrafo. |
| [setInLineParagraph](#setInLineParagraph-boolean-) | Obtém ou define se o parágrafo está em linha. O padrão é falso. |
| [setInNewPage](#setInNewPage-boolean-) | Obtém ou define um valor booleano que força este parágrafo a ser gerado em uma nova página. O padrão é falso. |
| [setKeptWithNext](#setKeptWithNext-boolean-) | Obtém ou define um valor booleano que indica se o parágrafo atual permanece na mesma página junto com o próximo parágrafo. O padrão é falso. |
| [setMargin](#setMargin-com.aspose.pdf.MarginInfo-) | Obtém ou define uma margem externa para o parágrafo. |
| [setVerticalAlignment](#setVerticalAlignment-com.aspose.pdf.VerticalAlignment-) | Obtém ou define um alinhamento vertical do parágrafo. |

### PositionSettings {#PositionSettings--}
```
public PositionSettings()
```



### getHorizontalAlignment {#getHorizontalAlignment--}
```
public final HorizontalAlignment getHorizontalAlignment()
```

Obtém ou define um alinhamento horizontal do parágrafo.

**Returns:**
Elemento HorizontalAlignment

### getMargin {#getMargin--}
```
public final MarginInfo getMargin()
```

Obtém ou define uma margem externa para o parágrafo.

**Returns:**
Instância MarginInfo

### getVerticalAlignment {#getVerticalAlignment--}
```
public final VerticalAlignment getVerticalAlignment()
```

Obtém ou define um alinhamento vertical do parágrafo.

**Returns:**
Elemento VerticalAlignment

### isFirstParagraphInColumn {#isFirstParagraphInColumn--}
```
public final boolean isFirstParagraphInColumn()
```

Obtém ou define um valor booleano que indica se este parágrafo ficará na próxima coluna. O padrão é falso.

**Returns:**
valor booleano

### isInLineParagraph {#isInLineParagraph--}
```
public final boolean isInLineParagraph()
```

Obtém ou define se o parágrafo está em linha. O padrão é falso.

**Returns:**
valor booleano

### isInNewPage {#isInNewPage--}
```
public final boolean isInNewPage()
```

Obtém ou define um valor booleano que força este parágrafo a ser gerado em uma nova página. O padrão é falso.

**Returns:**
valor booleano

### isKeptWithNext {#isKeptWithNext--}
```
public final boolean isKeptWithNext()
```

Obtém ou define um valor booleano que indica se o parágrafo atual permanece na mesma página junto com o próximo parágrafo. O padrão é falso.

**Returns:**
valor booleano

### setFirstParagraphInColumn {#setFirstParagraphInColumn-boolean-}
```
public final void setFirstParagraphInColumn(boolean value)
```

Obtém ou define um valor booleano que indica se este parágrafo ficará na próxima coluna. O padrão é falso.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | valor booleano |

### setHorizontalAlignment {#setHorizontalAlignment-com.aspose.pdf.HorizontalAlignment-}
Obtém ou define um alinhamento horizontal do parágrafo.

### setInLineParagraph {#setInLineParagraph-boolean-}
```
public final void setInLineParagraph(boolean value)
```

Obtém ou define se o parágrafo está em linha. O padrão é falso.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | valor booleano |

### setInNewPage {#setInNewPage-boolean-}
```
public final void setInNewPage(boolean value)
```

Obtém ou define um valor booleano que força este parágrafo a ser gerado em uma nova página. O padrão é falso.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | valor booleano |

### setKeptWithNext {#setKeptWithNext-boolean-}
```
public final void setKeptWithNext(boolean value)
```

Obtém ou define um valor booleano que indica se o parágrafo atual permanece na mesma página junto com o próximo parágrafo. O padrão é falso.

**Parameters:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| valor |  | valor booleano |

### setMargin {#setMargin-com.aspose.pdf.MarginInfo-}
Obtém ou define uma margem externa para o parágrafo.

### setVerticalAlignment {#setVerticalAlignment-com.aspose.pdf.VerticalAlignment-}
Obtém ou define um alinhamento vertical do parágrafo.
