---
title: HtmlSaveOptions.CssClassNamesPrefix
second_title: Aspose.PDF for .NET API Reference
description: Campo HtmlSaveOptions. Quando o conversor PDFtoHTML gera os CSSs de resultado, nomes de classes CSS (algo como .stl_01 ... .stl_NN) são gerados e usados no CSS de resultado. Esta propriedade permite definir forçosamente o prefixo do nome da classe. Por exemplo, se você quiser que todos os nomes de classes comecem com my_prefix_, ou seja, sejam algo como my_prefix_1 ... my_prefix_NNN, então basta atribuir my_prefix_ a esta propriedade antes da conversão. Se esta propriedade permanecer inalterada, ou seja, null será deixado como valor, então o conversor gerará os nomes das classes por conta própria, será algo como .stl_01 ... .stl_NN
type: docs
weight: 250
url: /pt/net/aspose.pdf/htmlsaveoptions/cssclassnamesprefix/
---
## Campo HtmlSaveOptions.CssClassNamesPrefix

Quando o conversor PDFtoHTML gera os CSSs de resultado, nomes de classes CSS (algo como ".stl_01 {}" ... ".stl_NN {}) são gerados e usados no CSS de resultado. Esta propriedade permite definir forçosamente o prefixo do nome da classe. Por exemplo, se você quiser que todos os nomes de classes comecem com 'my_prefix_' (ou seja, sejam algo como 'my_prefix_1' ... 'my_prefix_NNN'), então basta atribuir 'my_prefix_' a esta propriedade antes da conversão. Se esta propriedade permanecer inalterada (ou seja, null será deixado como valor), então o conversor gerará os nomes das classes por conta própria (será algo como ".stl_01 {}" ... ".stl_NN {}")

```csharp
public string CssClassNamesPrefix;
```

### Veja Também

* classe [HtmlSaveOptions](../)
* namespace [Aspose.Pdf](../../../aspose.pdf/)
* assembly [Aspose.PDF](../../../)