---
title: "FormattedText"
second_title: "Aspose.PDF para Python via .NET Referência da API"
description: "Classe que representa texto formatado. Contém informações sobre o texto e sua cor, tamanho e estilo."
type: docs
weight: 130
url: /pt/python-net/aspose.pdf.facades/formattedtext/
---

## FormattedText class

Classe que representa texto formatado. Contém informações sobre o texto e sua cor, tamanho e estilo.

O tipo FormattedText expõe os seguintes membros:
## Construtores
| Nome | Descrição |
| :- | :- |
| FormattedText() | Inicializa FormattedText. |
| FormattedText(text) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, font_color, font_style, encoding_type, embedded, text_size) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, font_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, color, text_font, text_encoding, embedded, text_size) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, back_color, text_font, encoding, embedded, text_size) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, back_color, font_name, text_encoding, embedded, font_size) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, back_color) | Inicializa uma nova instância da classe FormattedText |
| FormattedText(text, text_color, font_name, text_encoding, embedded, font_size) | Inicializa uma nova instância da classe FormattedText |
## Propriedades
| Nome | Descrição |
| :- | :- |
| text_height | Obtém a altura do texto. |
| text_width | Obtém a largura do texto. |
## Métodos
| Nome | Descrição |
| :- | :- |
| add_new_line_text(new_line_text) | Adiciona uma nova linha ao objeto FormattedText e define newLineText como o texto da linha seguinte. |
| add_new_line_text(new_line_text, line_spacing) | Adiciona uma nova linha ao objeto FormattedText e define newLineText como o texto da linha seguinte. |
| is_cjk() | Verifica se o texto é CJK (Chinês, Japonês ou Coreano). |
| set_cjk_font_style() | Altera o estilo de fonte do FormattedText para a fonte CJK (Chinês, Japonês ou Coreano). |

### Veja Também

* namespace [aspose.pdf.facades](/pdf/python-net/aspose.pdf.facades/)
* assembly [Aspose.PDF](/pdf/python-net/)

