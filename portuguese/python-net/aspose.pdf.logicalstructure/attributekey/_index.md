---
title: "AttributeKey"
second_title: "Aspose.PDF para Python via .NET Referência da API"
description: "Representa as chaves de atributos padrão."
type: docs
weight: 40
url: /pt/python-net/aspose.pdf.logicalstructure/attributekey/
---

## AttributeKey class

Representa as chaves de atributos padrão.

O tipo AttributeKey expõe os seguintes membros:
## Propriedades
| Nome | Descrição |
| :- | :- |
| key | Obter a Chave de Atributo. |
| owner | Obter o Proprietário do Atributo. |
| PLACEMENT | Atributo Placement (proprietário do atributo Layout). |
| WRITING_MODE | Atributo WritingMode (proprietário do atributo Layout). |
| BACKGROUND_COLOR | Atributo BackgroundColor (proprietário do atributo Layout). |
| BORDER_COLOR | Atributo BorderColor (proprietário do atributo Layout). |
| BORDER_STYLE | Atributo BorderStyle (proprietário do atributo Layout). |
| BORDER_THICKNESS | Atributo BorderThickness (proprietário do atributo Layout). |
| PADDING | Atributo Padding (proprietário do atributo Layout). |
| COLOR | Atributo Color (proprietário do atributo Layout). |
| SPACE_BEFORE | Atributo SpaceBefore (proprietário do atributo Layout). |
| SPACE_AFTER | Atributo SpaceAfter (proprietário do atributo Layout). |
| START_INDENT | Atributo StartIndent (proprietário do atributo Layout). |
| END_INDENT | Atributo EndIndent (proprietário do atributo Layout). |
| TEXT_INDENT | Atributo TextIndent (proprietário do atributo Layout). |
| TEXT_ALIGN | Atributo TextAlign (proprietário do atributo Layout). |
| B_BOX | Atributo BBox (proprietário do atributo Layout). |
| WIDTH | Atributo Width (proprietário do atributo Layout). |
| HEIGHT | Atributo Height (proprietário do atributo Layout). |
| BLOCK_ALIGN | Atributo BlockAlign (proprietário do atributo Layout). |
| INLINE_ALIGN | Atributo InlineAlign (proprietário do atributo Layout). |
| T_BORDER_STYLE | Atributo TBorderStyle (proprietário do atributo Layout). |
| T_PADDING | Atributo TPadding (proprietário do atributo Layout). |
| BASELINE_SHIFT | Atributo BaselineShift (proprietário do atributo Layout). |
| LINE_HEIGHT | Atributo LineHeight (Layout proprietário do atributo). |
| TEXT_DECORATION_COLOR | Atributo TextDecorationColor (Layout proprietário do atributo). |
| TEXT_DECORATION_THICKNESS | Atributo TextDecorationThickness (Layout proprietário do atributo). |
| TEXT_DECORATION_TYPE | Atributo TextDecorationType (Layout proprietário do atributo). |
| RUBY_ALIGN | Atributo RubyAlign (Layout proprietário do atributo). |
| RUBY_POSITION | Atributo RubyPosition (Layout proprietário do atributo). |
| GLYPH_ORIENTATION_VERTICAL | Atributo GlyphOrientationVertical (Layout proprietário do atributo). |
| COLUMN_COUNT | Atributo ColumnCount (Layout proprietário do atributo). |
| COLUMN_GAP | Atributo ColumnGap (Layout proprietário do atributo). |
| COLUMN_WIDTHS | Atributo ColumnWidths (Layout proprietário do atributo). |
| LIST_NUMBERING | Atributo ListNumbering (List proprietário do atributo). |
| ROLE | Atributo Role (PrintField proprietário do atributo). |
| CHECKED | Atributo Checked (proprietário do atributo PrintField). |
| DESC | Atributo Desc (proprietário do atributo PrintField). |
| ROW_SPAN | Atributo RowSpan (proprietário do atributo Table). |
| COL_SPAN | Atributo ColSpan (proprietário do atributo Table). |
| HEADERS | Atributo Headers (proprietário do atributo Table). |
| SCOPE | Atributo Scope (proprietário do atributo Table). |
| SUMMARY | Atributo Summary (proprietário do atributo Table). |

### Veja Também

* namespace [aspose.pdf.logicalstructure](/pdf/python-net/aspose.pdf.logicalstructure/)
* assembly [Aspose.PDF](/pdf/python-net/)

