---
title: "TiffDevice"
second_title: "Aspose.PDF para Python via .NET Referência da API"
description: "Esta classe ajuda a salvar páginas do documento PDF, uma a uma, em uma única imagem TIFF."
type: docs
weight: 150
url: /pt/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Esta classe ajuda a salvar páginas do documento PDF, uma a uma, em uma única imagem TIFF.

O tipo TiffDevice expõe os seguintes membros:
## Construtores
| Nome | Descrição |
| :- | :- |
| TiffDevice(resolution) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(resolution, settings) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(resolution, settings, converter) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(settings) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(settings, converter) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice() | Inicializa uma nova instância da classe [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) com configurações padrão. |
| TiffDevice(width, height, resolution, settings) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(page_size, resolution, settings) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(width, height, resolution) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(page_size, resolution) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(width, height, settings) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(width, height, settings, converter) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(page_size, settings, converter) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(page_size, settings) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(width, height) | Inicializa uma nova instância da classe TiffDevice |
| TiffDevice(page_size) | Inicializa uma nova instância da classe TiffDevice |
## Propriedades
| Nome | Descrição |
| :- | :- |
| rendering_options | Obtém ou define as opções de renderização. |
| form_presentation_mode | Obtém ou define o modo de apresentação do formulário. |
| configurações | Obtém configurações para mapear pdf em imagem tiff. |
| resolution | Obtém a resolução da imagem. |
| largura | Obtém a largura de saída da imagem. |
| altura | Obtém a altura de saída da imagem. |
## Métodos
| Nome | Descrição |
| :- | :- |
| process(document, from_page, to_page, output) | Converte determinadas páginas do documento em tiff e salva no fluxo de saída. |
| process(document, output) | Converte determinadas páginas do documento em tiff e salva no fluxo de saída. |
| process(document, output_file_name) | Converte determinadas páginas do documento em tiff e salva no fluxo de saída. |
| process(document, from_page, to_page, output_file_name) | Converte determinadas páginas do documento em tiff e salva no fluxo de saída. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Realiza binarização Bradley para o fluxo de entrada. |

### Veja Também

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

