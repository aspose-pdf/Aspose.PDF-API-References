---
title: "PrintPaperSource"
linktitle: "PrintPaperSource"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Gibt das Papierfach an, aus dem der Drucker das Papier bezieht."
type: docs
weight: 120
url: /de/java/com.aspose.pdf.printing/printpapersource/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.printing.PrintPaperSource

```
public class PrintPaperSource extends Object
```

Gibt das Papierfach an, aus dem der Drucker das Papier bezieht.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [PrintPaperSource](#PrintPaperSource--) | Initialisiert eine neue Instanz der Klasse PaperSource. |
| [PrintPaperSource](#PrintPaperSource-java.lang.String-int-) | Initialisiert eine neue Instanz der Klasse PaperSource. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getKind](#getKind--) | Liest die Papierquelle. |
| [getRawKind](#getRawKind--) | Liest oder setzt den Integer, der einen der PaperSourceKind-Werte oder einen benutzerdefinierten Wert darstellt. |
| [getSourceName](#getSourceName--) | Liest oder setzt den Namen der Papierquelle. |
| [setRawKind](#setRawKind-int-) | Liest oder setzt den Integer, der einen der PaperSourceKind-Werte oder einen benutzerdefinierten Wert darstellt. |
| [setSourceName](#setSourceName-java.lang.String-) | Liest oder setzt den Namen der Papierquelle. |
| [toString](#toString--) | Infrastruktur. Liefert Informationen über die PrintPaperSource in Zeichenkettenform. |

### PrintPaperSource {#PrintPaperSource--}
```
public PrintPaperSource()
```

Initialisiert eine neue Instanz der Klasse PaperSource.

### PrintPaperSource {#PrintPaperSource-java.lang.String-int-}
Initialisiert eine neue Instanz der Klasse PaperSource.

### getKind {#getKind--}
```
public int getKind()
```

Liest die Papierquelle.

**Returns:**
int-Wert

### getRawKind {#getRawKind--}
```
public int getRawKind()
```

Liest oder setzt den Integer, der einen der PaperSourceKind-Werte oder einen benutzerdefinierten Wert darstellt.

**Returns:**
int-Wert

### getSourceName {#getSourceName--}
```
public String getSourceName()
```

Liest oder setzt den Namen der Papierquelle.

**Returns:**
String Wert

### setRawKind {#setRawKind-int-}
```
public void setRawKind(int value)
```

Liest oder setzt den Integer, der einen der PaperSourceKind-Werte oder einen benutzerdefinierten Wert darstellt.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  | int-Wert |

### setSourceName {#setSourceName-java.lang.String-}
Liest oder setzt den Namen der Papierquelle.

### toString {#toString--}
```
public String toString()
```

Infrastruktur. Liefert Informationen über die PrintPaperSource in Zeichenkettenform.

**Returns:**
String Wert
