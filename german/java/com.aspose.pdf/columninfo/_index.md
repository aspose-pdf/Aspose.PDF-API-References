---
title: "ColumnInfo"
linktitle: "ColumnInfo"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Diese Klasse stellt Informationen einer Spalte dar."
type: docs
weight: 730
url: /de/java/com.aspose.pdf/columninfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ColumnInfo

```
public final class ColumnInfo extends Object
```

Diese Klasse stellt Informationen einer Spalte dar.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [ColumnInfo](#ColumnInfo--) | Initialisiert eine neue Instanz der ColumnInfo-Klasse. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getColumnCount](#getColumnCount--) | Gibt den int-Wert zurück, der die Anzahl der Spalten angibt. |
| [getColumnSpacing](#getColumnSpacing--) | <p> Liest oder legt eine Zeichenkette fest, die den Abstand zwischen den Spalten enthält. Der Wert jedes Abstands sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm und Zoll werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch". </p><hr> <p> Wenn diese Eigenschaft nicht gesetzt ist, wird für jeden Abstand der Standardwert 0 verwendet. </p> |
| [getColumnWidths](#getColumnWidths--) | Gibt eine Zeichenkette zurück oder legt sie fest, die die Breite der Spalten enthält. Der Wert jeder Spalte sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm, Zoll und Prozentsatz der verfügbaren Breite werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch" |
| [setColumnCount](#setColumnCount-int-) | Legt den int-Wert fest, der die Anzahl der Spalten angibt. |
| [setColumnSpacing](#setColumnSpacing-java.lang.String-) | <p> Liest oder legt eine Zeichenkette fest, die den Abstand zwischen den Spalten enthält. Der Wert jedes Abstands sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm und Zoll werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch". </p><hr> <p> Wenn diese Eigenschaft nicht gesetzt ist, wird für jeden Abstand der Standardwert 0 verwendet. </p> |
| [setColumnWidths](#setColumnWidths-java.lang.String-) | Gibt eine Zeichenkette zurück oder legt sie fest, die die Breite der Spalten enthält. Der Wert jeder Spalte sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm, Zoll und Prozentsatz der verfügbaren Breite werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch" |

### ColumnInfo {#ColumnInfo--}
```
public ColumnInfo()
```

Initialisiert eine neue Instanz der ColumnInfo-Klasse.

### getColumnCount {#getColumnCount--}
```
public int getColumnCount()
```

Gibt den int-Wert zurück, der die Anzahl der Spalten angibt.

**Returns:**
Spaltenanzahl

### getColumnSpacing {#getColumnSpacing--}
```
public String getColumnSpacing()
```

<p> Liest oder legt eine Zeichenkette fest, die den Abstand zwischen den Spalten enthält. Der Wert jedes Abstands sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm und Zoll werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch". </p><hr> <p> Wenn diese Eigenschaft nicht gesetzt ist, wird für jeden Abstand der Standardwert 0 verwendet. </p>

**Returns:**
String Wert

### getColumnWidths {#getColumnWidths--}
```
public String getColumnWidths()
```

Gibt eine Zeichenkette zurück oder legt sie fest, die die Breite der Spalten enthält. Der Wert jeder Spalte sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm, Zoll und Prozentsatz der verfügbaren Breite werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch"

**Returns:**
String Wert

### setColumnCount {#setColumnCount-int-}
```
public void setColumnCount(int value)
```

Legt den int-Wert fest, der die Anzahl der Spalten angibt.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  | Spaltenanzahl |

### setColumnSpacing {#setColumnSpacing-java.lang.String-}
<p> Liest oder legt eine Zeichenkette fest, die den Abstand zwischen den Spalten enthält. Der Wert jedes Abstands sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm und Zoll werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch". </p><hr> <p> Wenn diese Eigenschaft nicht gesetzt ist, wird für jeden Abstand der Standardwert 0 verwendet. </p>

### setColumnWidths {#setColumnWidths-java.lang.String-}
Gibt eine Zeichenkette zurück oder legt sie fest, die die Breite der Spalten enthält. Der Wert jeder Spalte sollte durch ein Leerzeichen getrennt werden. Die Standardmaßeinheit ist Punkt, aber cm, Zoll und Prozentsatz der verfügbaren Breite werden ebenfalls unterstützt. Zum Beispiel, "120 2.5cm 1.5inch"
