---
title: "ExplicitDestinationType"
linktitle: "ExplicitDestinationType"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Zählt die Typen expliziter Ziele auf."
type: docs
weight: 1290
url: /de/java/com.aspose.pdf/explicitdestinationtype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ExplicitDestinationType > com.aspose.pdf.ExplicitDestinationType, java.lang.Enum < ExplicitDestinationType >, com.aspose.pdf.ExplicitDestinationType

**All Implemented Interfaces:**
Serializable, Comparable < ExplicitDestinationType >

```
public enum ExplicitDestinationType extends Enum < ExplicitDestinationType >
```

Zählt die Typen expliziter Ziele auf.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Fit](#Fit) | Zeigt die Seite an, wobei ihr Inhalt so vergrößert wird, dass die gesamte Seite sowohl horizontal als auch vertikal in das Fenster passt. |
| [FitB](#FitB) | Zeigt die Seite an, wobei ihr Inhalt so vergrößert wird, dass die Begrenzungsbox vollständig sowohl horizontal als auch vertikal in das Fenster passt. |
| [FitBH](#FitBH) | Zeigt die Seite an, wobei die vertikale Koordinate oben am oberen Rand des Fensters positioniert ist und der Seiteninhalt so vergrößert wird, dass die gesamte Breite seiner Begrenzungsbox in das Fenster passt. |
| [FitBV](#FitBV) | Zeigt die Seite an, wobei die horizontale Koordinate links am linken Rand des Fensters positioniert ist und der Seiteninhalt so vergrößert wird, dass die gesamte Höhe seiner Begrenzungsbox in das Fenster passt. |
| [FitH](#FitH) | Zeigt die Seite an, wobei die vertikale Koordinate oben am oberen Rand des Fensters positioniert ist und der Seiteninhalt so vergrößert wird, dass die gesamte Breite der Seite in das Fenster passt. |
| [FitR](#FitR) | Zeigt die Seite an, wobei ihr Inhalt so vergrößert wird, dass das durch die Koordinaten links, unten, rechts und oben angegebene Rechteck vollständig sowohl horizontal als auch vertikal in das Fenster passt. |
| [FitV](#FitV) | Zeige die Seite an, wobei die horizontale Koordinate links am linken Rand des Fensters positioniert ist und der Inhalt der Seite gerade so vergrößert wird, dass die gesamte Höhe der Seite in das Fenster passt. |
| [XYZ](#XYZ) | Zeige die Seite an, wobei die Koordinaten (links, oben) in der oberen linken Ecke des Fensters positioniert sind und der Inhalt der Seite um den Zoom-Faktor vergrößert wird. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Fit {#Fit}
```
public static final ExplicitDestinationType Fit
```

Zeigt die Seite an, wobei ihr Inhalt so vergrößert wird, dass die gesamte Seite sowohl horizontal als auch vertikal in das Fenster passt.

### FitB {#FitB}
```
public static final ExplicitDestinationType FitB
```

Zeigt die Seite an, wobei ihr Inhalt so vergrößert wird, dass die Begrenzungsbox vollständig sowohl horizontal als auch vertikal in das Fenster passt.

### FitBH {#FitBH}
```
public static final ExplicitDestinationType FitBH
```

Zeigt die Seite an, wobei die vertikale Koordinate oben am oberen Rand des Fensters positioniert ist und der Seiteninhalt so vergrößert wird, dass die gesamte Breite seiner Begrenzungsbox in das Fenster passt.

### FitBV {#FitBV}
```
public static final ExplicitDestinationType FitBV
```

Zeigt die Seite an, wobei die horizontale Koordinate links am linken Rand des Fensters positioniert ist und der Seiteninhalt so vergrößert wird, dass die gesamte Höhe seiner Begrenzungsbox in das Fenster passt.

### FitH {#FitH}
```
public static final ExplicitDestinationType FitH
```

Zeigt die Seite an, wobei die vertikale Koordinate oben am oberen Rand des Fensters positioniert ist und der Seiteninhalt so vergrößert wird, dass die gesamte Breite der Seite in das Fenster passt.

### FitR {#FitR}
```
public static final ExplicitDestinationType FitR
```

Zeigt die Seite an, wobei ihr Inhalt so vergrößert wird, dass das durch die Koordinaten links, unten, rechts und oben angegebene Rechteck vollständig sowohl horizontal als auch vertikal in das Fenster passt.

### FitV {#FitV}
```
public static final ExplicitDestinationType FitV
```

Zeige die Seite an, wobei die horizontale Koordinate links am linken Rand des Fensters positioniert ist und der Inhalt der Seite gerade so vergrößert wird, dass die gesamte Höhe der Seite in das Fenster passt.

### XYZ {#XYZ}
```
public static final ExplicitDestinationType XYZ
```

Zeige die Seite an, wobei die Koordinaten (links, oben) in der oberen linken Ecke des Fensters positioniert sind und der Inhalt der Seite um den Zoom-Faktor vergrößert wird.

### getByValue {#getByValue-int-}
```
public static ExplicitDestinationType getByValue(int value)
```



**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static ExplicitDestinationType [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
