---
title: "RenditionOperation"
linktitle: "RenditionOperation"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Der Vorgang, der ausgeführt werden soll, wenn die Aktion ausgelöst wird."
type: docs
weight: 4190
url: /de/java/com.aspose.pdf/renditionoperation/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.Enum, com.aspose.pdf.RenditionOperation

```
public final class RenditionOperation extends com.aspose.ms.System.Enum
```

Der Vorgang, der ausgeführt werden soll, wenn die Aktion ausgelöst wird.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Pause](#Pause) | Pausiere jede Rendition, die in Verbindung mit der Anmerkung abgespielt wird. |
| [PlayResume](#PlayResume) | Spiele die angegebene Rendition ab und verknüpfe sie mit der Anmerkung. Wenn bereits eine Rendition mit der Anmerkung verknüpft ist, setze die Rendition fort, falls sie pausiert ist. |
| [PlayStop](#PlayStop) | Wenn keine Rendition mit der Anmerkung verknüpft ist, spiele die angegebene Rendition ab und verknüpfe sie mit der Anmerkung. Wenn bereits eine Rendition mit der Anmerkung verknüpft ist, wird sie gestoppt und die neue Rendition wird mit der Anmerkung verknüpft. |
| [Resume](#Resume) | Setze jede Rendition, die in Verbindung mit der Anmerkung abgespielt wird, fort. |
| [Stop](#Stop) | Stoppe jede Rendition, die in Verbindung mit der Anmerkung abgespielt wird. |
| [Undefined](#Undefined) | Operation nicht definiert. |

### Pause {#Pause}
```
public static final int Pause
```

Pausiere jede Rendition, die in Verbindung mit der Anmerkung abgespielt wird.

### PlayResume {#PlayResume}
```
public static final int PlayResume
```

Spiele die angegebene Rendition ab und verknüpfe sie mit der Anmerkung. Wenn bereits eine Rendition mit der Anmerkung verknüpft ist, setze die Rendition fort, falls sie pausiert ist.

### PlayStop {#PlayStop}
```
public static final int PlayStop
```

Wenn keine Rendition mit der Anmerkung verknüpft ist, spiele die angegebene Rendition ab und verknüpfe sie mit der Anmerkung. Wenn bereits eine Rendition mit der Anmerkung verknüpft ist, wird sie gestoppt und die neue Rendition wird mit der Anmerkung verknüpft.

### Resume {#Resume}
```
public static final int Resume
```

Setze jede Rendition, die in Verbindung mit der Anmerkung abgespielt wird, fort.

### Stop {#Stop}
```
public static final int Stop
```

Stoppe jede Rendition, die in Verbindung mit der Anmerkung abgespielt wird.

### Undefined {#Undefined}
```
public static final int Undefined
```

Operation nicht definiert.
