---
title: "HtmlSaveOptions.CssSavingInfo"
linktitle: "HtmlSaveOptions.CssSavingInfo"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Diese Klasse repräsentiert einen Datensatz, der sich auf das benutzerdefinierte Speichern von CSS während der Konvertierung von PDF in das HTML-Format bezieht."
type: docs
weight: 2010
url: /de/java/com.aspose.pdf/htmlsaveoptions.csssavinginfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.HtmlSaveOptions.CssSavingInfo

```
public static class HtmlSaveOptions.CssSavingInfo extends Object
```

Diese Klasse repräsentiert einen Datensatz, der sich auf das benutzerdefinierte Speichern von CSS während der Konvertierung von PDF in das HTML-Format bezieht.

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getContentStream](#getContentStream--) | Vom Konverter festgelegt. Stellt den Binärinhalt des gespeicherten CSS dar |
| [getCssNumber](#getCssNumber--) | Vom Konverter festgelegt. Während der Konvertierung werden mehrere CSS‑Dateien erstellt. Diese Eigenschaft zeigt die Ordnungszahl der gespeicherten CSS‑Datei während der Konvertierung an. Sie kann in der Logik von benutzerdefiniertem Code verwendet werden, um zu entscheiden, wie der CSS‑Inhalt verarbeitet oder wo er gespeichert werden soll. |
| [getSupposedURL](#getSupposedURL--) | Vom Konverter festgelegt. Angenommener Dateiname, der vom Konverter zum Code der benutzerdefinierten Methode übergeht. Kann im benutzerdefinierten Code verwendet werden, um zu entscheiden, wie verarbeitet wird oder wo der Inhalt gespeichert wird. |
| [setContentStream](#setContentStream-java.io.InputStream-) | Vom Konverter festgelegt. Stellt den Binärinhalt des gespeicherten CSS dar |
| [setCssNumber](#setCssNumber-int-) | Vom Konverter festgelegt. Während der Konvertierung werden mehrere CSS‑Dateien erstellt. Diese Eigenschaft zeigt die Ordnungszahl der gespeicherten CSS‑Datei während der Konvertierung an. Sie kann in der Logik von benutzerdefiniertem Code verwendet werden, um zu entscheiden, wie der CSS‑Inhalt verarbeitet oder wo er gespeichert werden soll. |
| [setSupposedURL](#setSupposedURL-java.lang.String-) | Vom Konverter festgelegt. Angenommener Dateiname, der vom Konverter zum Code der benutzerdefinierten Methode übergeht. Kann im benutzerdefinierten Code verwendet werden, um zu entscheiden, wie verarbeitet wird oder wo der Inhalt gespeichert wird. |

### getContentStream {#getContentStream--}
```
public InputStream getContentStream()
```

Vom Konverter festgelegt. Stellt den Binärinhalt des gespeicherten CSS dar

**Returns:**
InputStream-Instanz

### getCssNumber {#getCssNumber--}
```
public int getCssNumber()
```

Vom Konverter festgelegt. Während der Konvertierung werden mehrere CSS‑Dateien erstellt. Diese Eigenschaft zeigt die Ordnungszahl der gespeicherten CSS‑Datei während der Konvertierung an. Sie kann in der Logik von benutzerdefiniertem Code verwendet werden, um zu entscheiden, wie der CSS‑Inhalt verarbeitet oder wo er gespeichert werden soll.

**Returns:**
int-Wert

### getSupposedURL {#getSupposedURL--}
```
public String getSupposedURL()
```

Vom Konverter festgelegt. Angenommener Dateiname, der vom Konverter zum Code der benutzerdefinierten Methode übergeht. Kann im benutzerdefinierten Code verwendet werden, um zu entscheiden, wie verarbeitet wird oder wo der Inhalt gespeichert wird.

**Returns:**
String Wert

### setContentStream {#setContentStream-java.io.InputStream-}
Vom Konverter festgelegt. Stellt den Binärinhalt des gespeicherten CSS dar

### setCssNumber {#setCssNumber-int-}
```
public void setCssNumber(int cssNumber)
```

Vom Konverter festgelegt. Während der Konvertierung werden mehrere CSS‑Dateien erstellt. Diese Eigenschaft zeigt die Ordnungszahl der gespeicherten CSS‑Datei während der Konvertierung an. Sie kann in der Logik von benutzerdefiniertem Code verwendet werden, um zu entscheiden, wie der CSS‑Inhalt verarbeitet oder wo er gespeichert werden soll.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| cssNumber |  | int-Wert |

### setSupposedURL {#setSupposedURL-java.lang.String-}
Vom Konverter festgelegt. Angenommener Dateiname, der vom Konverter zum Code der benutzerdefinierten Methode übergeht. Kann im benutzerdefinierten Code verwendet werden, um zu entscheiden, wie verarbeitet wird oder wo der Inhalt gespeichert wird.
