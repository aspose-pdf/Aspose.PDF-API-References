---
title: "HorizontalAlignment"
linktitle: "HorizontalAlignment"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Beschreibt die horizontale Ausrichtung."
type: docs
weight: 1930
url: /de/java/com.aspose.pdf/horizontalalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < HorizontalAlignment > com.aspose.pdf.HorizontalAlignment, java.lang.Enum < HorizontalAlignment >, com.aspose.pdf.HorizontalAlignment

**All Implemented Interfaces:**
Serializable, Comparable < HorizontalAlignment >

```
public enum HorizontalAlignment extends Enum < HorizontalAlignment >
```

Beschreibt die horizontale Ausrichtung.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Center](#Center) | Zentrierte Ausrichtung. |
| [FullJustify](#FullJustify) | Ähnlich wie die 'Justify'-Ausrichtung, außer dass die allerletzte Zeile im 'Justify'-Modus nur linksbündig ausgerichtet wird, während im 'FullJustify'-Modus alle Zeilen links- und rechtsbündig ausgerichtet sind. |
| [Justify](#Justify) | Blocksatz-Ausrichtung. |
| [Left](#Left) | Linksbündig ausrichten. |
| [None](#None) | Keine Ausrichtung. |
| [Right](#Right) | Rechtsbündig ausrichten. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) | Anzahl erhalten von |
| [valueOf](#valueOf-int-) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Center {#Center}
```
public static final HorizontalAlignment Center
```

Zentrierte Ausrichtung.

### FullJustify {#FullJustify}
```
public static final HorizontalAlignment FullJustify
```

Ähnlich wie die 'Justify'-Ausrichtung, außer dass die allerletzte Zeile im 'Justify'-Modus nur linksbündig ausgerichtet wird, während im 'FullJustify'-Modus alle Zeilen links- und rechtsbündig ausgerichtet sind.

### Justify {#Justify}
```
public static final HorizontalAlignment Justify
```

Blocksatz-Ausrichtung.

### Left {#Left}
```
public static final HorizontalAlignment Left
```

Linksbündig ausrichten.

### None {#None}
```
public static final HorizontalAlignment None
```

Keine Ausrichtung.

### Right {#Right}
```
public static final HorizontalAlignment Right
```

Rechtsbündig ausrichten.

### getValue {#getValue--}
```
public int getValue()
```

Anzahl erhalten von

**Returns:**
int-Wert des HorizontalAlignment-Elements.

### valueOf {#valueOf-int-}
```
public static HorizontalAlignment valueOf(int alignmentType)
```



**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| alignmentType |  |  |

### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static HorizontalAlignment [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
