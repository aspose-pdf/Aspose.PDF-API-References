---
title: "CaptionPosition"
linktitle: "CaptionPosition"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Aufzählung der Positionierung der Beschriftung der Anmerkung."
type: docs
weight: 460
url: /de/java/com.aspose.pdf/captionposition/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < CaptionPosition > com.aspose.pdf.CaptionPosition, java.lang.Enum < CaptionPosition >, com.aspose.pdf.CaptionPosition

**All Implemented Interfaces:**
Serializable, Comparable < CaptionPosition >

```
public enum CaptionPosition extends Enum < CaptionPosition >
```

Aufzählung der Positionierung der Beschriftung der Anmerkung.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Inline](#Inline) | Die Beschriftung wird innerhalb der Zeile zentriert (Standardwert). |
| [Top](#Top) | Die Beschriftung wird über der Zeile angezeigt. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Inline {#Inline}
```
public static final CaptionPosition Inline
```

Die Beschriftung wird innerhalb der Zeile zentriert (Standardwert).

### Top {#Top}
```
public static final CaptionPosition Top
```

Die Beschriftung wird über der Zeile angezeigt.

### getByValue {#getByValue-int-}
```
public static CaptionPosition getByValue(int value)
```



**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static CaptionPosition [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
