---
title: "RegexManager"
linktitle: "RegexManager"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt einen Wrapper für reguläre Ausdrucksoperationen mit konfigurierbaren Timeout-Einstellungen bereit."
type: docs
weight: 4130
url: /de/java/com.aspose.pdf/regexmanager/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.RegexManager

```
public class RegexManager extends Object
```

Stellt einen Wrapper für reguläre Ausdrucksoperationen mit konfigurierbaren Timeout-Einstellungen bereit.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [RegexManager](#RegexManager--) |  |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getMatchTimeout](#getMatchTimeout--) | Liest oder setzt das Timeout für Regex‑Operationen in der gesamten Bibliothek. Der Standardwert beträgt 1000 ms. Wert: Ein {@link double}, das die Standard‑Timeout‑Dauer darstellt. |
| [setMatchTimeout](#setMatchTimeout-int-) | Liest oder setzt das Timeout für Regex‑Operationen in der gesamten Bibliothek. Der Standardwert beträgt 1000 ms. Wert: |

### RegexManager {#RegexManager--}
```
public RegexManager()
```



### getMatchTimeout {#getMatchTimeout--}
```
public static int getMatchTimeout()
```

Liest oder setzt das Timeout für Regex‑Operationen in der gesamten Bibliothek. Der Standardwert beträgt 1000 ms. Wert: Ein {@link double}, das die Standard‑Timeout‑Dauer darstellt.

**Returns:**
int-Wert

### setMatchTimeout {#setMatchTimeout-int-}
```
public static void setMatchTimeout(int value)
```

Liest oder setzt das Timeout für Regex‑Operationen in der gesamten Bibliothek. Der Standardwert beträgt 1000 ms. Wert:

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  | Ein, das die Standard‑Timeout‑Dauer in Millisekunden darstellt. |
