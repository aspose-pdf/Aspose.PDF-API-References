---
title: "TextEditOptions.FontReplace"
linktitle: "TextEditOptions.FontReplace"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Verhalten beim Ersetzen von Schriftarten."
type: docs
weight: 4990
url: /de/java/com.aspose.pdf/texteditoptions.fontreplace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextEditOptions.FontReplace > com.aspose.pdf.TextEditOptions.FontReplace, java.lang.Enum < TextEditOptions.FontReplace >, com.aspose.pdf.TextEditOptions.FontReplace

**All Implemented Interfaces:**
Serializable, Comparable < TextEditOptions.FontReplace >

```
public static enum TextEditOptions.FontReplace extends Enum < TextEditOptions.FontReplace >
```

Verhalten beim Ersetzen von Schriftarten.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Default](#Default) | Während des Schriftartenaustauschs wurden keine zusätzlichen Änderungen vorgenommen. |
| [RemoveUnusedFonts](#RemoveUnusedFonts) | Schriftarten, die während des Schriftartenaustauschs ungenutzt werden, werden aus dem resultierenden Dokument entfernt. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Default {#Default}
```
public static final TextEditOptions.FontReplace Default
```

Während des Schriftartenaustauschs wurden keine zusätzlichen Änderungen vorgenommen.

### RemoveUnusedFonts {#RemoveUnusedFonts}
```
public static final TextEditOptions.FontReplace RemoveUnusedFonts
```

Schriftarten, die während des Schriftartenaustauschs ungenutzt werden, werden aus dem resultierenden Dokument entfernt.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static TextEditOptions.FontReplace [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
