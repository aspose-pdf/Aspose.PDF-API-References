---
title: "Measure"
linktitle: "Measure"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Klasse, die das Measure-Koordinatensystem beschreibt."
type: docs
weight: 2930
url: /de/java/com.aspose.pdf/measure/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.Measure

```
public class Measure extends Object
```

Klasse, die das Measure-Koordinatensystem beschreibt.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [Measure](#Measure-com.aspose.pdf.Annotation-) | Erstellt ein Measure‑Objekt für Messanmerkungen. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getAngleFormat](#getAngleFormat--) | Ein Zahlenformat‑Array für die Messung von Winkeln. |
| [getAreaFormat](#getAreaFormat--) | Ein Zahlenformat‑Array für die Messung von Flächen. |
| [getDistanceFormat](#getDistanceFormat--) | Ein Zahlenformat‑Array für die Messung von Entfernungen in beliebiger Richtung. |
| [getOrigin](#getOrigin--) | Punkt, der den Ursprung des Messkoordinatensystems in den Standard‑Benutzerraumkoordinaten festlegt. |
| [getScaleRatio](#getScaleRatio--) | Eine Textzeichenfolge, die das Maßstabsverhältnis der Zeichnung ausdrückt. |
| [getSlopeFormat](#getSlopeFormat--) | Ein Zahlenformat‑Array für die Messung der Steigung einer Linie. |
| [getXFormat](#getXFormat--) | Ein Zahlenformat‑Array für die Messung der Änderung entlang der X‑Achse und, falls Y nicht vorhanden ist, ebenfalls entlang der Y‑Achse. |
| [getXYFactor](#getXYFactor--) | Ein Faktor, der verwendet werden soll, um die größten Einheiten entlang der Y‑Achse in die größten Einheiten entlang der X‑Achse umzuwandeln. |
| [getYFormat](#getYFormat--) | Ein Zahlenformat‑Array für die Messung der Änderung entlang der Y‑Achse. |
| [setAngleFormat](#setAngleFormat-com.aspose.pdf.Measure.NumberFormatList-) | Ein Zahlenformat‑Array für die Messung von Winkeln. |
| [setAreaFormat](#setAreaFormat-com.aspose.pdf.Measure.NumberFormatList-) | Ein Zahlenformat‑Array für die Messung von Flächen. |
| [setDistanceFormat](#setDistanceFormat-com.aspose.pdf.Measure.NumberFormatList-) | Ein Zahlenformat‑Array für die Messung von Entfernungen in beliebiger Richtung. |
| [setOrigin](#setOrigin-com.aspose.pdf.Point-) | Punkt, der den Ursprung des Messkoordinatensystems in den Standard‑Benutzerraumkoordinaten festlegt. |
| [setScaleRatio](#setScaleRatio-java.lang.String-) |  |
| [setSlopeFormat](#setSlopeFormat-com.aspose.pdf.Measure.NumberFormatList-) | Ein Zahlenformat‑Array für die Messung der Steigung einer Linie. |
| [setXFormat](#setXFormat-com.aspose.pdf.Measure.NumberFormatList-) | Ein Zahlenformat‑Array für die Messung der Änderung entlang der X‑Achse und, falls Y nicht vorhanden ist, ebenfalls entlang der Y‑Achse. |
| [setXYFactor](#setXYFactor-double-) | Ein Faktor, der verwendet werden soll, um die größten Einheiten entlang der Y‑Achse in die größten Einheiten entlang der X‑Achse umzuwandeln. |
| [setYFormat](#setYFormat-com.aspose.pdf.Measure.NumberFormatList-) | Ein Zahlenformat‑Array für die Messung der Änderung entlang der Y‑Achse. |

### Measure {#Measure-com.aspose.pdf.Annotation-}
Erstellt ein Measure‑Objekt für Messanmerkungen.

### getAngleFormat {#getAngleFormat--}
```
public Measure.NumberFormatList getAngleFormat()
```

Ein Zahlenformat‑Array für die Messung von Winkeln.

**Returns:**
NumberFormatList‑Wert

### getAreaFormat {#getAreaFormat--}
```
public Measure.NumberFormatList getAreaFormat()
```

Ein Zahlenformat‑Array für die Messung von Flächen.

**Returns:**
NumberFormatList‑Wert

### getDistanceFormat {#getDistanceFormat--}
```
public Measure.NumberFormatList getDistanceFormat()
```

Ein Zahlenformat‑Array für die Messung von Entfernungen in beliebiger Richtung.

**Returns:**
NumberFormatList‑Wert

### getOrigin {#getOrigin--}
```
public Point getOrigin()
```

Punkt, der den Ursprung des Messkoordinatensystems in den Standard‑Benutzerraumkoordinaten festlegt.

**Returns:**
Point-Objekt

### getScaleRatio {#getScaleRatio--}
```
public String getScaleRatio()
```

Eine Textzeichenfolge, die das Maßstabsverhältnis der Zeichnung ausdrückt.

**Returns:**
String‑Objekt

### getSlopeFormat {#getSlopeFormat--}
```
public Measure.NumberFormatList getSlopeFormat()
```

Ein Zahlenformat‑Array für die Messung der Steigung einer Linie.

**Returns:**
NumberFormatList‑Wert

### getXFormat {#getXFormat--}
```
public Measure.NumberFormatList getXFormat()
```

Ein Zahlenformat‑Array für die Messung der Änderung entlang der X‑Achse und, falls Y nicht vorhanden ist, ebenfalls entlang der Y‑Achse.

**Returns:**
NumberFormatList‑Wert

### getXYFactor {#getXYFactor--}
```
public double getXYFactor()
```

Ein Faktor, der verwendet werden soll, um die größten Einheiten entlang der Y‑Achse in die größten Einheiten entlang der X‑Achse umzuwandeln.

**Returns:**
double-Wert

### getYFormat {#getYFormat--}
```
public Measure.NumberFormatList getYFormat()
```

Ein Zahlenformat‑Array für die Messung der Änderung entlang der Y‑Achse.

**Returns:**
NumberFormatList‑Wert

### setAngleFormat {#setAngleFormat-com.aspose.pdf.Measure.NumberFormatList-}
Ein Zahlenformat‑Array für die Messung von Winkeln.

### setAreaFormat {#setAreaFormat-com.aspose.pdf.Measure.NumberFormatList-}
Ein Zahlenformat‑Array für die Messung von Flächen.

### setDistanceFormat {#setDistanceFormat-com.aspose.pdf.Measure.NumberFormatList-}
Ein Zahlenformat‑Array für die Messung von Entfernungen in beliebiger Richtung.

### setOrigin {#setOrigin-com.aspose.pdf.Point-}
Punkt, der den Ursprung des Messkoordinatensystems in den Standard‑Benutzerraumkoordinaten festlegt.

### setScaleRatio {#setScaleRatio-java.lang.String-}


### setSlopeFormat {#setSlopeFormat-com.aspose.pdf.Measure.NumberFormatList-}
Ein Zahlenformat‑Array für die Messung der Steigung einer Linie.

### setXFormat {#setXFormat-com.aspose.pdf.Measure.NumberFormatList-}
Ein Zahlenformat‑Array für die Messung der Änderung entlang der X‑Achse und, falls Y nicht vorhanden ist, ebenfalls entlang der Y‑Achse.

### setXYFactor {#setXYFactor-double-}
```
public void setXYFactor(double value)
```

Ein Faktor, der verwendet werden soll, um die größten Einheiten entlang der Y‑Achse in die größten Einheiten entlang der X‑Achse umzuwandeln.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  | double-Wert |

### setYFormat {#setYFormat-com.aspose.pdf.Measure.NumberFormatList-}
Ein Zahlenformat‑Array für die Messung der Änderung entlang der Y‑Achse.
