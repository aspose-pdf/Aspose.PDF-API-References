---
title: "PageMode"
linktitle: "PageMode"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Klasse beschreibt die verwendeten Komponenten der Dokumentenseite."
type: docs
weight: 3430
url: /de/java/com.aspose.pdf/pagemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PageMode > com.aspose.pdf.PageMode, java.lang.Enum < PageMode >, com.aspose.pdf.PageMode

**All Implemented Interfaces:**
Serializable, Comparable < PageMode >

```
public enum PageMode extends Enum < PageMode >
```

Klasse beschreibt die verwendeten Komponenten der Dokumentenseite.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [FullScreen](#FullScreen) | FullScreenFull-screen-Modus, ohne Menüleiste, Fenstersteuerungen oder sonstige Fenster sichtbar. |
| [UseAttachments](#UseAttachments) | Anlagen-Panel sichtbar. |
| [UseNone](#UseNone) | Verwenden Sie keine Komponenten. |
| [UseOC](#UseOC) | Optionaler Inhaltsgruppen-Panel sichtbar. |
| [UseOutlines](#UseOutlines) | Dokumenten-Gliederung sichtbar. |
| [UseThumbs](#UseThumbs) | Miniaturbilder sichtbar. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### FullScreen {#FullScreen}
```
public static final PageMode FullScreen
```

FullScreenFull-screen-Modus, ohne Menüleiste, Fenstersteuerungen oder sonstige Fenster sichtbar.

### UseAttachments {#UseAttachments}
```
public static final PageMode UseAttachments
```

Anlagen-Panel sichtbar.

### UseNone {#UseNone}
```
public static final PageMode UseNone
```

Verwenden Sie keine Komponenten.

### UseOC {#UseOC}
```
public static final PageMode UseOC
```

Optionaler Inhaltsgruppen-Panel sichtbar.

### UseOutlines {#UseOutlines}
```
public static final PageMode UseOutlines
```

Dokumenten-Gliederung sichtbar.

### UseThumbs {#UseThumbs}
```
public static final PageMode UseThumbs
```

Miniaturbilder sichtbar.

### getByValue {#getByValue-int-}
```
public static PageMode getByValue(int value)
```



**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static PageMode [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
