---
title: "PolyIntent"
linktitle: "PolyIntent"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Enumeriert die Absichten der Polygon- oder Polylinien-Anmerkung."
type: docs
weight: 3910
url: /de/java/com.aspose.pdf/polyintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PolyIntent > com.aspose.pdf.PolyIntent, java.lang.Enum < PolyIntent >, com.aspose.pdf.PolyIntent

**All Implemented Interfaces:**
Serializable, Comparable < PolyIntent >

```
public enum PolyIntent extends Enum < PolyIntent >
```

Enumeriert die Absichten der Polygon- oder Polylinien-Anmerkung.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [PolygonCloud](#PolygonCloud) | Bedeutet, dass die Anmerkung als Wolkenobjekt fungieren soll. |
| [PolygonDimension](#PolygonDimension) | Zeigt an, dass die Polygon-Anmerkung als Maßangabe fungieren soll. |
| [PolyLineDimension](#PolyLineDimension) | Zeigt an, dass die Polylinien-Anmerkung als Maßangabe fungieren soll. |
| [Undefined](#Undefined) | Undefinierter Zustand. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### PolygonCloud {#PolygonCloud}
```
public static final PolyIntent PolygonCloud
```

Bedeutet, dass die Anmerkung als Wolkenobjekt fungieren soll.

### PolygonDimension {#PolygonDimension}
```
public static final PolyIntent PolygonDimension
```

Zeigt an, dass die Polygon-Anmerkung als Maßangabe fungieren soll.

### PolyLineDimension {#PolyLineDimension}
```
public static final PolyIntent PolyLineDimension
```

Zeigt an, dass die Polylinien-Anmerkung als Maßangabe fungieren soll.

### Undefined {#Undefined}
```
public static final PolyIntent Undefined
```

Undefinierter Zustand.

### getByValue {#getByValue-int-}
```
public static PolyIntent getByValue(int value)
```



**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static PolyIntent [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
