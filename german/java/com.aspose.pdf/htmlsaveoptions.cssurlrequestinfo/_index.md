---
title: "HtmlSaveOptions.CssUrlRequestInfo"
linktitle: "HtmlSaveOptions.CssUrlRequestInfo"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Repräsentiert einen Datensatz, der sich auf die Anforderung vom Konverter an benutzerdefinierten Code bezieht, um die gewünschte URL (oder URL-Vorlage) des betreffenden CSS zu erhalten."
type: docs
weight: 2040
url: /de/java/com.aspose.pdf/htmlsaveoptions.cssurlrequestinfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.HtmlSaveOptions.CssUrlRequestInfo

```
public static class HtmlSaveOptions.CssUrlRequestInfo extends Object
```

Repräsentiert einen Datensatz, der sich auf die Anforderung vom Konverter an benutzerdefinierten Code bezieht, um die gewünschte URL (oder URL-Vorlage) des betreffenden CSS zu erhalten.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [CssUrlRequestInfo](#CssUrlRequestInfo--) | Erstellt eine Instanz von CssUrlRequestInfo |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [isCustomProcessingCancelled](#isCustomProcessingCancelled--) | Sollte von benutzerdefiniertem Code gesetzt werden, wenn er die URL, die im erzeugten HTML zur Referenzierung dieses CSS verwendet wird, nicht festlegen kann oder darf. Ist sie 'true', wird die CSS‑Datei auf standardmäßige Weise an einem standardmäßigen Ort gespeichert. |
| [setCustomProcessingCancelled](#setCustomProcessingCancelled-boolean-) | Sollte von benutzerdefiniertem Code gesetzt werden, wenn er die URL, die im erzeugten HTML zur Referenzierung dieses CSS verwendet wird, nicht festlegen kann oder darf. Ist sie 'true', wird die CSS‑Datei auf standardmäßige Weise an einem standardmäßigen Ort gespeichert. |

### CssUrlRequestInfo {#CssUrlRequestInfo--}
```
public CssUrlRequestInfo()
```

Erstellt eine Instanz von CssUrlRequestInfo

### isCustomProcessingCancelled {#isCustomProcessingCancelled--}
```
public boolean isCustomProcessingCancelled()
```

Sollte von benutzerdefiniertem Code gesetzt werden, wenn er die URL, die im erzeugten HTML zur Referenzierung dieses CSS verwendet wird, nicht festlegen kann oder darf. Ist sie 'true', wird die CSS‑Datei auf standardmäßige Weise an einem standardmäßigen Ort gespeichert.

**Returns:**
boolescher Wert

### setCustomProcessingCancelled {#setCustomProcessingCancelled-boolean-}
```
public void setCustomProcessingCancelled(boolean customProcessingCancelled)
```

Sollte von benutzerdefiniertem Code gesetzt werden, wenn er die URL, die im erzeugten HTML zur Referenzierung dieses CSS verwendet wird, nicht festlegen kann oder darf. Ist sie 'true', wird die CSS‑Datei auf standardmäßige Weise an einem standardmäßigen Ort gespeichert.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| customProcessingCancelled |  | boolescher Wert |
