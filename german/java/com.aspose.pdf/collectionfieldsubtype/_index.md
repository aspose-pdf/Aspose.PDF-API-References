---
title: "CollectionFieldSubtype"
linktitle: "CollectionFieldSubtype"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt den Subtyp‑Parameter eines Feldes in einer Schema‑Sammlung dar."
type: docs
weight: 630
url: /de/java/com.aspose.pdf/collectionfieldsubtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.Enum, com.aspose.pdf.CollectionFieldSubtype

```
public final class CollectionFieldSubtype extends com.aspose.ms.System.Enum
```

Stellt den Subtyp‑Parameter eines Feldes in einer Schema‑Sammlung dar.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [CompressedSize](#CompressedSize) | (PDF 2.0) Die Felddaten sind die Länge des eingebetteten Dateistreams, wie durch den Length-Eintrag im Wörterbuch des eingebetteten Dateistreams angegeben, und die beiden Werte müssen identisch sein. |
| [CreationDate](#CreationDate) | Die Felddaten müssen das Erstellungsdatum des eingebetteten Dateistreams sein, wie durch den CreationDate-Eintrag in der eingebetteten Datei angegeben. |
| [D](#D) | Ein Datumstyp. Die Felddaten müssen als PDF-Datumszeichenkette gespeichert werden. |
| [Desc](#Desc) | Die Felddaten müssen den Dateinamen des eingebetteten Dateistreams enthalten, wie durch den UF-Eintrag der Dateispezifikation angegeben, falls vorhanden; andernfalls durch den F-Eintrag der Dateispezifikation. |
| [F](#F) | Die Felddaten müssen den Dateinamen des eingebetteten Dateistreams enthalten, wie durch den UF-Eintrag der Dateispezifikation angegeben, falls vorhanden; andernfalls durch den F-Eintrag der Dateispezifikation. |
| [ModDate](#ModDate) | Die Felddaten müssen das Änderungsdatum des eingebetteten Dateistreams sein, wie durch den ModDate-Eintrag im Parameterwörterbuch der eingebetteten Datei angegeben. |
| [N](#N) | Ein Zahlentyp. Die Felddaten müssen als PDF-Nummer gespeichert werden. |
| [None](#None) | Der Subtyp ist nicht definiert. |
| [S](#S) | Ein Texttyp. Die Felddaten müssen als PDF-Textzeichenkette gespeichert werden. |
| [Size](#Size) | Die Felddaten sollen die Größe der eingebetteten Datei sein, wie durch den Size-Eintrag im Parameterwörterbuch der eingebetteten Datei angegeben. |

### CompressedSize {#CompressedSize}
```
public static final int CompressedSize
```

(PDF 2.0) Die Felddaten sind die Länge des eingebetteten Dateistreams, wie durch den Length-Eintrag im Wörterbuch des eingebetteten Dateistreams angegeben, und die beiden Werte müssen identisch sein.

### CreationDate {#CreationDate}
```
public static final int CreationDate
```

Die Felddaten müssen das Erstellungsdatum des eingebetteten Dateistreams sein, wie durch den CreationDate-Eintrag in der eingebetteten Datei angegeben.

### D {#D}
```
public static final int D
```

Ein Datumstyp. Die Felddaten müssen als PDF-Datumszeichenkette gespeichert werden.

### Desc {#Desc}
```
public static final int Desc
```

Die Felddaten müssen den Dateinamen des eingebetteten Dateistreams enthalten, wie durch den UF-Eintrag der Dateispezifikation angegeben, falls vorhanden; andernfalls durch den F-Eintrag der Dateispezifikation.

### F {#F}
```
public static final int F
```

Die Felddaten müssen den Dateinamen des eingebetteten Dateistreams enthalten, wie durch den UF-Eintrag der Dateispezifikation angegeben, falls vorhanden; andernfalls durch den F-Eintrag der Dateispezifikation.

### ModDate {#ModDate}
```
public static final int ModDate
```

Die Felddaten müssen das Änderungsdatum des eingebetteten Dateistreams sein, wie durch den ModDate-Eintrag im Parameterwörterbuch der eingebetteten Datei angegeben.

### N {#N}
```
public static final int N
```

Ein Zahlentyp. Die Felddaten müssen als PDF-Nummer gespeichert werden.

### None {#None}
```
public static final int None
```

Der Subtyp ist nicht definiert.

### S {#S}
```
public static final int S
```

Ein Texttyp. Die Felddaten müssen als PDF-Textzeichenkette gespeichert werden.

### Size {#Size}
```
public static final int Size
```

Die Felddaten sollen die Größe der eingebetteten Datei sein, wie durch den Size-Eintrag im Parameterwörterbuch der eingebetteten Datei angegeben.
