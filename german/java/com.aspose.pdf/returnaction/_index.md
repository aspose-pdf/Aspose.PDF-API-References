---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Enum stellte eine Programmarbeitsablauf-Aktion dar, falls die Methode {@code IWarningCallback.Warning(WarningInfo)} aufgerufen wird."
type: docs
weight: 4240
url: /de/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

Enum stellte eine Programmarbeitsablauf-Aktion dar, falls die Methode {@code IWarningCallback.Warning(WarningInfo)} aufgerufen wird.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Abort](#Abort) | Wird zum Abbrechen des Ablaufs verwendet. |
| [Continue](#Continue) | Wird zum Fortsetzen des Ablaufs verwendet. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

Wird zum Abbrechen des Ablaufs verwendet.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

Wird zum Fortsetzen des Ablaufs verwendet.

### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static ReturnAction [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
