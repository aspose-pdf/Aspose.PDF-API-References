---
title: "TextAlignment"
linktitle: "TextAlignment"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Ausrichtung des Textes in der Anmerkung."
type: docs
weight: 4910
url: /de/java/com.aspose.pdf/textalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextAlignment > com.aspose.pdf.TextAlignment, java.lang.Enum < TextAlignment >, com.aspose.pdf.TextAlignment

**All Implemented Interfaces:**
Serializable, Comparable < TextAlignment >

```
public enum TextAlignment extends Enum < TextAlignment >
```

Ausrichtung des Textes in der Anmerkung.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Center](#Center) | Text ist zentriert. |
| [Left](#Left) | Text ist linksbündig ausgerichtet. |
| [Right](#Right) | Text ist rechtsbündig ausgerichtet. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Center {#Center}
```
public static final TextAlignment Center
```

Text ist zentriert.

### Left {#Left}
```
public static final TextAlignment Left
```

Text ist linksbündig ausgerichtet.

### Right {#Right}
```
public static final TextAlignment Right
```

Text ist rechtsbündig ausgerichtet.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static TextAlignment [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
