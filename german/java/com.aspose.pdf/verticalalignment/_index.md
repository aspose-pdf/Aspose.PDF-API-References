---
title: "VerticalAlignment"
linktitle: "VerticalAlignment"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Aufzählung möglicher vertikaler Ausrichtungswerte."
type: docs
weight: 5450
url: /de/java/com.aspose.pdf/verticalalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < VerticalAlignment > com.aspose.pdf.VerticalAlignment, java.lang.Enum < VerticalAlignment >, com.aspose.pdf.VerticalAlignment

**All Implemented Interfaces:**
Serializable, Comparable < VerticalAlignment >

```
public enum VerticalAlignment extends Enum < VerticalAlignment >
```

Aufzählung möglicher vertikaler Ausrichtungswerte.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Bottom](#Bottom) | Ausrichten nach unten. |
| [Center](#Center) | Zentrierte Ausrichtung. |
| [None](#None) | Keine Ausrichtung (verwenden Sie die angegebene Position). |
| [Top](#Top) | Ausrichten nach oben. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) | Anzahl erhalten von |
| [valueOf](#valueOf-int-) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Bottom {#Bottom}
```
public static final VerticalAlignment Bottom
```

Ausrichten nach unten.

### Center {#Center}
```
public static final VerticalAlignment Center
```

Zentrierte Ausrichtung.

### None {#None}
```
public static final VerticalAlignment None
```

Keine Ausrichtung (verwenden Sie die angegebene Position).

### Top {#Top}
```
public static final VerticalAlignment Top
```

Ausrichten nach oben.

### getValue {#getValue--}
```
public int getValue()
```

Anzahl erhalten von

**Returns:**
int-Wert des VerticalAlignment-Elements

### valueOf {#valueOf-int-}
```
public static VerticalAlignment valueOf(int alignmentType)
```



**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| alignmentType |  |  |

### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static VerticalAlignment [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
