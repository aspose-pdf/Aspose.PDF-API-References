---
title: "TeXFragment"
linktitle: "TeXFragment"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt ein LaTeX‑Fragment dar."
type: docs
weight: 4860
url: /de/java/com.aspose.pdf/texfragment/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.BaseParagraph com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.BaseParagraph, com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.FormattedFragment, com.aspose.pdf.TeXFragment

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public class TeXFragment extends FormattedFragment
```

Stellt ein LaTeX‑Fragment dar.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [TeXFragment](#TeXFragment-java.lang.String-) | Initialisiert eine neue Instanz der HtmlFragment-Klasse. |
| [TeXFragment](#TeXFragment-java.lang.String-boolean-) | Initialisiert eine neue Instanz der HtmlFragment-Klasse. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [deepClone](#deepClone--) | Klont Fragment. |
| [getLatexLoadOptionsOfInstance](#getLatexLoadOptionsOfInstance--) | Ruft die TeXLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet. |
| [getTeXLoadOptionsOfInstance](#getTeXLoadOptionsOfInstance--) | Ruft die TeXLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet. |
| [setLatexLoadOptionsOfInstance](#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | Ruft die TeXLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet. |
| [setTeXLoadOptionsOfInstance](#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | Ruft die LatexLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet. |

### TeXFragment {#TeXFragment-java.lang.String-}
Initialisiert eine neue Instanz der HtmlFragment-Klasse.

### TeXFragment {#TeXFragment-java.lang.String-boolean-}
Initialisiert eine neue Instanz der HtmlFragment-Klasse.

### deepClone {#deepClone--}
```
public Object deepClone()
```

Klont Fragment.

**Returns:**
Fragment geklont.

### getLatexLoadOptionsOfInstance {#getLatexLoadOptionsOfInstance--}
```
@Deprecated public final TeXLoadOptions getLatexLoadOptionsOfInstance()
```

Ruft die TeXLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet.

**Returns:**
TeXLoadOptions-Instanz @deprecated Bitte verwenden Sie TeXLoadOptionsOfInstance stattdessen.

### getTeXLoadOptionsOfInstance {#getTeXLoadOptionsOfInstance--}
```
public TeXLoadOptions getTeXLoadOptionsOfInstance()
```

Ruft die TeXLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet.

**Returns:**
TeXLoadOptions-Instanz

### setLatexLoadOptionsOfInstance {#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
Ruft die TeXLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet.

### setTeXLoadOptionsOfInstance {#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
Ruft die LatexLoadOptions ab oder legt sie fest, die zum Laden (und Rendern) von LaTeX in diese Klasseninstanz verwendet werden. Bitte verwenden Sie sie, wenn es notwendig ist, eine spezifische Einstellung für den Import von LaTeX für diese oder jene Instanz zu nutzen (z. B. wenn diese oder jene Instanz einen bestimmten BasePath für importiertes LaTeX verwenden soll oder einen bestimmten Loader für externe Ressourcen verwenden soll). Wenn der Parameter standardmäßig (null) ist, werden die Standard‑LaTeX‑Ladeoptionen verwendet.
