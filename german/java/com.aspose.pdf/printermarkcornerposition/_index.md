---
title: "PrinterMarkCornerPosition"
linktitle: "PrinterMarkCornerPosition"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt eine Position einer Markierung in einer Ecke einer Seite dar."
type: docs
weight: 4000
url: /de/java/com.aspose.pdf/printermarkcornerposition/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PrinterMarkCornerPosition > com.aspose.pdf.PrinterMarkCornerPosition, java.lang.Enum < PrinterMarkCornerPosition >, com.aspose.pdf.PrinterMarkCornerPosition

**All Implemented Interfaces:**
Serializable, Comparable < PrinterMarkCornerPosition >

```
public enum PrinterMarkCornerPosition extends Enum < PrinterMarkCornerPosition >
```

Stellt eine Position einer Markierung in einer Ecke einer Seite dar.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [BottomLeft](#BottomLeft) | Positionieren Sie das Markierungszeichen in der unteren linken Ecke. |
| [BottomRight](#BottomRight) | Positionieren Sie das Markierungszeichen in der unteren rechten Ecke. |
| [TopLeft](#TopLeft) | Positionieren Sie das Markierungszeichen in der oberen linken Ecke. |
| [TopRight](#TopRight) | Positionieren Sie das Markierungszeichen in der oberen rechten Ecke. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### BottomLeft {#BottomLeft}
```
public static final PrinterMarkCornerPosition BottomLeft
```

Positionieren Sie das Markierungszeichen in der unteren linken Ecke.

### BottomRight {#BottomRight}
```
public static final PrinterMarkCornerPosition BottomRight
```

Positionieren Sie das Markierungszeichen in der unteren rechten Ecke.

### TopLeft {#TopLeft}
```
public static final PrinterMarkCornerPosition TopLeft
```

Positionieren Sie das Markierungszeichen in der oberen linken Ecke.

### TopRight {#TopRight}
```
public static final PrinterMarkCornerPosition TopRight
```

Positionieren Sie das Markierungszeichen in der oberen rechten Ecke.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static PrinterMarkCornerPosition [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
