---
title: "MemoryCleaner"
linktitle: "MemoryCleaner"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Repräsentiert die Klasse MemoryCleaner."
type: docs
weight: 3010
url: /de/java/com.aspose.pdf/memorycleaner/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MemoryCleaner

```
public class MemoryCleaner extends Object
```

Repräsentiert die Klasse MemoryCleaner.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [MemoryCleaner](#MemoryCleaner--) |  |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [clear](#clear--) | Löscht Heap‑Speicher von statischen Aspose.PDF‑Instanzen (statisch und threadLocal) und entfernt alle temporären Dateien. |
| [clearAllTempFiles](#clearAllTempFiles--) | Löscht temporäre Dateien, die von Aspose.PDF im System‑Temp‑Verzeichnis erstellt wurden. |
| [clearCurrentThreadLocals](#clearCurrentThreadLocals--) | Löscht sicher den Heap‑Speicher von ThreadLocal‑Instanzen für den aktuellen Thread. |
| [clearFontRegistry](#clearFontRegistry--) | Löscht FontRegistry‑Informationen aus dem Heap‑Speicher. |
| [clearKeepingFontSettings](#clearKeepingFontSettings--) | Löscht Heap‑Speicher von statischen Aspose.PDF‑Instanzen (statisch und threadLocal) und entfernt alle temporären Dateien. |
| [clearLicense](#clearLicense--) | Löscht Lizenzinformationen aus dem Heap‑Speicher. |
| [clearStaticInstances](#clearStaticInstances--) | Löscht Heap‑Speicher von statischen Aspose.PDF‑Instanzen. |

### MemoryCleaner {#MemoryCleaner--}
```
public MemoryCleaner()
```



### clear {#clear--}
```
public static void clear()
```

Löscht Heap‑Speicher von statischen Aspose.PDF‑Instanzen (statisch und threadLocal) und entfernt alle temporären Dateien.

### clearAllTempFiles {#clearAllTempFiles--}
```
public static void clearAllTempFiles()
```

Löscht temporäre Dateien, die von Aspose.PDF im System‑Temp‑Verzeichnis erstellt wurden.

### clearCurrentThreadLocals {#clearCurrentThreadLocals--}
```
public static void clearCurrentThreadLocals()
```

Löscht sicher den Heap‑Speicher von ThreadLocal‑Instanzen für den aktuellen Thread.

### clearFontRegistry {#clearFontRegistry--}
```
public static void clearFontRegistry()
```

Löscht FontRegistry‑Informationen aus dem Heap‑Speicher.

### clearKeepingFontSettings {#clearKeepingFontSettings--}
```
public static void clearKeepingFontSettings()
```

Löscht Heap‑Speicher von statischen Aspose.PDF‑Instanzen (statisch und threadLocal) und entfernt alle temporären Dateien.

### clearLicense {#clearLicense--}
```
public static void clearLicense()
```

Löscht Lizenzinformationen aus dem Heap‑Speicher.

### clearStaticInstances {#clearStaticInstances--}
```
public static void clearStaticInstances()
```

Löscht Heap‑Speicher von statischen Aspose.PDF‑Instanzen.
