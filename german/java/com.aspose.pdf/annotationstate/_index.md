---
title: "AnnotationState"
linktitle: "AnnotationState"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Die Aufzählung der Zustände, auf die die ursprüngliche Anmerkung gesetzt werden kann."
type: docs
weight: 110
url: /de/java/com.aspose.pdf/annotationstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationState > com.aspose.pdf.AnnotationState, java.lang.Enum < AnnotationState >, com.aspose.pdf.AnnotationState

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationState >

```
public enum AnnotationState extends Enum < AnnotationState >
```

Die Aufzählung der Zustände, auf die die ursprüngliche Anmerkung gesetzt werden kann.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Accepted](#Accepted) | Der Benutzer stimmt der Änderung zu. |
| [Cancelled](#Cancelled) | Die Änderung wurde abgebrochen. |
| [Completed](#Completed) | Die Änderung wurde abgeschlossen. |
| [Marked](#Marked) | Die Anmerkung wurde vom Benutzer markiert. |
| [None](#None) | Der Benutzer hat nichts zur Änderung angegeben. |
| [Rejected](#Rejected) | Der Benutzer ist mit der Änderung nicht einverstanden. |
| [Undefined](#Undefined) | Nicht definierter Zustand. |
| [Unmarked](#Unmarked) | Die Anmerkung wurde vom Benutzer nicht markiert. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Accepted {#Accepted}
```
public static final AnnotationState Accepted
```

Der Benutzer stimmt der Änderung zu.

### Cancelled {#Cancelled}
```
public static final AnnotationState Cancelled
```

Die Änderung wurde abgebrochen.

### Completed {#Completed}
```
public static final AnnotationState Completed
```

Die Änderung wurde abgeschlossen.

### Marked {#Marked}
```
public static final AnnotationState Marked
```

Die Anmerkung wurde vom Benutzer markiert.

### None {#None}
```
public static final AnnotationState None
```

Der Benutzer hat nichts zur Änderung angegeben.

### Rejected {#Rejected}
```
public static final AnnotationState Rejected
```

Der Benutzer ist mit der Änderung nicht einverstanden.

### Undefined {#Undefined}
```
public static final AnnotationState Undefined
```

Nicht definierter Zustand.

### Unmarked {#Unmarked}
```
public static final AnnotationState Unmarked
```

Die Anmerkung wurde vom Benutzer nicht markiert.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static AnnotationState [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
