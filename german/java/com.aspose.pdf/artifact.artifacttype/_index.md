---
title: "Artifact.ArtifactType"
linktitle: "Artifact.ArtifactType"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Aufzählung möglicher Artefakt‑Typen."
type: docs
weight: 210
url: /de/java/com.aspose.pdf/artifact.artifacttype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < Artifact.ArtifactType > com.aspose.pdf.Artifact.ArtifactType, java.lang.Enum < Artifact.ArtifactType >, com.aspose.pdf.Artifact.ArtifactType

**All Implemented Interfaces:**
Serializable, Comparable < Artifact.ArtifactType >

```
public static enum Artifact.ArtifactType extends Enum < Artifact.ArtifactType >
```

Aufzählung möglicher Artefakt‑Typen.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Background](#Background) | Hintergrund-Artefakte. |
| [Layout](#Layout) | Layout-Artefakte. |
| [Page](#Page) | Seiten-Artefakte. |
| [Pagination](#Pagination) | Paginierungsartefakte. |
| [Undefined](#Undefined) | Der Artefakttyp ist nicht definiert oder unbekannt. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Background {#Background}
```
public static final Artifact.ArtifactType Background
```

Hintergrund-Artefakte.

### Layout {#Layout}
```
public static final Artifact.ArtifactType Layout
```

Layout-Artefakte.

### Page {#Page}
```
public static final Artifact.ArtifactType Page
```

Seiten-Artefakte.

### Pagination {#Pagination}
```
public static final Artifact.ArtifactType Pagination
```

Paginierungsartefakte.

### Undefined {#Undefined}
```
public static final Artifact.ArtifactType Undefined
```

Der Artefakttyp ist nicht definiert oder unbekannt.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static Artifact.ArtifactType [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
