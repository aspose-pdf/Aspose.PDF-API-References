---
title: "Zeilen"
linktitle: "Zeilen"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt eine Zeilensammlung einer Tabelle dar."
type: docs
weight: 4340
url: /de/java/com.aspose.pdf/rows/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.Rows

**All Implemented Interfaces:**
Iterable < Row >

```
public final class Rows extends Object implements Iterable < Row >
```

Stellt eine Zeilensammlung einer Tabelle dar.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [Rows](#Rows--) |  |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [add](#add--) | Zeile zur Sammlung hinzufügen. |
| [add](#add-com.aspose.pdf.Row-) | Zeile zur Sammlung hinzufügen. |
| [get_Item](#get_Item-int-) | Liefert die Zeile. |
| [getCount](#getCount--) | Die Elementanzahl. |
| [indexOf](#indexOf-com.aspose.pdf.Row-) | Gibt den Index der Zeile in der Sammlung zurück. |
| [iterator_Rename_Namesake](#iterator_Rename_Namesake--) | ReservedForInternalUse <p> Nur für interne Verwendung. |
| [iterator](#iterator--) | Liefert den Enumerator der Sammlung. |
| [remove](#remove-com.aspose.pdf.Row-) | Entfernt die Zeile aus der Sammlung. |
| [removeAt](#removeAt-int-) | Entfernt die Zeile an der Position aus der Sammlung. |
| [removeRange](#removeRange-int-int-) | Entfernt das Zeilenset aus der Sammlung. |
| [set_Item](#set_Item-int-com.aspose.pdf.Row-) | Setzt die Zeile. |

### Rows {#Rows--}
```
public Rows()
```



### add {#add--}
```
public Row add()
```

Zeile zur Sammlung hinzufügen.

**Returns:**
Die neue Zeile

### add {#add-com.aspose.pdf.Row-}
Zeile zur Sammlung hinzufügen.

**Returns:**
Die neue Zeile

### get_Item {#get_Item-int-}
```
public Row get_Item(int index)
```

Liefert die Zeile.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| index |  | Der Zeilenindex. |

**Returns:**
Zeilenobjekt

### getCount {#getCount--}
```
public int getCount()
```

Die Elementanzahl.

**Returns:**
int-Wert

### indexOf {#indexOf-com.aspose.pdf.Row-}
Gibt den Index der Zeile in der Sammlung zurück.

### iterator_Rename_Namesake {#iterator_Rename_Namesake--}
```
public com.aspose.ms.System.Collections.IEnumerator iterator_Rename_Namesake()
```

ReservedForInternalUse <p> Nur für interne Verwendung.

**Returns:**
IEnumerator-Objekt

### iterator {#iterator--}
```
public Iterator < Row > iterator()
```

Liefert den Enumerator der Sammlung.

**Returns:**
Iterator von Row-Instanzen

### remove {#remove-com.aspose.pdf.Row-}
Entfernt die Zeile aus der Sammlung.

### removeAt {#removeAt-int-}
```
public void removeAt(int index)
```

Entfernt die Zeile an der Position aus der Sammlung.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| index |  | Der Sammlungsindex. |

### removeRange {#removeRange-int-int-}
```
public void removeRange(int index, int count)
```

Entfernt das Zeilenset aus der Sammlung.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| index |  | Der Sammlungsindex. |
| Anzahl |  | Die Zeilenanzahl. |

### set_Item {#set_Item-int-com.aspose.pdf.Row-}
Setzt die Zeile.
