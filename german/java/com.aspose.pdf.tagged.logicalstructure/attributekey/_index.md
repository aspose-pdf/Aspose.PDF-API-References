---
title: "AttributeKey"
linktitle: "AttributeKey"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt Standard-Attributschlüssel dar."
type: docs
weight: 10
url: /de/java/com.aspose.pdf.tagged.logicalstructure/attributekey/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.logicalstructure.AttributeKey

```
public final class AttributeKey extends Object
```

Stellt Standard-Attributschlüssel dar.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [BackgroundColor](#BackgroundColor) | BackgroundColor-Attribut (Layout-Attributbesitzer). |
| [BaselineShift](#BaselineShift) | BaselineShift-Attribut (Layout-Attributbesitzer). |
| [BBox](#BBox) | BBox-Attribut (Layout-Attributbesitzer). |
| [BlockAlign](#BlockAlign) | BlockAlign-Attribut (Layout-Attributbesitzer). |
| [BorderColor](#BorderColor) | BorderColor-Attribut (Layout-Attributbesitzer). |
| [BorderStyle](#BorderStyle) | BorderStyle-Attribut (Layout-Attributbesitzer). |
| [BorderThickness](#BorderThickness) | BorderThickness-Attribut (Layout-Attributbesitzer). |
| [Checked](#Checked) | Checked-Attribut (PrintField-Attributbesitzer). |
| [Color](#Color) | Color-Attribut (Layout-Attributbesitzer). |
| [ColSpan](#ColSpan) | ColSpan-Attribut (Table-Attributbesitzer). |
| [ColumnCount](#ColumnCount) | ColumnCount-Attribut (Layout-Attributbesitzer). |
| [ColumnGap](#ColumnGap) | ColumnGap-Attribut (Layout-Attributbesitzer). |
| [ColumnWidths](#ColumnWidths) | ColumnWidths-Attribut (Layout-Attributbesitzer). |
| [Desc](#Desc) | Desc-Attribut (PrintField-Attributbesitzer). |
| [EndIndent](#EndIndent) | EndIndent-Attribut (Layout-Attributbesitzer). |
| [GlyphOrientationVertical](#GlyphOrientationVertical) | GlyphOrientationVertical-Attribut (Layout-Attributbesitzer). |
| [Headers](#Headers) | Headers-Attribut (Table-Attributbesitzer). |
| [Height](#Height) | Height Attribut (Layout Attribut Eigentümer). |
| [InlineAlign](#InlineAlign) | InlineAlign Attribut (Layout Attribut Eigentümer). |
| [LineHeight](#LineHeight) | LineHeight Attribut (Layout Attribut Eigentümer). |
| [ListNumbering](#ListNumbering) | ListNumbering Attribut (List Attribut Eigentümer). |
| [Padding](#Padding) | Padding Attribut (Layout Attribut Eigentümer). |
| [Placement](#Placement) | Placement Attribut (Layout Attribut Eigentümer). |
| [Role](#Role) | Role Attribut (PrintField Attribut Eigentümer). |
| [RowSpan](#RowSpan) | RowSpan Attribut (Table Attribut Eigentümer). |
| [RubyAlign](#RubyAlign) | RubyAlign Attribut (Layout Attribut Eigentümer). |
| [RubyPosition](#RubyPosition) | RubyPosition Attribut (Layout Attribut Eigentümer). |
| [Scope](#Scope) | Scope Attribut (Table Attribut Eigentümer). |
| [SpaceAfter](#SpaceAfter) | SpaceAfter Attribut (Layout Attribut Eigentümer). |
| [SpaceBefore](#SpaceBefore) | SpaceBefore Attribut (Layout Attribut Eigentümer). |
| [StartIndent](#StartIndent) | StartIndent Attribut (Layout Attribut Eigentümer). |
| [Summary](#Summary) | Summary Attribut (Table Attribut Eigentümer). |
| [TBorderStyle](#TBorderStyle) | TBorderStyle Attribut (Layout Attribut Eigentümer). |
| [TextAlign](#TextAlign) | TextAlign Attribut (Layout Attribut Eigentümer). |
| [TextDecorationColor](#TextDecorationColor) | TextDecorationColor Attribut (Layout Attribut Eigentümer). |
| [TextDecorationThickness](#TextDecorationThickness) | TextDecorationThickness Attribut (Layout Attribut Eigentümer). |
| [TextDecorationType](#TextDecorationType) | TextDecorationType Attribut (Layout Attribut Eigentümer). |
| [TextIndent](#TextIndent) | TextIndent Attribut (Layout Attribut Eigentümer). |
| [TPadding](#TPadding) | TPadding Attribut (Layout Attribut Eigentümer). |
| [Width](#Width) | Width Attribut (Layout Attribut Eigentümer). |
| [WritingMode](#WritingMode) | WritingMode Attribut (Layout Attribut Eigentümer). |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getKey](#getKey--) | Abrufen Attributschlüssel. Wert: Attributschlüssel. |
| [getOwner](#getOwner--) | Attribute Owner abrufen. Wert: Attribute Owner. |
| [hashCode](#hashCode--) |  |
| [to_AttributeKey](#to_AttributeKey-java.lang.String-) | Führt eine explizite Konvertierung von {@link String} zu {@link AttributeKey} durch. |
| [toString](#toString--) | Gibt einen String zurück, der das aktuelle Objekt darstellt. |

### BackgroundColor {#BackgroundColor}
```
public static final AttributeKey BackgroundColor
```

BackgroundColor-Attribut (Layout-Attributbesitzer).

### BaselineShift {#BaselineShift}
```
public static final AttributeKey BaselineShift
```

BaselineShift-Attribut (Layout-Attributbesitzer).

### BBox {#BBox}
```
public static final AttributeKey BBox
```

BBox-Attribut (Layout-Attributbesitzer).

### BlockAlign {#BlockAlign}
```
public static final AttributeKey BlockAlign
```

BlockAlign-Attribut (Layout-Attributbesitzer).

### BorderColor {#BorderColor}
```
public static final AttributeKey BorderColor
```

BorderColor-Attribut (Layout-Attributbesitzer).

### BorderStyle {#BorderStyle}
```
public static final AttributeKey BorderStyle
```

BorderStyle-Attribut (Layout-Attributbesitzer).

### BorderThickness {#BorderThickness}
```
public static final AttributeKey BorderThickness
```

BorderThickness-Attribut (Layout-Attributbesitzer).

### Checked {#Checked}
```
public static final AttributeKey Checked
```

Checked-Attribut (PrintField-Attributbesitzer).

### Color {#Color}
```
public static final AttributeKey Color
```

Color-Attribut (Layout-Attributbesitzer).

### ColSpan {#ColSpan}
```
public static final AttributeKey ColSpan
```

ColSpan-Attribut (Table-Attributbesitzer).

### ColumnCount {#ColumnCount}
```
public static final AttributeKey ColumnCount
```

ColumnCount-Attribut (Layout-Attributbesitzer).

### ColumnGap {#ColumnGap}
```
public static final AttributeKey ColumnGap
```

ColumnGap-Attribut (Layout-Attributbesitzer).

### ColumnWidths {#ColumnWidths}
```
public static final AttributeKey ColumnWidths
```

ColumnWidths-Attribut (Layout-Attributbesitzer).

### Desc {#Desc}
```
public static final AttributeKey Desc
```

Desc-Attribut (PrintField-Attributbesitzer).

### EndIndent {#EndIndent}
```
public static final AttributeKey EndIndent
```

EndIndent-Attribut (Layout-Attributbesitzer).

### GlyphOrientationVertical {#GlyphOrientationVertical}
```
public static final AttributeKey GlyphOrientationVertical
```

GlyphOrientationVertical-Attribut (Layout-Attributbesitzer).

### Headers {#Headers}
```
public static final AttributeKey Headers
```

Headers-Attribut (Table-Attributbesitzer).

### Height {#Height}
```
public static final AttributeKey Height
```

Height Attribut (Layout Attribut Eigentümer).

### InlineAlign {#InlineAlign}
```
public static final AttributeKey InlineAlign
```

InlineAlign Attribut (Layout Attribut Eigentümer).

### LineHeight {#LineHeight}
```
public static final AttributeKey LineHeight
```

LineHeight Attribut (Layout Attribut Eigentümer).

### ListNumbering {#ListNumbering}
```
public static final AttributeKey ListNumbering
```

ListNumbering Attribut (List Attribut Eigentümer).

### Padding {#Padding}
```
public static final AttributeKey Padding
```

Padding Attribut (Layout Attribut Eigentümer).

### Placement {#Placement}
```
public static final AttributeKey Placement
```

Placement Attribut (Layout Attribut Eigentümer).

### Role {#Role}
```
public static final AttributeKey Role
```

Role Attribut (PrintField Attribut Eigentümer).

### RowSpan {#RowSpan}
```
public static final AttributeKey RowSpan
```

RowSpan Attribut (Table Attribut Eigentümer).

### RubyAlign {#RubyAlign}
```
public static final AttributeKey RubyAlign
```

RubyAlign Attribut (Layout Attribut Eigentümer).

### RubyPosition {#RubyPosition}
```
public static final AttributeKey RubyPosition
```

RubyPosition Attribut (Layout Attribut Eigentümer).

### Scope {#Scope}
```
public static final AttributeKey Scope
```

Scope Attribut (Table Attribut Eigentümer).

### SpaceAfter {#SpaceAfter}
```
public static final AttributeKey SpaceAfter
```

SpaceAfter Attribut (Layout Attribut Eigentümer).

### SpaceBefore {#SpaceBefore}
```
public static final AttributeKey SpaceBefore
```

SpaceBefore Attribut (Layout Attribut Eigentümer).

### StartIndent {#StartIndent}
```
public static final AttributeKey StartIndent
```

StartIndent Attribut (Layout Attribut Eigentümer).

### Summary {#Summary}
```
public static final AttributeKey Summary
```

Summary Attribut (Table Attribut Eigentümer).

### TBorderStyle {#TBorderStyle}
```
public static final AttributeKey TBorderStyle
```

TBorderStyle Attribut (Layout Attribut Eigentümer).

### TextAlign {#TextAlign}
```
public static final AttributeKey TextAlign
```

TextAlign Attribut (Layout Attribut Eigentümer).

### TextDecorationColor {#TextDecorationColor}
```
public static final AttributeKey TextDecorationColor
```

TextDecorationColor Attribut (Layout Attribut Eigentümer).

### TextDecorationThickness {#TextDecorationThickness}
```
public static final AttributeKey TextDecorationThickness
```

TextDecorationThickness Attribut (Layout Attribut Eigentümer).

### TextDecorationType {#TextDecorationType}
```
public static final AttributeKey TextDecorationType
```

TextDecorationType Attribut (Layout Attribut Eigentümer).

### TextIndent {#TextIndent}
```
public static final AttributeKey TextIndent
```

TextIndent Attribut (Layout Attribut Eigentümer).

### TPadding {#TPadding}
```
public static final AttributeKey TPadding
```

TPadding Attribut (Layout Attribut Eigentümer).

### Width {#Width}
```
public static final AttributeKey Width
```

Width Attribut (Layout Attribut Eigentümer).

### WritingMode {#WritingMode}
```
public static final AttributeKey WritingMode
```

WritingMode Attribut (Layout Attribut Eigentümer).

### getKey {#getKey--}
```
public final String getKey()
```

Abrufen Attributschlüssel. Wert: Attributschlüssel.

**Returns:**
String Wert

### getOwner {#getOwner--}
```
public final AttributeOwnerStandard getOwner()
```

Attribute Owner abrufen. Wert: Attribute Owner.

**Returns:**
AttributeKey-Instanz

### hashCode {#hashCode--}
```
public int hashCode()
```



### to_AttributeKey {#to_AttributeKey-java.lang.String-}
Führt eine explizite Konvertierung von {@link String} zu {@link AttributeKey} durch.

### toString {#toString--}
```
public String toString()
```

Gibt einen String zurück, der das aktuelle Objekt darstellt.

**Returns:**
String, der das aktuelle Objekt darstellt.
