---
title: "StructureTypeCategory"
linktitle: "StructureTypeCategory"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt Kategorien von Standard-Strukturtypen dar."
type: docs
weight: 120
url: /de/java/com.aspose.pdf.tagged.logicalstructure/structuretypecategory/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.logicalstructure.StructureTypeCategory

```
public final class StructureTypeCategory extends Object
```

Stellt Kategorien von Standard-Strukturtypen dar.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [BLSEs](#BLSEs) | Block-Level-Strukturelemente (BLSEs) beschreiben das Gesamtlayout des Inhalts auf der Seite und verlaufen in Block-Progressions-Richtung. |
| [GroupingElements](#GroupingElements) | Gruppierungselemente gruppieren andere Elemente zu Sequenzen oder Hierarchien, enthalten jedoch keinen Inhalt direkt und haben keinen direkten Einfluss auf das Layout. |
| [IllustrationElements](#IllustrationElements) | Illustrationselemente sind kompakte Sequenzen von Inhalt, in Seiteninhaltsreihenfolge, die als einheitliche Objekte in Bezug auf das Seitenlayout betrachtet werden. Eine Illustration kann entweder als BLSE oder als ILSE behandelt werden. |
| [ILSEs](#ILSEs) | Inline-Level-Strukturelemente (ILSEs) beschreiben das Layout des Inhalts innerhalb eines BLSE und verlaufen in Inline-Progressions-Richtung. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [to_StructureTypeCategory](#to_StructureTypeCategory-java.lang.String-) | Führt eine explizite Konvertierung von {@link String} zu {@link StructureTypeCategory} durch. |
| [toString](#toString--) | Gibt einen String zurück, der das aktuelle Objekt darstellt. |

### BLSEs {#BLSEs}
```
public static final StructureTypeCategory BLSEs
```

Block-Level-Strukturelemente (BLSEs) beschreiben das Gesamtlayout des Inhalts auf der Seite und verlaufen in Block-Progressions-Richtung.

### GroupingElements {#GroupingElements}
```
public static final StructureTypeCategory GroupingElements
```

Gruppierungselemente gruppieren andere Elemente zu Sequenzen oder Hierarchien, enthalten jedoch keinen Inhalt direkt und haben keinen direkten Einfluss auf das Layout.

### IllustrationElements {#IllustrationElements}
```
public static final StructureTypeCategory IllustrationElements
```

Illustrationselemente sind kompakte Sequenzen von Inhalt, in Seiteninhaltsreihenfolge, die als einheitliche Objekte in Bezug auf das Seitenlayout betrachtet werden. Eine Illustration kann entweder als BLSE oder als ILSE behandelt werden.

### ILSEs {#ILSEs}
```
public static final StructureTypeCategory ILSEs
```

Inline-Level-Strukturelemente (ILSEs) beschreiben das Layout des Inhalts innerhalb eines BLSE und verlaufen in Inline-Progressions-Richtung.

### to_StructureTypeCategory {#to_StructureTypeCategory-java.lang.String-}
Führt eine explizite Konvertierung von {@link String} zu {@link StructureTypeCategory} durch.

### toString {#toString--}
```
public String toString()
```

Gibt einen String zurück, der das aktuelle Objekt darstellt.

**Returns:**
String, der das aktuelle Objekt darstellt.
