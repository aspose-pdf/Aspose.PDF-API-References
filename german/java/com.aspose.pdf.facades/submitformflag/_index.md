---
title: "SubmitFormFlag"
linktitle: "SubmitFormFlag"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Aufzählung möglicher Submit-Formular-Flags."
type: docs
weight: 730
url: /de/java/com.aspose.pdf.facades/submitformflag/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < SubmitFormFlag > com.aspose.pdf.facades.SubmitFormFlag, java.lang.Enum < SubmitFormFlag >, com.aspose.pdf.facades.SubmitFormFlag

**All Implemented Interfaces:**
Serializable, Comparable < SubmitFormFlag >

```
public enum SubmitFormFlag extends Enum < SubmitFormFlag >
```

Aufzählung möglicher Submit-Formular-Flags.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Fdf](#Fdf) | Daten werden im FDF-Format zurückgegeben. |
| [FdfWithComments](#FdfWithComments) | Daten werden im FDF-Format mit Kommentaren zurückgegeben. |
| [Html](#Html) | Daten werden im HTML-Format zurückgegeben. |
| [Pdf](#Pdf) | Die gesamte PDF-Datei wird übermittelt. |
| [Xfdf](#Xfdf) | Daten werden im XFDF-Format zurückgegeben. |
| [XfdfWithComments](#XfdfWithComments) | Daten werden im XFDF-Format mit Kommentaren zurückgegeben. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Fdf {#Fdf}
```
public static final SubmitFormFlag Fdf
```

Daten werden im FDF-Format zurückgegeben.

### FdfWithComments {#FdfWithComments}
```
public static final SubmitFormFlag FdfWithComments
```

Daten werden im FDF-Format mit Kommentaren zurückgegeben.

### Html {#Html}
```
public static final SubmitFormFlag Html
```

Daten werden im HTML-Format zurückgegeben.

### Pdf {#Pdf}
```
public static final SubmitFormFlag Pdf
```

Die gesamte PDF-Datei wird übermittelt.

### Xfdf {#Xfdf}
```
public static final SubmitFormFlag Xfdf
```

Daten werden im XFDF-Format zurückgegeben.

### XfdfWithComments {#XfdfWithComments}
```
public static final SubmitFormFlag XfdfWithComments
```

Daten werden im XFDF-Format mit Kommentaren zurückgegeben.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static SubmitFormFlag [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
