---
title: "ImageMergeMode"
linktitle: "ImageMergeMode"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt Modi zum Zusammenführen von Bildern dar."
type: docs
weight: 270
url: /de/java/com.aspose.pdf.facades/imagemergemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ImageMergeMode > com.aspose.pdf.facades.ImageMergeMode, java.lang.Enum < ImageMergeMode >, com.aspose.pdf.facades.ImageMergeMode

**All Implemented Interfaces:**
Serializable, Comparable < ImageMergeMode >

```
public enum ImageMergeMode extends Enum < ImageMergeMode >
```

Stellt Modi zum Zusammenführen von Bildern dar.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Center](#Center) | Bilder zentriert ausgerichtet. |
| [Horizontal](#Horizontal) | Bilder horizontal zusammengeführt. |
| [Vertical](#Vertical) | Bilder vertikal zusammengeführt. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Center {#Center}
```
public static final ImageMergeMode Center
```

Bilder zentriert ausgerichtet.

### Horizontal {#Horizontal}
```
public static final ImageMergeMode Horizontal
```

Bilder horizontal zusammengeführt.

### Vertical {#Vertical}
```
public static final ImageMergeMode Vertical
```

Bilder vertikal zusammengeführt.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static ImageMergeMode [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
