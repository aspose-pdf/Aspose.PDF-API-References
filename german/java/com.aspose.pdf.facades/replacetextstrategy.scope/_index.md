---
title: "ReplaceTextStrategy.Scope"
linktitle: "ReplaceTextStrategy.Scope"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Geltungsbereich, in dem die Ersetzen‑Text‑Operation angewendet wird, standardmäßig REPLACE_FIRST."
type: docs
weight: 670
url: /de/java/com.aspose.pdf.facades/replacetextstrategy.scope/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReplaceTextStrategy.Scope > com.aspose.pdf.facades.ReplaceTextStrategy.Scope, java.lang.Enum < ReplaceTextStrategy.Scope >, com.aspose.pdf.facades.ReplaceTextStrategy.Scope

**All Implemented Interfaces:**
Serializable, Comparable < ReplaceTextStrategy.Scope >

```
public static enum ReplaceTextStrategy.Scope extends Enum < ReplaceTextStrategy.Scope >
```

Geltungsbereich, in dem die Ersetzen‑Text‑Operation angewendet wird, standardmäßig REPLACE_FIRST.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [ReplaceAll](#ReplaceAll) | Ersetze alle Textvorkommen auf allen betroffenen Seiten |
| [ReplaceFirst](#ReplaceFirst) | Ersetzen Sie nur das erste Vorkommen des Textes auf jeder betroffenen Seite |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### ReplaceAll {#ReplaceAll}
```
public static final ReplaceTextStrategy.Scope ReplaceAll
```

Ersetze alle Textvorkommen auf allen betroffenen Seiten

### ReplaceFirst {#ReplaceFirst}
```
public static final ReplaceTextStrategy.Scope ReplaceFirst
```

Ersetzen Sie nur das erste Vorkommen des Textes auf jeder betroffenen Seite

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static ReplaceTextStrategy.Scope [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
