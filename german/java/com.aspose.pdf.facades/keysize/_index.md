---
title: "KeySize"
linktitle: "KeySize"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Definiert verschiedene Schlüssellängen, die zum Verschlüsseln von PDF-Dokumenten verwendet werden können."
type: docs
weight: 340
url: /de/java/com.aspose.pdf.facades/keysize/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < KeySize > com.aspose.pdf.facades.KeySize, java.lang.Enum < KeySize >, com.aspose.pdf.facades.KeySize

**All Implemented Interfaces:**
Serializable, Comparable < KeySize >

```
public enum KeySize extends Enum < KeySize >
```

Definiert verschiedene Schlüssellängen, die zum Verschlüsseln von PDF-Dokumenten verwendet werden können.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [x128](#x128) | 128‑Bit‑Schlüssel. |
| [x256](#x256) | 256‑Bit‑Schlüssel. |
| [x40](#x40) | 40‑Bit‑Schlüssel. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### x128 {#x128}
```
public static final KeySize x128
```

128‑Bit‑Schlüssel.

### x256 {#x256}
```
public static final KeySize x256
```

256‑Bit‑Schlüssel.

### x40 {#x40}
```
public static final KeySize x40
```

40‑Bit‑Schlüssel.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static KeySize [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
