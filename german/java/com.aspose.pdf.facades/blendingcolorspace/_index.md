---
title: "BlendingColorSpace"
linktitle: "BlendingColorSpace"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Klasse, die den Blending-Farbraum darstellt."
type: docs
weight: 50
url: /de/java/com.aspose.pdf.facades/blendingcolorspace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < BlendingColorSpace > com.aspose.pdf.facades.BlendingColorSpace, java.lang.Enum < BlendingColorSpace >, com.aspose.pdf.facades.BlendingColorSpace

**All Implemented Interfaces:**
Serializable, Comparable < BlendingColorSpace >

```
public enum BlendingColorSpace extends Enum < BlendingColorSpace >
```

Klasse, die den Blending-Farbraum darstellt.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [Auto](#Auto) | Der Mischfarbraum wird automatisch bestimmt. |
| [DeviceCMYK](#DeviceCMYK) | Der Mischfarbraum ist auf DeviceCMYK eingestellt. |
| [DeviceRGB](#DeviceRGB) | Der Mischfarbraum ist auf DeviceRGB eingestellt. |
| [DontChange](#DontChange) | Der Mischfarbraum wurde nicht geändert. |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück. |
| [values](#values--) | Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält. |

### Auto {#Auto}
```
public static final BlendingColorSpace Auto
```

Der Mischfarbraum wird automatisch bestimmt.

### DeviceCMYK {#DeviceCMYK}
```
public static final BlendingColorSpace DeviceCMYK
```

Der Mischfarbraum ist auf DeviceCMYK eingestellt.

### DeviceRGB {#DeviceRGB}
```
public static final BlendingColorSpace DeviceRGB
```

Der Mischfarbraum ist auf DeviceRGB eingestellt.

### DontChange {#DontChange}
```
public static final BlendingColorSpace DontChange
```

Der Mischfarbraum wurde nicht geändert.

### getByValue {#getByValue-int-}
```
public static BlendingColorSpace getByValue(int value)
```



**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Gibt die Enum-Konstante dieses Typs mit dem angegebenen Namen zurück.

### values {#values--}
```
public static BlendingColorSpace [] values()
```

Gibt ein Array zurück, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält.

**Returns:**
ein Array, das die Konstanten dieses Enum-Typs in der Reihenfolge ihrer Deklaration enthält
