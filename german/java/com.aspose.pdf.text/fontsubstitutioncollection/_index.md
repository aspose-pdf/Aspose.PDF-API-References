---
title: "FontSubstitutionCollection"
linktitle: "FontSubstitutionCollection"
second_title: "Aspose.PDF für Java API-Referenz"
description: "Stellt eine Sammlung von Schriftart‑Ersetzungsstrategien dar."
type: docs
weight: 60
url: /de/java/com.aspose.pdf.text/fontsubstitutioncollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.text.FontSubstitutionCollection

**All Implemented Interfaces:**
Iterable < FontSubstitution >

```
public final class FontSubstitutionCollection extends Object implements Iterable < FontSubstitution >
```

Stellt eine Sammlung von Schriftart‑Ersetzungsstrategien dar.

## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [FontSubstitutionCollection](#FontSubstitutionCollection--) | Initialisiert das Sammlungsobjekt |

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [add](#add-com.aspose.pdf.text.FontSubstitution-) | Fügt ein neues Schriftart-Ersetzungsobjekt zur Sammlung hinzu. |
| [clear](#clear--) | Leert die Schriftart-Ersetzungssammlung. |
| [contains](#contains-com.aspose.pdf.text.FontSubstitution-) | Bestimmt, ob ein Element in der Sammlung ist. |
| [delete](#delete-com.aspose.pdf.text.FontSubstitution-) | Nur intern löscht das Schriftart-Ersetzungselement. |
| [getItem](#getItem-int-) | Ruft das Schriftartelement am angegebenen Index ab. |
| [getSyncRoot](#getSyncRoot--) | Liefert ein Objekt, das zur Synchronisation des Zugriffs auf die Sammlung verwendet werden kann. |
| [isSynchronized](#isSynchronized--) | Liefert einen Wert, der angibt, ob der Zugriff auf die Sammlung synchronisiert (thread‑sicher) ist. |
| [iterator](#iterator--) | Gibt einen Enumerator für die gesamte Sammlung zurück. |
| [remove](#remove-com.aspose.pdf.text.FontSubstitution-) | Löscht das Schriftart-Ersetzungselement. |
| [size](#size--) | Ermittelt die Anzahl der tatsächlich in der Sammlung enthaltenen Font-Objektelemente. |

### FontSubstitutionCollection {#FontSubstitutionCollection--}
```
public FontSubstitutionCollection()
```

Initialisiert das Sammlungsobjekt

### add {#add-com.aspose.pdf.text.FontSubstitution-}
Fügt ein neues Schriftart-Ersetzungsobjekt zur Sammlung hinzu.

### clear {#clear--}
```
public void clear()
```

Leert die Schriftart-Ersetzungssammlung.

### contains {#contains-com.aspose.pdf.text.FontSubstitution-}
Bestimmt, ob ein Element in der Sammlung ist.

### delete {#delete-com.aspose.pdf.text.FontSubstitution-}
Nur intern löscht das Schriftart-Ersetzungselement.

### getItem {#getItem-int-}
```
public FontSubstitution getItem(int index)
```

Ruft das Schriftartelement am angegebenen Index ab.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| index |  | Index innerhalb der Sammlung. |

**Returns:**
FontSubstitution-Objekt.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Liefert ein Objekt, das zur Synchronisation des Zugriffs auf die Sammlung verwendet werden kann.

**Returns:**
Objekt für Synchronisation

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Liefert einen Wert, der angibt, ob der Zugriff auf die Sammlung synchronisiert (thread‑sicher) ist.

**Returns:**
boolescher Wert

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.Generic.List.Enumerator< FontSubstitution > iterator()
```

Gibt einen Enumerator für die gesamte Sammlung zurück.

**Returns:**
Enumerator-Objekt

### remove {#remove-com.aspose.pdf.text.FontSubstitution-}
Löscht das Schriftart-Ersetzungselement.

### size {#size--}
```
public int size()
```

Ermittelt die Anzahl der tatsächlich in der Sammlung enthaltenen Font-Objektelemente.

**Returns:**
int-Wert
