---
title: TaggedContent
second_title: Aspose.PDF für .NET-API-Referenz
description: Erhält Zugriff auf TaggedPdf-Inhalte.
type: docs
weight: 480
url: /de/net/aspose.pdf/document/taggedcontent/
---
## Document.TaggedContent property

Erhält Zugriff auf TaggedPdf-Inhalte.

```csharp
public ITaggedContent TaggedContent { get; }
```

### Beispiele

Das Beispiel zeigt, wie man getaggte Inhalte zum Erstellen neuer Dokumente mit Kopfzeilen, Absätzen und Bildern verwendet.

```csharp
// Neues Dokument erstellen
Document document = new Document();

// Holen Sie sich den markierten Inhalt
ITaggedContent taggedContent = document.TaggedContent;

// Sprache für Dokument festlegen
taggedContent.SetLanguage("en-US");

// Titel für PDF-Dokument festlegen
taggedContent.SetTitle("Example document");

// Abschnitt erstellen und hinzufügen
SectElement sect = taggedContent.CreateSectElement();
taggedContent.RootElement.AppendChild(sect);

// Kopfzeile erstellen
HeaderElement h1 = taggedContent.CreateHeaderElement(1);
h1.SetText("The Header");
sect.AppendChild(h1);

// Absatz erstellen
ParagraphElement p = taggedContent.CreateParagraphElement();
p.SetTag("Paragraph");
p.SetText("The text of paragraph.");
sect.AppendChild(p);

// Abbildung erstellen
IllustrationElement figure1 = taggedContent.CreateFigureElement();
sect.AppendChild(figure1);
figure1.AlternativeText = "Figure 1";
figure1.Title = "Image 1";
figure1.SetTag("Fig");
figure1.SetImage("path/of/image.jpg");

// Dokument speichern
document.Save("example.pdf");
```

### Siehe auch

* interface [ITaggedContent](../../../aspose.pdf.tagged/itaggedcontent)
* class [Document](../../document)
* namensraum [Aspose.Pdf](../../document)
* Montage [Aspose.PDF](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.PDF.dll -->
