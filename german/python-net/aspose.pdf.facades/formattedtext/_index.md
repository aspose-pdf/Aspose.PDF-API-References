---
title: "FormattedText"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Klasse, die formatierte Texte darstellt. Enthält Informationen über den Text sowie dessen Farbe, Größe und Stil."
type: docs
weight: 130
url: /de/python-net/aspose.pdf.facades/formattedtext/
---

## FormattedText class

Klasse, die formatierte Texte darstellt. Enthält Informationen über den Text sowie dessen Farbe, Größe und Stil.

Der Typ FormattedText stellt die folgenden Mitglieder bereit:
## Konstruktoren
| Name | Beschreibung |
| :- | :- |
| FormattedText() | Initialisiert FormattedText. |
| FormattedText(text) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, font_color, font_style, encoding_type, embedded, text_size) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, font_color, text_font, text_encoding, embedded, text_size, line_spacing) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, color, text_font, text_encoding, embedded, text_size) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, text_font, text_encoding, embedded, text_size, line_spacing) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, back_color, text_font, encoding, embedded, text_size) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, back_color, font_name, text_encoding, embedded, font_size) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, back_color) | Initialisiert eine neue Instanz der FormattedText-Klasse |
| FormattedText(text, text_color, font_name, text_encoding, embedded, font_size) | Initialisiert eine neue Instanz der FormattedText-Klasse |
## Eigenschaften
| Name | Beschreibung |
| :- | :- |
| text_height | Ermittelt die Höhe des Textes. |
| text_width | Ermittelt die Breite des Textes. |
## Methoden
| Name | Beschreibung |
| :- | :- |
| add_new_line_text(new_line_text) | Fügt dem FormattedText-Objekt eine neue Zeile hinzu und setzt newLineText auf den Text der nächsten Zeile. |
| add_new_line_text(new_line_text, line_spacing) | Fügt dem FormattedText-Objekt eine neue Zeile hinzu und setzt newLineText auf den Text der nächsten Zeile. |
| is_cjk() | Überprüft, ob der Text CJK (Chinesisch, Japanisch oder Koreanisch) ist. |
| set_cjk_font_style() | Ändert den Schriftstil von FormattedText für CJK (Chinesisch, Japanisch oder Koreanisch). |

### Siehe auch

* namespace [aspose.pdf.facades](/pdf/python-net/aspose.pdf.facades/)
* assembly [Aspose.PDF](/pdf/python-net/)

