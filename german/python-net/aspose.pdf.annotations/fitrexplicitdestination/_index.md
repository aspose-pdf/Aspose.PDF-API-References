---
title: "FitRExplicitDestination"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Stellt ein explizites Ziel dar, das die Seite mit ihrem Inhalt so vergrößert anzeigt, dass das durch die Koordinaten left, bottom, right und top definierte Rechteck vollständig sowohl horizontal als auch vertikal in das Fenster passt. Wenn die erforderlichen horizontalen und vertikalen Vergrößerungsfaktoren unterschiedlich sind, wird der kleinere der beiden verwendet, wobei das Rechteck in der anderen Dimension im Fenster zentriert wird. Ein Nullwert für einen der Parameter kann zu unvorhersehbarem Verhalten führen."
type: docs
weight: 230
url: /de/python-net/aspose.pdf.annotations/fitrexplicitdestination/
---

## FitRExplicitDestination class

Stellt ein explizites Ziel dar, das die Seite mit ihrem Inhalt so vergrößert anzeigt, dass das durch die Koordinaten left, bottom, right und top definierte Rechteck vollständig sowohl horizontal als auch vertikal in das Fenster passt. Wenn die erforderlichen horizontalen und vertikalen Vergrößerungsfaktoren unterschiedlich sind, wird der kleinere der beiden verwendet, wobei das Rechteck in der anderen Dimension im Fenster zentriert wird. Ein Nullwert für einen der Parameter kann zu unvorhersehbarem Verhalten führen.

Der Typ FitRExplicitDestination stellt die folgenden Mitglieder bereit:
## Konstruktoren
| Name | Beschreibung |
| :- | :- |
| FitRExplicitDestination(page, left, bottom, right, top) | Initialisiert eine neue Instanz der Klasse FitRExplicitDestination |
| FitRExplicitDestination(document, page_number, left, bottom, right, top) | Initialisiert eine neue Instanz der Klasse FitRExplicitDestination |
| FitRExplicitDestination(page_number, left, bottom, right, top) | Initialisiert eine neue Instanz der Klasse FitRExplicitDestination |
## Eigenschaften
| Name | Beschreibung |
| :- | :- |
| page | Liefert das Zielseitenobjekt |
| page_number | Liefert die Zielseitennummer |
| left | Liest die linke horizontale Koordinate des sichtbaren Rechtecks. |
| bottom | Liest die untere vertikale Koordinate des sichtbaren Rechtecks. |
| rechts | Liest die rechte horizontale Koordinate des sichtbaren Rechtecks. |
| top | Liest die obere vertikale Koordinate des sichtbaren Rechtecks. |
## Methoden
| Name | Beschreibung |
| :- | :- |
| create_destination(page, type, values) | Erstellt Instanzen von abgeleiteten Klassen von ExplicitDestination. |
| create_destination(doc, page_number, type, values) | Erstellt Instanzen von abgeleiteten Klassen von ExplicitDestination. |
| create_destination(page_number, type, values) | Erstellt Instanzen von abgeleiteten Klassen von ExplicitDestination. |
| to_string() | Konvertiert den Objektzustand in einen Zeichenkettenwert. Beispiel: "1 FitR 100 200 300 400". |

### Siehe auch

* namespace [aspose.pdf.annotations](/pdf/python-net/aspose.pdf.annotations/)
* assembly [Aspose.PDF](/pdf/python-net/)

