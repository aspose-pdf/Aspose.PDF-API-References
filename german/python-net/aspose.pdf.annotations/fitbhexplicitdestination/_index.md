---
title: "FitBHExplicitDestination"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Stellt ein explizites Ziel dar, das die Seite mit der vertikalen Koordinate top am oberen Rand des Fensters positioniert und den Seiteninhalt so vergrößert, dass die gesamte Breite ihres Begrenzungsrahmens in das Fenster passt. Ein Nullwert für top gibt an, dass der aktuelle Wert dieses Parameters unverändert beibehalten wird."
type: docs
weight: 190
url: /de/python-net/aspose.pdf.annotations/fitbhexplicitdestination/
---

## FitBHExplicitDestination class

Stellt ein explizites Ziel dar, das die Seite mit der vertikalen Koordinate top am oberen Rand des Fensters positioniert und den Seiteninhalt so vergrößert, dass die gesamte Breite ihres Begrenzungsrahmens in das Fenster passt. Ein Nullwert für top gibt an, dass der aktuelle Wert dieses Parameters unverändert beibehalten wird.

Der Typ FitBHExplicitDestination stellt die folgenden Mitglieder bereit:
## Konstruktoren
| Name | Beschreibung |
| :- | :- |
| FitBHExplicitDestination(page, top) | Initialisiert eine neue Instanz der Klasse FitBHExplicitDestination |
| FitBHExplicitDestination(document, page_number, top) | Initialisiert eine neue Instanz der Klasse FitBHExplicitDestination |
| FitBHExplicitDestination(page_number, top) | Initialisiert eine neue Instanz der Klasse FitBHExplicitDestination |
## Eigenschaften
| Name | Beschreibung |
| :- | :- |
| page | Liefert das Zielseitenobjekt |
| page_number | Liefert die Zielseitennummer |
| top | Gibt die vertikale Koordinate top zurück, die am oberen Rand des Fensters positioniert ist. |
## Methoden
| Name | Beschreibung |
| :- | :- |
| create_destination(page, type, values) | Erstellt Instanzen von abgeleiteten Klassen von ExplicitDestination. |
| create_destination(doc, page_number, type, values) | Erstellt Instanzen von abgeleiteten Klassen von ExplicitDestination. |
| create_destination(page_number, type, values) | Erstellt Instanzen von abgeleiteten Klassen von ExplicitDestination. |
| to_string() | Konvertiert den Objektzustand in einen Zeichenkettenwert. Beispiel: "1 FitBH 100". |

### Siehe auch

* namespace [aspose.pdf.annotations](/pdf/python-net/aspose.pdf.annotations/)
* assembly [Aspose.PDF](/pdf/python-net/)

