---
title: "ExplicitDestinationType"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Enumeriert die Typen expliziter Ziele."
type: docs
weight: 1020
url: /de/python-net/aspose.pdf.annotations/explicitdestinationtype/
---

## ExplicitDestinationType enumeration

Enumeriert die Typen expliziter Ziele.

## Members
| Member-Name | Beschreibung |
| :- | :- |
| XYZ | Zeigt die Seite mit den Koordinaten (left,�top) an, die in der oberen linken Ecke des Fensters positioniert sind<br/>            und der Inhalt der Seite um den Faktor zoom vergrößert wird. Ein Nullwert für einen der Parameter<br/>            left, top oder zoom gibt an, dass der aktuelle Wert dieses Parameters unverändert beibehalten wird. <br/>            Ein Zoomwert von 0 hat dieselbe Bedeutung wie ein Nullwert. |
| FIT | Zeigt die Seite an, wobei ihr Inhalt gerade so vergrößert wird, dass die gesamte Seite in das Fenster passt<br/>            sowohl horizontal als auch vertikal. Wenn die erforderlichen horizontalen und vertikalen Vergrößerungsfaktoren<br/>            unterschiedlich sind, wird der kleinere der beiden verwendet, wobei die Seite in der anderen Dimension im Fenster zentriert wird. |
| FIT_H | Zeigt die Seite an, wobei die vertikale Koordinate top am oberen Rand des Fensters positioniert ist und<br/>            der Inhalt der Seite gerade so vergrößert wird, dass die gesamte Breite der Seite in das Fenster passt.<br/>            Ein Nullwert für top gibt an, dass der aktuelle Wert dieses Parameters unverändert beibehalten wird. |
| FIT_V | Zeigt die Seite an, wobei die horizontale Koordinate left am linken Rand des Fensters positioniert ist<br/>            und der Inhalt der Seite gerade so vergrößert wird, dass die gesamte Höhe der Seite in das Fenster passt.<br/>            Ein Nullwert für left gibt an, dass der aktuelle Wert dieses Parameters unverändert beibehalten wird. |
| FIT_R | Zeigt die Seite an, wobei ihr Inhalt gerade so vergrößert wird, dass das durch die<br/>            Koordinaten left, bottom, right und top angegebene Rechteck vollständig in das Fenster passt, sowohl horizontal als auch vertikal.<br/>            Wenn die erforderlichen horizontalen und vertikalen Vergrößerungsfaktoren unterschiedlich sind, wird der kleinere der beiden verwendet, wobei das Rechteck in der anderen Dimension im Fenster zentriert wird. Ein Nullwert für einen der<br/>            Parameter kann zu unvorhersehbarem Verhalten führen. |
| FIT_B | Zeigt die Seite an, wobei ihr Inhalt gerade so vergrößert wird, dass ihre Begrenzungsbox vollständig in<br/>            das Fenster passt, sowohl horizontal als auch vertikal. Wenn die erforderlichen horizontalen und vertikalen Vergrößerungsfaktoren<br/>            unterschiedlich sind, wird der kleinere der beiden verwendet, wobei die Begrenzungsbox in der anderen Dimension im Fenster<br/>            zentriert wird. |
| FIT_BH | Zeigt die Seite an, wobei die vertikale Koordinate top am oberen Rand des Fensters positioniert ist und der<br/>            Inhalt der Seite gerade so vergrößert wird, dass die gesamte Breite ihrer Begrenzungsbox in das Fenster passt.<br/>            Ein Nullwert für top gibt an, dass der aktuelle Wert dieses Parameters unverändert beibehalten wird. |
| FIT_BV | Zeigen Sie die Seite an, wobei die horizontale Koordinate left am linken Rand des Fensters positioniert ist und die<br/>            Inhalte der Seite so weit vergrößert werden, dass die gesamte Höhe ihrer Begrenzungsbox in das Fenster passt.<br/>            Ein Nullwert für left gibt an, dass der aktuelle Wert dieses Parameters unverändert beibehalten wird. |

### Siehe auch

* namespace [aspose.pdf.annotations](/pdf/python-net/aspose.pdf.annotations/)
* assembly [Aspose.PDF](/pdf/python-net/)

