---
title: "TiffDevice"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Diese Klasse hilft, PDF-Dokumentseiten einzeln in ein einziges TIFF-Bild zu speichern."
type: docs
weight: 150
url: /de/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Diese Klasse hilft, PDF-Dokumentseiten einzeln in ein einziges TIFF-Bild zu speichern.

Der Typ TiffDevice stellt die folgenden Mitglieder bereit:
## Konstruktoren
| Name | Beschreibung |
| :- | :- |
| TiffDevice(resolution) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(resolution, settings) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(resolution, settings, converter) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(settings) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(settings, converter) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice() | Initialisiert eine neue Instanz der [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) Klasse mit den Standardeinstellungen. |
| TiffDevice(width, height, resolution, settings) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(page_size, resolution, settings) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(width, height, resolution) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(page_size, resolution) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(width, height, settings) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(width, height, settings, converter) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(page_size, settings, converter) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(page_size, settings) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(width, height) | Initialisiert eine neue Instanz der Klasse TiffDevice |
| TiffDevice(page_size) | Initialisiert eine neue Instanz der Klasse TiffDevice |
## Eigenschaften
| Name | Beschreibung |
| :- | :- |
| rendering_options | Liest oder setzt die Rendering-Optionen. |
| form_presentation_mode | Liest oder setzt den Präsentationsmodus des Formulars. |
| settings | Ruft Einstellungen für die Zuordnung von PDF in ein TIFF-Bild ab. |
| resolution | Ermittelt Bildauflösung. |
| breite | Ermittelt Bildausgabe-Breite. |
| höhe | Ermittelt Bildausgabe-Höhe. |
## Methoden
| Name | Beschreibung |
| :- | :- |
| process(document, from_page, to_page, output) | Konvertiert bestimmte Dokumentseiten in TIFF und speichert sie im Ausgabestream. |
| process(document, output) | Konvertiert bestimmte Dokumentseiten in TIFF und speichert sie im Ausgabestream. |
| process(document, output_file_name) | Konvertiert bestimmte Dokumentseiten in TIFF und speichert sie im Ausgabestream. |
| process(document, from_page, to_page, output_file_name) | Konvertiert bestimmte Dokumentseiten in TIFF und speichert sie im Ausgabestream. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Führt Bradley-Binarisierung für den Eingabestream durch. |

### Siehe auch

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

