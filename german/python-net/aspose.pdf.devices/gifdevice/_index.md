---
title: "GifDevice"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Stellt ein Bildgerät dar, das dabei hilft, PDF-Dokumentseiten im GIF-Format zu speichern."
type: docs
weight: 60
url: /de/python-net/aspose.pdf.devices/gifdevice/
---

## GifDevice class

Stellt ein Bildgerät dar, das dabei hilft, PDF-Dokumentseiten im GIF-Format zu speichern.

Der GifDevice‑Typ stellt die folgenden Member bereit:
## Konstruktoren
| Name | Beschreibung |
| :- | :- |
| GifDevice() | Initialisiert eine neue Instanz der [GifDevice](/pdf/python-net/aspose.pdf.devices/gifdevice/) Klasse mit Standardauflösung. |
| GifDevice(resolution) | Initialisiert eine neue Instanz der GifDevice‑Klasse |
| GifDevice(width, height, resolution) | Initialisiert eine neue Instanz der GifDevice‑Klasse |
| GifDevice(page_size, resolution) | Initialisiert eine neue Instanz der GifDevice‑Klasse |
| GifDevice(width, height) | Initialisiert eine neue Instanz der GifDevice‑Klasse |
| GifDevice(page_size) | Initialisiert eine neue Instanz der GifDevice‑Klasse |
## Eigenschaften
| Name | Beschreibung |
| :- | :- |
| coordinate_type | Liest oder setzt den Seitenkoordinatentyp (Media-/Crop-Boxen). Der CropBox-Wert wird standardmäßig verwendet. |
| rendering_options | Liest oder setzt die Rendering-Optionen. |
| form_presentation_mode | Liest oder setzt den Präsentationsmodus des Formulars. |
| resolution | Ermittelt Bildauflösung. |
| breite | Ermittelt Bildausgabe-Breite. |
| höhe | Ermittelt Bildausgabe-Höhe. |
## Methoden
| Name | Beschreibung |
| :- | :- |
| process(page, output) | Konvertiert die Seite in ein GIF und speichert sie im Ausgabestream. |
| process(page, output_file_name) | Konvertiert die Seite in ein GIF und speichert sie im Ausgabestream. |

### Siehe auch

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

