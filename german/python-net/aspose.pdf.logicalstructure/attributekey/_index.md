---
title: "AttributeKey"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Stellt Standard-Attributschlüssel dar."
type: docs
weight: 40
url: /de/python-net/aspose.pdf.logicalstructure/attributekey/
---

## AttributeKey class

Stellt Standard-Attributschlüssel dar.

Der AttributeKey-Typ stellt die folgenden Mitglieder bereit:
## Eigenschaften
| Name | Beschreibung |
| :- | :- |
| key | Ruft Attribute Key ab. |
| owner | Ruft Attribute Owner ab: |
| PLACEMENT | Placement-Attribut (Layout-Attributbesitzer). |
| WRITING_MODE | WritingMode-Attribut (Layout-Attributbesitzer). |
| BACKGROUND_COLOR | BackgroundColor-Attribut (Layout-Attributbesitzer). |
| BORDER_COLOR | BorderColor-Attribut (Layout-Attributbesitzer). |
| BORDER_STYLE | BorderStyle-Attribut (Layout-Attributbesitzer). |
| BORDER_THICKNESS | BorderThickness-Attribut (Layout-Attributbesitzer). |
| PADDING | Padding-Attribut (Layout-Attributbesitzer). |
| COLOR | Color-Attribut (Layout-Attributbesitzer). |
| SPACE_BEFORE | SpaceBefore-Attribut (Layout-Attributbesitzer). |
| SPACE_AFTER | SpaceAfter-Attribut (Layout-Attributbesitzer). |
| START_INDENT | StartIndent-Attribut (Layout-Attributbesitzer). |
| END_INDENT | EndIndent-Attribut (Layout-Attributbesitzer). |
| TEXT_INDENT | TextIndent-Attribut (Layout-Attributbesitzer). |
| TEXT_ALIGN | TextAlign-Attribut (Layout-Attributbesitzer). |
| B_BOX | BBox-Attribut (Layout-Attributbesitzer). |
| WIDTH | Width-Attribut (Layout-Attributbesitzer). |
| HEIGHT | Height-Attribut (Layout-Attributbesitzer). |
| BLOCK_ALIGN | BlockAlign-Attribut (Layout-Attributbesitzer). |
| INLINE_ALIGN | InlineAlign-Attribut (Layout-Attributbesitzer). |
| T_BORDER_STYLE | TBorderStyle-Attribut (Layout-Attributbesitzer). |
| T_PADDING | TPadding-Attribut (Layout-Attributbesitzer). |
| BASELINE_SHIFT | BaselineShift-Attribut (Layout-Attributbesitzer). |
| LINE_HEIGHT | LineHeight-Attribut (Layout-Attributbesitzer). |
| TEXT_DECORATION_COLOR | TextDecorationColor-Attribut (Layout-Attributbesitzer). |
| TEXT_DECORATION_THICKNESS | TextDecorationThickness-Attribut (Layout-Attributbesitzer). |
| TEXT_DECORATION_TYPE | TextDecorationType-Attribut (Layout-Attributbesitzer). |
| RUBY_ALIGN | RubyAlign-Attribut (Layout-Attributbesitzer). |
| RUBY_POSITION | RubyPosition-Attribut (Layout-Attributbesitzer). |
| GLYPH_ORIENTATION_VERTICAL | GlyphOrientationVertical-Attribut (Layout-Attributbesitzer). |
| COLUMN_COUNT | ColumnCount-Attribut (Layout-Attributbesitzer). |
| COLUMN_GAP | ColumnGap-Attribut (Layout-Attributbesitzer). |
| COLUMN_WIDTHS | ColumnWidths-Attribut (Layout-Attributbesitzer). |
| LIST_NUMBERING | ListNumbering-Attribut (List-Attributbesitzer). |
| ROLE | Role-Attribut (PrintField-Attributbesitzer). |
| GEPRÜFT | Checked-Attribut (PrintField-Attributbesitzer). |
| DESC | Desc-Attribut (PrintField-Attributbesitzer). |
| ROW_SPAN | RowSpan-Attribut (Table-Attributbesitzer). |
| COL_SPAN | ColSpan-Attribut (Table-Attributbesitzer). |
| HEADERS | Headers-Attribut (Table-Attributbesitzer). |
| SCOPE | Scope-Attribut (Table-Attributbesitzer). |
| SUMMARY | Summary-Attribut (Table-Attributbesitzer). |

### Siehe auch

* namespace [aspose.pdf.logicalstructure](/pdf/python-net/aspose.pdf.logicalstructure/)
* assembly [Aspose.PDF](/pdf/python-net/)

