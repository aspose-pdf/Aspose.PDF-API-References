---
title: "HtmlSaveOptions"
second_title: "Aspose.PDF für Python via .NET API‑Referenz"
description: "Speicheroptionen für den Export in das HTML-Format"
type: docs
weight: 490
url: /de/python-net/aspose.pdf/htmlsaveoptions/
---

## HtmlSaveOptions class

Speicheroptionen für den Export in das HTML-Format

Der Typ HtmlSaveOptions stellt die folgenden Member bereit:
## Konstruktoren
| Name | Beschreibung |
| :- | :- |
| HtmlSaveOptions() | Initialisiert eine neue Instanz der Klasse [HtmlSaveOptions](/pdf/python-net/aspose.pdf/htmlsaveoptions/). |
| HtmlSaveOptions(document_type) | Initialisiert eine neue Instanz der Klasse HtmlSaveOptions |
| HtmlSaveOptions(fixed_layout) | Initialisiert eine neue Instanz der Klasse HtmlSaveOptions |
| HtmlSaveOptions(document_type, fixed_layout) | Initialisiert eine neue Instanz der Klasse HtmlSaveOptions |
## Eigenschaften
| Name | Beschreibung |
| :- | :- |
| warning_handler | Rückruf zur Behandlung aller erzeugten Warnungen. <br/>            Der WarningHandler gibt ein ReturnAction‑Enum‑Element zurück, das entweder Continue oder Abort angibt. <br/>            Continue ist die Standardaktion und das Save operation wird fortgesetzt, der Benutzer kann jedoch auch Abort zurückgeben, wodurch das Save operation beendet werden soll. |
| save_format | Format der Datenspeicherung. |
| close_response | Liest oder setzt den booleschen Wert, der angibt, ob das Response‑Objekt nach dem Speichern des Dokuments in die Antwort geschlossen wird. |
| extract_ocr_sublayer_only | Keine |
| try_merge_adjacent_same_background_images | Keine |
| document_type | Liest oder setzt den [HtmlDocumentType](/pdf/python-net/aspose.pdf/htmldocumenttype/). |
| compress_svg_graphics_if_any | Liest oder setzt das Flag, das angibt, ob<br/>            gefundene SVG-Grafiken (falls vorhanden) während des Speicherns komprimiert (gezippt) <br/>            in das SVGZ-Format umgewandelt werden. |
| split_css_into_pages | Wenn der Mehrseitige-Modus ausgewählt ist (d.h. 'SplitIntoPages' ist 'true'), <br/>            definiert dieses Attribut, ob für jede resultierende HTML‑Seite eine separate CSS‑Datei erstellt werden soll.<br/>            Standardmäßig ist dieses Attribut false, sodass ein großes gemeinsames CSS für alle erstellten Seiten erzeugt wird. Die Gesamtsumme aller<br/>            in diesem Modus erzeugten CSS‑Dateien (ein CSS pro Seite) ist normalerweise<br/>            viel größer als die Größe einer einzigen großen CSS‑Datei, weil in dem erstgenannten Fall <br/>            CSS‑Klassen in mehreren CSS‑Dateien für jede Seite dupliziert werden.<br/>            Daher sollte diese Einstellung nur verwendet werden, wenn Sie an einer späteren Verarbeitung jeder HTML‑Seite unabhängig interessiert sind und daher die Größe<br/>            des CSS jeder einzelnen Seite, getrennt betrachtet, das kritischste Problem darstellt. |
| split_into_pages | Liest oder setzt das Flag, das angibt, ob jede Seite des Quell‑<br/>            Dokuments in ein eigenes Ziel‑HTML‑Dokument konvertiert wird, <br/>            d.h. ob das resultierende HTML in mehrere HTML‑Seiten aufgeteilt wird. |
| explicit_list_of_saved_pages | Mit dieser Eigenschaft können Sie ausdrücklich festlegen <br/>            welche Seiten des Dokuments konvertiert werden sollen.<br/>            Seiten in dieser Liste müssen 1‑basierte Nummern haben. d.h. <br/>            gültige Seitennummern müssen aus dem Bereich (1...[NumberOfPagesInConvertedDocument]) stammen.<br/>            Die Reihenfolge, in der Seiten in dieser Liste erscheinen, beeinflusst nicht ihre<br/>            Reihenfolge in den resultierenden HTML‑Seiten – in den Ergebnisseiten werden sie immer in der Reihenfolge angezeigt, in der sie <br/>            im Quell‑PDF vorhanden sind.<br/>            Wenn diese Liste null ist (wie standardmäßig), werden alle Seiten konvertiert.<br/>            Wenn eine Seitennummer dieser Liste außerhalb des Bereichs der vorhandenen Seiten liegt (1-[amountOfPagesInDocument])<br/>            wird eine Ausnahme ausgelöst. |
| fixed_layout | Liest oder setzt einen Wert, der angibt, ob das HTML als Festlayout erstellt wird. |
| image_resolution | Liest oder setzt die Auflösung für die Bilddarstellung. |
| default_font_name | Gibt den Namen einer installierten Schriftart an, die verwendet wird, um<br/>            jede Dokumentenschriftart zu ersetzen, die nicht eingebettet und nicht im System installiert ist. <br/>            Wenn null, wird die Standardsubstitutionsschriftart verwendet. |
| batch_size | Definiert die Batch‑Größe, wenn das Attribut 'SplitOnPages=false' ist, wird das gesamte HTML, das alle Eingabe‑PDF‑Seiten darstellt, nicht<br/>            in verschiedene HTML‑Seiten aufgeteilt, sondern in einer großen Ergebnis‑HTML‑Datei zusammengefasst.<br/>            Jede Quell‑PDF‑Seite wird jedoch mit ihrem eigenen <br/>            rechteckigen Bereich in HTML dargestellt (falls nötig können diese Bereiche mit dem speziellen Attribut 'PageBorderIfAny' begrenzt werden, um die Kanten des Papierblatts zu zeigen).<br/>            Dieser Parameter definiert die Breite des Randes, der zwingend um die Ausgab‑HTML‑Bereiche, die die Seiten des Quell‑PDF‑Dokuments repräsentieren, gelassen wird.<br/>            Im Wesentlichen definiert er das garantierte Intervall zwischen den HTML‑Darstellungen der PDF‑„Papier“‑Seiten bei dieser Konvertierungsart. |
| font_sources | Schriftquellen von vorab gespeicherten Schriftarten. |
| additional_margin_width_in_points | Wenn das Attribut 'SplitOnPages=false' ist, wird das gesamte HTML, das alle Eingabe‑PDF‑Seiten darstellt, nicht<br/>            in verschiedene HTML‑Seiten aufgeteilt, sondern in einer großen Ergebnis‑HTML‑Datei zusammengefasst.<br/>            Jede Quell‑PDF‑Seite wird jedoch mit ihrem eigenen <br/>            rechteckigen Bereich in HTML dargestellt (falls nötig können diese Bereiche mit dem speziellen Attribut 'PageBorderIfAny' begrenzt werden, um die Kanten des Papierblatts zu zeigen).<br/>            Dieser Parameter definiert die Breite des Randes, der zwingend um die Ausgab‑HTML‑Bereiche, die die Seiten des Quell‑PDF‑Dokuments repräsentieren, gelassen wird.<br/>            Im Wesentlichen definiert er das garantierte Intervall zwischen den HTML‑Darstellungen der PDF‑„Papier“‑Seiten bei dieser Konvertierungsart. |
| use_z_order | Wenn das Attribut UseZORder auf true gesetzt ist, werden Grafiken und Text dem resultierenden HTML‑Dokument<br/>            entsprechend der Z‑Reihenfolge im ursprünglichen PDF‑Dokument hinzugefügt. Ist dieses Attribut false, werden alle Grafiken als einzelne Ebene eingefügt,<br/>            was bei überlappenden Objekten zu unnötigen Effekten führen kann. |
| convert_marked_content_to_layers | Wenn das Attribut ConvertMarkedContentToLayers auf true gesetzt ist, werden alle Elemente innerhalb eines PDF‑markierten<br/>            Inhalts (Layer) in ein HTML‑Div mit dem Attribut \"data-pdflayer\" eingefügt, das den Namen des Layers angibt.<br/>            Dieser Layer‑Name wird aus optionalen Eigenschaften des PDF‑markierten Inhalts extrahiert.<br/>            Ist dieses Attribut false (standardmäßig), werden keine Layers aus dem PDF‑markierten Inhalt erstellt. |
| minimal_line_width | Dieses Attribut legt die minimale Breite einer Grafik‑Pfadlinie fest.<br/>            Wenn die Linienstärke weniger als 1 px beträgt, rundet Adobe Acrobat sie auf diesen Wert. Dieses Attribut kann also<br/>            verwendet werden, um dieses Verhalten in HTML‑Browsern zu emulieren. |
| prevent_glyphs_grouping | Dieses Attribut schaltet den Modus ein, in dem Text‑Glyphen nicht zu Wörtern und Zeichenketten gruppiert werden.<br/>            Dieser Modus ermöglicht maximale Präzision bei der Positionierung der Glyphen auf der Seite und kann<br/>            für die Konvertierung von Dokumenten mit Noten oder Glyphen verwendet werden, die einzeln voneinander platziert werden sollen.<br/>            Dieser Parameter wird nur auf das Dokument angewendet, wenn der Wert des FixedLayout‑Attributs true ist. |
| simple_textbox_mode_grouping | Dieses Attribut legt eine sequentielle Gruppierung von Glyphen und Wörtern zu Zeichenketten fest.<br/>            Zum Beispiel haben Tags und Wörter in konvertiertem HTML unterschiedliche Reihenfolge, und Sie möchten, dass sie übereinstimmen.<br/>            Dieser Parameter wird nur auf das Dokument angewendet, wenn der Wert des FixedLayout‑Attributs true ist. |
| flow_layout_paragraph_full_width | Dieses Attribut legt den Absatztext in voller Breite für den Flow‑Modus fest, FixedLayout = false |
| render_text_as_image | Wenn das Attribut RenderTextAsImage auf true gesetzt ist, wird der Text aus der Quelle in HTML zu einem Bild.<br/>            Kann nützlich sein, um Text nicht auswählbar zu machen<br/>            oder wenn HTML‑Text nicht korrekt gerendert wird. |
| save_full_font | Gibt an, dass die vollständige Schriftart gespeichert wird und nur True‑Type‑Schriften unterstützt.<br/>            Standardmäßig ist SaveFullFont = false und der Konverter speichert das Subset der ursprünglichen Schriftart,<br/>            das zum Anzeigen des Textes im Dokument erforderlich ist. |
| antialiasing_processing | Dieser Parameter definiert die erforderlichen Antialiasing‑Maßnahmen bei der Konvertierung zusammengesetzter Hintergrundbilder von PDF zu HTML. |
| save_transparent_texts | Pdf kann transparente Texte enthalten, die in die Zwischenablage kopiert werden können (dies geschieht normalerweise, wenn das Dokument Bilder und daraus extrahierte OCR‑Texte enthält).<br/>            Diese Einstellung teilt dem Konverter mit, ob solche Texte als transparente<br/>            auswählbare Texte im resultierenden HTML gespeichert werden sollen. |
| save_shadowed_texts_as_transparent_texts | Pdf kann Texte enthalten, die von anderen Elementen (z. B. Bildern) überlagert werden, aber <br/>            in Acrobat Reader in die Zwischenablage kopiert werden können (dies geschieht normalerweise, wenn das Dokument Bilder und daraus extrahierte OCR‑Texte enthält).<br/>            Diese Einstellung teilt dem Konverter mit, ob solche Texte als transparente<br/>            auswählbare Texte im resultierenden HTML gespeichert werden sollen, um das Verhalten von Acrobat Reader nachzuahmen (andernfalls werden solche Texte normalerweise als verborgen gespeichert und können nicht in die Zwischenablage kopiert werden). |
| font_saving_mode | Definiert den Schriftart‑Speichermodus, der beim Speichern von PDF in das gewünschte Format verwendet wird. |
| page_border_if_any | Dieses Attribut stellt eine Gruppe von Einstellungen dar, die zum Zeichnen eines Rahmens (falls vorhanden) <br/>            im resultierenden HTML‑Dokument um den Bereich, der die Quell‑PDF‑Seite repräsentiert, verwendet werden.<br/>            Im Wesentlichen betrifft es die Darstellung der Papierkanten der Seite,<br/>            nicht den Seitenrahmen, der im PDF‑Seiteninhalt selbst referenziert wird. |
| page_margin_if_any | Dieses Attribut stellt eine Gruppe von zusätzlichen Seitenrändern (falls vorhanden) <br/>            im resultierenden HTML‑Dokument um den Bereich, der die Quell‑PDF‑Seite repräsentiert, dar. |
| letters_positioning_method | Legt den Modus der Positionierung von Buchstaben in Wörtern im resultierenden HTML fest. |
| exclude_font_name_list | Liste von im PDF eingebetteten Schriftartnamen, die nicht in HTML eingebettet werden sollen. |
| special_folder_for_svg_images | Liest oder setzt den Pfad zu einem Verzeichnis, in dem ausschließlich SVG‑Bilder gespeichert werden sollen, wenn sie <br/>            beim Speichern des Dokuments als HTML auftreten. Ist der Parameter leer oder null, <br/>            werden SVG‑Dateien (falls vorhanden) zusammen mit anderen Bilddateien (in der Nähe der Ausgabedatei) <br/>            oder in einem speziellen Bildordner (wenn er in der Option SpecialImagesFolderIfAny angegeben ist) gespeichert.<br/>            Dies hat keine Auswirkungen, wenn die Eigenschaft CustomImageSavingStrategy <br/>            erfolgreich verwendet wurde, um die entsprechende Bilddatei zu verarbeiten. |
| special_folder_for_all_images | Liest oder setzt den Pfad zu dem Verzeichnis, in dem alle Bilder gespeichert werden müssen, wenn sie <br/>            während des Speicherns des Dokuments als HTML gefunden werden. Wenn der Parameter leer oder null ist<br/>            werden Bilddateien (falls vorhanden) zusammen mit anderen Dateien, die mit HTML verknüpft sind, gespeichert<br/>            Es hat keine Auswirkung, wenn die CustomImageSavingStrategy<br/>            Eigenschaft erfolgreich verwendet wurde, um die relevante Bilddatei zu verarbeiten. |
| css_class_names_prefix | Wenn der PDFtoHTML-Konverter Ergebnis‑CSSs erzeugt, werden CSS‑Klassen­namen<br/>            (etwa \".stl_01 {}\" ... \".stl_NN {}\") generiert<br/>            und im Ergebnis‑CSS verwendet. Diese Eigenschaft ermöglicht das erzwungene Festlegen eines Klassen­namens‑Präfixes<br/>            Zum Beispiel, wenn Sie möchten, dass alle Klassen­namen mit 'my_prefix_' beginnen<br/>            (d. h. etwas wie 'my_prefix_1' ... 'my_prefix_NNN'), <br/>            dann weisen Sie dieser Eigenschaft vor der Konvertierung einfach 'my_prefix_' zu.<br/>            Wenn diese Eigenschaft unverändert bleibt (d. h. null bleibt als Wert), dann<br/>            erzeugt der Konverter die Klassen­namen selbst <br/>            (es wird etwas wie \".stl_01 {}\" ... \".stl_NN {}\" sein). |
| parts_embedding_mode | Definiert, ob referenzierte Dateien (HTML, Schriften, Bilder, CSS‑Dateien)<br/>            in die Haupt‑HTML‑Datei eingebettet oder als separate Binär‑Entitäten erzeugt werden. |
| html_markup_generation_mode | Manchmal gibt es spezielle Anforderungen an die Erzeugung von HTML‑Markup.<br/>            Dieser Parameter definiert HTML‑Vorbereitungsmodi, die während der Konvertierung von PDF zu HTML verwendet werden können, um solchen spezifischen Anforderungen gerecht zu werden. |
| raster_images_saving_mode | Eine konvertierte PDF kann Rasterbilder enthalten<br/>            Dieser Parameter definiert, wie diese während der Konvertierung von PDF zu HTML behandelt werden sollen. |
| remove_empty_areas_on_top_and_bottom | Definiert, ob im erzeugten HTML oben und unten leere Bereiche ohne Inhalt (falls vorhanden) entfernt werden. |
| font_encoding_strategy | Definiert eine spezielle Kodierungsregel, um die PDF‑Dekodierung für das aktuelle Dokument anzupassen. |
| pages_flow_type_depends_on_viewers_screen_size | Wenn das Attribut 'SplitOnPages=false' ist, wird das gesamte HTML, das alle Eingabe‑PDF‑Seiten repräsentiert, <br/>            in einer großen Ergebnis‑HTML‑Datei zusammengefasst. <br/>            Dieses Flag definiert, ob das Ergebnis‑HTML so erzeugt wird,<br/>            dass der Fluss der Bereiche, die PDF‑Seiten im Ergebnis‑HTML darstellen, von der Bildschirmauflösung des Betrachters abhängt. <br/>            Angenommen, die Breite des Bildschirms beim Betrachter ist groß genug, um zwei oder mehr Seiten nebeneinander horizontal anzuzeigen. Wenn dieses Flag auf true gesetzt ist, wird diese Möglichkeit genutzt (so viele Seiten wie möglich werden horizontal nebeneinander angezeigt, dann wird die nächste horizontale Gruppe von Seiten unter der ersten angezeigt).<br/>            Andernfalls fließen die Seiten so: Die nächste Seite wird immer unter der vorherigen angezeigt. |
| try_save_text_underlining_and_strikeouting_in_css | PDF selbst enthält keine Unterstreichungsmarkierungen für Texte. Es wird mit einer Linie unter dem Text emuliert.<br/>            Diese Option ermöglicht es dem Konverter, zu erraten, dass diese oder jene Linie eine Textunterstreichung ist<br/>            und diese Information in CSS zu setzen, anstatt die Unterstreichung grafisch zu zeichnen. |

### Siehe auch

* namespace [aspose.pdf](/pdf/python-net/aspose.pdf/)
* assembly [Aspose.PDF](/pdf/python-net/)

