---
title: "FontSubstitutionCollection"
linktitle: "FontSubstitutionCollection"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Représente une collection de stratégies de substitution de polices."
type: docs
weight: 60
url: /fr/java/com.aspose.pdf.text/fontsubstitutioncollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.text.FontSubstitutionCollection

**All Implemented Interfaces:**
Iterable < FontSubstitution >

```
public final class FontSubstitutionCollection extends Object implements Iterable < FontSubstitution >
```

Représente une collection de stratégies de substitution de polices.

## Constructeurs

| Constructeur | Description |
| --- | --- |
| [FontSubstitutionCollection](#FontSubstitutionCollection--) | Initialise l'objet de collection |

## Méthodes

| Méthode | Description |
| --- | --- |
| [add](#add-com.aspose.pdf.text.FontSubstitution-) | Ajoute un nouvel objet de substitution de police à la collection. |
| [clear](#clear--) | Efface la collection de substitution de police. |
| [contains](#contains-com.aspose.pdf.text.FontSubstitution-) | Détermine si un élément se trouve dans la collection. |
| [delete](#delete-com.aspose.pdf.text.FontSubstitution-) | Supprime l'élément de substitution de police uniquement pour un usage interne. |
| [getItem](#getItem-int-) | Obtient l'élément police à l'index spécifié. |
| [getSyncRoot](#getSyncRoot--) | Obtient un objet pouvant être utilisé pour synchroniser l'accès à la collection. |
| [isSynchronized](#isSynchronized--) | Obtient une valeur indiquant si l'accès à la collection est synchronisé (thread safe). |
| [iterator](#iterator--) | Renvoie un énumérateur pour l'ensemble de la collection. |
| [remove](#remove-com.aspose.pdf.text.FontSubstitution-) | Supprime l'élément de substitution de police. |
| [size](#size--) | Obtient le nombre d'éléments d'objet Font réellement contenus dans la collection. |

### FontSubstitutionCollection {#FontSubstitutionCollection--}
```
public FontSubstitutionCollection()
```

Initialise l'objet de collection

### add {#add-com.aspose.pdf.text.FontSubstitution-}
Ajoute un nouvel objet de substitution de police à la collection.

### clear {#clear--}
```
public void clear()
```

Efface la collection de substitution de police.

### contains {#contains-com.aspose.pdf.text.FontSubstitution-}
Détermine si un élément se trouve dans la collection.

### delete {#delete-com.aspose.pdf.text.FontSubstitution-}
Supprime l'élément de substitution de police uniquement pour un usage interne.

### getItem {#getItem-int-}
```
public FontSubstitution getItem(int index)
```

Obtient l'élément police à l'index spécifié.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| index |  | Index dans la collection. |

**Returns:**
Objet FontSubstitution.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Obtient un objet pouvant être utilisé pour synchroniser l'accès à la collection.

**Returns:**
Objet pour la synchronisation

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Obtient une valeur indiquant si l'accès à la collection est synchronisé (thread safe).

**Returns:**
valeur booléenne

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.Generic.List.Enumerator< FontSubstitution > iterator()
```

Renvoie un énumérateur pour l'ensemble de la collection.

**Returns:**
Objet énumérateur

### remove {#remove-com.aspose.pdf.text.FontSubstitution-}
Supprime l'élément de substitution de police.

### size {#size--}
```
public int size()
```

Obtient le nombre d'éléments d'objet Font réellement contenus dans la collection.

**Returns:**
valeur int
