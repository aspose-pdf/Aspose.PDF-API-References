---
title: "KeySize"
linktitle: "KeySize"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Définit différentes tailles de clé qui peuvent être utilisées pour chiffrer des documents PDF."
type: docs
weight: 340
url: /fr/java/com.aspose.pdf.facades/keysize/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < KeySize > com.aspose.pdf.facades.KeySize, java.lang.Enum < KeySize >, com.aspose.pdf.facades.KeySize

**All Implemented Interfaces:**
Serializable, Comparable < KeySize >

```
public enum KeySize extends Enum < KeySize >
```

Définit différentes tailles de clé qui peuvent être utilisées pour chiffrer des documents PDF.

## Champs

| Champ | Description |
| --- | --- |
| [x128](#x128) | Clé de 128 bits. |
| [x256](#x256) | Clé de 256 bits. |
| [x40](#x40) | Clé de 40 bits. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### x128 {#x128}
```
public static final KeySize x128
```

Clé de 128 bits.

### x256 {#x256}
```
public static final KeySize x256
```

Clé de 256 bits.

### x40 {#x40}
```
public static final KeySize x40
```

Clé de 40 bits.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static KeySize [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
