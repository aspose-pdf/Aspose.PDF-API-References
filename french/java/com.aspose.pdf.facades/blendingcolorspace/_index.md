---
title: "BlendingColorSpace"
linktitle: "BlendingColorSpace"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Classe représentant l'espace couleur de mélange."
type: docs
weight: 50
url: /fr/java/com.aspose.pdf.facades/blendingcolorspace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < BlendingColorSpace > com.aspose.pdf.facades.BlendingColorSpace, java.lang.Enum < BlendingColorSpace >, com.aspose.pdf.facades.BlendingColorSpace

**All Implemented Interfaces:**
Serializable, Comparable < BlendingColorSpace >

```
public enum BlendingColorSpace extends Enum < BlendingColorSpace >
```

Classe représentant l'espace couleur de mélange.

## Champs

| Champ | Description |
| --- | --- |
| [Auto](#Auto) | L'espace de couleur de fusion est déterminé automatiquement. |
| [DeviceCMYK](#DeviceCMYK) | L'espace de couleur de fusion est défini sur DeviceCMYK. |
| [DeviceRGB](#DeviceRGB) | L'espace de couleur de fusion est défini sur DeviceRGB. |
| [DontChange](#DontChange) | L'espace de couleur de fusion n'est pas modifié. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Auto {#Auto}
```
public static final BlendingColorSpace Auto
```

L'espace de couleur de fusion est déterminé automatiquement.

### DeviceCMYK {#DeviceCMYK}
```
public static final BlendingColorSpace DeviceCMYK
```

L'espace de couleur de fusion est défini sur DeviceCMYK.

### DeviceRGB {#DeviceRGB}
```
public static final BlendingColorSpace DeviceRGB
```

L'espace de couleur de fusion est défini sur DeviceRGB.

### DontChange {#DontChange}
```
public static final BlendingColorSpace DontChange
```

L'espace de couleur de fusion n'est pas modifié.

### getByValue {#getByValue-int-}
```
public static BlendingColorSpace getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static BlendingColorSpace [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
