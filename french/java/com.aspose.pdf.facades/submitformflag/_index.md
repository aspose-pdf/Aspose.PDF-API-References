---
title: "SubmitFormFlag"
linktitle: "SubmitFormFlag"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Énumération des indicateurs de soumission de formulaire possibles."
type: docs
weight: 730
url: /fr/java/com.aspose.pdf.facades/submitformflag/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < SubmitFormFlag > com.aspose.pdf.facades.SubmitFormFlag, java.lang.Enum < SubmitFormFlag >, com.aspose.pdf.facades.SubmitFormFlag

**All Implemented Interfaces:**
Serializable, Comparable < SubmitFormFlag >

```
public enum SubmitFormFlag extends Enum < SubmitFormFlag >
```

Énumération des indicateurs de soumission de formulaire possibles.

## Champs

| Champ | Description |
| --- | --- |
| [Fdf](#Fdf) | Les données seront renvoyées au format FDF. |
| [FdfWithComments](#FdfWithComments) | Les données seront renvoyées au format FDF avec des commentaires. |
| [Html](#Html) | Les données seront renvoyées au format HTML. |
| [Pdf](#Pdf) | Le fichier PDF complet sera soumis. |
| [Xfdf](#Xfdf) | Les données seront renvoyées au format XFDF. |
| [XfdfWithComments](#XfdfWithComments) | Les données seront renvoyées au format XFDF avec des commentaires. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Fdf {#Fdf}
```
public static final SubmitFormFlag Fdf
```

Les données seront renvoyées au format FDF.

### FdfWithComments {#FdfWithComments}
```
public static final SubmitFormFlag FdfWithComments
```

Les données seront renvoyées au format FDF avec des commentaires.

### Html {#Html}
```
public static final SubmitFormFlag Html
```

Les données seront renvoyées au format HTML.

### Pdf {#Pdf}
```
public static final SubmitFormFlag Pdf
```

Le fichier PDF complet sera soumis.

### Xfdf {#Xfdf}
```
public static final SubmitFormFlag Xfdf
```

Les données seront renvoyées au format XFDF.

### XfdfWithComments {#XfdfWithComments}
```
public static final SubmitFormFlag XfdfWithComments
```

Les données seront renvoyées au format XFDF avec des commentaires.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static SubmitFormFlag [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
