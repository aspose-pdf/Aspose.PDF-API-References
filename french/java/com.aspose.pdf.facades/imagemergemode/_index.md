---
title: "ImageMergeMode"
linktitle: "ImageMergeMode"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Représente les modes de fusion d'images."
type: docs
weight: 270
url: /fr/java/com.aspose.pdf.facades/imagemergemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ImageMergeMode > com.aspose.pdf.facades.ImageMergeMode, java.lang.Enum < ImageMergeMode >, com.aspose.pdf.facades.ImageMergeMode

**All Implemented Interfaces:**
Serializable, Comparable < ImageMergeMode >

```
public enum ImageMergeMode extends Enum < ImageMergeMode >
```

Représente les modes de fusion d'images.

## Champs

| Champ | Description |
| --- | --- |
| [Center](#Center) | Images alignés au centre. |
| [Horizontal](#Horizontal) | Images fusionnés horizontalement. |
| [Vertical](#Vertical) | Images fusionnés verticalement. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Center {#Center}
```
public static final ImageMergeMode Center
```

Images alignés au centre.

### Horizontal {#Horizontal}
```
public static final ImageMergeMode Horizontal
```

Images fusionnés horizontalement.

### Vertical {#Vertical}
```
public static final ImageMergeMode Vertical
```

Images fusionnés verticalement.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static ImageMergeMode [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
