---
title: "CollectionFieldSubtype"
linktitle: "CollectionFieldSubtype"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Représente le paramètre de sous-type d'un champ dans une collection de schéma."
type: docs
weight: 630
url: /fr/java/com.aspose.pdf/collectionfieldsubtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.Enum, com.aspose.pdf.CollectionFieldSubtype

```
public final class CollectionFieldSubtype extends com.aspose.ms.System.Enum
```

Représente le paramètre de sous-type d'un champ dans une collection de schéma.

## Champs

| Champ | Description |
| --- | --- |
| [CompressedSize](#CompressedSize) | (PDF 2.0) Les données du champ sont la longueur du flux de fichier incorporé, telle qu’identifiée par l’entrée Length dans le dictionnaire du flux de fichier incorporé, et les deux valeurs doivent être identiques. |
| [CreationDate](#CreationDate) | Les données du champ doivent être la date de création du flux de fichier incorporé, telle qu’identifiée par l’entrée CreationDate dans le fichier incorporé. |
| [D](#D) | Un type date. Les données du champ doivent être stockées sous forme de chaîne de date PDF. |
| [Desc](#Desc) | Les données du champ doivent être le nom de fichier du flux de fichier incorporé, tel qu’identifié par l’entrée UF de la spécification du fichier, si présent ; sinon par l’entrée F de la spécification du fichier. |
| [F](#F) | Les données du champ doivent être le nom de fichier du flux de fichier incorporé, tel qu’identifié par l’entrée UF de la spécification du fichier, si présent ; sinon par l’entrée F de la spécification du fichier. |
| [ModDate](#ModDate) | Les données du champ doivent être la date de modification du flux de fichier incorporé, telle qu'identifiée par l'entrée ModDate dans le dictionnaire des paramètres du fichier incorporé. |
| [N](#N) | Un type nombre. Les données du champ doivent être stockées sous forme de nombre PDF. |
| [None](#None) | Le sous‑type n'est pas défini. |
| [S](#S) | Un type texte. Les données du champ doivent être stockées sous forme de chaîne texte PDF. |
| [Size](#Size) | Les données du champ doivent être la taille du fichier incorporé, telle qu'identifiée par l'entrée Size dans le dictionnaire des paramètres du fichier incorporé. |

### CompressedSize {#CompressedSize}
```
public static final int CompressedSize
```

(PDF 2.0) Les données du champ sont la longueur du flux de fichier incorporé, telle qu’identifiée par l’entrée Length dans le dictionnaire du flux de fichier incorporé, et les deux valeurs doivent être identiques.

### CreationDate {#CreationDate}
```
public static final int CreationDate
```

Les données du champ doivent être la date de création du flux de fichier incorporé, telle qu’identifiée par l’entrée CreationDate dans le fichier incorporé.

### D {#D}
```
public static final int D
```

Un type date. Les données du champ doivent être stockées sous forme de chaîne de date PDF.

### Desc {#Desc}
```
public static final int Desc
```

Les données du champ doivent être le nom de fichier du flux de fichier incorporé, tel qu’identifié par l’entrée UF de la spécification du fichier, si présent ; sinon par l’entrée F de la spécification du fichier.

### F {#F}
```
public static final int F
```

Les données du champ doivent être le nom de fichier du flux de fichier incorporé, tel qu’identifié par l’entrée UF de la spécification du fichier, si présent ; sinon par l’entrée F de la spécification du fichier.

### ModDate {#ModDate}
```
public static final int ModDate
```

Les données du champ doivent être la date de modification du flux de fichier incorporé, telle qu'identifiée par l'entrée ModDate dans le dictionnaire des paramètres du fichier incorporé.

### N {#N}
```
public static final int N
```

Un type nombre. Les données du champ doivent être stockées sous forme de nombre PDF.

### None {#None}
```
public static final int None
```

Le sous‑type n'est pas défini.

### S {#S}
```
public static final int S
```

Un type texte. Les données du champ doivent être stockées sous forme de chaîne texte PDF.

### Size {#Size}
```
public static final int Size
```

Les données du champ doivent être la taille du fichier incorporé, telle qu'identifiée par l'entrée Size dans le dictionnaire des paramètres du fichier incorporé.
