---
title: "CaptionPosition"
linktitle: "CaptionPosition"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Énumération du positionnement de la légende de l'annotation."
type: docs
weight: 460
url: /fr/java/com.aspose.pdf/captionposition/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < CaptionPosition > com.aspose.pdf.CaptionPosition, java.lang.Enum < CaptionPosition >, com.aspose.pdf.CaptionPosition

**All Implemented Interfaces:**
Serializable, Comparable < CaptionPosition >

```
public enum CaptionPosition extends Enum < CaptionPosition >
```

Énumération du positionnement de la légende de l'annotation.

## Champs

| Champ | Description |
| --- | --- |
| [Inline](#Inline) | La légende sera centrée à l'intérieur de la ligne (valeur par défaut). |
| [Top](#Top) | La légende sera au-dessus de la ligne. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Inline {#Inline}
```
public static final CaptionPosition Inline
```

La légende sera centrée à l'intérieur de la ligne (valeur par défaut).

### Top {#Top}
```
public static final CaptionPosition Top
```

La légende sera au-dessus de la ligne.

### getByValue {#getByValue-int-}
```
public static CaptionPosition getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static CaptionPosition [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
