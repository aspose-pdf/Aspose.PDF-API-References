---
title: "ExtractImageMode"
linktitle: "ExtractImageMode"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Définit différents modes pouvant être utilisés lors de l'extraction d'images à partir de documents."
type: docs
weight: 1360
url: /fr/java/com.aspose.pdf/extractimagemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ExtractImageMode > com.aspose.pdf.ExtractImageMode, java.lang.Enum < ExtractImageMode >, com.aspose.pdf.ExtractImageMode

**All Implemented Interfaces:**
Serializable, Comparable < ExtractImageMode >

```
public enum ExtractImageMode extends Enum < ExtractImageMode >
```

Définit différents modes pouvant être utilisés lors de l'extraction d'images à partir de documents.

## Champs

| Champ | Description |
| --- | --- |
| [ActuallyUsed](#ActuallyUsed) | Définit le mode d'extraction d'image dans lequel seules les images réellement affichées sur une page sont extraites. |
| [DefinedInResources](#DefinedInResources) | Définit le mode d'extraction d'image dans lequel toutes les images définies dans les ressources d'une page particulière sont extraites. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### ActuallyUsed {#ActuallyUsed}
```
public static final ExtractImageMode ActuallyUsed
```

Définit le mode d'extraction d'image dans lequel seules les images réellement affichées sur une page sont extraites.

### DefinedInResources {#DefinedInResources}
```
public static final ExtractImageMode DefinedInResources
```

Définit le mode d'extraction d'image dans lequel toutes les images définies dans les ressources d'une page particulière sont extraites.

### getByValue {#getByValue-int-}
```
public static ExtractImageMode getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static ExtractImageMode [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
