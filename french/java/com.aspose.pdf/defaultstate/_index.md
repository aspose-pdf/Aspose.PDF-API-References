---
title: "DefaultState"
linktitle: "DefaultState"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Représente l'état par défaut d'une couche PDF."
type: docs
weight: 950
url: /fr/java/com.aspose.pdf/defaultstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < DefaultState > com.aspose.pdf.DefaultState, java.lang.Enum < DefaultState >, com.aspose.pdf.DefaultState

**All Implemented Interfaces:**
Serializable, Comparable < DefaultState >

```
public enum DefaultState extends Enum < DefaultState >
```

Représente l'état par défaut d'une couche PDF.

## Champs

| Champ | Description |
| --- | --- |
| [Hidden](#Hidden) | L'état par défaut est masqué. |
| [Visible](#Visible) | L'état par défaut est visible. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Hidden {#Hidden}
```
public static final DefaultState Hidden
```

L'état par défaut est masqué.

### Visible {#Visible}
```
public static final DefaultState Visible
```

L'état par défaut est visible.

### getByValue {#getByValue-int-}
```
public static DefaultState getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static DefaultState [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
