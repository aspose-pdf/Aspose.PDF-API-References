---
title: "AnnotationStateModel"
linktitle: "AnnotationStateModel"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Le modèle d'état correspondant à l'état de l'annotation."
type: docs
weight: 120
url: /fr/java/com.aspose.pdf/annotationstatemodel/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationStateModel > com.aspose.pdf.AnnotationStateModel, java.lang.Enum < AnnotationStateModel >, com.aspose.pdf.AnnotationStateModel

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationStateModel >

```
public enum AnnotationStateModel extends Enum < AnnotationStateModel >
```

Le modèle d'état correspondant à l'état de l'annotation.

## Champs

| Champ | Description |
| --- | --- |
| [Marked](#Marked) | L'annotation a été marquée (ou démarquée) par l'utilisateur. |
| [Review](#Review) | L'annotation a été examinée (acceptée, rejetée, annulée, terminée, aucune) par l'utilisateur. |
| [Undefined](#Undefined) | Modèle d'état non défini. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Marked {#Marked}
```
public static final AnnotationStateModel Marked
```

L'annotation a été marquée (ou démarquée) par l'utilisateur.

### Review {#Review}
```
public static final AnnotationStateModel Review
```

L'annotation a été examinée (acceptée, rejetée, annulée, terminée, aucune) par l'utilisateur.

### Undefined {#Undefined}
```
public static final AnnotationStateModel Undefined
```

Modèle d'état non défini.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static AnnotationStateModel [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
