---
title: "AnnotationState"
linktitle: "AnnotationState"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "L'énumération des états auxquels l'annotation originale peut être définie."
type: docs
weight: 110
url: /fr/java/com.aspose.pdf/annotationstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationState > com.aspose.pdf.AnnotationState, java.lang.Enum < AnnotationState >, com.aspose.pdf.AnnotationState

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationState >

```
public enum AnnotationState extends Enum < AnnotationState >
```

L'énumération des états auxquels l'annotation originale peut être définie.

## Champs

| Champ | Description |
| --- | --- |
| [Accepted](#Accepted) | L'utilisateur accepte le changement. |
| [Cancelled](#Cancelled) | Le changement a été annulé. |
| [Completed](#Completed) | Le changement a été complété. |
| [Marked](#Marked) | L'annotation a été marquée par l'utilisateur. |
| [None](#None) | L'utilisateur n'a indiqué aucune information concernant le changement. |
| [Rejected](#Rejected) | L'utilisateur n'est pas d'accord avec le changement. |
| [Undefined](#Undefined) | État non défini. |
| [Unmarked](#Unmarked) | L'annotation n'a pas été marquée par l'utilisateur. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Accepted {#Accepted}
```
public static final AnnotationState Accepted
```

L'utilisateur accepte le changement.

### Cancelled {#Cancelled}
```
public static final AnnotationState Cancelled
```

Le changement a été annulé.

### Completed {#Completed}
```
public static final AnnotationState Completed
```

Le changement a été complété.

### Marked {#Marked}
```
public static final AnnotationState Marked
```

L'annotation a été marquée par l'utilisateur.

### None {#None}
```
public static final AnnotationState None
```

L'utilisateur n'a indiqué aucune information concernant le changement.

### Rejected {#Rejected}
```
public static final AnnotationState Rejected
```

L'utilisateur n'est pas d'accord avec le changement.

### Undefined {#Undefined}
```
public static final AnnotationState Undefined
```

État non défini.

### Unmarked {#Unmarked}
```
public static final AnnotationState Unmarked
```

L'annotation n'a pas été marquée par l'utilisateur.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static AnnotationState [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
