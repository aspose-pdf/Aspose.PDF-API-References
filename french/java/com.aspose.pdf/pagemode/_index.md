---
title: "PageMode"
linktitle: "PageMode"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "La classe décrit les composants utilisés de la page du document."
type: docs
weight: 3430
url: /fr/java/com.aspose.pdf/pagemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PageMode > com.aspose.pdf.PageMode, java.lang.Enum < PageMode >, com.aspose.pdf.PageMode

**All Implemented Interfaces:**
Serializable, Comparable < PageMode >

```
public enum PageMode extends Enum < PageMode >
```

La classe décrit les composants utilisés de la page du document.

## Champs

| Champ | Description |
| --- | --- |
| [FullScreen](#FullScreen) | FullScreen mode plein écran, sans barre de menus, contrôles de fenêtre, ou toute autre fenêtre visible. |
| [UseAttachments](#UseAttachments) | Panneau des pièces jointes visible. |
| [UseNone](#UseNone) | N'utilisez aucun composant. |
| [UseOC](#UseOC) | Panneau du groupe de contenu optionnel visible. |
| [UseOutlines](#UseOutlines) | Plan du document visible. |
| [UseThumbs](#UseThumbs) | Miniatures visibles. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### FullScreen {#FullScreen}
```
public static final PageMode FullScreen
```

FullScreen mode plein écran, sans barre de menus, contrôles de fenêtre, ou toute autre fenêtre visible.

### UseAttachments {#UseAttachments}
```
public static final PageMode UseAttachments
```

Panneau des pièces jointes visible.

### UseNone {#UseNone}
```
public static final PageMode UseNone
```

N'utilisez aucun composant.

### UseOC {#UseOC}
```
public static final PageMode UseOC
```

Panneau du groupe de contenu optionnel visible.

### UseOutlines {#UseOutlines}
```
public static final PageMode UseOutlines
```

Plan du document visible.

### UseThumbs {#UseThumbs}
```
public static final PageMode UseThumbs
```

Miniatures visibles.

### getByValue {#getByValue-int-}
```
public static PageMode getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static PageMode [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
