---
title: "PDF3DActivation"
linktitle: "PDF3DActivation"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Enum PDF3DActivation : ensemble des modes d'activation d'annotation 3D."
type: docs
weight: 3550
url: /fr/java/com.aspose.pdf/pdf3dactivation/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PDF3DActivation > com.aspose.pdf.PDF3DActivation, java.lang.Enum < PDF3DActivation >, com.aspose.pdf.PDF3DActivation

**All Implemented Interfaces:**
Serializable, Comparable < PDF3DActivation >

```
public enum PDF3DActivation extends Enum < PDF3DActivation >
```

Enum PDF3DActivation : ensemble des modes d'activation d'annotation 3D.

## Champs

| Champ | Description |
| --- | --- |
| [activatedUserOrScriptAction](#activatedUserOrScriptAction) | L'action activée par l'utilisateur ou le script |
| [activeWhenOpen](#activeWhenOpen) | Actif à l'ouverture |
| [activeWhenVisible](#activeWhenVisible) | Actif lorsqu'il est visible |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### activatedUserOrScriptAction {#activatedUserOrScriptAction}
```
public static final PDF3DActivation activatedUserOrScriptAction
```

L'action activée par l'utilisateur ou le script

### activeWhenOpen {#activeWhenOpen}
```
public static final PDF3DActivation activeWhenOpen
```

Actif à l'ouverture

### activeWhenVisible {#activeWhenVisible}
```
public static final PDF3DActivation activeWhenVisible
```

Actif lorsqu'il est visible

### getByValue {#getByValue-int-}
```
public static PDF3DActivation getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static PDF3DActivation [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
