---
title: "TextParagraphCollection"
linktitle: "TextParagraphCollection"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Représente une collection de paragraphes de texte"
type: docs
weight: 5230
url: /fr/java/com.aspose.pdf/textparagraphcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextParagraphCollection

**All Implemented Interfaces:**
Itérable < TextParagraph >

```
public final class TextParagraphCollection extends Object implements Iterable < TextParagraph >
```

Représente une collection de paragraphes de texte

## Méthodes

| Méthode | Description |
| --- | --- |
| [add](#add-com.aspose.pdf.TextParagraph-) | Ajoute l'élément de paragraphe de texte à l'index spécifié. |
| [clear](#clear--) | Efface tous les éléments de la collection. |
| [contains](#contains-com.aspose.pdf.TextParagraph-) | Détermine si la collection contient une valeur spécifique. |
| [copyTo](#copyTo-com.aspose.pdf.TextParagraph:A-int-) | Copie l'intégralité de la collection dans un tableau unidimensionnel compatible, en commençant à l'index spécifié du tableau cible. |
| [get_Item](#get_Item-int-) | Obtient l'élément de paragraphe de texte à l'index spécifié. |
| [getSyncRoot](#getSyncRoot--) | Obtient un objet pouvant être utilisé pour synchroniser l'accès à la collection. |
| [isReadOnly](#isReadOnly--) | Obtient une valeur indiquant si la collection est en lecture seule |
| [isSynchronized](#isSynchronized--) | Obtient une valeur indiquant si l'accès à la collection est synchronisé (thread safe). |
| [iterator](#iterator--) | Renvoie un énumérateur pour l'ensemble de la collection. |
| [remove](#remove-com.aspose.pdf.TextParagraph-) | Supprime l'élément spécifié de la collection. |
| [size](#size--) | Obtient le nombre d'éléments d'objet {@code TextParagraph} réellement contenus dans la collection. |

### add {#add-com.aspose.pdf.TextParagraph-}
Ajoute l'élément de paragraphe de texte à l'index spécifié.

### clear {#clear--}
```
public void clear()
```

Efface tous les éléments de la collection.

### contains {#contains-com.aspose.pdf.TextParagraph-}
Détermine si la collection contient une valeur spécifique.

### copyTo {#copyTo-com.aspose.pdf.TextParagraph:A-int-}
Copie l'intégralité de la collection dans un tableau unidimensionnel compatible, en commençant à l'index spécifié du tableau cible.

### get_Item {#get_Item-int-}
```
public TextParagraph get_Item(int index)
```

Obtient l'élément de paragraphe de texte à l'index spécifié.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| index |  | valeur int |

**Returns:**
Objet TextParagraph

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Obtient un objet pouvant être utilisé pour synchroniser l'accès à la collection.

**Returns:**
Élément d'objet

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Obtient une valeur indiquant si la collection est en lecture seule

**Returns:**
valeur booléenne

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Obtient une valeur indiquant si l'accès à la collection est synchronisé (thread safe).

**Returns:**
valeur booléenne

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator< TextParagraph > iterator()
```

Renvoie un énumérateur pour l'ensemble de la collection.

**Returns:**
Objet IEnumerator

### remove {#remove-com.aspose.pdf.TextParagraph-}
Supprime l'élément spécifié de la collection.

### size {#size--}
```
public int size()
```

Obtient le nombre d'éléments d'objet {@code TextParagraph} réellement contenus dans la collection.

**Returns:**
valeur int
