---
title: "TextReplaceOptions.Scope"
linktitle: "TextReplaceOptions.Scope"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Portée où l'opération de remplacement de texte est appliquée REPLACE_FIRST par défaut. Cette option obsolète a été conservée pour la compatibilité. Elle affecte PdfContentEditor et n'a aucun effet sur."
type: docs
weight: 5280
url: /fr/java/com.aspose.pdf/textreplaceoptions.scope/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextReplaceOptions.Scope > com.aspose.pdf.TextReplaceOptions.Scope, java.lang.Enum < TextReplaceOptions.Scope >, com.aspose.pdf.TextReplaceOptions.Scope

**All Implemented Interfaces:**
Serializable, Comparable < TextReplaceOptions.Scope >

```
public static enum TextReplaceOptions.Scope extends Enum < TextReplaceOptions.Scope >
```

Portée où l'opération de remplacement de texte est appliquée REPLACE_FIRST par défaut. Cette option obsolète a été conservée pour la compatibilité. Elle affecte PdfContentEditor et n'a aucun effet sur TextFragmentAbsorber.

## Champs

| Champ | Description |
| --- | --- |
| [REPLACE_ALL](#REPLACE_ALL) | Remplacer toutes les occurrences de texte sur toutes les pages affectées |
| [REPLACE_FIRST](#REPLACE_FIRST) | Remplacer uniquement la première occurrence du texte sur chaque page affectée |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### REPLACE_ALL {#REPLACE_ALL}
```
public static final TextReplaceOptions.Scope REPLACE_ALL
```

Remplacer toutes les occurrences de texte sur toutes les pages affectées

### REPLACE_FIRST {#REPLACE_FIRST}
```
public static final TextReplaceOptions.Scope REPLACE_FIRST
```

Remplacer uniquement la première occurrence du texte sur chaque page affectée

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static TextReplaceOptions.Scope [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
