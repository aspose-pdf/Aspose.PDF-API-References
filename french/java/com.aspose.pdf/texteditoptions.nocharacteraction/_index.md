---
title: "TextEditOptions.NoCharacterAction"
linktitle: "TextEditOptions.NoCharacterAction"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Action à effectuer si la police ne contient pas le caractère requis"
type: docs
weight: 5010
url: /fr/java/com.aspose.pdf/texteditoptions.nocharacteraction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextEditOptions.NoCharacterAction > com.aspose.pdf.TextEditOptions.NoCharacterAction, java.lang.Enum < TextEditOptions.NoCharacterAction >, com.aspose.pdf.TextEditOptions.NoCharacterAction

**All Implemented Interfaces:**
Serializable, Comparable < TextEditOptions.NoCharacterAction >

```
public static enum TextEditOptions.NoCharacterAction extends Enum < TextEditOptions.NoCharacterAction >
```

Action à effectuer si la police ne contient pas le caractère requis

## Champs

| Champ | Description |
| --- | --- |
| [ReplaceAnyway](#ReplaceAnyway) | Remplacer le texte quoi qu'il en soit sans substitution de police |
| [ReplaceFonts](#ReplaceFonts) | Remplace les polices si nécessaire pour garantir que tous les caractères du texte peuvent être affichés. |
| [ThrowException](#ThrowException) | Lancer une exception |
| [UseCustomReplacementFont](#UseCustomReplacementFont) | Remplacer la police par la police de remplacement définie |
| [UseStandardFont](#UseStandardFont) | Remplacer la police par une police standard contenant le caractère requis |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### ReplaceAnyway {#ReplaceAnyway}
```
public static final TextEditOptions.NoCharacterAction ReplaceAnyway
```

Remplacer le texte quoi qu'il en soit sans substitution de police

### ReplaceFonts {#ReplaceFonts}
```
public static final TextEditOptions.NoCharacterAction ReplaceFonts
```

Remplace les polices si nécessaire pour garantir que tous les caractères du texte peuvent être affichés.

### ThrowException {#ThrowException}
```
public static final TextEditOptions.NoCharacterAction ThrowException
```

Lancer une exception

### UseCustomReplacementFont {#UseCustomReplacementFont}
```
public static final TextEditOptions.NoCharacterAction UseCustomReplacementFont
```

Remplacer la police par la police de remplacement définie

### UseStandardFont {#UseStandardFont}
```
public static final TextEditOptions.NoCharacterAction UseStandardFont
```

Remplacer la police par une police standard contenant le caractère requis

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static TextEditOptions.NoCharacterAction [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
