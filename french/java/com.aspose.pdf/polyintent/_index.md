---
title: "PolyIntent"
linktitle: "PolyIntent"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Énumère les intentions de l'annotation de polygone ou de polyligne."
type: docs
weight: 3910
url: /fr/java/com.aspose.pdf/polyintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PolyIntent > com.aspose.pdf.PolyIntent, java.lang.Enum < PolyIntent >, com.aspose.pdf.PolyIntent

**All Implemented Interfaces:**
Serializable, Comparable < PolyIntent >

```
public enum PolyIntent extends Enum < PolyIntent >
```

Énumère les intentions de l'annotation de polygone ou de polyligne.

## Champs

| Champ | Description |
| --- | --- |
| [PolygonCloud](#PolygonCloud) | Cela signifie que l'annotation est destinée à fonctionner comme un objet nuage. |
| [PolygonDimension](#PolygonDimension) | Indique que l'annotation polygonale est destinée à fonctionner comme une dimension. |
| [PolyLineDimension](#PolyLineDimension) | Indique que l'annotation polyline est destinée à fonctionner comme une dimension. |
| [Undefined](#Undefined) | État indéfini. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### PolygonCloud {#PolygonCloud}
```
public static final PolyIntent PolygonCloud
```

Cela signifie que l'annotation est destinée à fonctionner comme un objet nuage.

### PolygonDimension {#PolygonDimension}
```
public static final PolyIntent PolygonDimension
```

Indique que l'annotation polygonale est destinée à fonctionner comme une dimension.

### PolyLineDimension {#PolyLineDimension}
```
public static final PolyIntent PolyLineDimension
```

Indique que l'annotation polyline est destinée à fonctionner comme une dimension.

### Undefined {#Undefined}
```
public static final PolyIntent Undefined
```

État indéfini.

### getByValue {#getByValue-int-}
```
public static PolyIntent getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static PolyIntent [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
