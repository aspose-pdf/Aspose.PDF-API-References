---
title: "VerticalAlignment"
linktitle: "VerticalAlignment"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Énumération des valeurs d'alignement vertical possibles."
type: docs
weight: 5450
url: /fr/java/com.aspose.pdf/verticalalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < VerticalAlignment > com.aspose.pdf.VerticalAlignment, java.lang.Enum < VerticalAlignment >, com.aspose.pdf.VerticalAlignment

**All Implemented Interfaces:**
Serializable, Comparable < VerticalAlignment >

```
public enum VerticalAlignment extends Enum < VerticalAlignment >
```

Énumération des valeurs d'alignement vertical possibles.

## Champs

| Champ | Description |
| --- | --- |
| [Bottom](#Bottom) | Aligner en bas. |
| [Center](#Center) | Alignement centré. |
| [None](#None) | Pas d'alignement (utiliser la position spécifiée). |
| [Top](#Top) | Aligner en haut. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) | Obtenir le nombre de |
| [valueOf](#valueOf-int-) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Bottom {#Bottom}
```
public static final VerticalAlignment Bottom
```

Aligner en bas.

### Center {#Center}
```
public static final VerticalAlignment Center
```

Alignement centré.

### None {#None}
```
public static final VerticalAlignment None
```

Pas d'alignement (utiliser la position spécifiée).

### Top {#Top}
```
public static final VerticalAlignment Top
```

Aligner en haut.

### getValue {#getValue--}
```
public int getValue()
```

Obtenir le nombre de

**Returns:**
valeur int de l'élément VerticalAlignment

### valueOf {#valueOf-int-}
```
public static VerticalAlignment valueOf(int alignmentType)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| alignmentType |  |  |

### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static VerticalAlignment [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
