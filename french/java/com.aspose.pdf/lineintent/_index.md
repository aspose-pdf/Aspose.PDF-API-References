---
title: "LineIntent"
linktitle: "LineIntent"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Énumère les intentions de l'annotation de ligne."
type: docs
weight: 2750
url: /fr/java/com.aspose.pdf/lineintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LineIntent > com.aspose.pdf.LineIntent, java.lang.Enum < LineIntent >, com.aspose.pdf.LineIntent

**All Implemented Interfaces:**
Serializable, Comparable < LineIntent >

```
public enum LineIntent extends Enum < LineIntent >
```

Énumère les intentions de l'annotation de ligne.

## Champs

| Champ | Description |
| --- | --- |
| [LineArrow](#LineArrow) | Indique que l'annotation est destinée à fonctionner comme une flèche. |
| [LineDimension](#LineDimension) | Indique que l'annotation est destinée à fonctionner comme une ligne de dimension. |
| [Undefined](#Undefined) | État indéfini. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### LineArrow {#LineArrow}
```
public static final LineIntent LineArrow
```

Indique que l'annotation est destinée à fonctionner comme une flèche.

### LineDimension {#LineDimension}
```
public static final LineIntent LineDimension
```

Indique que l'annotation est destinée à fonctionner comme une ligne de dimension.

### Undefined {#Undefined}
```
public static final LineIntent Undefined
```

État indéfini.

### getByValue {#getByValue-int-}
```
public static LineIntent getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static LineIntent [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
