---
title: "ColumnInfo"
linktitle: "ColumnInfo"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Cette classe représente les informations d'une colonne."
type: docs
weight: 730
url: /fr/java/com.aspose.pdf/columninfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ColumnInfo

```
public final class ColumnInfo extends Object
```

Cette classe représente les informations d'une colonne.

## Constructeurs

| Constructeur | Description |
| --- | --- |
| [ColumnInfo](#ColumnInfo--) | Initialise une nouvelle instance de la classe ColumnInfo. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getColumnCount](#getColumnCount--) | Obtient la valeur int qui indique le nombre de colonnes. |
| [getColumnSpacing](#getColumnSpacing--) | <p> Obtient ou définit une chaîne qui contient l'espacement entre les colonnes. La valeur de chaque espacement doit être séparée par un espace. L'unité par défaut est le point, mais les cm et les pouces sont également pris en charge. Par exemple,"120 2.5cm 1.5inch". </p><hr> <p> Si cette propriété n'est pas définie, la valeur par défaut 0 sera utilisée pour chaque espacement. </p> |
| [getColumnWidths](#getColumnWidths--) | Obtient ou définit une chaîne qui contient la largeur des colonnes. La valeur de chaque colonne doit être séparée par un espace. L'unité par défaut est le point, mais les cm, les pouces et le pourcentage de la largeur disponible sont également pris en charge. Par exemple,"120 2.5cm 1.5inch" |
| [setColumnCount](#setColumnCount-int-) | Définit la valeur int qui indique le nombre de colonnes. |
| [setColumnSpacing](#setColumnSpacing-java.lang.String-) | <p> Obtient ou définit une chaîne qui contient l'espacement entre les colonnes. La valeur de chaque espacement doit être séparée par un espace. L'unité par défaut est le point, mais les cm et les pouces sont également pris en charge. Par exemple,"120 2.5cm 1.5inch". </p><hr> <p> Si cette propriété n'est pas définie, la valeur par défaut 0 sera utilisée pour chaque espacement. </p> |
| [setColumnWidths](#setColumnWidths-java.lang.String-) | Obtient ou définit une chaîne qui contient la largeur des colonnes. La valeur de chaque colonne doit être séparée par un espace. L'unité par défaut est le point, mais les cm, les pouces et le pourcentage de la largeur disponible sont également pris en charge. Par exemple,"120 2.5cm 1.5inch" |

### ColumnInfo {#ColumnInfo--}
```
public ColumnInfo()
```

Initialise une nouvelle instance de la classe ColumnInfo.

### getColumnCount {#getColumnCount--}
```
public int getColumnCount()
```

Obtient la valeur int qui indique le nombre de colonnes.

**Returns:**
nombre de colonnes

### getColumnSpacing {#getColumnSpacing--}
```
public String getColumnSpacing()
```

<p> Obtient ou définit une chaîne qui contient l'espacement entre les colonnes. La valeur de chaque espacement doit être séparée par un espace. L'unité par défaut est le point, mais les cm et les pouces sont également pris en charge. Par exemple,"120 2.5cm 1.5inch". </p><hr> <p> Si cette propriété n'est pas définie, la valeur par défaut 0 sera utilisée pour chaque espacement. </p>

**Returns:**
valeur String

### getColumnWidths {#getColumnWidths--}
```
public String getColumnWidths()
```

Obtient ou définit une chaîne qui contient la largeur des colonnes. La valeur de chaque colonne doit être séparée par un espace. L'unité par défaut est le point, mais les cm, les pouces et le pourcentage de la largeur disponible sont également pris en charge. Par exemple,"120 2.5cm 1.5inch"

**Returns:**
valeur String

### setColumnCount {#setColumnCount-int-}
```
public void setColumnCount(int value)
```

Définit la valeur int qui indique le nombre de colonnes.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  | nombre de colonnes |

### setColumnSpacing {#setColumnSpacing-java.lang.String-}
<p> Obtient ou définit une chaîne qui contient l'espacement entre les colonnes. La valeur de chaque espacement doit être séparée par un espace. L'unité par défaut est le point, mais les cm et les pouces sont également pris en charge. Par exemple,"120 2.5cm 1.5inch". </p><hr> <p> Si cette propriété n'est pas définie, la valeur par défaut 0 sera utilisée pour chaque espacement. </p>

### setColumnWidths {#setColumnWidths-java.lang.String-}
Obtient ou définit une chaîne qui contient la largeur des colonnes. La valeur de chaque colonne doit être séparée par un espace. L'unité par défaut est le point, mais les cm, les pouces et le pourcentage de la largeur disponible sont également pris en charge. Par exemple,"120 2.5cm 1.5inch"
