---
title: "CoordinateOrigin"
linktitle: "CoordinateOrigin"
second_title: "Référence de l'API Aspose.PDF pour Java"
description:
type: docs
weight: 840
url: /fr/java/com.aspose.pdf/coordinateorigin/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < CoordinateOrigin > com.aspose.pdf.CoordinateOrigin, java.lang.Enum < CoordinateOrigin >, com.aspose.pdf.CoordinateOrigin

**All Implemented Interfaces:**
Serializable, Comparable < CoordinateOrigin >

```
public enum CoordinateOrigin extends Enum < CoordinateOrigin >
```



## Champs

| Champ | Description |
| --- | --- |
| [BaseLine](#BaseLine) | La plus petite coordonnée du texte doit être considérée comme la ligne de base. |
| [Descender](#Descender) | La plus petite coordonnée du texte doit être considérée comme la ligne de descente. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### BaseLine {#BaseLine}
```
public static final CoordinateOrigin BaseLine
```

La plus petite coordonnée du texte doit être considérée comme la ligne de base.

### Descender {#Descender}
```
public static final CoordinateOrigin Descender
```

La plus petite coordonnée du texte doit être considérée comme la ligne de descente.

### getByValue {#getByValue-int-}
```
public static CoordinateOrigin getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static CoordinateOrigin [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
