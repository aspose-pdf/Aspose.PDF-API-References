---
title: "HorizontalAlignment"
linktitle: "HorizontalAlignment"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Décrit l'alignement horizontal."
type: docs
weight: 1930
url: /fr/java/com.aspose.pdf/horizontalalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < HorizontalAlignment > com.aspose.pdf.HorizontalAlignment, java.lang.Enum < HorizontalAlignment >, com.aspose.pdf.HorizontalAlignment

**All Implemented Interfaces:**
Serializable, Comparable < HorizontalAlignment >

```
public enum HorizontalAlignment extends Enum < HorizontalAlignment >
```

Décrit l'alignement horizontal.

## Champs

| Champ | Description |
| --- | --- |
| [Center](#Center) | Alignement centré. |
| [FullJustify](#FullJustify) | Similaire à l'alignement 'Justify', sauf que la toute dernière ligne sera uniquement alignée à gauche en mode 'Justify', tandis qu'en mode 'FullJustify' toutes les lignes seront alignées à gauche et à droite. |
| [Justify](#Justify) | Alignement Justify. |
| [Left](#Left) | Aligner à gauche. |
| [None](#None) | Pas d'alignement. |
| [Right](#Right) | Aligner à droite. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) | Obtenir le nombre de |
| [valueOf](#valueOf-int-) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Center {#Center}
```
public static final HorizontalAlignment Center
```

Alignement centré.

### FullJustify {#FullJustify}
```
public static final HorizontalAlignment FullJustify
```

Similaire à l'alignement 'Justify', sauf que la toute dernière ligne sera uniquement alignée à gauche en mode 'Justify', tandis qu'en mode 'FullJustify' toutes les lignes seront alignées à gauche et à droite.

### Justify {#Justify}
```
public static final HorizontalAlignment Justify
```

Alignement Justify.

### Left {#Left}
```
public static final HorizontalAlignment Left
```

Aligner à gauche.

### None {#None}
```
public static final HorizontalAlignment None
```

Pas d'alignement.

### Right {#Right}
```
public static final HorizontalAlignment Right
```

Aligner à droite.

### getValue {#getValue--}
```
public int getValue()
```

Obtenir le nombre de

**Returns:**
Valeur int de l'élément HorizontalAlignment

### valueOf {#valueOf-int-}
```
public static HorizontalAlignment valueOf(int alignmentType)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| alignmentType |  |  |

### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static HorizontalAlignment [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
