---
title: "TextAlignment"
linktitle: "TextAlignment"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Alignement du texte dans l'annotation."
type: docs
weight: 4910
url: /fr/java/com.aspose.pdf/textalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextAlignment > com.aspose.pdf.TextAlignment, java.lang.Enum < TextAlignment >, com.aspose.pdf.TextAlignment

**All Implemented Interfaces:**
Serializable, Comparable < TextAlignment >

```
public enum TextAlignment extends Enum < TextAlignment >
```

Alignement du texte dans l'annotation.

## Champs

| Champ | Description |
| --- | --- |
| [Center](#Center) | Le texte est centré. |
| [Left](#Left) | Le texte est aligné à gauche. |
| [Right](#Right) | Le texte est aligné à droite. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Center {#Center}
```
public static final TextAlignment Center
```

Le texte est centré.

### Left {#Left}
```
public static final TextAlignment Left
```

Le texte est aligné à gauche.

### Right {#Right}
```
public static final TextAlignment Right
```

Le texte est aligné à droite.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static TextAlignment [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
