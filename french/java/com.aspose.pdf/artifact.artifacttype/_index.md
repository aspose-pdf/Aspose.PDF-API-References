---
title: "Artifact.ArtifactType"
linktitle: "Artifact.ArtifactType"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Énumération des types d'artefacts possibles."
type: docs
weight: 210
url: /fr/java/com.aspose.pdf/artifact.artifacttype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < Artifact.ArtifactType > com.aspose.pdf.Artifact.ArtifactType, java.lang.Enum < Artifact.ArtifactType >, com.aspose.pdf.Artifact.ArtifactType

**All Implemented Interfaces:**
Serializable, Comparable < Artifact.ArtifactType >

```
public static enum Artifact.ArtifactType extends Enum < Artifact.ArtifactType >
```

Énumération des types d'artefacts possibles.

## Champs

| Champ | Description |
| --- | --- |
| [Background](#Background) | Artefacts d'arrière-plan. |
| [Layout](#Layout) | Artefacts de mise en page. |
| [Page](#Page) | Artefacts de page. |
| [Pagination](#Pagination) | Artefacts de pagination. |
| [Undefined](#Undefined) | Le type d'artefact n'est pas défini ou est inconnu. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Background {#Background}
```
public static final Artifact.ArtifactType Background
```

Artefacts d'arrière-plan.

### Layout {#Layout}
```
public static final Artifact.ArtifactType Layout
```

Artefacts de mise en page.

### Page {#Page}
```
public static final Artifact.ArtifactType Page
```

Artefacts de page.

### Pagination {#Pagination}
```
public static final Artifact.ArtifactType Pagination
```

Artefacts de pagination.

### Undefined {#Undefined}
```
public static final Artifact.ArtifactType Undefined
```

Le type d'artefact n'est pas défini ou est inconnu.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static Artifact.ArtifactType [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
