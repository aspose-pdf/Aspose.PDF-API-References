---
title: "AbsorbedRow"
linktitle: "AbsorbedRow"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Représente une ligne de tableau qui existe sur la page"
type: docs
weight: 20
url: /fr/java/com.aspose.pdf/absorbedrow/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.AbsorbedRow

**All Implemented Interfaces:**
ITableElement, Comparable < AbsorbedRow >

```
public class AbsorbedRow extends Object implements ITableElement , Comparable < AbsorbedRow >
```

Représente une ligne de tableau qui existe sur la page

## Méthodes

| Méthode | Description |
| --- | --- |
| [compareTo](#compareTo-com.aspose.pdf.AbsorbedRow-) | Compare l'objet AbsorbedRow actuel avec un autre objet AbsorbedRow et renvoie un entier indiquant si l'objet actuel le précède, le suit ou se trouve à la même position dans l'ordre de tri que l'autre objet. |
| [getCellList](#getCellList--) | Obtient la IList en lecture seule contenant les cellules de la ligne |
| [getRectangle](#getRectangle--) | Obtient le rectangle qui décrit la position de la ligne sur la page |

### compareTo {#compareTo-com.aspose.pdf.AbsorbedRow-}
Compare l'objet AbsorbedRow actuel avec un autre objet AbsorbedRow et renvoie un entier indiquant si l'objet actuel le précède, le suit ou se trouve à la même position dans l'ordre de tri que l'autre objet.

### getCellList {#getCellList--}
```
public List < AbsorbedCell > getCellList()
```

Obtient la IList en lecture seule contenant les cellules de la ligne

**Returns:**
Liste d'objets AbsorbedCell

### getRectangle {#getRectangle--}
```
public Rectangle getRectangle()
```

Obtient le rectangle qui décrit la position de la ligne sur la page

**Returns:**
Instance de Rectangle
