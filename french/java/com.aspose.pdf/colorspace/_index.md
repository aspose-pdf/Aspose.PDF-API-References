---
title: "ColorSpace"
linktitle: "ColorSpace"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "L'énumération des espaces colorimétriques."
type: docs
weight: 700
url: /fr/java/com.aspose.pdf/colorspace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ColorSpace > com.aspose.pdf.ColorSpace, java.lang.Enum < ColorSpace >, com.aspose.pdf.ColorSpace

**All Implemented Interfaces:**
Serializable, Comparable < ColorSpace >

```
public enum ColorSpace extends Enum < ColorSpace >
```

L'énumération des espaces colorimétriques.

## Champs

| Champ | Description |
| --- | --- |
| [DeviceCMYK](#DeviceCMYK) | L'espace couleur CMYK dépendant du dispositif. |
| [DeviceGray](#DeviceGray) | L'espace couleur gris dépendant du dispositif. |
| [DeviceRGB](#DeviceRGB) | L'espace couleur RGB dépendant du dispositif. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### DeviceCMYK {#DeviceCMYK}
```
public static final ColorSpace DeviceCMYK
```

L'espace couleur CMYK dépendant du dispositif.

### DeviceGray {#DeviceGray}
```
public static final ColorSpace DeviceGray
```

L'espace couleur gris dépendant du dispositif.

### DeviceRGB {#DeviceRGB}
```
public static final ColorSpace DeviceRGB
```

L'espace couleur RGB dépendant du dispositif.

### getByValue {#getByValue-int-}
```
public static ColorSpace getByValue(int value)
```



**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static ColorSpace [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
