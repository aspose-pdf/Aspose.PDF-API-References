---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "Enum représente une action de flux de travail du programme en cas d'appel de la méthode {@code IWarningCallback.Warning(WarningInfo)}."
type: docs
weight: 4240
url: /fr/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

Enum représente une action de flux de travail du programme en cas d'appel de la méthode {@code IWarningCallback.Warning(WarningInfo)}.

## Champs

| Champ | Description |
| --- | --- |
| [Abort](#Abort) | Utilisé pour interrompre le flux. |
| [Continue](#Continue) | Utilisé pour poursuivre le flux. |

## Méthodes

| Méthode | Description |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | Renvoie la constante d'énumération de ce type avec le nom spécifié. |
| [values](#values--) | Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

Utilisé pour interrompre le flux.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

Utilisé pour poursuivre le flux.

### valueOf {#valueOf-java.lang.String-}
Renvoie la constante d'énumération de ce type avec le nom spécifié.

### values {#values--}
```
public static ReturnAction [] values()
```

Renvoie un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées.

**Returns:**
un tableau contenant les constantes de ce type d'énumération, dans l'ordre où elles sont déclarées
