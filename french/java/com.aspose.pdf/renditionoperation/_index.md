---
title: "RenditionOperation"
linktitle: "RenditionOperation"
second_title: "Référence de l'API Aspose.PDF pour Java"
description: "L'opération à effectuer lorsque l'action est déclenchée."
type: docs
weight: 4190
url: /fr/java/com.aspose.pdf/renditionoperation/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.Enum, com.aspose.pdf.RenditionOperation

```
public final class RenditionOperation extends com.aspose.ms.System.Enum
```

L'opération à effectuer lorsque l'action est déclenchée.

## Champs

| Champ | Description |
| --- | --- |
| [Pause](#Pause) | Mettre en pause toute rendition en cours d'exécution en association avec l'annotation. |
| [PlayResume](#PlayResume) | Lire la rendition spécifiée, en l'associant à l'annotation. Si une rendition est déjà associée à l'annotation, reprendre la rendition si elle est en pause. |
| [PlayStop](#PlayStop) | Si aucune rendition n'est associée à l'annotation, lire la rendition spécifiée, en l'associant à l'annotation. Si une rendition est déjà associée à l'annotation, elle doit être arrêtée, et la nouvelle rendition doit être associée à l'annotation. |
| [Resume](#Resume) | Reprendre toute rendition en cours de lecture en association avec l'annotation. |
| [Stop](#Stop) | Arrêter toute rendition en cours de lecture en association avec l'annotation. |
| [Undefined](#Undefined) | Opération non définie. |

### Pause {#Pause}
```
public static final int Pause
```

Mettre en pause toute rendition en cours d'exécution en association avec l'annotation.

### PlayResume {#PlayResume}
```
public static final int PlayResume
```

Lire la rendition spécifiée, en l'associant à l'annotation. Si une rendition est déjà associée à l'annotation, reprendre la rendition si elle est en pause.

### PlayStop {#PlayStop}
```
public static final int PlayStop
```

Si aucune rendition n'est associée à l'annotation, lire la rendition spécifiée, en l'associant à l'annotation. Si une rendition est déjà associée à l'annotation, elle doit être arrêtée, et la nouvelle rendition doit être associée à l'annotation.

### Resume {#Resume}
```
public static final int Resume
```

Reprendre toute rendition en cours de lecture en association avec l'annotation.

### Stop {#Stop}
```
public static final int Stop
```

Arrêter toute rendition en cours de lecture en association avec l'annotation.

### Undefined {#Undefined}
```
public static final int Undefined
```

Opération non définie.
