---
title: GetNextPageText
second_title: Référence de l'API Aspose.PDF pour .NET
description: Enregistre le texte dune page dans un fichier.
type: docs
weight: 180
url: /fr/net/aspose.pdf.facades/pdfextractor/getnextpagetext/
---
## GetNextPageText(string) {#getnextpagetext_1}

Enregistre le texte d'une page dans un fichier.

```csharp
public void GetNextPageText(string outputFile)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| outputFile | String | Le chemin et le nom du fichier pour enregistrer le texte. |

### Exemples

L'exemple montre l'utilisation de la méthode GetNextPageText dans le scénario d'extraction de texte.

```csharp
PdfExtractor extractor = new PdfExtractor();
extractor.BindPdf(TestPath + @"Aspose.Pdf.Kit.Pdf");
extractor.ExtractText(Encoding.Unicode);
String prefix = TestPath + @"Aspose.Pdf.Kit";
String suffix = ".txt";
int pageCount = 1;
while (extractor.HasNextPageText())
{
    extractor.GetNextPageText(prefix + pageCount + suffix);
    pageCount++;
}
```

```csharp
Dim extractor As PdfExtractor =  New PdfExtractor() 
extractor.BindPdf(TestPath + "Aspose.Pdf.Kit.Pdf")
extractor.ExtractText(Encoding.Unicode)
Dim prefix As String =  TestPath + "Aspose.Pdf.Kit" 
Dim suffix As String =  ".txt" 
Dim pageCount As Integer =  1 
While extractor.HasNextPageText()
    extractor.GetNextPageText(prefix + pageCount + suffix)
    pageCount = pageCount + 1
End While
```

### Voir également

* class [PdfExtractor](../../pdfextractor)
* espace de noms [Aspose.Pdf.Facades](../../pdfextractor)
* Assemblée [Aspose.PDF](../../../)

---

## GetNextPageText(Stream) {#getnextpagetext}

Enregistre le texte d'une page à diffuser.

```csharp
public void GetNextPageText(Stream outputStream)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| outputStream | Stream | Le flux pour enregistrer le texte. |

### Exemples

L'exemple montre la[`GetNextPageText`](../getnextpagetext) utilisation de la méthode dans le scénario d'extraction de texte.

```csharp
PdfExtractor extractor = new PdfExtractor();
extractor.BindPdf(TestPath + @"Aspose.Pdf.Kit.Pdf");
extractor.ExtractText(Encoding.Unicode);
String prefix = TestPath + @"Aspose.Pdf.Kit";
String suffix = ".txt";
int pageCount = 1;
while (extractor.HasNextPageText())
{
    FileStream fs = new FileStream(prefix + pageCount + suffix, FileMode.Create);
    extractor.GetNextPageText(prefix + pageCount + suffix);
    fs.Close();
    pageCount++;
}
```

### Voir également

* class [PdfExtractor](../../pdfextractor)
* espace de noms [Aspose.Pdf.Facades](../../pdfextractor)
* Assemblée [Aspose.PDF](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.PDF.dll -->
