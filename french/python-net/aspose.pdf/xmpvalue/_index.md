---
title: "XmpValue"
second_title: "Référence de l'API Aspose.PDF pour Python via .NET"
description: "Représente la valeur XMP"
type: docs
weight: 1790
url: /fr/python-net/aspose.pdf/xmpvalue/
---

## XmpValue class

Représente la valeur XMP

Le type XmpValue expose les membres suivants :
## Constructeurs
| Nom | Description |
| :- | :- |
| XmpValue(value) | Initialise une nouvelle instance de la classe XmpValue |
| XmpValue(value) | Initialise une nouvelle instance de la classe XmpValue |
| XmpValue(value) | Initialise une nouvelle instance de la classe XmpValue |
| XmpValue(value) | Initialise une nouvelle instance de la classe XmpValue |
| XmpValue(array) | Initialise une nouvelle instance de la classe XmpValue |
## Propriétés
| Nom | Description |
| :- | :- |
| is_string | Renvoie true si la valeur est une chaîne. |
| is_integer | Renvoie true si la valeur est un entier. |
| is_double | Renvoie true si la valeur est un nombre à virgule flottante. |
| is_date_time | Renvoie true si la valeur est de type DateTime. |
| is_field | Renvoie true si XmpValue est un champ. |
| is_named_value | Renvoie vrai si XmpValue est une valeur nommée. |
| is_raw | La valeur n'est pas prise en charge/inconnue et du code XML brut est fourni. |
| is_named_values | Renvoie vrai si XmpValue représente des valeurs nommées. |
| is_structure | Renvoie vrai si XmpValue représente une structure. |
| is_array | Renvoie vrai si XmpValue est un tableau. |
## Méthodes
| Nom | Description |
| :- | :- |
| to_string_value() | Convertit en chaîne. |
| to_integer() | Convertit en entier. |
| to_double() | Convertit en nombre à virgule flottante. |
| to_date_time() | Convertit en date et heure. |
| to_array() | Renvoie un tableau. |
| to_structure() | Renvoie la valeur XMP sous forme de structure (ensemble de champs). |
| to_field() | Renvoie la valeur XMP sous forme de champ XMP. |

### Voir aussi

* namespace [aspose.pdf](/pdf/python-net/aspose.pdf/)
* assembly [Aspose.PDF](/pdf/python-net/)

