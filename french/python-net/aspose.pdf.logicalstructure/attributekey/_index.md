---
title: "AttributeKey"
second_title: "Référence de l'API Aspose.PDF pour Python via .NET"
description: "Représente les clés d'attribut standard."
type: docs
weight: 40
url: /fr/python-net/aspose.pdf.logicalstructure/attributekey/
---

## AttributeKey class

Représente les clés d'attribut standard.

Le type AttributeKey expose les membres suivants:
## Propriétés
| Nom | Description |
| :- | :- |
| key | Obtenir Attribute Key. |
| owner | Obtenir Attribute Owner. |
| PLACEMENT | Attribut Placement (propriétaire de l'attribut Layout). |
| WRITING_MODE | Attribut WritingMode (propriétaire de l'attribut Layout). |
| BACKGROUND_COLOR | Attribut BackgroundColor (propriétaire de l'attribut Layout). |
| BORDER_COLOR | Attribut BorderColor (propriétaire de l'attribut Layout). |
| BORDER_STYLE | Attribut BorderStyle (propriétaire de l'attribut Layout). |
| BORDER_THICKNESS | Attribut BorderThickness (propriétaire de l'attribut Layout). |
| PADDING | Attribut Padding (propriétaire de l'attribut Layout). |
| COLOR | Attribut Color (propriétaire de l'attribut Layout). |
| SPACE_BEFORE | Attribut SpaceBefore (propriétaire de l'attribut Layout). |
| SPACE_AFTER | Attribut SpaceAfter (propriétaire de l'attribut de mise en page). |
| START_INDENT | Attribut StartIndent (propriétaire de l'attribut de mise en page). |
| END_INDENT | Attribut EndIndent (propriétaire de l'attribut de mise en page). |
| TEXT_INDENT | Attribut TextIndent (propriétaire de l'attribut de mise en page). |
| TEXT_ALIGN | Attribut TextAlign (propriétaire de l'attribut de mise en page). |
| B_BOX | Attribut BBox (propriétaire de l'attribut de mise en page). |
| WIDTH | Attribut Width (propriétaire de l'attribut de mise en page). |
| HEIGHT | Attribut Height (propriétaire de l'attribut de mise en page). |
| BLOCK_ALIGN | Attribut BlockAlign (propriétaire de l'attribut de mise en page). |
| INLINE_ALIGN | Attribut InlineAlign (propriétaire de l'attribut de mise en page). |
| T_BORDER_STYLE | Attribut TBorderStyle (propriétaire de l'attribut de mise en page). |
| T_PADDING | Attribut TPadding (propriétaire de l'attribut de mise en page). |
| BASELINE_SHIFT | Attribut BaselineShift (propriétaire de l'attribut de mise en page). |
| LINE_HEIGHT | Attribut LineHeight (Layout attribut propriétaire). |
| TEXT_DECORATION_COLOR | Attribut TextDecorationColor (Layout attribut propriétaire). |
| TEXT_DECORATION_THICKNESS | Attribut TextDecorationThickness (Layout attribut propriétaire). |
| TEXT_DECORATION_TYPE | Attribut TextDecorationType (Layout attribut propriétaire). |
| RUBY_ALIGN | Attribut RubyAlign (Layout attribut propriétaire). |
| RUBY_POSITION | Attribut RubyPosition (Layout attribut propriétaire). |
| GLYPH_ORIENTATION_VERTICAL | Attribut GlyphOrientationVertical (Layout attribut propriétaire). |
| COLUMN_COUNT | Attribut ColumnCount (Layout attribut propriétaire). |
| COLUMN_GAP | Attribut ColumnGap (Layout attribut propriétaire). |
| COLUMN_WIDTHS | Attribut ColumnWidths (Layout attribut propriétaire). |
| LIST_NUMBERING | Attribut ListNumbering (List attribut propriétaire). |
| ROLE | Attribut Role (PrintField attribut propriétaire). |
| CHECKED | Attribut Checked (propriétaire de l'attribut PrintField). |
| DESC | Attribut Desc (propriétaire de l'attribut PrintField). |
| ROW_SPAN | Attribut RowSpan (propriétaire de l'attribut Table). |
| COL_SPAN | Attribut ColSpan (propriétaire de l'attribut Table). |
| HEADERS | Attribut Headers (propriétaire de l'attribut Table). |
| SCOPE | Attribut Scope (propriétaire de l'attribut Table). |
| SUMMARY | Attribut Summary (propriétaire de l'attribut Table). |

### Voir aussi

* namespace [aspose.pdf.logicalstructure](/pdf/python-net/aspose.pdf.logicalstructure/)
* assembly [Aspose.PDF](/pdf/python-net/)

