---
title: "TiffDevice"
second_title: "Référence de l'API Aspose.PDF pour Python via .NET"
description: "Cette classe aide à enregistrer les pages du document PDF, page par page, dans une seule image TIFF."
type: docs
weight: 150
url: /fr/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Cette classe aide à enregistrer les pages du document PDF, page par page, dans une seule image TIFF.

Le type TiffDevice expose les membres suivants :
## Constructeurs
| Nom | Description |
| :- | :- |
| TiffDevice(resolution) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(resolution, settings) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(resolution, settings, converter) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(settings) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(settings, converter) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice() | Initialise une nouvelle instance de la classe [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) avec les paramètres par défaut. |
| TiffDevice(width, height, resolution, settings) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(page_size, resolution, settings) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(width, height, resolution) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(page_size, resolution) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(width, height, settings) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(width, height, settings, converter) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(page_size, settings, converter) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(page_size, settings) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(width, height) | Initialise une nouvelle instance de la classe TiffDevice |
| TiffDevice(page_size) | Initialise une nouvelle instance de la classe TiffDevice |
## Propriétés
| Nom | Description |
| :- | :- |
| rendering_options | Obtient ou définit les options de rendu. |
| form_presentation_mode | Obtient ou définit le mode de présentation du formulaire. |
| paramètres | Obtient les paramètres pour mapper le pdf en image tiff. |
| resolution | Obtient la résolution de l'image. |
| largeur | Obtient la largeur de sortie de l'image. |
| hauteur | Obtient la hauteur de sortie de l'image. |
## Méthodes
| Nom | Description |
| :- | :- |
| process(document, from_page, to_page, output) | Convertit certaines pages du document en tiff et les enregistre dans le flux de sortie. |
| process(document, output) | Convertit certaines pages du document en tiff et les enregistre dans le flux de sortie. |
| process(document, output_file_name) | Convertit certaines pages du document en tiff et les enregistre dans le flux de sortie. |
| process(document, from_page, to_page, output_file_name) | Convertit certaines pages du document en tiff et les enregistre dans le flux de sortie. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Effectue la binarisation Bradley pour le flux d'entrée. |

### Voir aussi

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

