---
title: "Re"
linktitle: "Re"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "فئة تمثّل عامل re (إضافة مستطيل إلى المسار)."
type: docs
weight: 460
url: /ar/java/com.aspose.pdf.operators/re/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.Operator com.aspose.pdf.operators.Re, com.aspose.pdf.Operator, com.aspose.pdf.operators.Re

```
public class Re extends Operator
```

فئة تمثّل عامل re (إضافة مستطيل إلى المسار).

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [Re](#Re--) | منشئ لاستخراج الأهداف. |
| [Re](#Re-double-double-double-double-) | منشئ لكتابة البرنامج. |
| [Re](#Re-int-com.aspose.pdf.engine.commondata.pagecontent.operators.pathconstructionoperators.AppendRectangle-) | منشئ لاستخراج الأهداف. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [accept](#accept-com.aspose.pdf.IOperatorSelector-) | يقبل كائن الزائر لمعالجة المشغل. |
| [getHeight](#getHeight--) | ارتفاع المستطيل. |
| [getWidth](#getWidth--) | يحصل على عرض المستطيل. |
| [getX](#getX--) | الإحداثي X لأكثر جانب أيسر من المستطيل. |
| [getY](#getY--) | الإحداثي Y للجانب السفلي من المستطيل. |
| [setHeight](#setHeight-double-) | ارتفاع المستطيل. |
| [setWidth](#setWidth-double-) | يضبط عرض المستطيل. |
| [setX](#setX-double-) | الإحداثي X لأكثر جانب أيسر من المستطيل. |
| [setY](#setY-double-) | الإحداثي Y للجانب السفلي من المستطيل. |
| [toString](#toString--) | يعيد تمثيل النص للمشغل. |

### Re {#Re--}
```
public Re()
```

منشئ لاستخراج الأهداف.

### Re {#Re-double-double-double-double-}
```
public Re(double x, double y, double width, double height)
```

منشئ لكتابة البرنامج.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| x |  | الإحداثي X للزاوية السفلية اليسرى للمستطيل. |
| y |  | الإحداثي Y للزاوية السفلية اليسرى للمستطيل. |
| العرض |  | عرض المستطيل. |
| الارتفاع |  | ارتفاع المستطيل. |

### Re {#Re-int-com.aspose.pdf.engine.commondata.pagecontent.operators.pathconstructionoperators.AppendRectangle-}
منشئ لاستخراج الأهداف.

### accept {#accept-com.aspose.pdf.IOperatorSelector-}
يقبل كائن الزائر لمعالجة المشغل.

### getHeight {#getHeight--}
```
public double getHeight()
```

ارتفاع المستطيل.

**Returns:**
ارتفاع المستطيل.

### getWidth {#getWidth--}
```
public double getWidth()
```

يحصل على عرض المستطيل.

**Returns:**
عرض المستطيل.

### getX {#getX--}
```
public double getX()
```

الإحداثي X لأكثر جانب أيسر من المستطيل.

**Returns:**
قيمة double

### getY {#getY--}
```
public double getY()
```

الإحداثي Y للجانب السفلي من المستطيل.

**Returns:**
قيمة double

### setHeight {#setHeight-double-}
```
public void setHeight(double value)
```

ارتفاع المستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | ارتفاع المستطيل. |

### setWidth {#setWidth-double-}
```
public void setWidth(double value)
```

يضبط عرض المستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | عرض المستطيل. |

### setX {#setX-double-}
```
public void setX(double value)
```

الإحداثي X لأكثر جانب أيسر من المستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة double |

### setY {#setY-double-}
```
public void setY(double value)
```

الإحداثي Y للجانب السفلي من المستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة double |

### toString {#toString--}
```
public String toString()
```

يعيد تمثيل النص للمشغل.

**Returns:**
تمثيل النص للمشغل.
