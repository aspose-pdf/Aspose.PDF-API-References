---
title: "LineCap"
linktitle: "LineCap"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يجب أن يحدد نمط نهاية الخط الشكل الذي سيُستخدم في نهايات المسارات الفرعية المفتوحة (والشرطات، إن وجدت) عند رسمها."
type: docs
weight: 360
url: /ar/java/com.aspose.pdf.operators/linecap/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.operators.LineCap, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.operators.LineCap, com.aspose.ms.System.Enum, com.aspose.pdf.operators.LineCap

```
public final class LineCap extends com.aspose.ms.System.Enum
```

يجب أن يحدد نمط نهاية الخط الشكل الذي سيُستخدم في نهايات المسارات الفرعية المفتوحة (والشرطات، إن وجدت) عند رسمها.

## الحقول

| حقل | الوصف |
| --- | --- |
| [ButtCap](#ButtCap) | قمة مسطحة. يجب أن يكون الخط مسطحًا عند نقطة النهاية للمسار. لا يجب أن يكون هناك امتداد بعد نهاية المسار. |
| [RoundCap](#RoundCap) | قمة مستديرة. يجب رسم قوس نصف دائري بقطر يساوي عرض الخط حول نقطة النهاية ويجب تعبئته. |
| [SquareCap](#SquareCap) | قمة مربعة بارزة. يجب أن يستمر الخط بعد نقطة النهاية للمسار بمسافة تساوي نصف عرض الخط ويجب أن يكون مسطحًا. |

### ButtCap {#ButtCap}
```
public static final int ButtCap
```

قمة مسطحة. يجب أن يكون الخط مسطحًا عند نقطة النهاية للمسار. لا يجب أن يكون هناك امتداد بعد نهاية المسار.

### RoundCap {#RoundCap}
```
public static final int RoundCap
```

قمة مستديرة. يجب رسم قوس نصف دائري بقطر يساوي عرض الخط حول نقطة النهاية ويجب تعبئته.

### SquareCap {#SquareCap}
```
public static final int SquareCap
```

قمة مربعة بارزة. يجب أن يستمر الخط بعد نقطة النهاية للمسار بمسافة تساوي نصف عرض الخط ويجب أن يكون مسطحًا.
