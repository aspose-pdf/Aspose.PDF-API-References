---
title: "PageRange"
linktitle: "PageRange"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل نطاق الصفحات لإعدادات الرأس والتذييل."
type: docs
weight: 180
url: /ar/java/com.aspose.pdf.artifacts.pagination/pagerange/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.artifacts.pagination.PageRange

```
public final class PageRange extends Object
```

يمثل نطاق الصفحات لإعدادات الرأس والتذييل.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [PageRange](#PageRange--) |  |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getEnd](#getEnd--) | يحصل أو يضبط رقم الصفحة النهائية. |
| [getEven](#getEven--) | يحصل أو يضبط الإعداد للصفحات الزوجية. |
| [getOdd](#getOdd--) | يحصل أو يضبط الإعداد للصفحات الفردية. |
| [getStart](#getStart--) | يحصل أو يضبط رقم الصفحة الابتدائي. |
| [setEnd](#setEnd-int-) | يحصل أو يضبط رقم الصفحة النهائية. |
| [setEven](#setEven-byte-) | يحصل أو يضبط الإعداد للصفحات الزوجية. |
| [setOdd](#setOdd-byte-) | يحصل أو يضبط الإعداد للصفحات الفردية. |
| [setStart](#setStart-int-) | يحصل أو يضبط رقم الصفحة الابتدائي. |

### PageRange {#PageRange--}
```
public PageRange()
```



### getEnd {#getEnd--}
```
public final int getEnd()
```

يحصل أو يضبط رقم الصفحة النهائية.

**Returns:**
قيمة int

### getEven {#getEven--}
```
public final byte getEven()
```

يحصل أو يضبط الإعداد للصفحات الزوجية.

**Returns:**
قيمة بايت

### getOdd {#getOdd--}
```
public final byte getOdd()
```

يحصل أو يضبط الإعداد للصفحات الفردية.

**Returns:**
قيمة بايت

### getStart {#getStart--}
```
public final int getStart()
```

يحصل أو يضبط رقم الصفحة الابتدائي.

**Returns:**
قيمة int

### setEnd {#setEnd-int-}
```
public final void setEnd(int value)
```

يحصل أو يضبط رقم الصفحة النهائية.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة int |

### setEven {#setEven-byte-}
```
public final void setEven(byte value)
```

يحصل أو يضبط الإعداد للصفحات الزوجية.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة بايت |

### setOdd {#setOdd-byte-}
```
public final void setOdd(byte value)
```

يحصل أو يضبط الإعداد للصفحات الفردية.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة بايت |

### setStart {#setStart-int-}
```
public final void setStart(int value)
```

يحصل أو يضبط رقم الصفحة الابتدائي.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة int |
