---
title: "PageInfo"
linktitle: "PageInfo"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل معلومات الصفحة لمولد pdf."
type: docs
weight: 3370
url: /ar/java/com.aspose.pdf/pageinfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PageInfo

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public class PageInfo extends Object implements com.aspose.ms.System.ICloneable
```

يمثل معلومات الصفحة لمولد pdf.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [PageInfo](#PageInfo--) | المنشئ الافتراضي |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [deepClone](#deepClone--) | استنساخ معلومات الصفحة. |
| [getAnyMargin](#getAnyMargin--) | يحصل أو يضبط هامش الصفحة لأي صفحة باستثناء الأولى. |
| [getDefaultTextState](#getDefaultTextState--) | يحصل على الخط الافتراضي. |
| [getHeight](#getHeight--) | يحصل على ارتفاع الصفحة. |
| [getMargin](#getMargin--) | يحصل على هامش الصفحة. |
| [getPureHeight](#getPureHeight--) | يحصل على الارتفاع الصافي للصفحة بدون هوامش. |
| [getWidth](#getWidth--) | يحصل على عرض الصفحة. |
| [isLandscape](#isLandscape--) | يحصل على ما إذا كانت الصفحة في وضعية أفقية. |
| [setAnyMargin](#setAnyMargin-com.aspose.pdf.MarginInfo-) | يحصل أو يضبط هامش الصفحة لأي صفحة باستثناء الأولى. |
| [setDefaultTextState](#setDefaultTextState-com.aspose.pdf.TextState-) | يضبط الخط الافتراضي. |
| [setHeight](#setHeight-double-) | يضبط ارتفاع الصفحة. |
| [setLandscape](#setLandscape-boolean-) | يضبط ما إذا كانت الصفحة في وضعية أفقية. |
| [setMargin](#setMargin-com.aspose.pdf.MarginInfo-) | يضبط هامش الصفحة. |
| [setWidth](#setWidth-double-) | يضبط عرض الصفحة. |

### PageInfo {#PageInfo--}
```
public PageInfo()
```

المنشئ الافتراضي

### deepClone {#deepClone--}
```
public Object deepClone()
```

استنساخ معلومات الصفحة.

**Returns:**
الكائن المستنسخ

### getAnyMargin {#getAnyMargin--}
```
public final MarginInfo getAnyMargin()
```

يحصل أو يضبط هامش الصفحة لأي صفحة باستثناء الأولى.

**Returns:**
مثيل MarginInfo

### getDefaultTextState {#getDefaultTextState--}
```
public TextState getDefaultTextState()
```

يحصل على الخط الافتراضي.

**Returns:**
مثيل TextState

### getHeight {#getHeight--}
```
public double getHeight()
```

يحصل على ارتفاع الصفحة.

**Returns:**
قيمة double

### getMargin {#getMargin--}
```
public MarginInfo getMargin()
```

يحصل على هامش الصفحة.

**Returns:**
قيمة MarginInfo

### getPureHeight {#getPureHeight--}
```
public double getPureHeight()
```

يحصل على الارتفاع الصافي للصفحة بدون هوامش.

**Returns:**
قيمة double

### getWidth {#getWidth--}
```
public double getWidth()
```

يحصل على عرض الصفحة.

**Returns:**
قيمة double

### isLandscape {#isLandscape--}
```
public boolean isLandscape()
```

يحصل على ما إذا كانت الصفحة في وضعية أفقية.

**Returns:**
قيمة منطقية

### setAnyMargin {#setAnyMargin-com.aspose.pdf.MarginInfo-}
يحصل أو يضبط هامش الصفحة لأي صفحة باستثناء الأولى.

### setDefaultTextState {#setDefaultTextState-com.aspose.pdf.TextState-}
يضبط الخط الافتراضي.

### setHeight {#setHeight-double-}
```
public final void setHeight(double value)
```

يضبط ارتفاع الصفحة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة double |

### setLandscape {#setLandscape-boolean-}
```
public void setLandscape(boolean value)
```

يضبط ما إذا كانت الصفحة في وضعية أفقية.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |

### setMargin {#setMargin-com.aspose.pdf.MarginInfo-}
يضبط هامش الصفحة.

### setWidth {#setWidth-double-}
```
public void setWidth(double value)
```

يضبط عرض الصفحة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة double |
