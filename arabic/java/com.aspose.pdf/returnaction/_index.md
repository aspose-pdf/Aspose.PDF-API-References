---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "التعداد يمثل إجراء سير عمل البرنامج في حالة استدعاء طريقة {@code IWarningCallback.Warning(WarningInfo)}."
type: docs
weight: 4240
url: /ar/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

التعداد يمثل إجراء سير عمل البرنامج في حالة استدعاء طريقة {@code IWarningCallback.Warning(WarningInfo)}.

## الحقول

| حقل | الوصف |
| --- | --- |
| [Abort](#Abort) | يُستخدم لإلغاء التدفق. |
| [Continue](#Continue) | يُستخدم لاستمرار التدفق. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | يعيد ثابت التعداد لهذا النوع بالاسم المحدد. |
| [values](#values--) | يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

يُستخدم لإلغاء التدفق.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

يُستخدم لاستمرار التدفق.

### valueOf {#valueOf-java.lang.String-}
يعيد ثابت التعداد لهذا النوع بالاسم المحدد.

### values {#values--}
```
public static ReturnAction [] values()
```

يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه.

**Returns:**
مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه
