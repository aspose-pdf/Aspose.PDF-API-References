---
title: "PrinterMarksKind"
linktitle: "PrinterMarksKind"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يحدد أنواع علامات الطابعة التي ستُضاف إلى مستند. يحتوي هذا التعداد على سمة {@link FlagsAttribute} التي تسمح بدمج قيم أعضائه بطريقة بتية."
type: docs
weight: 4020
url: /ar/java/com.aspose.pdf/printermarkskind/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.PrinterMarksKind, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.PrinterMarksKind, com.aspose.ms.System.Enum, com.aspose.pdf.PrinterMarksKind

```
public final class PrinterMarksKind extends com.aspose.ms.System.Enum
```

يحدد أنواع علامات الطابعة التي ستُضاف إلى مستند. يحتوي هذا التعداد على سمة {@link FlagsAttribute} التي تسمح بدمج قيم أعضائه بطريقة بتية.

## الحقول

| حقل | الوصف |
| --- | --- |
| [All](#All) | يحدد أنه يجب إضافة جميع أنواع علامات الطابعة. |
| [BleedMarks](#BleedMarks) | يحدد أنه يجب إضافة علامات التمدد. |
| [ColorBars](#ColorBars) | يحدد أنه يجب إضافة أشرطة الألوان. |
| [None](#None) | يحدد أنه لا يجب إضافة أي علامات طابعة. |
| [PageInformation](#PageInformation) | يحدد أنه يجب إضافة معلومات الصفحة. |
| [RegistrationMarks](#RegistrationMarks) | يحدد أنه يجب إضافة علامات التسجيل. |
| [TrimMarks](#TrimMarks) | يحدد أنه يجب إضافة علامات القص. |

### All {#All}
```
public static final int All
```

يحدد أنه يجب إضافة جميع أنواع علامات الطابعة.

### BleedMarks {#BleedMarks}
```
public static final int BleedMarks
```

يحدد أنه يجب إضافة علامات التمدد.

### ColorBars {#ColorBars}
```
public static final int ColorBars
```

يحدد أنه يجب إضافة أشرطة الألوان.

### None {#None}
```
public static final int None
```

يحدد أنه لا يجب إضافة أي علامات طابعة.

### PageInformation {#PageInformation}
```
public static final int PageInformation
```

يحدد أنه يجب إضافة معلومات الصفحة.

### RegistrationMarks {#RegistrationMarks}
```
public static final int RegistrationMarks
```

يحدد أنه يجب إضافة علامات التسجيل.

### TrimMarks {#TrimMarks}
```
public static final int TrimMarks
```

يحدد أنه يجب إضافة علامات القص.
