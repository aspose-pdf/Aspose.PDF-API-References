---
title: "SignatureCustomAppearance"
linktitle: "SignatureCustomAppearance"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "فئة مجردة تمثل كائن مظهر مخصص للتوقيع."
type: docs
weight: 4500
url: /ar/java/com.aspose.pdf/signaturecustomappearance/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.SignatureCustomAppearance

```
public final class SignatureCustomAppearance extends Object
```

فئة مجردة تمثل كائن مظهر مخصص للتوقيع.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [SignatureCustomAppearance](#SignatureCustomAppearance--) | يُنشئ مثيلًا جديدًا للفئة {@link SignatureCustomAppearance}. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getBackgroundColor](#getBackgroundColor--) | يحصل/يضبط لون الخلفية. القيمة الافتراضية: Transparent. |
| [getContactInfoLabel](#getContactInfoLabel--) | يحصل/يضبط تسمية معلومات الاتصال. القيمة الافتراضية: "Contact". |
| [getCulture](#getCulture--) | يحصل/يضبط قيمة معلومات الثقافة. القيمة الافتراضية: InvariantCulture. |
| [getDateSignedAtLabel](#getDateSignedAtLabel--) | يحصل/يضبط تسمية تاريخ التوقيع. القيمة الافتراضية: "Date". |
| [getDateTimeFormat](#getDateTimeFormat--) | يحصل/يضبط تنسيق التاريخ والوقت. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss". |
| [getDateTimeLocalFormat](#getDateTimeLocalFormat--) | يحصل/يضبط تنسيق التاريخ والوقت المحلي. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss zzz". |
| [getDigitalSignedLabel](#getDigitalSignedLabel--) | يحصل/يضبط تسمية التوقيع الرقمي. القيمة الافتراضية: "Digitally signed by". |
| [getDigitalSubjectFormat](#getDigitalSubjectFormat--) | يحصل/يضبط تنسيق ترتيب العناصر في سلسلة Subject. أمثلة النتيجة: C=UK, CN=Org, O=Organization أو CN=Org, C=UK, O=Organization أو O=Organization |
| [getFontFamilyName](#getFontFamilyName--) | يحصل/يضبط اسم عائلة الخط. يجب أن يكون موجودًا في المستند. القيمة الافتراضية: Arial. |
| [getFontSize](#getFontSize--) | يحصل/يضبط حجم الخط. القيمة الافتراضية: 10. |
| [getForegroundColor](#getForegroundColor--) | يحصل/يضبط لون المقدمة (لون النص). القيمة الافتراضية: Blue. |
| [getLocationLabel](#getLocationLabel--) | يحصل/يضبط تسمية الموقع. القيمة الافتراضية: "Location". |
| [getReasonLabel](#getReasonLabel--) | يحصل/يضبط تسمية السبب. القيمة الافتراضية: "Reason". |
| [getRotation](#getRotation--) | يحصل أو يضبط دوران التوقيع. |
| [isForegroundImage](#isForegroundImage--) | يحصل أو يضبط قيمة تشير إلى ما إذا كانت الصورة في مظهر التوقيع مرسومة كصورة أمامية. القيمة الافتراضية: false. |
| [isShowContactInfo](#isShowContactInfo--) | يحصل/يضبط رؤية معلومات الاتصال. القيمة الافتراضية: true. |
| [isShowLocation](#isShowLocation--) | يحصل/يضبط رؤية الموقع. القيمة الافتراضية: true. |
| [isShowReason](#isShowReason--) | يحصل/يضبط رؤية السبب. القيمة الافتراضية: true. |
| [isUseDigitalSubjectFormat](#isUseDigitalSubjectFormat--) | يحصل/يضبط حالة الاستخدام لـ {@code DigitalSubjectFormat}({@link #getDigitalSubjectFormat}/{@link #setDigitalSubjectFormat(int[])}). |
| [setBackgroundColor](#setBackgroundColor-com.aspose.pdf.Color-) | يحصل/يضبط لون الخلفية. القيمة الافتراضية: Transparent. |
| [setContactInfoLabel](#setContactInfoLabel-java.lang.String-) | يحصل/يضبط تسمية معلومات الاتصال. القيمة الافتراضية: "Contact". |
| [setCulture](#setCulture-java.util.Locale-) | يحصل/يضبط قيمة معلومات الثقافة. |
| [setDateSignedAtLabel](#setDateSignedAtLabel-java.lang.String-) | يحصل/يضبط تسمية تاريخ التوقيع. القيمة الافتراضية: "Date". |
| [setDateTimeFormat](#setDateTimeFormat-java.lang.String-) | يحصل/يضبط تنسيق التاريخ والوقت. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss". |
| [setDateTimeLocalFormat](#setDateTimeLocalFormat-java.lang.String-) | يحصل/يضبط تنسيق التاريخ والوقت المحلي. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss zzz". |
| [setDigitalSignedLabel](#setDigitalSignedLabel-java.lang.String-) | يحصل/يضبط تسمية التوقيع الرقمي. القيمة الافتراضية: "Digitally signed by". |
| [setDigitalSubjectFormat](#setDigitalSubjectFormat-int:A-) | يحصل/يضبط تنسيق ترتيب العناصر في سلسلة Subject. أمثلة النتيجة: C=UK, CN=Org, O=Organization أو CN=Org, C=UK, O=Organization أو O=Organization |
| [setFontFamilyName](#setFontFamilyName-java.lang.String-) | يحصل/يضبط اسم عائلة الخط. يجب أن يكون موجودًا في المستند. القيمة الافتراضية: Arial. |
| [setFontSize](#setFontSize-double-) | يحصل/يضبط حجم الخط. القيمة الافتراضية: 10. |
| [setForegroundColor](#setForegroundColor-com.aspose.pdf.Color-) | يحصل/يضبط لون المقدمة (لون النص). القيمة الافتراضية: Blue. |
| [setForegroundImage](#setForegroundImage-boolean-) | يحصل أو يضبط قيمة تشير إلى ما إذا كانت الصورة في مظهر التوقيع مرسومة كصورة أمامية. القيمة الافتراضية: false. |
| [setLocationLabel](#setLocationLabel-java.lang.String-) | يحصل/يضبط تسمية الموقع. القيمة الافتراضية: "Location". |
| [setReasonLabel](#setReasonLabel-java.lang.String-) | يحصل/يضبط تسمية السبب. القيمة الافتراضية: "Reason". |
| [setRotation](#setRotation-com.aspose.pdf.Rotation-) | يحصل أو يضبط دوران التوقيع. |
| [setShowContactInfo](#setShowContactInfo-boolean-) | يحصل/يضبط رؤية معلومات الاتصال. القيمة الافتراضية: true. |
| [setShowLocation](#setShowLocation-boolean-) | يحصل/يضبط رؤية الموقع. القيمة الافتراضية: true. |
| [setShowReason](#setShowReason-boolean-) | يحصل/يضبط رؤية السبب. القيمة الافتراضية: true. |
| [setUseDigitalSubjectFormat](#setUseDigitalSubjectFormat-boolean-) | يحصل/يضبط حالة الاستخدام لـ {@code DigitalSubjectFormat}({@link #getDigitalSubjectFormat}/{@link #setDigitalSubjectFormat(int[])}). |

### SignatureCustomAppearance {#SignatureCustomAppearance--}
```
public SignatureCustomAppearance()
```

يُنشئ مثيلًا جديدًا للفئة {@link SignatureCustomAppearance}.

### getBackgroundColor {#getBackgroundColor--}
```
public final Color getBackgroundColor()
```

يحصل/يضبط لون الخلفية. القيمة الافتراضية: Transparent.

**Returns:**
مثيل com.aspose.pdf.Color

### getContactInfoLabel {#getContactInfoLabel--}
```
public final String getContactInfoLabel()
```

يحصل/يضبط تسمية معلومات الاتصال. القيمة الافتراضية: "Contact".

**Returns:**
قيمة سلسلة

### getCulture {#getCulture--}
```
public final Locale getCulture()
```

يحصل/يضبط قيمة معلومات الثقافة. القيمة الافتراضية: InvariantCulture.

**Returns:**
قيمة Locale

### getDateSignedAtLabel {#getDateSignedAtLabel--}
```
public final String getDateSignedAtLabel()
```

يحصل/يضبط تسمية تاريخ التوقيع. القيمة الافتراضية: "Date".

**Returns:**
قيمة سلسلة

### getDateTimeFormat {#getDateTimeFormat--}
```
public final String getDateTimeFormat()
```

يحصل/يضبط تنسيق التاريخ والوقت. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss".

**Returns:**
قيمة سلسلة

### getDateTimeLocalFormat {#getDateTimeLocalFormat--}
```
public final String getDateTimeLocalFormat()
```

يحصل/يضبط تنسيق التاريخ والوقت المحلي. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss zzz".

**Returns:**
قيمة سلسلة

### getDigitalSignedLabel {#getDigitalSignedLabel--}
```
public final String getDigitalSignedLabel()
```

يحصل/يضبط تسمية التوقيع الرقمي. القيمة الافتراضية: "Digitally signed by".

**Returns:**
قيمة سلسلة

### getDigitalSubjectFormat {#getDigitalSubjectFormat--}
```
public final int[] getDigitalSubjectFormat()
```

يحصل/يضبط تنسيق ترتيب العناصر في سلسلة Subject. أمثلة النتيجة: C=UK, CN=Org, O=Organization أو CN=Org, C=UK, O=Organization أو O=Organization

**Returns:**
مصفوفة من int @see SubjectNameElements

### getFontFamilyName {#getFontFamilyName--}
```
public final String getFontFamilyName()
```

يحصل/يضبط اسم عائلة الخط. يجب أن يكون موجودًا في المستند. القيمة الافتراضية: Arial.

**Returns:**
قيمة سلسلة

### getFontSize {#getFontSize--}
```
public final double getFontSize()
```

يحصل/يضبط حجم الخط. القيمة الافتراضية: 10.

**Returns:**
قيمة double

### getForegroundColor {#getForegroundColor--}
```
public final Color getForegroundColor()
```

يحصل/يضبط لون المقدمة (لون النص). القيمة الافتراضية: Blue.

**Returns:**
مثيل com.aspose.pdf.Color

### getLocationLabel {#getLocationLabel--}
```
public final String getLocationLabel()
```

يحصل/يضبط تسمية الموقع. القيمة الافتراضية: "Location".

**Returns:**
قيمة سلسلة

### getReasonLabel {#getReasonLabel--}
```
public final String getReasonLabel()
```

يحصل/يضبط تسمية السبب. القيمة الافتراضية: "Reason".

**Returns:**
قيمة سلسلة

### getRotation {#getRotation--}
```
public final Rotation getRotation()
```

يحصل أو يضبط دوران التوقيع.

**Returns:**
عنصر الدوران

### isForegroundImage {#isForegroundImage--}
```
public final boolean isForegroundImage()
```

يحصل أو يضبط قيمة تشير إلى ما إذا كانت الصورة في مظهر التوقيع مرسومة كصورة أمامية. القيمة الافتراضية: false.

**Returns:**
قيمة منطقية

### isShowContactInfo {#isShowContactInfo--}
```
public final boolean isShowContactInfo()
```

يحصل/يضبط رؤية معلومات الاتصال. القيمة الافتراضية: true.

**Returns:**
قيمة منطقية

### isShowLocation {#isShowLocation--}
```
public final boolean isShowLocation()
```

يحصل/يضبط رؤية الموقع. القيمة الافتراضية: true.

**Returns:**
قيمة منطقية

### isShowReason {#isShowReason--}
```
public final boolean isShowReason()
```

يحصل/يضبط رؤية السبب. القيمة الافتراضية: true.

**Returns:**
قيمة منطقية

### isUseDigitalSubjectFormat {#isUseDigitalSubjectFormat--}
```
public final boolean isUseDigitalSubjectFormat()
```

يحصل/يضبط حالة الاستخدام لـ {@code DigitalSubjectFormat}({@link #getDigitalSubjectFormat}/{@link #setDigitalSubjectFormat(int[])}).

**Returns:**
قيمة منطقية

### setBackgroundColor {#setBackgroundColor-com.aspose.pdf.Color-}
يحصل/يضبط لون الخلفية. القيمة الافتراضية: Transparent.

### setContactInfoLabel {#setContactInfoLabel-java.lang.String-}
يحصل/يضبط تسمية معلومات الاتصال. القيمة الافتراضية: "Contact".

### setCulture {#setCulture-java.util.Locale-}
يحصل/يضبط قيمة معلومات الثقافة.

### setDateSignedAtLabel {#setDateSignedAtLabel-java.lang.String-}
يحصل/يضبط تسمية تاريخ التوقيع. القيمة الافتراضية: "Date".

### setDateTimeFormat {#setDateTimeFormat-java.lang.String-}
يحصل/يضبط تنسيق التاريخ والوقت. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss".

### setDateTimeLocalFormat {#setDateTimeLocalFormat-java.lang.String-}
يحصل/يضبط تنسيق التاريخ والوقت المحلي. القيمة الافتراضية: "yyyy.MM.dd HH:mm:ss zzz".

### setDigitalSignedLabel {#setDigitalSignedLabel-java.lang.String-}
يحصل/يضبط تسمية التوقيع الرقمي. القيمة الافتراضية: "Digitally signed by".

### setDigitalSubjectFormat {#setDigitalSubjectFormat-int:A-}
```
public final void setDigitalSubjectFormat(int[] value)
```

يحصل/يضبط تنسيق ترتيب العناصر في سلسلة Subject. أمثلة النتيجة: C=UK, CN=Org, O=Organization أو CN=Org, C=UK, O=Organization أو O=Organization

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | مصفوفة من int @see SubjectNameElements |

### setFontFamilyName {#setFontFamilyName-java.lang.String-}
يحصل/يضبط اسم عائلة الخط. يجب أن يكون موجودًا في المستند. القيمة الافتراضية: Arial.

### setFontSize {#setFontSize-double-}
```
public final void setFontSize(double value)
```

يحصل/يضبط حجم الخط. القيمة الافتراضية: 10.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة double |

### setForegroundColor {#setForegroundColor-com.aspose.pdf.Color-}
يحصل/يضبط لون المقدمة (لون النص). القيمة الافتراضية: Blue.

### setForegroundImage {#setForegroundImage-boolean-}
```
public final void setForegroundImage(boolean value)
```

يحصل أو يضبط قيمة تشير إلى ما إذا كانت الصورة في مظهر التوقيع مرسومة كصورة أمامية. القيمة الافتراضية: false.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |

### setLocationLabel {#setLocationLabel-java.lang.String-}
يحصل/يضبط تسمية الموقع. القيمة الافتراضية: "Location".

### setReasonLabel {#setReasonLabel-java.lang.String-}
يحصل/يضبط تسمية السبب. القيمة الافتراضية: "Reason".

### setRotation {#setRotation-com.aspose.pdf.Rotation-}
يحصل أو يضبط دوران التوقيع.

### setShowContactInfo {#setShowContactInfo-boolean-}
```
public final void setShowContactInfo(boolean value)
```

يحصل/يضبط رؤية معلومات الاتصال. القيمة الافتراضية: true.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |

### setShowLocation {#setShowLocation-boolean-}
```
public final void setShowLocation(boolean value)
```

يحصل/يضبط رؤية الموقع. القيمة الافتراضية: true.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |

### setShowReason {#setShowReason-boolean-}
```
public final void setShowReason(boolean value)
```

يحصل/يضبط رؤية السبب. القيمة الافتراضية: true.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |

### setUseDigitalSubjectFormat {#setUseDigitalSubjectFormat-boolean-}
```
public final void setUseDigitalSubjectFormat(boolean value)
```

يحصل/يضبط حالة الاستخدام لـ {@code DigitalSubjectFormat}({@link #getDigitalSubjectFormat}/{@link #setDigitalSubjectFormat(int[])}).

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |
