---
title: "IconFit"
linktitle: "IconFit"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يصف كيفية عرض أيقونة ملاحظة الودجت داخل مستطيل الملاحظة الخاص بها."
type: docs
weight: 2210
url: /ar/java/com.aspose.pdf/iconfit/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.IconFit

```
public final class IconFit extends Object
```

يصف كيفية عرض أيقونة ملاحظة الودجت داخل مستطيل الملاحظة الخاص بها.

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getLeftoverBottom](#getLeftoverBottom--) | يحصل على المساحة المخصصة في أسفل الأيقونة. |
| [getLeftoverLeft](#getLeftoverLeft--) | يحصل على المساحة المخصصة في يسار الأيقونة. |
| [getScalingMode](#getScalingMode--) | نوع التحجيم الذي سيُستخدم. |
| [getScalingReason](#getScalingReason--) | يحصل على سبب التحجيم. |
| [isSpreadOnBorder](#isSpreadOnBorder--) | إذا كان صحيحًا، يشير إلى أن مظهر الزر يجب أن يُحجم ليتناسب بالكامل داخل حدود التعليق التوضيحي دون أخذ عرض خط الحدود في الاعتبار. |
| [nameToScalingMode](#nameToScalingMode-java.lang.String-) | يحوّل اسم وضع التحجيم إلى كائن ScalingMode. |
| [nameToScalingReason](#nameToScalingReason-java.lang.String-) | يحوّل اسم سبب التحجيم إلى كائن ScalingReason. |
| [scalingModeToName](#scalingModeToName-int-) | يحوّل كائن وضع التحجيم إلى اسم. |
| [scalingReasonToName](#scalingReasonToName-int-) | يحوّل كائن سبب التحجيم إلى اسم. |
| [setLeftoverBottom](#setLeftoverBottom-double-) | يضبط المساحة المخصصة في أسفل الأيقونة. |
| [setLeftoverLeft](#setLeftoverLeft-double-) | يضبط المساحة المخصصة في يسار الأيقونة. |
| [setScalingMode](#setScalingMode-int-) | نوع التحجيم الذي سيُستخدم. |
| [setScalingReason](#setScalingReason-int-) | يضبط سبب التحجيم. |
| [setSpreadOnBorder](#setSpreadOnBorder-boolean-) | إذا كان صحيحًا، يشير إلى أن مظهر الزر يجب أن يُحجم ليتناسب بالكامل داخل حدود التعليق التوضيحي دون أخذ عرض خط الحدود في الاعتبار. |

### getLeftoverBottom {#getLeftoverBottom--}
```
public double getLeftoverBottom()
```

يحصل على المساحة المخصصة في أسفل الأيقونة.

**Returns:**
المساحة المخصصة في أسفل

### getLeftoverLeft {#getLeftoverLeft--}
```
public double getLeftoverLeft()
```

يحصل على المساحة المخصصة في يسار الأيقونة.

**Returns:**
المساحة المخصصة في يسار الأيقونة.

### getScalingMode {#getScalingMode--}
```
public int getScalingMode()
```

نوع التحجيم الذي سيُستخدم.

**Returns:**
قيمة ScalingMode @see ScalingMode

### getScalingReason {#getScalingReason--}
```
public int getScalingReason()
```

يحصل على سبب التحجيم.

**Returns:**
قيمة ScalingReason @see ScalingReason

### isSpreadOnBorder {#isSpreadOnBorder--}
```
public boolean isSpreadOnBorder()
```

إذا كان صحيحًا، يشير إلى أن مظهر الزر يجب أن يُحجم ليتناسب بالكامل داخل حدود التعليق التوضيحي دون أخذ عرض خط الحدود في الاعتبار.

**Returns:**
قيمة منطقية

### nameToScalingMode {#nameToScalingMode-java.lang.String-}
يحوّل اسم وضع التحجيم إلى كائن ScalingMode.

### nameToScalingReason {#nameToScalingReason-java.lang.String-}
يحوّل اسم سبب التحجيم إلى كائن ScalingReason.

### scalingModeToName {#scalingModeToName-int-}
```
public static String scalingModeToName(int mode)
```

يحوّل كائن وضع التحجيم إلى اسم.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| الوضع |  | كائن وضع التحجيم. |

**Returns:**
اسم وضع التحجيم.

### scalingReasonToName {#scalingReasonToName-int-}
```
public static String scalingReasonToName(int reason)
```

يحوّل كائن سبب التحجيم إلى اسم.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| السبب |  | كائن سبب التحجيم الذي سيتم تحويله. |

**Returns:**
اسم scaling reasong.

### setLeftoverBottom {#setLeftoverBottom-double-}
```
public void setLeftoverBottom(double value)
```

يضبط المساحة المخصصة في أسفل الأيقونة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | المساحة المخصصة في أسفل |

### setLeftoverLeft {#setLeftoverLeft-double-}
```
public void setLeftoverLeft(double value)
```

يضبط المساحة المخصصة في يسار الأيقونة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | المساحة المخصصة في يسار الأيقونة. |

### setScalingMode {#setScalingMode-int-}
```
public void setScalingMode(int value)
```

نوع التحجيم الذي سيُستخدم.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة ScalingMode @see ScalingMode |

### setScalingReason {#setScalingReason-int-}
```
public void setScalingReason(int value)
```

يضبط سبب التحجيم.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة ScalingReason @see ScalingReason |

### setSpreadOnBorder {#setSpreadOnBorder-boolean-}
```
public void setSpreadOnBorder(boolean value)
```

إذا كان صحيحًا، يشير إلى أن مظهر الزر يجب أن يُحجم ليتناسب بالكامل داخل حدود التعليق التوضيحي دون أخذ عرض خط الحدود في الاعتبار.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |
