---
title: "TextExtractionError"
linktitle: "TextExtractionError"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يصف حدوث خطأ استخراج النص في مستند PDF."
type: docs
weight: 5040
url: /ar/java/com.aspose.pdf/textextractionerror/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextExtractionError

```
public final class TextExtractionError extends Object
```

يصف حدوث خطأ استخراج النص في مستند PDF.

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getDescription](#getDescription--) | وصف موسع للخطأ. |
| [getExtractedText](#getExtractedText--) | النص الذي تم استخراجه فعليًا. |
| [getFontKey](#getFontKey--) | المفتاح (اسم PDF) لكائن الخط Font المستخدم لعرض النص الذي يسبب خطأ الاستخراج. |
| [getFontName](#getFontName--) | الاسم القابل للقراءة (الداخلي) لكائن الخط Font المستخدم لعرض النص الذي يسبب خطأ الاستخراج. |
| [getLocation](#getLocation--) | موقع الخطأ. |
| [getSummary](#getSummary--) | وصف مختصر للخطأ. |
| [toString](#toString--) | يرجع تمثيل النص. |

### getDescription {#getDescription--}
```
public String getDescription()
```

وصف موسع للخطأ.

**Returns:**
قيمة سلسلة

### getExtractedText {#getExtractedText--}
```
public String getExtractedText()
```

النص الذي تم استخراجه فعليًا.

**Returns:**
قيمة سلسلة

### getFontKey {#getFontKey--}
```
public String getFontKey()
```

المفتاح (اسم PDF) لكائن الخط Font المستخدم لعرض النص الذي يسبب خطأ الاستخراج.

**Returns:**
قيمة سلسلة

### getFontName {#getFontName--}
```
public String getFontName()
```

الاسم القابل للقراءة (الداخلي) لكائن الخط Font المستخدم لعرض النص الذي يسبب خطأ الاستخراج.

**Returns:**
قيمة سلسلة

### getLocation {#getLocation--}
```
public TextExtractionErrorLocation getLocation()
```

موقع الخطأ.

**Returns:**
مثيل TextExtractionErrorLocation

### getSummary {#getSummary--}
```
public String getSummary()
```

وصف مختصر للخطأ.

**Returns:**
قيمة سلسلة

### toString {#toString--}
```
public String toString()
```

يرجع تمثيل النص.

**Returns:**
تمثيل النص
