---
title: "HighlightingMode"
linktitle: "HighlightingMode"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يسرد وضع تمييز التعليق التوضيحي، التأثير البصري الذي يُستخدم عندما يتم ضغط زر الفأرة أو الإبقاء عليه داخل المنطقة النشطة."
type: docs
weight: 1920
url: /ar/java/com.aspose.pdf/highlightingmode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < HighlightingMode > com.aspose.pdf.HighlightingMode, java.lang.Enum < HighlightingMode >, com.aspose.pdf.HighlightingMode

**All Implemented Interfaces:**
Serializable, Comparable < HighlightingMode >

```
public enum HighlightingMode extends Enum < HighlightingMode >
```

يسرد وضع تمييز التعليق التوضيحي، التأثير البصري الذي يُستخدم عندما يتم ضغط زر الفأرة أو الإبقاء عليه داخل المنطقة النشطة.

## الحقول

| حقل | الوصف |
| --- | --- |
| [Invert](#Invert) | اعكس محتويات مستطيل التعليق التوضيحي. |
| [None](#None) | بدون تمييز. |
| [Outline](#Outline) | اعكس حدود التعليق التوضيحي. |
| [Push](#Push) | اعرض مظهر التعليق التوضيحي السفلي، إذا كان موجودًا. |
| [Toggle](#Toggle) | نفس Push (المفضلة). |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | يعيد ثابت التعداد لهذا النوع بالاسم المحدد. |
| [values](#values--) | يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه. |

### Invert {#Invert}
```
public static final HighlightingMode Invert
```

اعكس محتويات مستطيل التعليق التوضيحي.

### None {#None}
```
public static final HighlightingMode None
```

بدون تمييز.

### Outline {#Outline}
```
public static final HighlightingMode Outline
```

اعكس حدود التعليق التوضيحي.

### Push {#Push}
```
public static final HighlightingMode Push
```

اعرض مظهر التعليق التوضيحي السفلي، إذا كان موجودًا.

### Toggle {#Toggle}
```
public static final HighlightingMode Toggle
```

نفس Push (المفضلة).

### getByValue {#getByValue-int-}
```
public static HighlightingMode getByValue(int value)
```



**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
يعيد ثابت التعداد لهذا النوع بالاسم المحدد.

### values {#values--}
```
public static HighlightingMode [] values()
```

يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه.

**Returns:**
مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه
