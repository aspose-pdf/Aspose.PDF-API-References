---
title: "TextSegmentCollection"
linktitle: "TextSegmentCollection"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل مجموعة من مقاطع النص"
type: docs
weight: 5310
url: /ar/java/com.aspose.pdf/textsegmentcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextSegmentCollection

**All Implemented Interfaces:**
Iterable < TextSegment >

```
public final class TextSegmentCollection extends Object implements Iterable < TextSegment >
```

يمثل مجموعة من مقاطع النص

## الطرق

| طريقة | الوصف |
| --- | --- |
| [add](#add-com.aspose.pdf.TextSegment-) | يضيف عنصر مقطع النص في الفهرس المحدد. |
| [clear](#clear--) | يمسح جميع العناصر من المجموعة. |
| [contains](#contains-com.aspose.pdf.TextSegment-) | يحدّد ما إذا كانت المجموعة تحتوي على قيمة محددة. |
| [copyTo](#copyTo-com.aspose.pdf.TextSegment:A-int-) | ينسخ المجموعة بالكامل إلى مصفوفة أحادية البعد متوافقة، بدءًا من الفهرس المحدد للمصفوفة الهدف. |
| [delete](#delete-int-) | يحذف عنصر مقطع النص في الفهرس المحدد. |
| [get_Item](#get_Item-int-) | يحصل على عنصر مقطع النص في الفهرس المحدد. |
| [getSyncRoot](#getSyncRoot--) | يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة. |
| [isReadOnly](#isReadOnly--) | يحصل على قيمة تشير إلى ما إذا كانت المجموعة للقراءة فقط |
| [isSynchronized](#isSynchronized--) | يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط). |
| [iterator](#iterator--) | يعيد عدّادًا للمجموعة بالكامل. |
| [remove](#remove-com.aspose.pdf.TextSegment-) | يحذف العنصر المحدد من المجموعة. |
| [size](#size--) | يحصل على عدد عناصر كائن {@code TextSegment} الموجودة فعليًا في المجموعة. |

### add {#add-com.aspose.pdf.TextSegment-}
يضيف عنصر مقطع النص في الفهرس المحدد.

### clear {#clear--}
```
public void clear()
```

يمسح جميع العناصر من المجموعة.

### contains {#contains-com.aspose.pdf.TextSegment-}
يحدّد ما إذا كانت المجموعة تحتوي على قيمة محددة.

### copyTo {#copyTo-com.aspose.pdf.TextSegment:A-int-}
ينسخ المجموعة بالكامل إلى مصفوفة أحادية البعد متوافقة، بدءًا من الفهرس المحدد للمصفوفة الهدف.

### delete {#delete-int-}
```
public void delete(int index)
```

يحذف عنصر مقطع النص في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | قيمة int |

### get_Item {#get_Item-int-}
```
public TextSegment get_Item(int index)
```

يحصل على عنصر مقطع النص في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | الفهرس داخل المجموعة. |

**Returns:**
كائن TextSegment.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة.

**Returns:**
عنصر Object

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

يحصل على قيمة تشير إلى ما إذا كانت المجموعة للقراءة فقط

**Returns:**
قيمة منطقية

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط).

**Returns:**
قيمة منطقية

### iterator {#iterator--}
```
public Iterator < TextSegment > iterator()
```

يعيد عدّادًا للمجموعة بالكامل.

**Returns:**
كائن عدّاد.

### remove {#remove-com.aspose.pdf.TextSegment-}
يحذف العنصر المحدد من المجموعة.

### size {#size--}
```
public int size()
```

يحصل على عدد عناصر كائن {@code TextSegment} الموجودة فعليًا في المجموعة.

**Returns:**
قيمة int
