---
title: "BitmapInfo"
linktitle: "BitmapInfo"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "كائن يحتوي على مصفوفة من البكسلات ومعلومات bitmap."
type: docs
weight: 300
url: /ar/java/com.aspose.pdf/bitmapinfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.BitmapInfo

```
public class BitmapInfo extends Object
```

كائن يحتوي على مصفوفة من البكسلات ومعلومات bitmap.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [BitmapInfo](#BitmapInfo-byte:A-int-int-int-) | ينشئ نسخة جديدة من الفئة. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getFormat](#getFormat--) | يحصل على تنسيق البكسل للـ bitmap. |
| [getHeight](#getHeight--) | يحصل على ارتفاع الـ bitmap. |
| [getPixelBytes](#getPixelBytes--) | يحصل على مصفوفة البكسلات. |
| [getWidth](#getWidth--) | يحصل على عرض الـ bitmap. |

### BitmapInfo {#BitmapInfo-byte:A-int-int-int-}
```
public BitmapInfo(byte[] pixelBytes, int width, int height, int format)
```

ينشئ نسخة جديدة من الفئة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| pixelBytes |  | مصفوفة البكسلات. |
| العرض |  | عرض الـ bitmap. |
| الارتفاع |  | ارتفاع الـ bitmap. |
| تنسيق |  | تنسيق البكسل للـ bitmap. @see BitmapInfo |

### getFormat {#getFormat--}
```
public final int getFormat()
```

يحصل على تنسيق البكسل للـ bitmap.

**Returns:**
عنصر int value PixelFormat

### getHeight {#getHeight--}
```
public final int getHeight()
```

يحصل على ارتفاع الـ bitmap.

**Returns:**
قيمة int

### getPixelBytes {#getPixelBytes--}
```
public final byte[] getPixelBytes()
```

يحصل على مصفوفة البكسلات.

**Returns:**
مصفوفة byte[]

### getWidth {#getWidth--}
```
public final int getWidth()
```

يحصل على عرض الـ bitmap.

**Returns:**
قيمة int
