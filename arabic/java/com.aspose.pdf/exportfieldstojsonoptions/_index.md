---
title: "ExportFieldsToJsonOptions"
linktitle: "ExportFieldsToJsonOptions"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل خيارات تصدير حقول النموذج إلى تنسيق Json. يرث من {@link ExportFieldsOptions} ويضيف خيارات محددة لتصدير Json."
type: docs
weight: 1320
url: /ar/java/com.aspose.pdf/exportfieldstojsonoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ExportFieldsOptions com.aspose.pdf.ExportFieldsToJsonOptions, com.aspose.pdf.ExportFieldsOptions, com.aspose.pdf.ExportFieldsToJsonOptions

```
public class ExportFieldsToJsonOptions extends ExportFieldsOptions
```

يمثل خيارات تصدير حقول النموذج إلى تنسيق Json. يرث من {@link ExportFieldsOptions} ويضيف خيارات محددة لتصدير Json.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [ExportFieldsToJsonOptions](#ExportFieldsToJsonOptions--) | يقوم بإنشاء نسخة جديدة من الفئة {@link ExportFieldsToJsonOptions} class. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getWriteIndented](#getWriteIndented--) | يحصل أو يعيّن قيمة تشير إلى ما إذا كان يجب تنسيق مخرجات Json بفراغات. القيمة: {@code true} إذا كان يجب تنسيق مخرجات Json بفراغات؛ وإلا {@code false}. |
| [setWriteIndented](#setWriteIndented-boolean-) | يحصل أو يعيّن قيمة تشير إلى ما إذا كان يجب تنسيق مخرجات Json بفراغات. القيمة: {@code true} إذا كان يجب تنسيق مخرجات Json بفراغات؛ وإلا {@code false}. |

### ExportFieldsToJsonOptions {#ExportFieldsToJsonOptions--}
```
public ExportFieldsToJsonOptions()
```

يقوم بإنشاء نسخة جديدة من الفئة {@link ExportFieldsToJsonOptions} class.

### getWriteIndented {#getWriteIndented--}
```
public final boolean getWriteIndented()
```

يحصل أو يعيّن قيمة تشير إلى ما إذا كان يجب تنسيق مخرجات Json بفراغات. القيمة: {@code true} إذا كان يجب تنسيق مخرجات Json بفراغات؛ وإلا {@code false}.

**Returns:**
قيمة منطقية

### setWriteIndented {#setWriteIndented-boolean-}
```
public final void setWriteIndented(boolean value)
```

يحصل أو يعيّن قيمة تشير إلى ما إذا كان يجب تنسيق مخرجات Json بفراغات. القيمة: {@code true} إذا كان يجب تنسيق مخرجات Json بفراغات؛ وإلا {@code false}.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |
