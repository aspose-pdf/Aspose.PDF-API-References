---
title: "PDF3DCrossSectionArray"
linktitle: "PDF3DCrossSectionArray"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "فئة PDF3DCrossSectionArray."
type: docs
weight: 3600
url: /ar/java/com.aspose.pdf/pdf3dcrosssectionarray/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DCrossSectionArray

```
public class PDF3DCrossSectionArray extends Object
```

فئة PDF3DCrossSectionArray.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [PDF3DCrossSectionArray](#PDF3DCrossSectionArray-com.aspose.pdf.IDocument-) | ينشئ مثيلًا جديدًا من الفئة {@code PDF3DCrossSectionArray}. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [add](#add-com.aspose.pdf.PDF3DCrossSection-) | يضيف المقطع العرضي المحدد إلى مصفوفة العروض. |
| [get_Item](#get_Item-int-) | يحصل أو يضبط {@code PDF3DCrossSection} في الفهرس المحدد. |
| [getCount](#getCount--) | يحصل على عدد المقاطع العرضية. |
| [removeAll](#removeAll--) | يزيل جميع المقاطع العرضية من المصفوفة. |
| [removeAt](#removeAt-int-) | يزيل المقطع العرضي من المصفوفة في الفهرس المحدد. |
| [set_Item](#set_Item-int-com.aspose.pdf.PDF3DCrossSection-) | يحصل أو يضبط {@code PDF3DCrossSection} في الفهرس المحدد. |

### PDF3DCrossSectionArray {#PDF3DCrossSectionArray-com.aspose.pdf.IDocument-}
ينشئ مثيلًا جديدًا من الفئة {@code PDF3DCrossSectionArray}.

### add {#add-com.aspose.pdf.PDF3DCrossSection-}
يضيف المقطع العرضي المحدد إلى مصفوفة العروض.

### get_Item {#get_Item-int-}
```
public PDF3DCrossSection get_Item(int index)
```

يحصل أو يضبط {@code PDF3DCrossSection} في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | المؤشر. |

**Returns:**
المقاطع العرضية. @throws IndexOutOfRangeException فهرس غير صالح: يجب أن يكون الفهرس في النطاق [1..n] حيث n يساوي عدد المقاطع العرضية.

### getCount {#getCount--}
```
public int getCount()
```

يحصل على عدد المقاطع العرضية.

**Returns:**
قيمة int: عدد المقاطع العرضية.

### removeAll {#removeAll--}
```
public void removeAll()
```

يزيل جميع المقاطع العرضية من المصفوفة.

### removeAt {#removeAt-int-}
```
public void removeAt(int index)
```

يزيل المقطع العرضي من المصفوفة في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | فهرس المقاطع العرضية المحذوفة في المصفوفة. @throws IndexOutOfRangeException فهرس غير صالح: يجب أن يكون الفهرس في النطاق [1..n] حيث n يساوي عدد المقاطع العرضية. |

### set_Item {#set_Item-int-com.aspose.pdf.PDF3DCrossSection-}
يحصل أو يضبط {@code PDF3DCrossSection} في الفهرس المحدد.
