---
title: "WarningType"
linktitle: "WarningType"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "/ * نوع التحذير الممثَّل في التعداد. / * /"
type: docs
weight: 5490
url: /ar/java/com.aspose.pdf/warningtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.Enum, com.aspose.pdf.WarningType

```
public final class WarningType extends com.aspose.ms.System.Enum
```

/ * نوع التحذير الممثَّل في التعداد. / * /

## الحقول

| حقل | الوصف |
| --- | --- |
| [CompatibilityIssue](#CompatibilityIssue) | مشكلة معروفة قد تمنع فتح المستند بواسطة بعض وكلاء المستخدم، أو إصدارات سابقة من وكلاء المستخدم. |
| [DataLoss](#DataLoss) | النص/الرسم البياني/الصورة أو أي بيانات أخرى مفقودة تمامًا إما من شجرة المستند بعد التحميل، أو من المستند المُنشأ بعد الحفظ. |
| [InvalidInputStreamType](#InvalidInputStreamType) | نوع تدفق الإدخال غير صالح |
| [MajorFormattingLoss](#MajorFormattingLoss) | فقدان كبير في التنسيق مقارنة بالمستند الأصلي. يُستخدم هذا عندما يكون فقدان التنسيق كبيرًا لكن البيانات لا تزال موجودة. |
| [MinorFormattingLoss](#MinorFormattingLoss) | فقدان طفيف في التنسيق مقارنة بالمستند الأصلي. يُستخدم هذا لفقدان طفيف في الدقة. |
| [SourceFileCorruption](#SourceFileCorruption) | الملف تالف. |
| [UnexpectedContent](#UnexpectedContent) | الملف يحتوي على محتوى غير متوقع. |

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

مشكلة معروفة قد تمنع فتح المستند بواسطة بعض وكلاء المستخدم، أو إصدارات سابقة من وكلاء المستخدم.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

النص/الرسم البياني/الصورة أو أي بيانات أخرى مفقودة تمامًا إما من شجرة المستند بعد التحميل، أو من المستند المُنشأ بعد الحفظ.

### InvalidInputStreamType {#InvalidInputStreamType}
```
public static final int InvalidInputStreamType
```

نوع تدفق الإدخال غير صالح

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

فقدان كبير في التنسيق مقارنة بالمستند الأصلي. يُستخدم هذا عندما يكون فقدان التنسيق كبيرًا لكن البيانات لا تزال موجودة.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

فقدان طفيف في التنسيق مقارنة بالمستند الأصلي. يُستخدم هذا لفقدان طفيف في الدقة.

### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

الملف تالف.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

الملف يحتوي على محتوى غير متوقع.
