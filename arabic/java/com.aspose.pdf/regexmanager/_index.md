---
title: "RegexManager"
linktitle: "RegexManager"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يوفر غلافًا لعمليات التعبيرات النمطية مع إعدادات مهلة قابلة للتكوين."
type: docs
weight: 4130
url: /ar/java/com.aspose.pdf/regexmanager/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.RegexManager

```
public class RegexManager extends Object
```

يوفر غلافًا لعمليات التعبيرات النمطية مع إعدادات مهلة قابلة للتكوين.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [RegexManager](#RegexManager--) |  |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getMatchTimeout](#getMatchTimeout--) | يحصل أو يضبط مهلة عمليات Regex عبر المكتبة. القيمة الافتراضية هي 1000 مللي ثانية. القيمة: {@link double} تمثّل مدة المهلة الافتراضية. |
| [setMatchTimeout](#setMatchTimeout-int-) | يحصل أو يضبط مهلة عمليات Regex عبر المكتبة. القيمة الافتراضية هي 1000 مللي ثانية. القيمة: |

### RegexManager {#RegexManager--}
```
public RegexManager()
```



### getMatchTimeout {#getMatchTimeout--}
```
public static int getMatchTimeout()
```

يحصل أو يضبط مهلة عمليات Regex عبر المكتبة. القيمة الافتراضية هي 1000 مللي ثانية. القيمة: {@link double} تمثّل مدة المهلة الافتراضية.

**Returns:**
قيمة int

### setMatchTimeout {#setMatchTimeout-int-}
```
public static void setMatchTimeout(int value)
```

يحصل أو يضبط مهلة عمليات Regex عبر المكتبة. القيمة الافتراضية هي 1000 مللي ثانية. القيمة:

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | A تمثّل مدة المهلة الافتراضية بالمللي ثانية. |
