---
title: "عنصر"
linktitle: "عنصر"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "فئة تمثل العنصر الأساسي للهيكل المنطقي."
type: docs
weight: 1180
url: /ar/java/com.aspose.pdf/element/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.Element

```
public abstract class Element extends Object
```

فئة تمثل العنصر الأساسي للهيكل المنطقي.

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getActualText](#getActualText--) | (اختياري؛ PDF 1.4) نص يُعد بديلاً دقيقًا لعنصر البنية وأطفاله. هذا النص البديل (الذي يجب أن يُطبق على أصغر جزء من المحتوى ممكن) مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى. |
| [getAlt](#getAlt--) | (اختياري) وصف بديل لعنصر البنية وأطفاله بصيغة قابلة للقراءة البشرية، وهو مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى. |
| [getChildren](#getChildren--) | يحصل على مجموعة العناصر الفرعية. |
| [getE](#getE--) | (اختياري؛ PDF 1.5) الشكل الموسع للاختصار. |
| [getLang](#getLang--) | (اختياري؛ PDF 1.4) لغة تحدد اللغة الطبيعية لجميع النصوص في عنصر البنية باستثناء الحالات التي يتم فيها تجاوزها بواسطة مواصفات اللغة لعناصر البنية المتداخلة أو المحتوى المعلَّم. |
| [remove](#remove--) | إزالة العنصر. |
| [setActualText](#setActualText-java.lang.String-) | (اختياري؛ PDF 1.4) نص يُعد بديلاً دقيقًا لعنصر البنية وأطفاله. هذا النص البديل (الذي يجب أن يُطبق على أصغر جزء من المحتوى ممكن) مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى. |
| [setAlt](#setAlt-java.lang.String-) | (اختياري) وصف بديل لعنصر البنية وأطفاله بصيغة قابلة للقراءة البشرية، وهو مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى. |
| [setE](#setE-java.lang.String-) | (اختياري؛ PDF 1.5) الشكل الموسع للاختصار. |
| [setLang](#setLang-java.lang.String-) | (اختياري؛ PDF 1.4) لغة تحدد اللغة الطبيعية لجميع النصوص في عنصر البنية باستثناء الحالات التي يتم فيها تجاوزها بواسطة مواصفات اللغة لعناصر البنية المتداخلة أو المحتوى المعلَّم. |

### getActualText {#getActualText--}
```
public String getActualText()
```

(اختياري؛ PDF 1.4) نص يُعد بديلاً دقيقًا لعنصر البنية وأطفاله. هذا النص البديل (الذي يجب أن يُطبق على أصغر جزء من المحتوى ممكن) مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى.

**Returns:**
كائن String

### getAlt {#getAlt--}
```
public String getAlt()
```

(اختياري) وصف بديل لعنصر البنية وأطفاله بصيغة قابلة للقراءة البشرية، وهو مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى.

**Returns:**
كائن String

### getChildren {#getChildren--}
```
public final ElementCollection getChildren()
```

يحصل على مجموعة العناصر الفرعية.

**Returns:**
مثيل ElementCollection

### getE {#getE--}
```
public String getE()
```

(اختياري؛ PDF 1.5) الشكل الموسع للاختصار.

**Returns:**
كائن String

### getLang {#getLang--}
```
public String getLang()
```

(اختياري؛ PDF 1.4) لغة تحدد اللغة الطبيعية لجميع النصوص في عنصر البنية باستثناء الحالات التي يتم فيها تجاوزها بواسطة مواصفات اللغة لعناصر البنية المتداخلة أو المحتوى المعلَّم.

**Returns:**
كائن String

### remove {#remove--}
```
public final void remove()
```

إزالة العنصر.

### setActualText {#setActualText-java.lang.String-}
(اختياري؛ PDF 1.4) نص يُعد بديلاً دقيقًا لعنصر البنية وأطفاله. هذا النص البديل (الذي يجب أن يُطبق على أصغر جزء من المحتوى ممكن) مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى.

### setAlt {#setAlt-java.lang.String-}
(اختياري) وصف بديل لعنصر البنية وأطفاله بصيغة قابلة للقراءة البشرية، وهو مفيد عند استخراج محتويات المستند لدعم إمكانية الوصول للمستخدمين ذوي الإعاقات أو لأغراض أخرى.

### setE {#setE-java.lang.String-}
(اختياري؛ PDF 1.5) الشكل الموسع للاختصار.

### setLang {#setLang-java.lang.String-}
(اختياري؛ PDF 1.4) لغة تحدد اللغة الطبيعية لجميع النصوص في عنصر البنية باستثناء الحالات التي يتم فيها تجاوزها بواسطة مواصفات اللغة لعناصر البنية المتداخلة أو المحتوى المعلَّم.
