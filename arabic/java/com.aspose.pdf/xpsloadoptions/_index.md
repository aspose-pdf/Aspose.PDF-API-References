---
title: "XpsLoadOptions"
linktitle: "XpsLoadOptions"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل خيارات تحميل/استيراد ملف xps إلى مستند PDF."
type: docs
weight: 5760
url: /ar/java/com.aspose.pdf/xpsloadoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.LoadOptions com.aspose.pdf.XpsLoadOptions, com.aspose.pdf.LoadOptions, com.aspose.pdf.XpsLoadOptions

**All Implemented Interfaces:**
IPipelineOptions

```
public final class XpsLoadOptions extends LoadOptions implements IPipelineOptions
```

يمثل خيارات تحميل/استيراد ملف xps إلى مستند PDF.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [XpsLoadOptions](#XpsLoadOptions--) | ينشئ خيارات التحميل الافتراضية لتحويل ملف xps إلى مستند pdf. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getBatchSize](#getBatchSize--) | يحدد حجم الدفعة إذا كان التحويل المجمع قابلًا للتطبيق على زوج صيغ المصدر والوجهة. |
| [setBatchSize](#setBatchSize-int-) | يحدد حجم الدفعة إذا كان التحويل المجمع قابلًا للتطبيق على زوج صيغ المصدر والوجهة. |

### XpsLoadOptions {#XpsLoadOptions--}
```
public XpsLoadOptions()
```

ينشئ خيارات التحميل الافتراضية لتحويل ملف xps إلى مستند pdf.

### getBatchSize {#getBatchSize--}
```
public final int getBatchSize()
```

يحدد حجم الدفعة إذا كان التحويل المجمع قابلًا للتطبيق على زوج صيغ المصدر والوجهة.

**Returns:**
قيمة int

### setBatchSize {#setBatchSize-int-}
```
public final void setBatchSize(int value)
```

يحدد حجم الدفعة إذا كان التحويل المجمع قابلًا للتطبيق على زوج صيغ المصدر والوجهة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة int |
