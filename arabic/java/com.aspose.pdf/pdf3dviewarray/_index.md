---
title: "PDF3DViewArray"
linktitle: "PDF3DViewArray"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "الفئة PDF3DViewArray."
type: docs
weight: 3660
url: /ar/java/com.aspose.pdf/pdf3dviewarray/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DViewArray

```
public class PDF3DViewArray extends Object
```

الفئة PDF3DViewArray.

## الطرق

| طريقة | الوصف |
| --- | --- |
| [add](#add-com.aspose.pdf.PDF3DView-) | يضيف العرض المحدد. |
| [get_Item](#get_Item-int-) | يحصل أو يضبط {@code PDF3DView} إلى مصفوفة العروض في الفهرس المحدد. |
| [getCount](#getCount--) | يحصل على عدد العروض. |
| [removeAll](#removeAll--) | يزيل جميع العروض. |
| [removeAt](#removeAt-int-) | يزيل العرض من مصفوفة العروض في الفهرس المحدد. |
| [set_Item](#set_Item-int-com.aspose.pdf.PDF3DView-) | يحصل أو يضبط {@code PDF3DView} إلى مصفوفة العروض في الفهرس المحدد. |

### add {#add-com.aspose.pdf.PDF3DView-}
يضيف العرض المحدد.

### get_Item {#get_Item-int-}
```
public PDF3DView get_Item(int index)
```

يحصل أو يضبط {@code PDF3DView} إلى مصفوفة العروض في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | المؤشر. |

**Returns:**
PDF3DView. @throws IndexOutOfRangeException فهرس غير صالح: يجب أن يكون الفهرس في النطاق [1..n] حيث n يساوي عدد العروض.

### getCount {#getCount--}
```
public int getCount()
```

يحصل على عدد العروض.

**Returns:**
int value: عدد العروض.

### removeAll {#removeAll--}
```
public void removeAll()
```

يزيل جميع العروض.

### removeAt {#removeAt-int-}
```
public void removeAt(int index)
```

يزيل العرض من مصفوفة العروض في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | فهرس العرض. @throws IndexOutOfRangeException فهرس غير صالح: يجب أن يكون الفهرس في النطاق [1..n] حيث n يساوي عدد العروض. |

### set_Item {#set_Item-int-com.aspose.pdf.PDF3DView-}
يحصل أو يضبط {@code PDF3DView} إلى مصفوفة العروض في الفهرس المحدد.
