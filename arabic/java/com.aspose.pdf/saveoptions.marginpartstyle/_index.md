---
title: "SaveOptions.MarginPartStyle"
linktitle: "SaveOptions.MarginPartStyle"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل معلومات عن جزء واحد من الهوامش (الأعلى، الأسفل، الجانب الأيسر أو الجانب الأيمن)"
type: docs
weight: 4420
url: /ar/java/com.aspose.pdf/saveoptions.marginpartstyle/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.SaveOptions.MarginPartStyle

```
public static class SaveOptions.MarginPartStyle extends Object
```

يمثل معلومات عن جزء واحد من الهوامش (الأعلى، الأسفل، الجانب الأيسر أو الجانب الأيمن)

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [MarginPartStyle](#MarginPartStyle-boolean-) | ينشئ نسخة من الفئة MarginPartStyle ويُهيئ قيمتها بالنقاط |
| [MarginPartStyle](#MarginPartStyle-int-) | ينشئ نسخة من الفئة MarginPartStyle ويضبط قيمتها بالنقاط |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getValueInPoints](#getValueInPoints--) | يمثل الهامش بالنقاط. يجب أن يكون عددًا أكبر من الصفر. |
| [isAuto](#isAuto--) | يحصل أو يضبط قيمة تشير إلى ما إذا كانت هذه النسخة تلقائية. القيمة: {@code true} إذا كانت هذه النسخة تلقائية؛ وإلا {@code false}. |
| [setAuto](#setAuto-boolean-) | يحصل أو يضبط قيمة تشير إلى ما إذا كانت هذه النسخة تلقائية. القيمة: {@code true} إذا كانت هذه النسخة تلقائية؛ وإلا {@code false}. |
| [setValueInPoints](#setValueInPoints-int-) | يمثل الهامش بالنقاط. يجب أن يكون عددًا أكبر من الصفر. |

### MarginPartStyle {#MarginPartStyle-boolean-}
```
public MarginPartStyle(boolean isAuto)
```

ينشئ نسخة من الفئة MarginPartStyle ويُهيئ قيمتها بالنقاط

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| isAuto |  | تحديد الهامش تلقائيًا |

### MarginPartStyle {#MarginPartStyle-int-}
```
public MarginPartStyle(int valueInPoints)
```

ينشئ نسخة من الفئة MarginPartStyle ويضبط قيمتها بالنقاط

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| valueInPoints |  | قيمة صحيحة بالنقاط |

### getValueInPoints {#getValueInPoints--}
```
public final int getValueInPoints()
```

يمثل الهامش بالنقاط. يجب أن يكون عددًا أكبر من الصفر.

**Returns:**
قيمة int

### isAuto {#isAuto--}
```
public final boolean isAuto()
```

يحصل أو يضبط قيمة تشير إلى ما إذا كانت هذه النسخة تلقائية. القيمة: {@code true} إذا كانت هذه النسخة تلقائية؛ وإلا {@code false}.

**Returns:**
قيمة منطقية

### setAuto {#setAuto-boolean-}
```
public final void setAuto(boolean value)
```

يحصل أو يضبط قيمة تشير إلى ما إذا كانت هذه النسخة تلقائية. القيمة: {@code true} إذا كانت هذه النسخة تلقائية؛ وإلا {@code false}.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة منطقية |

### setValueInPoints {#setValueInPoints-int-}
```
public final void setValueInPoints(int value)
```

يمثل الهامش بالنقاط. يجب أن يكون عددًا أكبر من الصفر.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة int |
