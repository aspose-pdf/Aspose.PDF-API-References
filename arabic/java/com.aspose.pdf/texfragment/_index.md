---
title: "TeXFragment"
linktitle: "TeXFragment"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل جزء LaTeX."
type: docs
weight: 4860
url: /ar/java/com.aspose.pdf/texfragment/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.BaseParagraph com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.BaseParagraph, com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.FormattedFragment, com.aspose.pdf.TeXFragment

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public class TeXFragment extends FormattedFragment
```

يمثل جزء LaTeX.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [TeXFragment](#TeXFragment-java.lang.String-) | ينشئ مثيلًا جديدًا من الفئة HtmlFragment. |
| [TeXFragment](#TeXFragment-java.lang.String-boolean-) | ينشئ مثيلًا جديدًا من الفئة HtmlFragment. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [deepClone](#deepClone--) | ينسخ الجزء. |
| [getLatexLoadOptionsOfInstance](#getLatexLoadOptionsOfInstance--) | يحصل أو يعيّن TeXLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية. |
| [getTeXLoadOptionsOfInstance](#getTeXLoadOptionsOfInstance--) | يحصل أو يعيّن TeXLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية. |
| [setLatexLoadOptionsOfInstance](#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | يحصل أو يعيّن TeXLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية. |
| [setTeXLoadOptionsOfInstance](#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | يحصل أو يعيّن LatexLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية. |

### TeXFragment {#TeXFragment-java.lang.String-}
ينشئ مثيلًا جديدًا من الفئة HtmlFragment.

### TeXFragment {#TeXFragment-java.lang.String-boolean-}
ينشئ مثيلًا جديدًا من الفئة HtmlFragment.

### deepClone {#deepClone--}
```
public Object deepClone()
```

ينسخ الجزء.

**Returns:**
تم نسخ الجزء.

### getLatexLoadOptionsOfInstance {#getLatexLoadOptionsOfInstance--}
```
@Deprecated public final TeXLoadOptions getLatexLoadOptionsOfInstance()
```

يحصل أو يعيّن TeXLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية.

**Returns:**
مثيل TeXLoadOptions @deprecated يرجى استخدام TeXLoadOptionsOfInstance بدلاً من ذلك.

### getTeXLoadOptionsOfInstance {#getTeXLoadOptionsOfInstance--}
```
public TeXLoadOptions getTeXLoadOptionsOfInstance()
```

يحصل أو يعيّن TeXLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية.

**Returns:**
مثيل TeXLoadOptions

### setLatexLoadOptionsOfInstance {#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
يحصل أو يعيّن TeXLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية.

### setTeXLoadOptionsOfInstance {#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
يحصل أو يعيّن LatexLoadOptions التي ستُستخدم لتحميل (وعرض) LaTeX في هذه النسخة من الفئة. يرجى استخدامها عندما يكون من الضروري استخدام إعداد محدد لاستيراد LaTeX لهذه أو لتلك النسخة (مثلاً عندما يجب على هذه أو تلك النسخة استخدام BasePath محدد لـ LaTeX المستورد أو يجب استخدام محمل موارد خارجية محدد). إذا كان المعامل افتراضيًا (null)، فستُستخدم خيارات تحميل LaTeX القياسية.
