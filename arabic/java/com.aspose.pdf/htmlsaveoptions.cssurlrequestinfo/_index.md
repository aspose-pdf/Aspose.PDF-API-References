---
title: "HtmlSaveOptions.CssUrlRequestInfo"
linktitle: "HtmlSaveOptions.CssUrlRequestInfo"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل مجموعة من البيانات المتعلقة بطلب من المحول إلى الكود المخصص للحصول على عنوان URL (أو قالب URL) المطلوب لملف CSS المعني."
type: docs
weight: 2040
url: /ar/java/com.aspose.pdf/htmlsaveoptions.cssurlrequestinfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.HtmlSaveOptions.CssUrlRequestInfo

```
public static class HtmlSaveOptions.CssUrlRequestInfo extends Object
```

يمثل مجموعة من البيانات المتعلقة بطلب من المحول إلى الكود المخصص للحصول على عنوان URL (أو قالب URL) المطلوب لملف CSS المعني.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [CssUrlRequestInfo](#CssUrlRequestInfo--) | إنشاء نسخة من CssUrlRequestInfo |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [isCustomProcessingCancelled](#isCustomProcessingCancelled--) | يجب تعيينه بواسطة كود مخصص إذا لم يكن بإمكانه أو لا ينبغي له تحديد URL الذي سيُستخدم في HTML المُولد للإشارة إلى هذا CSS. إذا كان 'true'، فسيتم حفظ ملف CSS بطريقة قياسية في الموقع القياسي. |
| [setCustomProcessingCancelled](#setCustomProcessingCancelled-boolean-) | يجب تعيينه بواسطة كود مخصص إذا لم يكن بإمكانه أو لا ينبغي له تحديد URL الذي سيُستخدم في HTML المُولد للإشارة إلى هذا CSS. إذا كان 'true'، فسيتم حفظ ملف CSS بطريقة قياسية في الموقع القياسي. |

### CssUrlRequestInfo {#CssUrlRequestInfo--}
```
public CssUrlRequestInfo()
```

إنشاء نسخة من CssUrlRequestInfo

### isCustomProcessingCancelled {#isCustomProcessingCancelled--}
```
public boolean isCustomProcessingCancelled()
```

يجب تعيينه بواسطة كود مخصص إذا لم يكن بإمكانه أو لا ينبغي له تحديد URL الذي سيُستخدم في HTML المُولد للإشارة إلى هذا CSS. إذا كان 'true'، فسيتم حفظ ملف CSS بطريقة قياسية في الموقع القياسي.

**Returns:**
قيمة منطقية

### setCustomProcessingCancelled {#setCustomProcessingCancelled-boolean-}
```
public void setCustomProcessingCancelled(boolean customProcessingCancelled)
```

يجب تعيينه بواسطة كود مخصص إذا لم يكن بإمكانه أو لا ينبغي له تحديد URL الذي سيُستخدم في HTML المُولد للإشارة إلى هذا CSS. إذا كان 'true'، فسيتم حفظ ملف CSS بطريقة قياسية في الموقع القياسي.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| customProcessingCancelled |  | قيمة منطقية |
