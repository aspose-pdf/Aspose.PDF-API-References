---
title: "AnnotationState"
linktitle: "AnnotationState"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "تعداد الحالات التي يمكن تعيين التعليق الأصلي إليها."
type: docs
weight: 110
url: /ar/java/com.aspose.pdf/annotationstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationState > com.aspose.pdf.AnnotationState, java.lang.Enum < AnnotationState >, com.aspose.pdf.AnnotationState

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationState >

```
public enum AnnotationState extends Enum < AnnotationState >
```

تعداد الحالات التي يمكن تعيين التعليق الأصلي إليها.

## الحقول

| حقل | الوصف |
| --- | --- |
| [Accepted](#Accepted) | المستخدم يوافق على التغيير. |
| [Cancelled](#Cancelled) | تم إلغاء التغيير. |
| [Completed](#Completed) | تم إكمال التغيير. |
| [Marked](#Marked) | تم وضع علامة على التعليق بواسطة المستخدم. |
| [None](#None) | المستخدم لم يشير إلى شيء بخصوص التغيير. |
| [Rejected](#Rejected) | المستخدم لا يوافق على التغيير. |
| [Undefined](#Undefined) | حالة غير معرفة. |
| [Unmarked](#Unmarked) | لم يتم وضع علامة على التعليق بواسطة المستخدم. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | يعيد ثابت التعداد لهذا النوع بالاسم المحدد. |
| [values](#values--) | يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه. |

### Accepted {#Accepted}
```
public static final AnnotationState Accepted
```

المستخدم يوافق على التغيير.

### Cancelled {#Cancelled}
```
public static final AnnotationState Cancelled
```

تم إلغاء التغيير.

### Completed {#Completed}
```
public static final AnnotationState Completed
```

تم إكمال التغيير.

### Marked {#Marked}
```
public static final AnnotationState Marked
```

تم وضع علامة على التعليق بواسطة المستخدم.

### None {#None}
```
public static final AnnotationState None
```

المستخدم لم يشير إلى شيء بخصوص التغيير.

### Rejected {#Rejected}
```
public static final AnnotationState Rejected
```

المستخدم لا يوافق على التغيير.

### Undefined {#Undefined}
```
public static final AnnotationState Undefined
```

حالة غير معرفة.

### Unmarked {#Unmarked}
```
public static final AnnotationState Unmarked
```

لم يتم وضع علامة على التعليق بواسطة المستخدم.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
يعيد ثابت التعداد لهذا النوع بالاسم المحدد.

### values {#values--}
```
public static AnnotationState [] values()
```

يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه.

**Returns:**
مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه
