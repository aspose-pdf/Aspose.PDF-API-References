---
title: "ParagraphAbsorberOptions"
linktitle: "ParagraphAbsorberOptions"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل خيارات لـ {@link ParagraphAbsorber}."
type: docs
weight: 3480
url: /ar/java/com.aspose.pdf/paragraphabsorberoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ParagraphAbsorberOptions

```
public final class ParagraphAbsorberOptions extends Object
```

يمثل خيارات لـ {@link ParagraphAbsorber}.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [ParagraphAbsorberOptions](#ParagraphAbsorberOptions--) | ينشئ مثيلًا جديدًا لكائن {@link ParagraphAbsorberOptions}. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getSearchRectangle](#getSearchRectangle--) | يحصل أو يضبط مستطيل البحث للفقرة. |
| [getSectionUnbreakingHorizontalOverride](#getSectionUnbreakingHorizontalOverride--) | يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم أفقي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005. |
| [getSectionUnbreakingVerticalOverride](#getSectionUnbreakingVerticalOverride--) | يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم عمودي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005. |
| [setSearchRectangle](#setSearchRectangle-com.aspose.pdf.Rectangle-) | يحصل أو يضبط مستطيل البحث للفقرة. |
| [setSectionUnbreakingHorizontalOverride](#setSectionUnbreakingHorizontalOverride-double-) | يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم أفقي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005. |
| [setSectionUnbreakingVerticalOverride](#setSectionUnbreakingVerticalOverride-double-) | يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم عمودي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005. |

### ParagraphAbsorberOptions {#ParagraphAbsorberOptions--}
```
public ParagraphAbsorberOptions()
```

ينشئ مثيلًا جديدًا لكائن {@link ParagraphAbsorberOptions}.

### getSearchRectangle {#getSearchRectangle--}
```
public final Rectangle getSearchRectangle()
```

يحصل أو يضبط مستطيل البحث للفقرة.

**Returns:**
مثيل Rectangle

### getSectionUnbreakingHorizontalOverride {#getSectionUnbreakingHorizontalOverride--}
```
public final double getSectionUnbreakingHorizontalOverride()
```

يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم أفقي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005.

**Returns:**
قيمة double

### getSectionUnbreakingVerticalOverride {#getSectionUnbreakingVerticalOverride--}
```
public final double getSectionUnbreakingVerticalOverride()
```

يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم عمودي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005.

**Returns:**
قيمة double

### setSearchRectangle {#setSearchRectangle-com.aspose.pdf.Rectangle-}
يحصل أو يضبط مستطيل البحث للفقرة.

### setSectionUnbreakingHorizontalOverride {#setSectionUnbreakingHorizontalOverride-double-}
```
public final void setSectionUnbreakingHorizontalOverride(double value)
```

يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم أفقي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة double |

### setSectionUnbreakingVerticalOverride {#setSectionUnbreakingVerticalOverride-double-}
```
public final void setSectionUnbreakingVerticalOverride(double value)
```

يحصل أو يضبط الحد الأقصى لطول المنطقة ذات 'مستوى التعبئة الصفرية' التي لن تُعامل كفاصل قسم عمودي فعلي (كنسبة من عرض / ارتفاع الصفحة). القيمة الافتراضية هي 0.005.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة double |
