---
title: "CollectionFieldSubtype"
linktitle: "CollectionFieldSubtype"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل معامل النوع الفرعي لحقل في مجموعة المخطط."
type: docs
weight: 630
url: /ar/java/com.aspose.pdf/collectionfieldsubtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.Enum, com.aspose.pdf.CollectionFieldSubtype

```
public final class CollectionFieldSubtype extends com.aspose.ms.System.Enum
```

يمثل معامل النوع الفرعي لحقل في مجموعة المخطط.

## الحقول

| حقل | الوصف |
| --- | --- |
| [CompressedSize](#CompressedSize) | (PDF 2.0) بيانات الحقل هي طول تدفق الملف المضمّن، كما هو محدد بواسطة إدخال Length في قاموس تدفق الملف المضمّن، ويجب أن تكون القيمتان متطابقتين. |
| [CreationDate](#CreationDate) | يجب أن تكون بيانات الحقل تاريخ إنشاء تدفق الملف المضمّن، كما هو محدد بواسطة إدخال CreationDate في الملف المضمّن |
| [D](#D) | نوع تاريخ. يجب تخزين بيانات الحقل كسلسلة تاريخ PDF. |
| [Desc](#Desc) | يجب أن تكون بيانات الحقل اسم ملف تدفق الملف المضمّن، كما هو محدد بواسطة إدخال UF في مواصفات الملف، إذا كان موجودًا؛ وإلا بواسطة إدخال F في مواصفات الملف |
| [F](#F) | يجب أن تكون بيانات الحقل اسم ملف تدفق الملف المضمّن، كما هو محدد بواسطة إدخال UF في مواصفات الملف، إذا كان موجودًا؛ وإلا بواسطة إدخال F في مواصفات الملف |
| [ModDate](#ModDate) | يجب أن تكون بيانات الحقل هي تاريخ التعديل لتدفق الملف المضمّن، كما هو محدد بواسطة إدخال ModDate في قاموس معلمات الملف المضمّن. |
| [N](#N) | نوع رقمي. يجب تخزين بيانات الحقل كرقم PDF. |
| [None](#None) | النوع الفرعي غير معرف. |
| [S](#S) | نوع نصي. يجب تخزين بيانات الحقل كسلسلة نصية PDF. |
| [Size](#Size) | يجب أن تكون بيانات الحقل هي حجم الملف المضمّن، كما هو محدد بواسطة إدخال Size في قاموس معلمات الملف المضمّن |

### CompressedSize {#CompressedSize}
```
public static final int CompressedSize
```

(PDF 2.0) بيانات الحقل هي طول تدفق الملف المضمّن، كما هو محدد بواسطة إدخال Length في قاموس تدفق الملف المضمّن، ويجب أن تكون القيمتان متطابقتين.

### CreationDate {#CreationDate}
```
public static final int CreationDate
```

يجب أن تكون بيانات الحقل تاريخ إنشاء تدفق الملف المضمّن، كما هو محدد بواسطة إدخال CreationDate في الملف المضمّن

### D {#D}
```
public static final int D
```

نوع تاريخ. يجب تخزين بيانات الحقل كسلسلة تاريخ PDF.

### Desc {#Desc}
```
public static final int Desc
```

يجب أن تكون بيانات الحقل اسم ملف تدفق الملف المضمّن، كما هو محدد بواسطة إدخال UF في مواصفات الملف، إذا كان موجودًا؛ وإلا بواسطة إدخال F في مواصفات الملف

### F {#F}
```
public static final int F
```

يجب أن تكون بيانات الحقل اسم ملف تدفق الملف المضمّن، كما هو محدد بواسطة إدخال UF في مواصفات الملف، إذا كان موجودًا؛ وإلا بواسطة إدخال F في مواصفات الملف

### ModDate {#ModDate}
```
public static final int ModDate
```

يجب أن تكون بيانات الحقل هي تاريخ التعديل لتدفق الملف المضمّن، كما هو محدد بواسطة إدخال ModDate في قاموس معلمات الملف المضمّن.

### N {#N}
```
public static final int N
```

نوع رقمي. يجب تخزين بيانات الحقل كرقم PDF.

### None {#None}
```
public static final int None
```

النوع الفرعي غير معرف.

### S {#S}
```
public static final int S
```

نوع نصي. يجب تخزين بيانات الحقل كسلسلة نصية PDF.

### Size {#Size}
```
public static final int Size
```

يجب أن تكون بيانات الحقل هي حجم الملف المضمّن، كما هو محدد بواسطة إدخال Size في قاموس معلمات الملف المضمّن
