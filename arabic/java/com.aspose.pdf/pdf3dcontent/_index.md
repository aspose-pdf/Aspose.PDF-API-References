---
title: "PDF3DContent"
linktitle: "PDF3DContent"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "فئة PDF3DContent."
type: docs
weight: 3580
url: /ar/java/com.aspose.pdf/pdf3dcontent/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DContent

```
public class PDF3DContent extends Object
```

فئة PDF3DContent.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [PDF3DContent](#PDF3DContent--) | يُنشئ نسخة جديدة من الفئة {@code PDF3DContent}. |
| [PDF3DContent](#PDF3DContent-java.lang.String-) | يُنشئ نسخة جديدة من الفئة {@code PDF3DContent}. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getAsByteArray](#getAsByteArray--) | يحصل على المحتوى ثلاثي الأبعاد كمصفوفة بايت. |
| [getAsStream](#getAsStream--) | يحصل على المحتوى ثلاثي الأبعاد كدفق. |
| [getExtension](#getExtension--) | يحصل على الامتداد . |
| [load](#load-java.lang.String-) | يحمّل المحتوى ثلاثي الأبعاد باستخدام اسم الملف المحدد. |
| [loadAsPRC](#loadAsPRC-byte:A-) | يحمّل المحتوى ثلاثي الأبعاد من مصفوفة بايت بصيغة PRC. |
| [loadAsPRC](#loadAsPRC-java.io.InputStream-) | يحمّل المحتوى ثلاثي الأبعاد من الدفق بصيغة PRC. |
| [loadAsPRC](#loadAsPRC-java.lang.String-) | يحمّل المحتوى ثلاثي الأبعاد بالاسم الملف المحدد بصيغة PRC. |
| [loadAsU3D](#loadAsU3D-byte:A-) | يحمّل المحتوى ثلاثي الأبعاد من مصفوفة بايت بصيغة U3D. |
| [loadAsU3D](#loadAsU3D-java.io.InputStream-) | يحمّل المحتوى ثلاثي الأبعاد من الدفق بصيغة U3D. |
| [loadAsU3D](#loadAsU3D-java.lang.String-) | يحمّل المحتوى ثلاثي الأبعاد بالاسم الملف المحدد بصيغة U3D. |
| [saveToFile](#saveToFile-java.lang.String-) | يحفظ المحتوى ثلاثي الأبعاد إلى ملف. |

### PDF3DContent {#PDF3DContent--}
```
public PDF3DContent()
```

يُنشئ نسخة جديدة من الفئة {@code PDF3DContent}.

### PDF3DContent {#PDF3DContent-java.lang.String-}
يُنشئ نسخة جديدة من الفئة {@code PDF3DContent}.

### getAsByteArray {#getAsByteArray--}
```
public byte[] getAsByteArray()
```

يحصل على المحتوى ثلاثي الأبعاد كمصفوفة بايت.

**Returns:**
System.Byte[].

### getAsStream {#getAsStream--}
```
public InputStream getAsStream()
```

يحصل على المحتوى ثلاثي الأبعاد كدفق.

**Returns:**
دفق.

### getExtension {#getExtension--}
```
public String getExtension()
```

يحصل على الامتداد .

**Returns:**
كائن سلسلة: الامتداد.

### load {#load-java.lang.String-}
يحمّل المحتوى ثلاثي الأبعاد باستخدام اسم الملف المحدد.

### loadAsPRC {#loadAsPRC-byte:A-}
```
public void loadAsPRC(byte[] stream)
```

يحمّل المحتوى ثلاثي الأبعاد من مصفوفة بايت بصيغة PRC.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| دفق |  | المجري. |

### loadAsPRC {#loadAsPRC-java.io.InputStream-}
يحمّل المحتوى ثلاثي الأبعاد من الدفق بصيغة PRC.

### loadAsPRC {#loadAsPRC-java.lang.String-}
يحمّل المحتوى ثلاثي الأبعاد بالاسم الملف المحدد بصيغة PRC.

### loadAsU3D {#loadAsU3D-byte:A-}
```
public void loadAsU3D(byte[] stream)
```

يحمّل المحتوى ثلاثي الأبعاد من مصفوفة بايت بصيغة U3D.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| دفق |  | المجري. |

### loadAsU3D {#loadAsU3D-java.io.InputStream-}
يحمّل المحتوى ثلاثي الأبعاد من الدفق بصيغة U3D.

### loadAsU3D {#loadAsU3D-java.lang.String-}
يحمّل المحتوى ثلاثي الأبعاد بالاسم الملف المحدد بصيغة U3D.

### saveToFile {#saveToFile-java.lang.String-}
يحفظ المحتوى ثلاثي الأبعاد إلى ملف.
