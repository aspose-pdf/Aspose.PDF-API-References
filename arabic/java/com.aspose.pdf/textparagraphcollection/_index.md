---
title: "TextParagraphCollection"
linktitle: "TextParagraphCollection"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل مجموعة فقرات النص"
type: docs
weight: 5230
url: /ar/java/com.aspose.pdf/textparagraphcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextParagraphCollection

**All Implemented Interfaces:**
قابل للتكرار < TextParagraph >

```
public final class TextParagraphCollection extends Object implements Iterable < TextParagraph >
```

يمثل مجموعة فقرات النص

## الطرق

| طريقة | الوصف |
| --- | --- |
| [add](#add-com.aspose.pdf.TextParagraph-) | يضيف عنصر الفقرة النصية في الفهرس المحدد. |
| [clear](#clear--) | يمسح جميع العناصر من المجموعة. |
| [contains](#contains-com.aspose.pdf.TextParagraph-) | يحدّد ما إذا كانت المجموعة تحتوي على قيمة محددة. |
| [copyTo](#copyTo-com.aspose.pdf.TextParagraph:A-int-) | ينسخ المجموعة بالكامل إلى مصفوفة أحادية البعد متوافقة، بدءًا من الفهرس المحدد للمصفوفة الهدف. |
| [get_Item](#get_Item-int-) | يحصل على عنصر الفقرة النصية في الفهرس المحدد. |
| [getSyncRoot](#getSyncRoot--) | يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة. |
| [isReadOnly](#isReadOnly--) | يحصل على قيمة تشير إلى ما إذا كانت المجموعة للقراءة فقط |
| [isSynchronized](#isSynchronized--) | يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط). |
| [iterator](#iterator--) | يعيد عدّادًا للمجموعة بالكامل. |
| [remove](#remove-com.aspose.pdf.TextParagraph-) | يحذف العنصر المحدد من المجموعة. |
| [size](#size--) | يحصل على عدد عناصر كائن {@code TextParagraph} الموجودة فعليًا في المجموعة. |

### add {#add-com.aspose.pdf.TextParagraph-}
يضيف عنصر الفقرة النصية في الفهرس المحدد.

### clear {#clear--}
```
public void clear()
```

يمسح جميع العناصر من المجموعة.

### contains {#contains-com.aspose.pdf.TextParagraph-}
يحدّد ما إذا كانت المجموعة تحتوي على قيمة محددة.

### copyTo {#copyTo-com.aspose.pdf.TextParagraph:A-int-}
ينسخ المجموعة بالكامل إلى مصفوفة أحادية البعد متوافقة، بدءًا من الفهرس المحدد للمصفوفة الهدف.

### get_Item {#get_Item-int-}
```
public TextParagraph get_Item(int index)
```

يحصل على عنصر الفقرة النصية في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | قيمة int |

**Returns:**
كائن TextParagraph

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة.

**Returns:**
عنصر Object

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

يحصل على قيمة تشير إلى ما إذا كانت المجموعة للقراءة فقط

**Returns:**
قيمة منطقية

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط).

**Returns:**
قيمة منطقية

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator< TextParagraph > iterator()
```

يعيد عدّادًا للمجموعة بالكامل.

**Returns:**
كائن IEnumerator

### remove {#remove-com.aspose.pdf.TextParagraph-}
يحذف العنصر المحدد من المجموعة.

### size {#size--}
```
public int size()
```

يحصل على عدد عناصر كائن {@code TextParagraph} الموجودة فعليًا في المجموعة.

**Returns:**
قيمة int
