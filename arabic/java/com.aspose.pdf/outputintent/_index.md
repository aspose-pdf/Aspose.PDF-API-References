---
title: "OutputIntent"
linktitle: "OutputIntent"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل نية إخراج تتطابق مع خصائص اللون لمستند PDF مع تلك الخاصة بجهاز الإخراج المستهدف أو بيئة الإنتاج التي سيُستخدم فيها المستند."
type: docs
weight: 3290
url: /ar/java/com.aspose.pdf/outputintent/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.OutputIntent

```
public final class OutputIntent extends Object
```

يمثل نية الإخراج التي تتطابق مع خصائص اللون لوثيقة PDF مع خصائص جهاز الإخراج المستهدف أو بيئة الإنتاج التي سيُطبع فيها المستند.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [OutputIntent](#OutputIntent-java.lang.String-) | ينشئ مثيلاً جديداً لفئة {@link OutputIntent} مع معرف حالة الإخراج المحدد. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getInfo](#getInfo--) | يحصل على نص قابل للقراءة البشرية يحتوي على معلومات إضافية أو تعليقات حول جهاز الهدف المقصود أو حالة الإنتاج. |
| [getOutputCondition](#getOutputCondition--) | يحصل على نص يحدد باختصار جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية. |
| [getOutputConditionIdentifier](#getOutputConditionIdentifier--) | يحصل على نص يحدد جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية أو الآلية. |
| [getRegistryName](#getRegistryName--) | يحصل على نص يحدد السجل الذي تم تعريف الحالة المحددة بواسطة {@code OutputConditionIdentifier}({@link #getOutputConditionIdentifier}) فيه. |
| [getSubtype](#getSubtype--) | يحصل على النوع الفرعي لنية الإخراج. |
| [setInfo](#setInfo-java.lang.String-) | يضبط نصًا قابلًا للقراءة البشرية يحتوي على معلومات إضافية أو تعليقات حول جهاز الهدف المقصود أو حالة الإنتاج. |
| [setOutputCondition](#setOutputCondition-java.lang.String-) | يحصل أو يضبط نصًا يحدد باختصار جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية. |
| [setOutputConditionIdentifier](#setOutputConditionIdentifier-java.lang.String-) | يضبط نصًا يحدد جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية أو الآلية. |
| [setRegistryName](#setRegistryName-java.lang.String-) | يضبط نصًا يحدد السجل الذي تم تعريف الحالة المحددة بواسطة {@code OutputConditionIdentifier}({@link #getOutputConditionIdentifier}/{@link #setOutputConditionIdentifier(String)}) فيه. |

### OutputIntent {#OutputIntent-java.lang.String-}
ينشئ مثيلاً جديداً لفئة {@link OutputIntent} مع معرف حالة الإخراج المحدد.

### getInfo {#getInfo--}
```
public final String getInfo()
```

يحصل على نص قابل للقراءة البشرية يحتوي على معلومات إضافية أو تعليقات حول جهاز الهدف المقصود أو حالة الإنتاج.

**Returns:**
قيمة سلسلة

### getOutputCondition {#getOutputCondition--}
```
public final String getOutputCondition()
```

يحصل على نص يحدد باختصار جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية.

**Returns:**
قيمة سلسلة

### getOutputConditionIdentifier {#getOutputConditionIdentifier--}
```
public final String getOutputConditionIdentifier()
```

يحصل على نص يحدد جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية أو الآلية.

**Returns:**
قيمة سلسلة

### getRegistryName {#getRegistryName--}
```
public final String getRegistryName()
```

يحصل على نص يحدد السجل الذي تم تعريف الحالة المحددة بواسطة {@code OutputConditionIdentifier}({@link #getOutputConditionIdentifier}) فيه.

**Returns:**
قيمة سلسلة

### getSubtype {#getSubtype--}
```
public final String getSubtype()
```

يحصل على النوع الفرعي لنية الإخراج.

**Returns:**
قيمة سلسلة

### setInfo {#setInfo-java.lang.String-}
يضبط نصًا قابلًا للقراءة البشرية يحتوي على معلومات إضافية أو تعليقات حول جهاز الهدف المقصود أو حالة الإنتاج.

### setOutputCondition {#setOutputCondition-java.lang.String-}
يحصل أو يضبط نصًا يحدد باختصار جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية.

### setOutputConditionIdentifier {#setOutputConditionIdentifier-java.lang.String-}
يضبط نصًا يحدد جهاز الإخراج المقصود أو حالة الإنتاج بصيغة قابلة للقراءة البشرية أو الآلية.

### setRegistryName {#setRegistryName-java.lang.String-}
يضبط نصًا يحدد السجل الذي تم تعريف الحالة المحددة بواسطة {@code OutputConditionIdentifier}({@link #getOutputConditionIdentifier}/{@link #setOutputConditionIdentifier(String)}) فيه.
