---
title: "ITociElement"
linktitle: "ITociElement"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل عنصرًا يمكنه أن يعمل كمدخل جدول محتويات (TOCI) في مستند PDF مُوسوم. تُجرد هذه الواجهة كلًا من عناصر TOCI القياسية وعنصر القائمة (LI)."
type: docs
weight: 80
url: /ar/java/com.aspose.pdf.tagged.logicalstructure.elements.grouping/itocielement/
---
```
public interface ITociElement
```

يمثل عنصرًا يمكن أن يعمل كإدخال جدول محتويات (TOCI) في مستند PDF معلم. هذه الواجهة تجريدية لكل من عناصر TOCI القياسية وعناصر عنصر القائمة (LI) المستخدمة في هياكل جدول المحتويات المتداخلة.

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getGetElement](#getGetElement--) | يحصل على عنصر PDF الأساسي الذي يمثل بنية TOCI هذه. |

### getGetElement {#getGetElement--}
```
StructureElement getGetElement()
```

يحصل على عنصر PDF الأساسي الذي يمثل بنية TOCI هذه.

**Returns:**
العنصر الذي يُشكل التمثيل الهيكلي لهذا مدخل جدول المحتويات.
