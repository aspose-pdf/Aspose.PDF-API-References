---
title: "CrashReportOptions"
linktitle: "CrashReportOptions"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "خيارات إنشاء تقرير الأعطال."
type: docs
weight: 50
url: /ar/java/com.aspose.pdf.exceptions/crashreportoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.exceptions.CrashReportOptions

```
public class CrashReportOptions extends Object
```

خيارات إنشاء تقرير الأعطال.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [CrashReportOptions](#CrashReportOptions-com.aspose.ms.System.Exception-) | ينشئ CrashReportOptions مع المعلمات الافتراضية. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getApplicationTitle](#getApplicationTitle--) | اسم المكتبة التي حدث فيها الاستثناء. |
| [getCrashReportDirectory](#getCrashReportDirectory--) | دليل الإخراج لتقرير العطل. بشكل افتراضي يتم تعيينه إلى الدليل الحالي. |
| [getCrashReportFilename](#getCrashReportFilename--) | اسم الملف لتقرير العطل. بشكل افتراضي، يتم إنشاؤه تلقائيًا بالتنسيق CrashReport_date_ticks.html |
| [getCrashReportPath](#getCrashReportPath--) | المسار الكامل لملف تقرير العطل |
| [getCustomMessage](#getCustomMessage--) | رسالة مخصصة لتضمينها في التقرير. يمكن أن تكون شيئًا مثل قيم المتغيرات أو تفاصيل أخرى تريد إرسالها. |
| [getException](#getException--) | الاستثناء الذي سيُبنى عليه تقرير العطل |
| [getLibraryVersion](#getLibraryVersion--) | إصدار المكتبة المستخدمة. |
| [setCrashReportDirectory](#setCrashReportDirectory-java.lang.String-) | دليل الإخراج لتقرير العطل. بشكل افتراضي يتم تعيينه إلى الدليل الحالي. |
| [setCrashReportFilename](#setCrashReportFilename-java.lang.String-) | اسم الملف لتقرير العطل. بشكل افتراضي، يتم إنشاؤه تلقائيًا بالتنسيق CrashReport_date_ticks.html |
| [setCustomMessage](#setCustomMessage-java.lang.String-) | رسالة مخصصة لتضمينها في التقرير. يمكن أن تكون شيئًا مثل قيم المتغيرات أو تفاصيل أخرى تريد إرسالها. |

### CrashReportOptions {#CrashReportOptions-com.aspose.ms.System.Exception-}
ينشئ CrashReportOptions مع المعلمات الافتراضية.

### getApplicationTitle {#getApplicationTitle--}
```
public final String getApplicationTitle()
```

اسم المكتبة التي حدث فيها الاستثناء.

**Returns:**
قيمة سلسلة

### getCrashReportDirectory {#getCrashReportDirectory--}
```
public final String getCrashReportDirectory()
```

دليل الإخراج لتقرير العطل. بشكل افتراضي يتم تعيينه إلى الدليل الحالي.

**Returns:**
قيمة سلسلة

### getCrashReportFilename {#getCrashReportFilename--}
```
public final String getCrashReportFilename()
```

اسم الملف لتقرير العطل. بشكل افتراضي، يتم إنشاؤه تلقائيًا بالتنسيق CrashReport_date_ticks.html

**Returns:**
قيمة سلسلة

### getCrashReportPath {#getCrashReportPath--}
```
public final String getCrashReportPath()
```

المسار الكامل لملف تقرير العطل

**Returns:**
قيمة سلسلة

### getCustomMessage {#getCustomMessage--}
```
public final String getCustomMessage()
```

رسالة مخصصة لتضمينها في التقرير. يمكن أن تكون شيئًا مثل قيم المتغيرات أو تفاصيل أخرى تريد إرسالها.

**Returns:**
قيمة سلسلة

### getException {#getException--}
```
public final com.aspose.ms.System.Exception getException()
```

الاستثناء الذي سيُبنى عليه تقرير العطل

**Returns:**
مثيل الاستثناء

### getLibraryVersion {#getLibraryVersion--}
```
public final String getLibraryVersion()
```

إصدار المكتبة المستخدمة.

**Returns:**
قيمة سلسلة

### setCrashReportDirectory {#setCrashReportDirectory-java.lang.String-}
دليل الإخراج لتقرير العطل. بشكل افتراضي يتم تعيينه إلى الدليل الحالي.

### setCrashReportFilename {#setCrashReportFilename-java.lang.String-}
اسم الملف لتقرير العطل. بشكل افتراضي، يتم إنشاؤه تلقائيًا بالتنسيق CrashReport_date_ticks.html

### setCustomMessage {#setCustomMessage-java.lang.String-}
رسالة مخصصة لتضمينها في التقرير. يمكن أن تكون شيئًا مثل قيم المتغيرات أو تفاصيل أخرى تريد إرسالها.
