---
title: "FontSubstitutionCollection"
linktitle: "FontSubstitutionCollection"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل مجموعة استراتيجيات استبدال الخطوط."
type: docs
weight: 60
url: /ar/java/com.aspose.pdf.text/fontsubstitutioncollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.text.FontSubstitutionCollection

**All Implemented Interfaces:**
Iterable < FontSubstitution >

```
public final class FontSubstitutionCollection extends Object implements Iterable < FontSubstitution >
```

يمثل مجموعة استراتيجيات استبدال الخطوط.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [FontSubstitutionCollection](#FontSubstitutionCollection--) | يقوم بتهيئة كائن المجموعة. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [add](#add-com.aspose.pdf.text.FontSubstitution-) | يضيف كائن استبدال الخط الجديد إلى المجموعة. |
| [clear](#clear--) | يمسح مجموعة استبدال الخط. |
| [contains](#contains-com.aspose.pdf.text.FontSubstitution-) | يحدد ما إذا كان العنصر موجودًا في المجموعة. |
| [delete](#delete-com.aspose.pdf.text.FontSubstitution-) | للاستخدام الداخلي فقط يحذف عنصر استبدال الخط. |
| [getItem](#getItem-int-) | يحصل على عنصر الخط في الفهرس المحدد. |
| [getSyncRoot](#getSyncRoot--) | يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة. |
| [isSynchronized](#isSynchronized--) | يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط). |
| [iterator](#iterator--) | يعيد عدّادًا للمجموعة بالكامل. |
| [remove](#remove-com.aspose.pdf.text.FontSubstitution-) | يحذف عنصر استبدال الخط. |
| [size](#size--) | يحصل على عدد عناصر كائن Font الموجودة فعليًا في المجموعة. |

### FontSubstitutionCollection {#FontSubstitutionCollection--}
```
public FontSubstitutionCollection()
```

يقوم بتهيئة كائن المجموعة.

### add {#add-com.aspose.pdf.text.FontSubstitution-}
يضيف كائن استبدال الخط الجديد إلى المجموعة.

### clear {#clear--}
```
public void clear()
```

يمسح مجموعة استبدال الخط.

### contains {#contains-com.aspose.pdf.text.FontSubstitution-}
يحدد ما إذا كان العنصر موجودًا في المجموعة.

### delete {#delete-com.aspose.pdf.text.FontSubstitution-}
للاستخدام الداخلي فقط يحذف عنصر استبدال الخط.

### getItem {#getItem-int-}
```
public FontSubstitution getItem(int index)
```

يحصل على عنصر الخط في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | الفهرس داخل المجموعة. |

**Returns:**
كائن FontSubstitution.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة.

**Returns:**
كائن للمزامنة

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط).

**Returns:**
قيمة منطقية

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.Generic.List.Enumerator< FontSubstitution > iterator()
```

يعيد عدّادًا للمجموعة بالكامل.

**Returns:**
كائن عداد

### remove {#remove-com.aspose.pdf.text.FontSubstitution-}
يحذف عنصر استبدال الخط.

### size {#size--}
```
public int size()
```

يحصل على عدد عناصر كائن Font الموجودة فعليًا في المجموعة.

**Returns:**
قيمة int
