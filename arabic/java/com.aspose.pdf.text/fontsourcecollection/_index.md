---
title: "FontSourceCollection"
linktitle: "FontSourceCollection"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل مجموعة مصادر الخطوط."
type: docs
weight: 40
url: /ar/java/com.aspose.pdf.text/fontsourcecollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.text.FontSourceCollection

**All Implemented Interfaces:**
Iterable < FontSource >

```
public final class FontSourceCollection extends Object implements Iterable < FontSource >
```

يمثل مجموعة مصادر الخطوط.

## الحقول

| حقل | الوصف |
| --- | --- |
| [CollectionChanged](#CollectionChanged) | حدث CollectionChanged |

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [FontSourceCollection](#FontSourceCollection--) | يقوم بتهيئة كائن المجموعة. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [add](#add-com.aspose.pdf.FontSource-) | يضيف كائن مصدر خط جديد إلى المجموعة. |
| [clear](#clear--) | يمسح مجموعة مصادر الخط. |
| [contains](#contains-com.aspose.pdf.FontSource-) | يحدد ما إذا كان العنصر موجودًا في المجموعة. |
| [copyTo](#copyTo-com.aspose.pdf.FontSource:A-int-) | ينسخ المجموعة بالكامل إلى مصفوفة أحادية البعد متوافقة، بدءًا من الفهرس المحدد للمصفوفة الهدف. |
| [delete](#delete-com.aspose.pdf.FontSource-) | يحذف عنصر مصدر الخط. |
| [getItem](#getItem-int-) | يحصل على عنصر الخط في الفهرس المحدد. |
| [getSyncRoot](#getSyncRoot--) | يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة. |
| [isSynchronized](#isSynchronized--) | يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط). |
| [iterator](#iterator--) | يعيد عدّادًا للمجموعة بالكامل. |
| [remove](#remove-com.aspose.pdf.FontSource-) | يحذف عنصر مصدر الخط. |
| [size](#size--) | يحصل على عدد عناصر كائن Font الموجودة فعليًا في المجموعة. |

### CollectionChanged {#CollectionChanged}
```
public final PdfEvent <com.aspose.ms.System.EventHandler> CollectionChanged
```

حدث CollectionChanged

### FontSourceCollection {#FontSourceCollection--}
```
public FontSourceCollection()
```

يقوم بتهيئة كائن المجموعة.

### add {#add-com.aspose.pdf.FontSource-}
يضيف كائن مصدر خط جديد إلى المجموعة.

### clear {#clear--}
```
public void clear()
```

يمسح مجموعة مصادر الخط.

### contains {#contains-com.aspose.pdf.FontSource-}
يحدد ما إذا كان العنصر موجودًا في المجموعة.

### copyTo {#copyTo-com.aspose.pdf.FontSource:A-int-}
ينسخ المجموعة بالكامل إلى مصفوفة أحادية البعد متوافقة، بدءًا من الفهرس المحدد للمصفوفة الهدف.

### delete {#delete-com.aspose.pdf.FontSource-}
يحذف عنصر مصدر الخط.

### getItem {#getItem-int-}
```
public FontSource getItem(int index)
```

يحصل على عنصر الخط في الفهرس المحدد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| index |  | الفهرس داخل المجموعة. |

**Returns:**
كائن مصدر الخط.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

يحصل على كائن يمكن استخدامه لمزامنة الوصول إلى المجموعة.

**Returns:**
عنصر Object

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

يحصل على قيمة تشير إلى ما إذا كان الوصول إلى المجموعة مُزامنًا (آمن للخيوط).

**Returns:**
قيمة منطقية

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator iterator()
```

يعيد عدّادًا للمجموعة بالكامل.

**Returns:**
كائن عدّاد.

### remove {#remove-com.aspose.pdf.FontSource-}
يحذف عنصر مصدر الخط.

### size {#size--}
```
public int size()
```

يحصل على عدد عناصر كائن Font الموجودة فعليًا في المجموعة.

**Returns:**
قيمة int
