---
title: "CompressionType"
linktitle: "CompressionType"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يُستخدم لتحديد قيمة المعامل الممررة إلى جهاز صورة TIFF."
type: docs
weight: 30
url: /ar/java/com.aspose.pdf.devices/compressiontype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < CompressionType > com.aspose.pdf.devices.CompressionType, java.lang.Enum < CompressionType >, com.aspose.pdf.devices.CompressionType

**All Implemented Interfaces:**
Serializable, Comparable < CompressionType >

```
public enum CompressionType extends Enum < CompressionType >
```

يُستخدم لتحديد قيمة المعامل الممررة إلى جهاز صورة TIFF.

## الحقول

| حقل | الوصف |
| --- | --- |
| [CCITT3](#CCITT3) | يحدد مخطط الضغط CCITT3. |
| [CCITT4](#CCITT4) | يحدد مخطط الضغط CCITT4. |
| [LZW](#LZW) | يحدد مخطط الضغط LZW. |
| [None](#None) | يحدد عدم وجود ضغط. |
| [RLE](#RLE) | يحدد مخطط الضغط RLE. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | يعيد ثابت التعداد لهذا النوع بالاسم المحدد. |
| [values](#values--) | يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه. |

### CCITT3 {#CCITT3}
```
public static final CompressionType CCITT3
```

يحدد مخطط الضغط CCITT3.

### CCITT4 {#CCITT4}
```
public static final CompressionType CCITT4
```

يحدد مخطط الضغط CCITT4.

### LZW {#LZW}
```
public static final CompressionType LZW
```

يحدد مخطط الضغط LZW.

### None {#None}
```
public static final CompressionType None
```

يحدد عدم وجود ضغط.

### RLE {#RLE}
```
public static final CompressionType RLE
```

يحدد مخطط الضغط RLE.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
يعيد ثابت التعداد لهذا النوع بالاسم المحدد.

### values {#values--}
```
public static CompressionType [] values()
```

يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه.

**Returns:**
مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه
