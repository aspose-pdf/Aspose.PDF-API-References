---
title: "Rectangle"
linktitle: "Rectangle"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل مستطيلًا."
type: docs
weight: 120
url: /ar/java/com.aspose.pdf.drawing/rectangle/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.drawing.Shape com.aspose.pdf.drawing.Rectangle, com.aspose.pdf.drawing.Shape, com.aspose.pdf.drawing.Rectangle

**All Implemented Interfaces:**
IBoundsCheckableItem

```
public final class Rectangle extends Shape
```

يمثل مستطيلًا.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [Rectangle](#Rectangle--) | منشئ |
| [Rectangle](#Rectangle-float-float-float-float-) | يقوم بتهيئة نسخة جديدة من الفئة {@code Rectangle}. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [checkBounds](#checkBounds-double-double-) | يتحقق مما إذا كان العنصر يتناسب مع أبعاد الحاوية المحددة (شاملاً). |
| [getBottom](#getBottom--) | يحصل على قيمة float تشير إلى الموضع السفلي للمستطيل. |
| [getHeight](#getHeight--) | يحصل على قيمة float تشير إلى ارتفاع المستطيل. |
| [getLeft](#getLeft--) | يحصل على قيمة float تشير إلى الموضع الأيسر للمستطيل. |
| [getRoundedCornerRadius](#getRoundedCornerRadius--) | يحصل على قيمة float تشير إلى نصف قطر زوايا المستطيل. |
| [getWidth](#getWidth--) | يحصل على قيمة float تشير إلى عرض المستطيل. |
| [setBottom](#setBottom-double-) | يضبط قيمة float تشير إلى الموضع السفلي للمستطيل. |
| [setHeight](#setHeight-double-) | يضبط قيمة float تشير إلى ارتفاع المستطيل. |
| [setLeft](#setLeft-double-) | يضبط قيمة float تشير إلى الموضع الأيسر للمستطيل. |
| [setRoundedCornerRadius](#setRoundedCornerRadius-double-) | يضبط قيمة float تشير إلى نصف قطر زوايا المستطيل. |
| [setWidth](#setWidth-double-) | يضبط قيمة float تشير إلى عرض المستطيل. |

### Rectangle {#Rectangle--}
```
public Rectangle()
```

منشئ

### Rectangle {#Rectangle-float-float-float-float-}
```
public Rectangle(float left, float bottom, float width, float height)
```

يقوم بتهيئة نسخة جديدة من الفئة {@code Rectangle}.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| اليسار |  | الموضع الأيسر للمستطيل. |
| الأسفل |  | الموضع السفلي للمستطيل. |
| العرض |  | عرض المستطيل. |
| الارتفاع |  | ارتفاع المستطيل. |

### checkBounds {#checkBounds-double-double-}
```
public boolean checkBounds(double containerWidth, double containerHeight)
```

يتحقق مما إذا كان العنصر يتناسب مع أبعاد الحاوية المحددة (شاملاً).

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| containerWidth |  |  |
| containerHeight |  |  |

**Returns:**
صحيح إذا كان يتناسب؛ وإلا، خطأ.

### getBottom {#getBottom--}
```
public double getBottom()
```

يحصل على قيمة float تشير إلى الموضع السفلي للمستطيل.

**Returns:**
القيمة التي تشير إلى الموضع السفلي للمستطيل.

### getHeight {#getHeight--}
```
public double getHeight()
```

يحصل على قيمة float تشير إلى ارتفاع المستطيل.

**Returns:**
القيمة التي تشير إلى ارتفاع المستطيل.

### getLeft {#getLeft--}
```
public double getLeft()
```

يحصل على قيمة float تشير إلى الموضع الأيسر للمستطيل.

**Returns:**
قيمة عائمة تشير إلى الموضع الأيسر للمستطيل.

### getRoundedCornerRadius {#getRoundedCornerRadius--}
```
public double getRoundedCornerRadius()
```

يحصل على قيمة float تشير إلى نصف قطر زوايا المستطيل.

**Returns:**
قيمة تشير إلى نصف قطر زوايا المستطيل.

### getWidth {#getWidth--}
```
public double getWidth()
```

يحصل على قيمة float تشير إلى عرض المستطيل.

**Returns:**
قيمة تشير إلى عرض المستطيل.

### setBottom {#setBottom-double-}
```
public void setBottom(double value)
```

يضبط قيمة float تشير إلى الموضع السفلي للمستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | القيمة التي تشير إلى الموضع السفلي للمستطيل. |

### setHeight {#setHeight-double-}
```
public void setHeight(double value)
```

يضبط قيمة float تشير إلى ارتفاع المستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | القيمة التي تشير إلى ارتفاع المستطيل. |

### setLeft {#setLeft-double-}
```
public void setLeft(double value)
```

يضبط قيمة float تشير إلى الموضع الأيسر للمستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة عائمة تشير إلى الموضع الأيسر للمستطيل. |

### setRoundedCornerRadius {#setRoundedCornerRadius-double-}
```
public void setRoundedCornerRadius(double value)
```

يضبط قيمة float تشير إلى نصف قطر زوايا المستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | تشير إلى نصف قطر زوايا المستطيل. |

### setWidth {#setWidth-double-}
```
public void setWidth(double value)
```

يضبط قيمة float تشير إلى عرض المستطيل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | تشير إلى عرض المستطيل. |
