---
title: "PrintPaperSource"
linktitle: "PrintPaperSource"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يحدد درج الورق الذي تستقبل منه الطابعة الورق."
type: docs
weight: 120
url: /ar/java/com.aspose.pdf.printing/printpapersource/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.printing.PrintPaperSource

```
public class PrintPaperSource extends Object
```

يحدد درج الورق الذي تستقبل منه الطابعة الورق.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [PrintPaperSource](#PrintPaperSource--) | ينشئ حالة جديدة من الفئة PaperSource. |
| [PrintPaperSource](#PrintPaperSource-java.lang.String-int-) | ينشئ حالة جديدة من الفئة PaperSource. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getKind](#getKind--) | يحصل على مصدر الورق. |
| [getRawKind](#getRawKind--) | يحصل أو يضبط العدد الصحيح الذي يمثل أحد قيم PaperSourceKind أو قيمة مخصصة. |
| [getSourceName](#getSourceName--) | يحصل أو يضبط اسم مصدر الورق. |
| [setRawKind](#setRawKind-int-) | يحصل أو يضبط العدد الصحيح الذي يمثل أحد قيم PaperSourceKind أو قيمة مخصصة. |
| [setSourceName](#setSourceName-java.lang.String-) | يحصل أو يضبط اسم مصدر الورق. |
| [toString](#toString--) | البنية التحتية. يوفر معلومات حول PrintPaperSource في شكل نصي. |

### PrintPaperSource {#PrintPaperSource--}
```
public PrintPaperSource()
```

ينشئ حالة جديدة من الفئة PaperSource.

### PrintPaperSource {#PrintPaperSource-java.lang.String-int-}
ينشئ حالة جديدة من الفئة PaperSource.

### getKind {#getKind--}
```
public int getKind()
```

يحصل على مصدر الورق.

**Returns:**
قيمة int

### getRawKind {#getRawKind--}
```
public int getRawKind()
```

يحصل أو يضبط العدد الصحيح الذي يمثل أحد قيم PaperSourceKind أو قيمة مخصصة.

**Returns:**
قيمة int

### getSourceName {#getSourceName--}
```
public String getSourceName()
```

يحصل أو يضبط اسم مصدر الورق.

**Returns:**
قيمة سلسلة

### setRawKind {#setRawKind-int-}
```
public void setRawKind(int value)
```

يحصل أو يضبط العدد الصحيح الذي يمثل أحد قيم PaperSourceKind أو قيمة مخصصة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة int |

### setSourceName {#setSourceName-java.lang.String-}
يحصل أو يضبط اسم مصدر الورق.

### toString {#toString--}
```
public String toString()
```

البنية التحتية. يوفر معلومات حول PrintPaperSource في شكل نصي.

**Returns:**
قيمة سلسلة
