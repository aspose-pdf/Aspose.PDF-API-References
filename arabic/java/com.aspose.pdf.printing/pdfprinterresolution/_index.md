---
title: "PdfPrinterResolution"
linktitle: "PdfPrinterResolution"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل الدقة التي تدعمها الطابعة."
type: docs
weight: 30
url: /ar/java/com.aspose.pdf.printing/pdfprinterresolution/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.printing.PdfPrinterResolution

```
public class PdfPrinterResolution extends Object
```

يمثل الدقة التي تدعمها الطابعة.

## المنشئات

| منشئ | الوصف |
| --- | --- |
| [PdfPrinterResolution](#PdfPrinterResolution--) | ينشئ مثيلاً جديداً من الفئة PrinterResolution. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getKind](#getKind--) | يضبط دقة الطابعة. |
| [getX](#getX--) | يحصل على دقة الطابعة الأفقية، بوحدة النقاط في البوصة. |
| [getY](#getY--) | يحصل على دقة الطابعة العمودية، بوحدة النقاط في البوصة. |
| [setKind](#setKind-int-) | يحصل أو يضبط دقة الطابعة. |
| [setX](#setX-int-) | يضبط دقة الطابعة الأفقية، بوحدة النقاط في البوصة. |
| [setY](#setY-int-) | يضبط دقة الطابعة العمودية، بوحدة النقاط في البوصة. |

### PdfPrinterResolution {#PdfPrinterResolution--}
```
public PdfPrinterResolution()
```

ينشئ مثيلاً جديداً من الفئة PrinterResolution.

### getKind {#getKind--}
```
public int getKind()
```

يضبط دقة الطابعة.

**Returns:**
عنصر PdfPrinterResolutionKind @see PdfPrinterResolutionKind

### getX {#getX--}
```
public int getX()
```

يحصل على دقة الطابعة الأفقية، بوحدة النقاط في البوصة.

**Returns:**
قيمة int

### getY {#getY--}
```
public int getY()
```

يحصل على دقة الطابعة العمودية، بوحدة النقاط في البوصة.

**Returns:**
قيمة int

### setKind {#setKind-int-}
```
public void setKind(int value)
```

يحصل أو يضبط دقة الطابعة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | عنصر PdfPrinterResolutionKind @see PdfPrinterResolutionKind |

### setX {#setX-int-}
```
public void setX(int value)
```

يضبط دقة الطابعة الأفقية، بوحدة النقاط في البوصة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة int |

### setY {#setY-int-}
```
public void setY(int value)
```

يضبط دقة الطابعة العمودية، بوحدة النقاط في البوصة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| قيمة |  | قيمة int |
