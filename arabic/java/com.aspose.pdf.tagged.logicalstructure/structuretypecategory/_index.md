---
title: "StructureTypeCategory"
linktitle: "StructureTypeCategory"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يمثل فئات لأنواع الهيكل القياسية."
type: docs
weight: 120
url: /ar/java/com.aspose.pdf.tagged.logicalstructure/structuretypecategory/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.logicalstructure.StructureTypeCategory

```
public final class StructureTypeCategory extends Object
```

يمثل فئات لأنواع الهيكل القياسية.

## الحقول

| حقل | الوصف |
| --- | --- |
| [BLSEs](#BLSEs) | عناصر البنية على مستوى الكتلة (BLSEs) تصف التخطيط العام للمحتوى على الصفحة، مع التقدم في اتجاه تقدم الكتلة. |
| [GroupingElements](#GroupingElements) | عناصر التجميع تجمع عناصر أخرى في تسلسلات أو هياكل شجرية لكنها لا تحتوي على محتوى مباشرة ولا تؤثر مباشرة على التخطيط. |
| [IllustrationElements](#IllustrationElements) | عناصر التوضيح هي تسلسلات مدمجة من المحتوى، بترتيب محتوى الصفحة، تُعتبر ككائنات موحدة بالنسبة لتخطيط الصفحة. يمكن اعتبار التوضيح إما كـ BLSE أو كـ ILSE. |
| [ILSEs](#ILSEs) | عناصر البنية على مستوى السطر (ILSEs) تصف تخطيط المحتوى داخل BLSE، مع التقدم في اتجاه تقدم السطر. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [to_StructureTypeCategory](#to_StructureTypeCategory-java.lang.String-) | يُجري تحويلًا صريحًا من {@link String} إلى {@link StructureTypeCategory}. |
| [toString](#toString--) | يعيد سلسلة تمثل الكائن الحالي. |

### BLSEs {#BLSEs}
```
public static final StructureTypeCategory BLSEs
```

عناصر البنية على مستوى الكتلة (BLSEs) تصف التخطيط العام للمحتوى على الصفحة، مع التقدم في اتجاه تقدم الكتلة.

### GroupingElements {#GroupingElements}
```
public static final StructureTypeCategory GroupingElements
```

عناصر التجميع تجمع عناصر أخرى في تسلسلات أو هياكل شجرية لكنها لا تحتوي على محتوى مباشرة ولا تؤثر مباشرة على التخطيط.

### IllustrationElements {#IllustrationElements}
```
public static final StructureTypeCategory IllustrationElements
```

عناصر التوضيح هي تسلسلات مدمجة من المحتوى، بترتيب محتوى الصفحة، تُعتبر ككائنات موحدة بالنسبة لتخطيط الصفحة. يمكن اعتبار التوضيح إما كـ BLSE أو كـ ILSE.

### ILSEs {#ILSEs}
```
public static final StructureTypeCategory ILSEs
```

عناصر البنية على مستوى السطر (ILSEs) تصف تخطيط المحتوى داخل BLSE، مع التقدم في اتجاه تقدم السطر.

### to_StructureTypeCategory {#to_StructureTypeCategory-java.lang.String-}
يُجري تحويلًا صريحًا من {@link String} إلى {@link StructureTypeCategory}.

### toString {#toString--}
```
public String toString()
```

يعيد سلسلة تمثل الكائن الحالي.

**Returns:**
سلسلة تمثل الكائن الحالي.
