---
title: "KeySize"
linktitle: "KeySize"
second_title: "مرجع API لـ Aspose.PDF للـ Java"
description: "يحدد أحجام المفاتيح المختلفة التي يمكن استخدامها لتشفير مستندات PDF."
type: docs
weight: 340
url: /ar/java/com.aspose.pdf.facades/keysize/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < KeySize > com.aspose.pdf.facades.KeySize, java.lang.Enum < KeySize >, com.aspose.pdf.facades.KeySize

**All Implemented Interfaces:**
Serializable, Comparable < KeySize >

```
public enum KeySize extends Enum < KeySize >
```

يحدد أحجام المفاتيح المختلفة التي يمكن استخدامها لتشفير مستندات PDF.

## الحقول

| حقل | الوصف |
| --- | --- |
| [x128](#x128) | مفتاح 128 بت. |
| [x256](#x256) | مفتاح 256 بت. |
| [x40](#x40) | مفتاح 40 بت. |

## الطرق

| طريقة | الوصف |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | يعيد ثابت التعداد لهذا النوع بالاسم المحدد. |
| [values](#values--) | يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه. |

### x128 {#x128}
```
public static final KeySize x128
```

مفتاح 128 بت.

### x256 {#x256}
```
public static final KeySize x256
```

مفتاح 256 بت.

### x40 {#x40}
```
public static final KeySize x40
```

مفتاح 40 بت.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
يعيد ثابت التعداد لهذا النوع بالاسم المحدد.

### values {#values--}
```
public static KeySize [] values()
```

يعيد مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه.

**Returns:**
مصفوفة تحتوي على ثوابت هذا النوع من التعداد، بالترتيب الذي تم إعلانه
