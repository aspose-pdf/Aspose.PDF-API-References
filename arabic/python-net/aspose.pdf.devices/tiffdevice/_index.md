---
title: "TiffDevice"
second_title: "مرجع API لـ Aspose.PDF للبايثون عبر .NET"
description: "تساعد هذه الفئة على حفظ صفحات مستند PDF صفحةً بصفحة في صورة TIFF واحدة."
type: docs
weight: 150
url: /ar/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

تساعد هذه الفئة على حفظ صفحات مستند PDF صفحةً بصفحة في صورة TIFF واحدة.

يعرض نوع TiffDevice الأعضاء التالية:
## المُنشئات
| الاسم | الوصف |
| :- | :- |
| TiffDevice(resolution) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(resolution, settings) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(resolution, settings, converter) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(settings) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(settings, converter) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice() | ينشئ مثيلاً جديدًا من الفئة [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) بالإعدادات الافتراضية. |
| TiffDevice(width, height, resolution, settings) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(page_size, resolution, settings) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(width, height, resolution) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(page_size, resolution) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(width, height, settings) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(width, height, settings, converter) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(page_size, settings, converter) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(page_size, settings) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(width, height) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
| TiffDevice(page_size) | ينشئ مثيلاً جديدًا من فئة TiffDevice |
## الخصائص
| الاسم | الوصف |
| :- | :- |
| rendering_options | يحصل أو يضبط خيارات العرض. |
| form_presentation_mode | يحصل أو يضبط وضع عرض النموذج. |
| الإعدادات | يحصل على الإعدادات لتعيين pdf إلى صورة tiff. |
| resolution | يحصل على دقة الصورة. |
| العرض | يحصل على عرض إخراج الصورة. |
| الارتفاع | يحصل على ارتفاع إخراج الصورة. |
## الطرق
| الاسم | الوصف |
| :- | :- |
| process(document, from_page, to_page, output) | يقوم بتحويل صفحات معينة من المستند إلى tiff ويحفظها في تدفق الإخراج. |
| process(document, output) | يقوم بتحويل صفحات معينة من المستند إلى tiff ويحفظها في تدفق الإخراج. |
| process(document, output_file_name) | يقوم بتحويل صفحات معينة من المستند إلى tiff ويحفظها في تدفق الإخراج. |
| process(document, from_page, to_page, output_file_name) | يقوم بتحويل صفحات معينة من المستند إلى tiff ويحفظها في تدفق الإخراج. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | قم بعملية ثنائية برادلي لتدفق الإدخال. |

### انظر أيضًا

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

