---
title: "FormattedText"
second_title: "مرجع API لـ Aspose.PDF للبايثون عبر .NET"
description: "فئة تمثل النص المنسق. تحتوي على معلومات حول النص ولونه وحجمه ونمطه."
type: docs
weight: 130
url: /ar/python-net/aspose.pdf.facades/formattedtext/
---

## FormattedText class

فئة تمثل النص المنسق. تحتوي على معلومات حول النص ولونه وحجمه ونمطه.

يعرض نوع FormattedText الأعضاء التالية:
## المُنشئات
| الاسم | الوصف |
| :- | :- |
| FormattedText() | يُهيئ FormattedText. |
| FormattedText(text) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, font_color, font_style, encoding_type, embedded, text_size) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, font_color, text_font, text_encoding, embedded, text_size, line_spacing) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, color, text_font, text_encoding, embedded, text_size) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, text_font, text_encoding, embedded, text_size, line_spacing) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, back_color, text_font, encoding, embedded, text_size) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, back_color, font_name, text_encoding, embedded, font_size) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, back_color) | يُهيئ نسخة جديدة من فئة FormattedText |
| FormattedText(text, text_color, font_name, text_encoding, embedded, font_size) | يُهيئ نسخة جديدة من فئة FormattedText |
## الخصائص
| الاسم | الوصف |
| :- | :- |
| text_height | يحصل على ارتفاع النص. |
| text_width | يحصل على عرض النص. |
## الطرق
| الاسم | الوصف |
| :- | :- |
| add_new_line_text(new_line_text) | يضيف سطرًا جديدًا إلى كائن FormattedText ويضبط newLineText على نص السطر التالي. |
| add_new_line_text(new_line_text, line_spacing) | يضيف سطرًا جديدًا إلى كائن FormattedText ويضبط newLineText على نص السطر التالي. |
| is_cjk() | يتحقق مما إذا كان النص CJK (الصينية أو اليابانية أو الكورية). |
| set_cjk_font_style() | يغيّر نمط خط FormattedText للخط CJK (الصينية أو اليابانية أو الكورية). |

### انظر أيضًا

* namespace [aspose.pdf.facades](/pdf/python-net/aspose.pdf.facades/)
* assembly [Aspose.PDF](/pdf/python-net/)

