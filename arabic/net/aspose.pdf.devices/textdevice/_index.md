---
title: TextDevice
second_title: Aspose.PDF لمرجع .NET API
description: يمثل فئة لتحويل صفحات مستند pdf إلى نص.
type: docs
weight: 1780
url: /ar/net/aspose.pdf.devices/textdevice/
---
## TextDevice class

يمثل فئة لتحويل صفحات مستند pdf إلى نص.

```csharp
public sealed class TextDevice : PageDevice
```

## المنشئون

| اسم | وصف |
| --- | --- |
| [TextDevice](textdevice#constructor)() | يقوم بتهيئة مثيل جديد لملف[`TextDevice`](../textdevice) مع وضع تنسيق النص الخام وترميز نص Unicode. |
| [TextDevice](textdevice#constructor_3)(Encoding) | يقوم بتهيئة مثيل جديد لملف[`TextDevice`](../textdevice) للترميز المحدد. |
| [TextDevice](textdevice#constructor_1)(TextExtractionOptions) | يقوم بتهيئة مثيل جديد لملف[`TextDevice`](../textdevice) مع خيارات استخراج النص. |
| [TextDevice](textdevice#constructor_2)(TextExtractionOptions, Encoding) | يقوم بتهيئة مثيل جديد لملف[`TextDevice`](../textdevice) للترميز المحدد مع خيارات استخراج النص. |

## الخصائص

| اسم | وصف |
| --- | --- |
| [Encoding](../../aspose.pdf.devices/textdevice/encoding) { get; set; } | الحصول على أو تعيين ترميز النص المستخرج. |
| [ExtractionOptions](../../aspose.pdf.devices/textdevice/extractionoptions) { get; set; } | الحصول على أو تعيين خيارات استخراج النص. |

## طُرق

| اسم | وصف |
| --- | --- |
| override [Process](../../aspose.pdf.devices/textdevice/process#process)(Page, Stream) | تحويل الصفحة وحفظها كتدفق نصي . |
| [Process](../../aspose.pdf.devices/pagedevice/process)(Page, string) | يقوم ببعض العمليات على الصفحة المحددة ويحفظ النتائج في الملف. |

### ملاحظات

ملف[`TextDevice`](../textdevice) يتم استخدام الكائن بشكل أساسي لاستخراج النص من صفحة pdf.

### أمثلة

يوضح المثال كيفية استخراج النص من صفحة مستند PDF الأولى.

```csharp
Document doc = new Document(inFile);
string extractedText;

using (MemoryStream ms = new MemoryStream())
{
    // إنشاء جهاز نصي
    TextDevice device = new TextDevice();

    // تحويل الصفحة وحفظ النص في الدفق
    device.Process(doc.Pages[1], ms);

    // استخدام النص المستخرج
    ms.Close();
    extractedText = Encoding.Unicode.GetString(ms.ToArray());
}
```

### أنظر أيضا

* class [PageDevice](../pagedevice)
* مساحة الاسم [Aspose.Pdf.Devices](../../aspose.pdf.devices)
* المجسم [Aspose.PDF](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.PDF.dll -->
