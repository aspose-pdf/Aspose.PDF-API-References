---
title: "TiffDevice"
second_title: "Aspose.PDF untuk Python via .NET Referensi API"
description: "Kelas ini membantu menyimpan halaman dokumen pdf satu per satu ke dalam satu gambar tiff."
type: docs
weight: 150
url: /id/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Kelas ini membantu menyimpan halaman dokumen pdf satu per satu ke dalam satu gambar tiff.

Tipe TiffDevice menampilkan anggota berikut:
## Konstruktor
| Nama | Deskripsi |
| :- | :- |
| TiffDevice(resolution) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(resolution, settings) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(resolution, settings, converter) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(settings) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(settings, converter) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice() | Menginisialisasi instance baru dari kelas [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) dengan pengaturan default. |
| TiffDevice(width, height, resolution, settings) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(page_size, resolution, settings) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(width, height, resolution) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(page_size, resolution) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(width, height, settings) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(width, height, settings, converter) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(page_size, settings, converter) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(page_size, settings) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(width, height) | Menginisialisasi instance baru dari kelas TiffDevice |
| TiffDevice(page_size) | Menginisialisasi instance baru dari kelas TiffDevice |
## Properti
| Nama | Deskripsi |
| :- | :- |
| rendering_options | Mendapatkan atau mengatur opsi rendering. |
| form_presentation_mode | Mendapatkan atau mengatur mode presentasi formulir. |
| pengaturan | Mendapatkan pengaturan untuk memetakan pdf ke gambar tiff. |
| resolution | Mendapatkan resolusi gambar. |
| lebar | Mendapatkan lebar output gambar. |
| tinggi | Mendapatkan tinggi output gambar. |
## Metode
| Nama | Deskripsi |
| :- | :- |
| process(document, from_page, to_page, output) | Mengonversi halaman dokumen tertentu menjadi tiff dan menyimpannya dalam aliran output. |
| process(document, output) | Mengonversi halaman dokumen tertentu menjadi tiff dan menyimpannya dalam aliran output. |
| process(document, output_file_name) | Mengonversi halaman dokumen tertentu menjadi tiff dan menyimpannya dalam aliran output. |
| process(document, from_page, to_page, output_file_name) | Mengonversi halaman dokumen tertentu menjadi tiff dan menyimpannya dalam aliran output. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Lakukan binarisasi Bradley untuk aliran masukan. |

### Lihat Juga

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

