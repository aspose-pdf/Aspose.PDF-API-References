---
title: "FormattedText"
second_title: "Aspose.PDF untuk Python via .NET Referensi API"
description: "Kelas yang merepresentasikan teks terformat. Berisi informasi tentang teks serta warna, ukuran, dan gaya nya."
type: docs
weight: 130
url: /id/python-net/aspose.pdf.facades/formattedtext/
---

## FormattedText class

Kelas yang merepresentasikan teks terformat. Berisi informasi tentang teks serta warna, ukuran, dan gaya nya.

Tipe FormattedText menampilkan anggota-anggota berikut:
## Konstruktor
| Nama | Deskripsi |
| :- | :- |
| FormattedText() | Menginisialisasi FormattedText. |
| FormattedText(text) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, font_color, font_style, encoding_type, embedded, text_size) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, font_color, text_font, text_encoding, embedded, text_size, line_spacing) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, color, text_font, text_encoding, embedded, text_size) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, text_font, text_encoding, embedded, text_size, line_spacing) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, back_color, text_font, encoding, embedded, text_size) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, back_color, font_name, text_encoding, embedded, font_size) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, back_color) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
| FormattedText(text, text_color, font_name, text_encoding, embedded, font_size) | Menginisialisasi sebuah instance baru dari kelas FormattedText |
## Properti
| Nama | Deskripsi |
| :- | :- |
| text_height | Mendapatkan tinggi teks. |
| text_width | Mendapatkan lebar teks. |
## Metode
| Nama | Deskripsi |
| :- | :- |
| add_new_line_text(new_line_text) | Menambahkan baris baru ke objek FormattedText dan mengatur newLineText ke teks baris berikutnya. |
| add_new_line_text(new_line_text, line_spacing) | Menambahkan baris baru ke objek FormattedText dan mengatur newLineText ke teks baris berikutnya. |
| is_cjk() | Memeriksa apakah teks merupakan CJK (Cina, Jepang, atau Korea). |
| set_cjk_font_style() | Mengubah gaya font FormattedText untuk font CJK (Cina, Jepang, atau Korea). |

### Lihat Juga

* namespace [aspose.pdf.facades](/pdf/python-net/aspose.pdf.facades/)
* assembly [Aspose.PDF](/pdf/python-net/)

