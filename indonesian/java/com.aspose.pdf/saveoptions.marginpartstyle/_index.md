---
title: "SaveOptions.MarginPartStyle"
linktitle: "SaveOptions.MarginPartStyle"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili informasi tentang satu bagian margin (atas, bawah, sisi kiri, atau sisi kanan)"
type: docs
weight: 4420
url: /id/java/com.aspose.pdf/saveoptions.marginpartstyle/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.SaveOptions.MarginPartStyle

```
public static class SaveOptions.MarginPartStyle extends Object
```

Mewakili informasi tentang satu bagian margin (atas, bawah, sisi kiri, atau sisi kanan)

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [MarginPartStyle](#MarginPartStyle-boolean-) | Membuat instance kelas MarginPartStyle dan menginisialisasi nilainya dalam poin. |
| [MarginPartStyle](#MarginPartStyle-int-) | Membuat instance kelas MarginPartStyle dan mengatur nilainya dalam poin. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getValueInPoints](#getValueInPoints--) | Mewakili margin dalam poin. Harus berupa angka lebih besar dari nol. |
| [isAuto](#isAuto--) | Mendapatkan atau mengatur nilai yang menunjukkan apakah instance ini otomatis. Nilai: {@code true} jika instance ini otomatis; jika tidak, {@code false}. |
| [setAuto](#setAuto-boolean-) | Mendapatkan atau mengatur nilai yang menunjukkan apakah instance ini otomatis. Nilai: {@code true} jika instance ini otomatis; jika tidak, {@code false}. |
| [setValueInPoints](#setValueInPoints-int-) | Mewakili margin dalam poin. Harus berupa angka lebih besar dari nol. |

### MarginPartStyle {#MarginPartStyle-boolean-}
```
public MarginPartStyle(boolean isAuto)
```

Membuat instance kelas MarginPartStyle dan menginisialisasi nilainya dalam poin.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| isAuto |  | Tandai margin otomatis |

### MarginPartStyle {#MarginPartStyle-int-}
```
public MarginPartStyle(int valueInPoints)
```

Membuat instance kelas MarginPartStyle dan mengatur nilainya dalam poin.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| valueInPoints |  | Nilai integer dalam poin |

### getValueInPoints {#getValueInPoints--}
```
public final int getValueInPoints()
```

Mewakili margin dalam poin. Harus berupa angka lebih besar dari nol.

**Returns:**
nilai int

### isAuto {#isAuto--}
```
public final boolean isAuto()
```

Mendapatkan atau mengatur nilai yang menunjukkan apakah instance ini otomatis. Nilai: {@code true} jika instance ini otomatis; jika tidak, {@code false}.

**Returns:**
nilai boolean

### setAuto {#setAuto-boolean-}
```
public final void setAuto(boolean value)
```

Mendapatkan atau mengatur nilai yang menunjukkan apakah instance ini otomatis. Nilai: {@code true} jika instance ini otomatis; jika tidak, {@code false}.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai |  | nilai boolean |

### setValueInPoints {#setValueInPoints-int-}
```
public final void setValueInPoints(int value)
```

Mewakili margin dalam poin. Harus berupa angka lebih besar dari nol.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai |  | nilai int |
