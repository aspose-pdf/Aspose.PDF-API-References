---
title: "HighlightingMode"
linktitle: "HighlightingMode"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Menumerasikan mode penyorotan anotasi, efek visual yang akan digunakan ketika tombol mouse ditekan atau ditahan di dalam area aktifnya."
type: docs
weight: 1920
url: /id/java/com.aspose.pdf/highlightingmode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < HighlightingMode > com.aspose.pdf.HighlightingMode, java.lang.Enum < HighlightingMode >, com.aspose.pdf.HighlightingMode

**All Implemented Interfaces:**
Serializable, Comparable < HighlightingMode >

```
public enum HighlightingMode extends Enum < HighlightingMode >
```

Menumerasikan mode penyorotan anotasi, efek visual yang akan digunakan ketika tombol mouse ditekan atau ditahan di dalam area aktifnya.

## Fields

| Field | Deskripsi |
| --- | --- |
| [Invert](#Invert) | Balikkan isi persegi panjang anotasi. |
| [None](#None) | Tidak ada penyorotan. |
| [Outline](#Outline) | Balikkan batas anotasi. |
| [Push](#Push) | Tampilkan tampilan bawah anotasi, jika ada. |
| [Toggle](#Toggle) | Sama dengan Push (yang lebih disukai). |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### Invert {#Invert}
```
public static final HighlightingMode Invert
```

Balikkan isi persegi panjang anotasi.

### None {#None}
```
public static final HighlightingMode None
```

Tidak ada penyorotan.

### Outline {#Outline}
```
public static final HighlightingMode Outline
```

Balikkan batas anotasi.

### Push {#Push}
```
public static final HighlightingMode Push
```

Tampilkan tampilan bawah anotasi, jika ada.

### Toggle {#Toggle}
```
public static final HighlightingMode Toggle
```

Sama dengan Push (yang lebih disukai).

### getByValue {#getByValue-int-}
```
public static HighlightingMode getByValue(int value)
```



**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static HighlightingMode [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
