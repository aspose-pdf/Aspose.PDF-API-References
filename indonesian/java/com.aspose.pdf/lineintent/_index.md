---
title: "LineIntent"
linktitle: "LineIntent"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Menumerasikan maksud anotasi garis."
type: docs
weight: 2750
url: /id/java/com.aspose.pdf/lineintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LineIntent > com.aspose.pdf.LineIntent, java.lang.Enum < LineIntent >, com.aspose.pdf.LineIntent

**All Implemented Interfaces:**
Serializable, Comparable < LineIntent >

```
public enum LineIntent extends Enum < LineIntent >
```

Menumerasikan maksud anotasi garis.

## Fields

| Field | Deskripsi |
| --- | --- |
| [LineArrow](#LineArrow) | Berarti anotasi dimaksudkan berfungsi sebagai panah. |
| [LineDimension](#LineDimension) | Berarti anotasi dimaksudkan berfungsi sebagai garis dimensi. |
| [Undefined](#Undefined) | Status tidak terdefinisi. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### LineArrow {#LineArrow}
```
public static final LineIntent LineArrow
```

Berarti anotasi dimaksudkan berfungsi sebagai panah.

### LineDimension {#LineDimension}
```
public static final LineIntent LineDimension
```

Berarti anotasi dimaksudkan berfungsi sebagai garis dimensi.

### Undefined {#Undefined}
```
public static final LineIntent Undefined
```

Status tidak terdefinisi.

### getByValue {#getByValue-int-}
```
public static LineIntent getByValue(int value)
```



**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static LineIntent [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
