---
title: "TextEditOptions.NoCharacterAction"
linktitle: "TextEditOptions.NoCharacterAction"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Tindakan yang harus dilakukan jika font tidak mengandung karakter yang diperlukan"
type: docs
weight: 5010
url: /id/java/com.aspose.pdf/texteditoptions.nocharacteraction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextEditOptions.NoCharacterAction > com.aspose.pdf.TextEditOptions.NoCharacterAction, java.lang.Enum < TextEditOptions.NoCharacterAction >, com.aspose.pdf.TextEditOptions.NoCharacterAction

**All Implemented Interfaces:**
Serializable, Comparable < TextEditOptions.NoCharacterAction >

```
public static enum TextEditOptions.NoCharacterAction extends Enum < TextEditOptions.NoCharacterAction >
```

Tindakan yang harus dilakukan jika font tidak mengandung karakter yang diperlukan

## Fields

| Field | Deskripsi |
| --- | --- |
| [ReplaceAnyway](#ReplaceAnyway) | Ganti teks apa pun tanpa substitusi font |
| [ReplaceFonts](#ReplaceFonts) | Mengganti font sesuai kebutuhan untuk memastikan semua karakter dalam teks dapat ditampilkan. |
| [ThrowException](#ThrowException) | Lempar pengecualian |
| [UseCustomReplacementFont](#UseCustomReplacementFont) | Ganti font dengan font pengganti yang ditentukan |
| [UseStandardFont](#UseStandardFont) | Ganti font ke font standar yang berisi karakter yang diperlukan |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### ReplaceAnyway {#ReplaceAnyway}
```
public static final TextEditOptions.NoCharacterAction ReplaceAnyway
```

Ganti teks apa pun tanpa substitusi font

### ReplaceFonts {#ReplaceFonts}
```
public static final TextEditOptions.NoCharacterAction ReplaceFonts
```

Mengganti font sesuai kebutuhan untuk memastikan semua karakter dalam teks dapat ditampilkan.

### ThrowException {#ThrowException}
```
public static final TextEditOptions.NoCharacterAction ThrowException
```

Lempar pengecualian

### UseCustomReplacementFont {#UseCustomReplacementFont}
```
public static final TextEditOptions.NoCharacterAction UseCustomReplacementFont
```

Ganti font dengan font pengganti yang ditentukan

### UseStandardFont {#UseStandardFont}
```
public static final TextEditOptions.NoCharacterAction UseStandardFont
```

Ganti font ke font standar yang berisi karakter yang diperlukan

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static TextEditOptions.NoCharacterAction [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
