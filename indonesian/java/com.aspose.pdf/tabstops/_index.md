---
title: "TabStops"
linktitle: "TabStops"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili koleksi objek {@code TabStop}."
type: docs
weight: 4850
url: /id/java/com.aspose.pdf/tabstops/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TabStops

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public class TabStops extends Object implements com.aspose.ms.System.ICloneable
```

Mewakili koleksi objek {@code TabStop}.

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [TabStops](#TabStops--) | Menginisialisasi sebuah instance baru dari kelas {@code TabStops}. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [add](#add--) | Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dan menambahkannya ke koleksi TabStops. |
| [add](#add-float-) | Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dengan posisi yang ditentukan dan menambahkannya ke koleksi TabStops. |
| [add](#add-float-int-) | Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dengan posisi dan tipe pemimpin yang ditentukan serta menambahkannya ke koleksi TabStops. |
| [add](#add-com.aspose.pdf.TabStop-) | Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dan menambahkannya ke koleksi TabStops. |
| [deepClone](#deepClone--) | Mengkloning objek {@code TabStops} baru. |
| [get_Item](#get_Item-int-) | Mendapatkan objek {@code TabStop} dari koleksi berdasarkan indeks TabStop. |
| [getCount](#getCount--) | Mengembalikan tabStops Count |
| [isReadOnly](#isReadOnly--) | Mendapatkan nilai yang menunjukkan bahwa instance {@code TabStops} ini sudah terhubung ke {@code TextFragment} dan menjadi readonly. |
| [set_Item](#set_Item-int-com.aspose.pdf.TabStop-) | Mengatur objek {@code TabStop} dari koleksi sesuai indeks TabStop. |

### TabStops {#TabStops--}
```
public TabStops()
```

Menginisialisasi sebuah instance baru dari kelas {@code TabStops}.

### add {#add--}
```
public TabStop add()
```

Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dan menambahkannya ke koleksi TabStops.

**Returns:**
Objek {@code TabStop} baru.

### add {#add-float-}
```
public TabStop add(float position)
```

Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dengan posisi yang ditentukan dan menambahkannya ke koleksi TabStops.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| posisi |  | Posisi tab stop. |

**Returns:**
Objek {@code TabStop} baru.

### add {#add-float-int-}
```
public TabStop add(float position, int leaderType)
```

Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dengan posisi dan tipe pemimpin yang ditentukan serta menambahkannya ke koleksi TabStops.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| posisi |  | Posisi tab stop. |
| leaderType |  | Tipe pemimpin tab stop. |

**Returns:**
Objek {@code TabStop} baru.

### add {#add-com.aspose.pdf.TabStop-}
Menginisialisasi sebuah instance baru dari kelas {@code TabStop} dan menambahkannya ke koleksi TabStops.

**Returns:**
Objek {@code TabStop} baru.

### deepClone {#deepClone--}
```
public Object deepClone()
```

Mengkloning objek {@code TabStops} baru.

**Returns:**
Objek {@code TabStops} baru.

### get_Item {#get_Item-int-}
```
public TabStop get_Item(int index)
```

Mendapatkan objek {@code TabStop} dari koleksi berdasarkan indeks TabStop.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| indeks |  | Indeks berbasis nol dari elemen dalam koleksi {@code TabStops}. |

**Returns:**
Objek {@code TabStop}.

### getCount {#getCount--}
```
public int getCount()
```

Mengembalikan tabStops Count

**Returns:**
nilai int

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Mendapatkan nilai yang menunjukkan bahwa instance {@code TabStops} ini sudah terhubung ke {@code TextFragment} dan menjadi readonly.

**Returns:**
nilai boolean

### set_Item {#set_Item-int-com.aspose.pdf.TabStop-}
Mengatur objek {@code TabStop} dari koleksi sesuai indeks TabStop.
