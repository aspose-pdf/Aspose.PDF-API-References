---
title: "CaptionPosition"
linktitle: "CaptionPosition"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Enumerasi penempatan caption anotasi."
type: docs
weight: 460
url: /id/java/com.aspose.pdf/captionposition/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < CaptionPosition > com.aspose.pdf.CaptionPosition, java.lang.Enum < CaptionPosition >, com.aspose.pdf.CaptionPosition

**All Implemented Interfaces:**
Serializable, Comparable < CaptionPosition >

```
public enum CaptionPosition extends Enum < CaptionPosition >
```

Enumerasi penempatan caption anotasi.

## Fields

| Field | Deskripsi |
| --- | --- |
| [Inline](#Inline) | Keterangan akan dipusatkan di dalam garis (nilai default). |
| [Top](#Top) | Keterangan akan berada di atas garis. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### Inline {#Inline}
```
public static final CaptionPosition Inline
```

Keterangan akan dipusatkan di dalam garis (nilai default).

### Top {#Top}
```
public static final CaptionPosition Top
```

Keterangan akan berada di atas garis.

### getByValue {#getByValue-int-}
```
public static CaptionPosition getByValue(int value)
```



**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static CaptionPosition [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
