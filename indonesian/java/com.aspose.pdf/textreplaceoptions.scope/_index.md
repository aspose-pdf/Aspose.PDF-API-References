---
title: "TextReplaceOptions.Scope"
linktitle: "TextReplaceOptions.Scope"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Ruang lingkup tempat operasi penggantian teks diterapkan REPLACE_FIRST secara default. Opsi usang ini dipertahankan untuk kompatibilitas. Ini memengaruhi PdfContentEditor dan tidak berpengaruh pada."
type: docs
weight: 5280
url: /id/java/com.aspose.pdf/textreplaceoptions.scope/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextReplaceOptions.Scope > com.aspose.pdf.TextReplaceOptions.Scope, java.lang.Enum < TextReplaceOptions.Scope >, com.aspose.pdf.TextReplaceOptions.Scope

**All Implemented Interfaces:**
Serializable, Comparable < TextReplaceOptions.Scope >

```
public static enum TextReplaceOptions.Scope extends Enum < TextReplaceOptions.Scope >
```

Lingkup di mana operasi ganti teks diterapkan REPLACE_FIRST secara default. Opsi usang ini dipertahankan untuk kompatibilitas. Ini memengaruhi PdfContentEditor dan tidak berpengaruh pada TextFragmentAbsorber.

## Fields

| Field | Deskripsi |
| --- | --- |
| [REPLACE_ALL](#REPLACE_ALL) | Ganti semua kemunculan teks pada semua halaman yang terpengaruh |
| [REPLACE_FIRST](#REPLACE_FIRST) | Ganti hanya kemunculan pertama teks pada setiap halaman yang terpengaruh |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### REPLACE_ALL {#REPLACE_ALL}
```
public static final TextReplaceOptions.Scope REPLACE_ALL
```

Ganti semua kemunculan teks pada semua halaman yang terpengaruh

### REPLACE_FIRST {#REPLACE_FIRST}
```
public static final TextReplaceOptions.Scope REPLACE_FIRST
```

Ganti hanya kemunculan pertama teks pada setiap halaman yang terpengaruh

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static TextReplaceOptions.Scope [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
