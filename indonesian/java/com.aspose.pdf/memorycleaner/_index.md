---
title: "MemoryCleaner"
linktitle: "MemoryCleaner"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili kelas MemoryCleaner."
type: docs
weight: 3010
url: /id/java/com.aspose.pdf/memorycleaner/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MemoryCleaner

```
public class MemoryCleaner extends Object
```

Mewakili kelas MemoryCleaner.

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [MemoryCleaner](#MemoryCleaner--) |  |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [clear](#clear--) | Membersihkan memori Heap dari instance Aspose.PDF statis (statis dan threadLocal) dan menghapus semua file sementara. |
| [clearAllTempFiles](#clearAllTempFiles--) | Membersihkan file sementara, yang dibuat oleh Aspose.PDF di direktori sementara sistem. |
| [clearCurrentThreadLocals](#clearCurrentThreadLocals--) | Dengan aman membersihkan memori Heap dari instance ThreadLocal untuk thread saat ini |
| [clearFontRegistry](#clearFontRegistry--) | Membersihkan informasi FontRegistry dari memori Heap. |
| [clearKeepingFontSettings](#clearKeepingFontSettings--) | Membersihkan memori Heap dari instance Aspose.PDF statis (statis dan threadLocal) dan menghapus semua file sementara. |
| [clearLicense](#clearLicense--) | Membersihkan informasi lisensi dari memori Heap. |
| [clearStaticInstances](#clearStaticInstances--) | Membersihkan memori Heap dari instance Aspose.PDF statis. |

### MemoryCleaner {#MemoryCleaner--}
```
public MemoryCleaner()
```



### clear {#clear--}
```
public static void clear()
```

Membersihkan memori Heap dari instance Aspose.PDF statis (statis dan threadLocal) dan menghapus semua file sementara.

### clearAllTempFiles {#clearAllTempFiles--}
```
public static void clearAllTempFiles()
```

Membersihkan file sementara, yang dibuat oleh Aspose.PDF di direktori sementara sistem.

### clearCurrentThreadLocals {#clearCurrentThreadLocals--}
```
public static void clearCurrentThreadLocals()
```

Dengan aman membersihkan memori Heap dari instance ThreadLocal untuk thread saat ini

### clearFontRegistry {#clearFontRegistry--}
```
public static void clearFontRegistry()
```

Membersihkan informasi FontRegistry dari memori Heap.

### clearKeepingFontSettings {#clearKeepingFontSettings--}
```
public static void clearKeepingFontSettings()
```

Membersihkan memori Heap dari instance Aspose.PDF statis (statis dan threadLocal) dan menghapus semua file sementara.

### clearLicense {#clearLicense--}
```
public static void clearLicense()
```

Membersihkan informasi lisensi dari memori Heap.

### clearStaticInstances {#clearStaticInstances--}
```
public static void clearStaticInstances()
```

Membersihkan memori Heap dari instance Aspose.PDF statis.
