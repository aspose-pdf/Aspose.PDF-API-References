---
title: "TextParagraphCollection"
linktitle: "TextParagraphCollection"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili koleksi paragraf teks"
type: docs
weight: 5230
url: /id/java/com.aspose.pdf/textparagraphcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextParagraphCollection

**All Implemented Interfaces:**
Iterable < TextParagraph >

```
public final class TextParagraphCollection extends Object implements Iterable < TextParagraph >
```

Mewakili koleksi paragraf teks

## Metode

| Metode | Deskripsi |
| --- | --- |
| [add](#add-com.aspose.pdf.TextParagraph-) | Menambahkan elemen paragraf teks pada indeks yang ditentukan. |
| [clear](#clear--) | Menghapus semua item dari koleksi. |
| [contains](#contains-com.aspose.pdf.TextParagraph-) | Menentukan apakah koleksi berisi nilai tertentu. |
| [copyTo](#copyTo-com.aspose.pdf.TextParagraph:A-int-) | Menyalin seluruh koleksi ke Array satu dimensi yang kompatibel, dimulai pada indeks yang ditentukan dari array target. |
| [get_Item](#get_Item-int-) | Mendapatkan elemen paragraf teks pada indeks yang ditentukan. |
| [getSyncRoot](#getSyncRoot--) | Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi. |
| [isReadOnly](#isReadOnly--) | Mendapatkan nilai yang menunjukkan apakah koleksi bersifat read-only |
| [isSynchronized](#isSynchronized--) | Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread). |
| [iterator](#iterator--) | Mengembalikan enumerator untuk seluruh koleksi. |
| [remove](#remove-com.aspose.pdf.TextParagraph-) | Menghapus item yang ditentukan dari koleksi. |
| [size](#size--) | Mendapatkan jumlah elemen objek {@code TextParagraph} yang sebenarnya terdapat dalam koleksi. |

### add {#add-com.aspose.pdf.TextParagraph-}
Menambahkan elemen paragraf teks pada indeks yang ditentukan.

### clear {#clear--}
```
public void clear()
```

Menghapus semua item dari koleksi.

### contains {#contains-com.aspose.pdf.TextParagraph-}
Menentukan apakah koleksi berisi nilai tertentu.

### copyTo {#copyTo-com.aspose.pdf.TextParagraph:A-int-}
Menyalin seluruh koleksi ke Array satu dimensi yang kompatibel, dimulai pada indeks yang ditentukan dari array target.

### get_Item {#get_Item-int-}
```
public TextParagraph get_Item(int index)
```

Mendapatkan elemen paragraf teks pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| indeks |  | nilai int |

**Returns:**
Objek TextParagraph

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi.

**Returns:**
Elemen objek

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Mendapatkan nilai yang menunjukkan apakah koleksi bersifat read-only

**Returns:**
nilai boolean

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread).

**Returns:**
nilai boolean

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator< TextParagraph > iterator()
```

Mengembalikan enumerator untuk seluruh koleksi.

**Returns:**
Objek IEnumerator

### remove {#remove-com.aspose.pdf.TextParagraph-}
Menghapus item yang ditentukan dari koleksi.

### size {#size--}
```
public int size()
```

Mendapatkan jumlah elemen objek {@code TextParagraph} yang sebenarnya terdapat dalam koleksi.

**Returns:**
nilai int
