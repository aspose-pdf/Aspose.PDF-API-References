---
title: "TextReplaceOptions.FontSizeAdjustment"
linktitle: "TextReplaceOptions.FontSizeAdjustment"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Menentukan kebijakan tentang bagaimana ukuran font teks harus disesuaikan agar muat dalam area yang menampungnya."
type: docs
weight: 5260
url: /id/java/com.aspose.pdf/textreplaceoptions.fontsizeadjustment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextReplaceOptions.FontSizeAdjustment > com.aspose.pdf.TextReplaceOptions.FontSizeAdjustment, java.lang.Enum < TextReplaceOptions.FontSizeAdjustment >, com.aspose.pdf.TextReplaceOptions.FontSizeAdjustment

**All Implemented Interfaces:**
Serializable, Comparable < TextReplaceOptions.FontSizeAdjustment >

```
public static enum TextReplaceOptions.FontSizeAdjustment extends Enum < TextReplaceOptions.FontSizeAdjustment >
```

Menentukan kebijakan tentang bagaimana ukuran font teks harus disesuaikan agar muat dalam area yang menampungnya.

## Fields

| Field | Deskripsi |
| --- | --- |
| [None](#None) | Ukuran font tidak diubah. |
| [ScaleToFill](#ScaleToFill) | Ukuran font disesuaikan (baik menyusut maupun membesar) agar teks mengisi batas persegi panjang sebanyak mungkin. |
| [ShrinkToFit](#ShrinkToFit) | Ukuran font dikurangi jika teks terlalu besar untuk muat dalam batas. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### None {#None}
```
public static final TextReplaceOptions.FontSizeAdjustment None
```

Ukuran font tidak diubah.

### ScaleToFill {#ScaleToFill}
```
public static final TextReplaceOptions.FontSizeAdjustment ScaleToFill
```

Ukuran font disesuaikan (baik menyusut maupun membesar) agar teks mengisi batas persegi panjang sebanyak mungkin.

### ShrinkToFit {#ShrinkToFit}
```
public static final TextReplaceOptions.FontSizeAdjustment ShrinkToFit
```

Ukuran font dikurangi jika teks terlalu besar untuk muat dalam batas.

### getByValue {#getByValue-int-}
```
public static TextReplaceOptions.FontSizeAdjustment getByValue(int value)
```



### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static TextReplaceOptions.FontSizeAdjustment [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
