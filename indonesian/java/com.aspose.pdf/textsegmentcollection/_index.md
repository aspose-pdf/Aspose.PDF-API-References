---
title: "TextSegmentCollection"
linktitle: "TextSegmentCollection"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili koleksi segmen teks"
type: docs
weight: 5310
url: /id/java/com.aspose.pdf/textsegmentcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.TextSegmentCollection

**All Implemented Interfaces:**
Iterable < TextSegment >

```
public final class TextSegmentCollection extends Object implements Iterable < TextSegment >
```

Mewakili koleksi segmen teks

## Metode

| Metode | Deskripsi |
| --- | --- |
| [add](#add-com.aspose.pdf.TextSegment-) | Menambahkan elemen segmen teks pada indeks yang ditentukan. |
| [clear](#clear--) | Menghapus semua item dari koleksi. |
| [contains](#contains-com.aspose.pdf.TextSegment-) | Menentukan apakah koleksi berisi nilai tertentu. |
| [copyTo](#copyTo-com.aspose.pdf.TextSegment:A-int-) | Menyalin seluruh koleksi ke Array satu dimensi yang kompatibel, dimulai pada indeks yang ditentukan dari array target. |
| [delete](#delete-int-) | Menghapus elemen segmen teks pada indeks yang ditentukan. |
| [get_Item](#get_Item-int-) | Mendapatkan elemen segmen teks pada indeks yang ditentukan. |
| [getSyncRoot](#getSyncRoot--) | Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi. |
| [isReadOnly](#isReadOnly--) | Mendapatkan nilai yang menunjukkan apakah koleksi bersifat read-only |
| [isSynchronized](#isSynchronized--) | Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread). |
| [iterator](#iterator--) | Mengembalikan enumerator untuk seluruh koleksi. |
| [remove](#remove-com.aspose.pdf.TextSegment-) | Menghapus item yang ditentukan dari koleksi. |
| [size](#size--) | Mendapatkan jumlah elemen objek {@code TextSegment} yang sebenarnya terdapat dalam koleksi. |

### add {#add-com.aspose.pdf.TextSegment-}
Menambahkan elemen segmen teks pada indeks yang ditentukan.

### clear {#clear--}
```
public void clear()
```

Menghapus semua item dari koleksi.

### contains {#contains-com.aspose.pdf.TextSegment-}
Menentukan apakah koleksi berisi nilai tertentu.

### copyTo {#copyTo-com.aspose.pdf.TextSegment:A-int-}
Menyalin seluruh koleksi ke Array satu dimensi yang kompatibel, dimulai pada indeks yang ditentukan dari array target.

### delete {#delete-int-}
```
public void delete(int index)
```

Menghapus elemen segmen teks pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| indeks |  | nilai int |

### get_Item {#get_Item-int-}
```
public TextSegment get_Item(int index)
```

Mendapatkan elemen segmen teks pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| indeks |  | Indeks dalam koleksi. |

**Returns:**
Objek TextSegment.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi.

**Returns:**
Elemen objek

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Mendapatkan nilai yang menunjukkan apakah koleksi bersifat read-only

**Returns:**
nilai boolean

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread).

**Returns:**
nilai boolean

### iterator {#iterator--}
```
public Iterator < TextSegment > iterator()
```

Mengembalikan enumerator untuk seluruh koleksi.

**Returns:**
Objek Enumerator.

### remove {#remove-com.aspose.pdf.TextSegment-}
Menghapus item yang ditentukan dari koleksi.

### size {#size--}
```
public int size()
```

Mendapatkan jumlah elemen objek {@code TextSegment} yang sebenarnya terdapat dalam koleksi.

**Returns:**
nilai int
