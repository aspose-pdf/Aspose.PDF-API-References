---
title: "Measure.NumberFormat.FractionStyle"
linktitle: "Measure.NumberFormat.FractionStyle"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Nilai yang menunjukkan cara nilai pecahan ditampilkan."
type: docs
weight: 2950
url: /id/java/com.aspose.pdf/measure.numberformat.fractionstyle/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < Measure.NumberFormat.FractionStyle > com.aspose.pdf.Measure.NumberFormat.FractionStyle, java.lang.Enum < Measure.NumberFormat.FractionStyle >, com.aspose.pdf.Measure.NumberFormat.FractionStyle

**All Implemented Interfaces:**
Serializable, Comparable < Measure.NumberFormat.FractionStyle >

```
public static enum Measure.NumberFormat.FractionStyle extends Enum < Measure.NumberFormat.FractionStyle >
```

Nilai yang menunjukkan cara nilai pecahan ditampilkan.

## Fields

| Field | Deskripsi |
| --- | --- |
| [Round](#Round) | Bulatkan nilai pecahan ke bilangan bulat terdekat. |
| [ShowAsDecimal](#ShowAsDecimal) | Tampilkan nilai pecahan sebagai pecahan desimal. |
| [ShowAsFraction](#ShowAsFraction) | Tampilkan nilai pecahan sebagai fraksi. |
| [Truncate](#Truncate) | Potong untuk mendapatkan satuan bulat. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### Round {#Round}
```
public static final Measure.NumberFormat.FractionStyle Round
```

Bulatkan nilai pecahan ke bilangan bulat terdekat.

### ShowAsDecimal {#ShowAsDecimal}
```
public static final Measure.NumberFormat.FractionStyle ShowAsDecimal
```

Tampilkan nilai pecahan sebagai pecahan desimal.

### ShowAsFraction {#ShowAsFraction}
```
public static final Measure.NumberFormat.FractionStyle ShowAsFraction
```

Tampilkan nilai pecahan sebagai fraksi.

### Truncate {#Truncate}
```
public static final Measure.NumberFormat.FractionStyle Truncate
```

Potong untuk mendapatkan satuan bulat.

### getByValue {#getByValue-int-}
```
public static Measure.NumberFormat.FractionStyle getByValue(int value)
```



### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static Measure.NumberFormat.FractionStyle [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
