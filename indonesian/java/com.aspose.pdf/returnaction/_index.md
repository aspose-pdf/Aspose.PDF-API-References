---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Enum mewakili aksi alur kerja program dalam kasus pemanggilan metode {@code IWarningCallback.Warning(WarningInfo)}."
type: docs
weight: 4240
url: /id/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

Enum mewakili aksi alur kerja program dalam kasus pemanggilan metode {@code IWarningCallback.Warning(WarningInfo)}.

## Fields

| Field | Deskripsi |
| --- | --- |
| [Abort](#Abort) | Digunakan untuk menghentikan alur. |
| [Continue](#Continue) | Digunakan untuk melanjutkan alur. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

Digunakan untuk menghentikan alur.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

Digunakan untuk melanjutkan alur.

### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static ReturnAction [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
