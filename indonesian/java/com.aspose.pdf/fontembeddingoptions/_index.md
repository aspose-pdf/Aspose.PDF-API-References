---
title: "FontEmbeddingOptions"
linktitle: "FontEmbeddingOptions"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Standar PDF/A mengharuskan semua font harus disematkan ke dalam dokumen. Kelas ini mencakup flag untuk kasus ketika tidak memungkinkan menyematkan beberapa font karena font tersebut tidak ada."
type: docs
weight: 1680
url: /id/java/com.aspose.pdf/fontembeddingoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.FontEmbeddingOptions

```
public class FontEmbeddingOptions extends Object
```

Standar PDF/A mengharuskan semua font harus disematkan ke dalam dokumen. Kelas ini mencakup flags untuk kasus ketika tidak memungkinkan menyematkan beberapa font karena font tersebut tidak ada pada PC tujuan.

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [FontEmbeddingOptions](#FontEmbeddingOptions--) | Menginisialisasi instance baru dari kelas {@link FontEmbeddingOptions}. Konstruktor ini menetapkan nilai default untuk properti {@code UseDefaultSubstitution}({@link #getUseDefaultSubstitution}/{@link #setUseDefaultSubstitution(boolean)}) menjadi {@code }. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getUseDefaultSubstitution](#getUseDefaultSubstitution--) | Menunjukkan apakah akan menggantikan font yang tidak disematkan menggunakan strategi substitusi font default. Secara default false; |
| [setUseDefaultSubstitution](#setUseDefaultSubstitution-boolean-) | Menunjukkan apakah akan menggantikan font yang tidak disematkan menggunakan strategi substitusi font default. Secara default false; |

### FontEmbeddingOptions {#FontEmbeddingOptions--}
```
public FontEmbeddingOptions()
```

Menginisialisasi instance baru dari kelas {@link FontEmbeddingOptions}. Konstruktor ini menetapkan nilai default untuk properti {@code UseDefaultSubstitution}({@link #getUseDefaultSubstitution}/{@link #setUseDefaultSubstitution(boolean)}) menjadi {@code }.

### getUseDefaultSubstitution {#getUseDefaultSubstitution--}
```
public boolean getUseDefaultSubstitution()
```

Menunjukkan apakah akan menggantikan font yang tidak disematkan menggunakan strategi substitusi font default. Secara default false;

**Returns:**
nilai boolean

### setUseDefaultSubstitution {#setUseDefaultSubstitution-boolean-}
```
public void setUseDefaultSubstitution(boolean value)
```

Menunjukkan apakah akan menggantikan font yang tidak disematkan menggunakan strategi substitusi font default. Secara default false;

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai |  | nilai boolean |
