---
title: "PageCoordinateType"
linktitle: "PageCoordinateType"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Menjelaskan tipe koordinat halaman. MediaBox = 0 CropBox = 1"
type: docs
weight: 3350
url: /id/java/com.aspose.pdf/pagecoordinatetype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PageCoordinateType > com.aspose.pdf.PageCoordinateType, java.lang.Enum < PageCoordinateType >, com.aspose.pdf.PageCoordinateType

**All Implemented Interfaces:**
Serializable, Comparable < PageCoordinateType >

```
public enum PageCoordinateType extends Enum < PageCoordinateType >
```

Menjelaskan tipe koordinat halaman. MediaBox = 0 CropBox = 1

## Fields

| Field | Deskripsi |
| --- | --- |
| [CropBox](#CropBox) | CropBox mendefinisikan wilayah tempat konten halaman akan dipotong. |
| [MediaBox](#MediaBox) | MediaBox digunakan untuk menentukan lebar dan tinggi halaman. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan. |
| [values](#values--) | Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan. |

### CropBox {#CropBox}
```
public static final PageCoordinateType CropBox
```

CropBox mendefinisikan wilayah tempat konten halaman akan dipotong.

### MediaBox {#MediaBox}
```
public static final PageCoordinateType MediaBox
```

MediaBox digunakan untuk menentukan lebar dan tinggi halaman.

### getByValue {#getByValue-int-}
```
public static PageCoordinateType getByValue(int value)
```



**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Mengembalikan konstanta enum dari tipe ini dengan nama yang ditentukan.

### values {#values--}
```
public static PageCoordinateType [] values()
```

Mengembalikan array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan.

**Returns:**
array yang berisi konstanta dari tipe enum ini, dalam urutan mereka dideklarasikan
