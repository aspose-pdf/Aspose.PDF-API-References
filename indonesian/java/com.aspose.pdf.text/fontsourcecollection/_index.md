---
title: "FontSourceCollection"
linktitle: "FontSourceCollection"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili koleksi sumber font."
type: docs
weight: 40
url: /id/java/com.aspose.pdf.text/fontsourcecollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.text.FontSourceCollection

**All Implemented Interfaces:**
Iterable < FontSource >

```
public final class FontSourceCollection extends Object implements Iterable < FontSource >
```

Mewakili koleksi sumber font.

## Fields

| Field | Deskripsi |
| --- | --- |
| [CollectionChanged](#CollectionChanged) | Peristiwa CollectionChanged |

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [FontSourceCollection](#FontSourceCollection--) | Menginisialisasi objek koleksi |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [add](#add-com.aspose.pdf.FontSource-) | Menambahkan objek sumber font baru ke koleksi. |
| [clear](#clear--) | Menghapus koleksi sumber font. |
| [contains](#contains-com.aspose.pdf.FontSource-) | Menentukan apakah sebuah elemen berada dalam koleksi. |
| [copyTo](#copyTo-com.aspose.pdf.FontSource:A-int-) | Menyalin seluruh koleksi ke Array satu dimensi yang kompatibel, dimulai pada indeks yang ditentukan dari array target. |
| [delete](#delete-com.aspose.pdf.FontSource-) | Menghapus elemen sumber font. |
| [getItem](#getItem-int-) | Mendapatkan elemen font pada indeks yang ditentukan. |
| [getSyncRoot](#getSyncRoot--) | Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi. |
| [isSynchronized](#isSynchronized--) | Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread). |
| [iterator](#iterator--) | Mengembalikan enumerator untuk seluruh koleksi. |
| [remove](#remove-com.aspose.pdf.FontSource-) | Menghapus elemen sumber font. |
| [size](#size--) | Mendapatkan jumlah elemen objek Font yang sebenarnya terdapat dalam koleksi. |

### CollectionChanged {#CollectionChanged}
```
public final PdfEvent <com.aspose.ms.System.EventHandler> CollectionChanged
```

Peristiwa CollectionChanged

### FontSourceCollection {#FontSourceCollection--}
```
public FontSourceCollection()
```

Menginisialisasi objek koleksi

### add {#add-com.aspose.pdf.FontSource-}
Menambahkan objek sumber font baru ke koleksi.

### clear {#clear--}
```
public void clear()
```

Menghapus koleksi sumber font.

### contains {#contains-com.aspose.pdf.FontSource-}
Menentukan apakah sebuah elemen berada dalam koleksi.

### copyTo {#copyTo-com.aspose.pdf.FontSource:A-int-}
Menyalin seluruh koleksi ke Array satu dimensi yang kompatibel, dimulai pada indeks yang ditentukan dari array target.

### delete {#delete-com.aspose.pdf.FontSource-}
Menghapus elemen sumber font.

### getItem {#getItem-int-}
```
public FontSource getItem(int index)
```

Mendapatkan elemen font pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| indeks |  | Indeks dalam koleksi. |

**Returns:**
Objek sumber font.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi.

**Returns:**
Elemen objek

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread).

**Returns:**
nilai boolean

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator iterator()
```

Mengembalikan enumerator untuk seluruh koleksi.

**Returns:**
Objek Enumerator.

### remove {#remove-com.aspose.pdf.FontSource-}
Menghapus elemen sumber font.

### size {#size--}
```
public int size()
```

Mendapatkan jumlah elemen objek Font yang sebenarnya terdapat dalam koleksi.

**Returns:**
nilai int
