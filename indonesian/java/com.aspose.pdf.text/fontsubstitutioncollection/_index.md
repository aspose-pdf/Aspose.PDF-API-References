---
title: "FontSubstitutionCollection"
linktitle: "FontSubstitutionCollection"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili koleksi strategi substitusi font."
type: docs
weight: 60
url: /id/java/com.aspose.pdf.text/fontsubstitutioncollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.text.FontSubstitutionCollection

**All Implemented Interfaces:**
Iterable < FontSubstitution >

```
public final class FontSubstitutionCollection extends Object implements Iterable < FontSubstitution >
```

Mewakili koleksi strategi substitusi font.

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [FontSubstitutionCollection](#FontSubstitutionCollection--) | Menginisialisasi objek koleksi |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [add](#add-com.aspose.pdf.text.FontSubstitution-) | Menambahkan objek substitusi font baru ke koleksi. |
| [clear](#clear--) | Membersihkan koleksi substitusi font. |
| [contains](#contains-com.aspose.pdf.text.FontSubstitution-) | Menentukan apakah sebuah elemen berada dalam koleksi. |
| [delete](#delete-com.aspose.pdf.text.FontSubstitution-) | Hanya untuk internal, menghapus elemen substitusi font. |
| [getItem](#getItem-int-) | Mendapatkan elemen font pada indeks yang ditentukan. |
| [getSyncRoot](#getSyncRoot--) | Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi. |
| [isSynchronized](#isSynchronized--) | Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread). |
| [iterator](#iterator--) | Mengembalikan enumerator untuk seluruh koleksi. |
| [remove](#remove-com.aspose.pdf.text.FontSubstitution-) | Menghapus elemen substitusi font. |
| [size](#size--) | Mendapatkan jumlah elemen objek Font yang sebenarnya terdapat dalam koleksi. |

### FontSubstitutionCollection {#FontSubstitutionCollection--}
```
public FontSubstitutionCollection()
```

Menginisialisasi objek koleksi

### add {#add-com.aspose.pdf.text.FontSubstitution-}
Menambahkan objek substitusi font baru ke koleksi.

### clear {#clear--}
```
public void clear()
```

Membersihkan koleksi substitusi font.

### contains {#contains-com.aspose.pdf.text.FontSubstitution-}
Menentukan apakah sebuah elemen berada dalam koleksi.

### delete {#delete-com.aspose.pdf.text.FontSubstitution-}
Hanya untuk internal, menghapus elemen substitusi font.

### getItem {#getItem-int-}
```
public FontSubstitution getItem(int index)
```

Mendapatkan elemen font pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| indeks |  | Indeks dalam koleksi. |

**Returns:**
objek FontSubstitution.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Mendapatkan objek yang dapat digunakan untuk menyinkronkan akses ke koleksi.

**Returns:**
Objek untuk sinkronisasi

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Mendapatkan nilai yang menunjukkan apakah akses ke koleksi disinkronkan (aman untuk thread).

**Returns:**
nilai boolean

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.Generic.List.Enumerator< FontSubstitution > iterator()
```

Mengembalikan enumerator untuk seluruh koleksi.

**Returns:**
Objek enumerator

### remove {#remove-com.aspose.pdf.text.FontSubstitution-}
Menghapus elemen substitusi font.

### size {#size--}
```
public int size()
```

Mendapatkan jumlah elemen objek Font yang sebenarnya terdapat dalam koleksi.

**Returns:**
nilai int
