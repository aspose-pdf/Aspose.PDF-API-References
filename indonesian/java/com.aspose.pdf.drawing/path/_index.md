---
title: "Path"
linktitle: "Path"
second_title: "Referensi API Aspose.PDF untuk Java"
description: "Mewakili busur."
type: docs
weight: 100
url: /id/java/com.aspose.pdf.drawing/path/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.drawing.Shape com.aspose.pdf.drawing.Path, com.aspose.pdf.drawing.Shape, com.aspose.pdf.drawing.Path

**All Implemented Interfaces:**
IBoundsCheckableItem

```
public final class Path extends Shape
```

Mewakili busur.

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [Path](#Path--) | Menginisialisasi instance baru dari kelas {@code Path}. |
| [Path](#Path-com.aspose.pdf.drawing.Shape:A-) | Menginisialisasi instance baru dari kelas {@code Path}. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [checkBounds](#checkBounds-double-double-) | Memeriksa apakah item cocok dengan dimensi kontainer yang diberikan (inklusif). |
| [getShapes](#getShapes--) | <p> Mendapatkan atau mengatur koleksi bentuk. </p> |
| [getShapesInternal](#getShapesInternal--) | Mendapatkan atau mengatur koleksi bentuk. |

### Path {#Path--}
```
public Path()
```

Menginisialisasi instance baru dari kelas {@code Path}.

### Path {#Path-com.aspose.pdf.drawing.Shape:A-}
Menginisialisasi instance baru dari kelas {@code Path}.

### checkBounds {#checkBounds-double-double-}
```
public boolean checkBounds(double containerWidth, double containerHeight)
```

Memeriksa apakah item cocok dengan dimensi kontainer yang diberikan (inklusif).

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| containerWidth |  |  |
| containerHeight |  |  |

**Returns:**
True jika cocok; jika tidak, false.

### getShapes {#getShapes--}
```
public List < Shape > getShapes()
```

<p> Mendapatkan atau mengatur koleksi bentuk. </p>

**Returns:**
{@code java.util.List<Shape> }objek

### getShapesInternal {#getShapesInternal--}
```
public com.aspose.ms.System.Collections.Generic.List< Shape > getShapesInternal()
```

Mendapatkan atau mengatur koleksi bentuk.

**Returns:**
objek internal
