---
title: "AttributeKey"
second_title: "Aspose.PDF for Python via .NET API 참조"
description: "표준 속성 키를 나타냅니다."
type: docs
weight: 40
url: /ko/python-net/aspose.pdf.logicalstructure/attributekey/
---

## AttributeKey class

표준 속성 키를 나타냅니다.

AttributeKey 유형은 다음 멤버를 노출합니다:
## 속성
| 이름 | 설명 |
| :- | :- |
| key | Attribute Key를 가져옵니다. |
| owner | Attribute Owner를 가져옵니다. |
| PLACEMENT | Placement 속성 (Layout attribute owner). |
| WRITING_MODE | WritingMode 속성 (Layout 속성 소유자). |
| BACKGROUND_COLOR | BackgroundColor 속성 (Layout 속성 소유자). |
| BORDER_COLOR | BorderColor 속성 (Layout 속성 소유자). |
| BORDER_STYLE | BorderStyle 속성 (Layout 속성 소유자). |
| BORDER_THICKNESS | BorderThickness 속성 (Layout 속성 소유자). |
| PADDING | Padding 속성 (Layout 속성 소유자). |
| COLOR | Color 속성 (Layout 속성 소유자). |
| SPACE_BEFORE | SpaceBefore 속성 (Layout 속성 소유자). |
| SPACE_AFTER | SpaceAfter 속성 (Layout 속성 소유자). |
| START_INDENT | StartIndent 속성 (Layout 속성 소유자). |
| END_INDENT | EndIndent 속성 (Layout 속성 소유자). |
| TEXT_INDENT | TextIndent 속성 (Layout 속성 소유자). |
| TEXT_ALIGN | TextAlign 속성 (Layout 속성 소유자). |
| B_BOX | BBox 속성 (레이아웃 속성 소유자). |
| WIDTH | Width 속성 (레이아웃 속성 소유자). |
| HEIGHT | Height 속성 (레이아웃 속성 소유자). |
| BLOCK_ALIGN | BlockAlign 속성 (레이아웃 속성 소유자). |
| INLINE_ALIGN | InlineAlign 속성 (레이아웃 속성 소유자). |
| T_BORDER_STYLE | TBorderStyle 속성 (레이아웃 속성 소유자). |
| T_PADDING | TPadding 속성 (레이아웃 속성 소유자). |
| BASELINE_SHIFT | BaselineShift 속성 (레이아웃 속성 소유자). |
| LINE_HEIGHT | LineHeight 속성 (레이아웃 속성 소유자). |
| TEXT_DECORATION_COLOR | TextDecorationColor 속성 (레이아웃 속성 소유자). |
| TEXT_DECORATION_THICKNESS | TextDecorationThickness 속성 (레이아웃 속성 소유자). |
| TEXT_DECORATION_TYPE | TextDecorationType 속성 (레이아웃 속성 소유자). |
| RUBY_ALIGN | RubyAlign 속성 (Layout attribute 소유자). |
| RUBY_POSITION | RubyPosition 속성 (Layout attribute 소유자). |
| GLYPH_ORIENTATION_VERTICAL | GlyphOrientationVertical 속성 (Layout attribute 소유자). |
| COLUMN_COUNT | ColumnCount 속성 (Layout attribute 소유자). |
| COLUMN_GAP | ColumnGap 속성 (Layout attribute 소유자). |
| COLUMN_WIDTHS | ColumnWidths 속성 (Layout attribute 소유자). |
| LIST_NUMBERING | ListNumbering 속성 (List attribute 소유자). |
| ROLE | Role 속성 (PrintField attribute 소유자). |
| CHECKED | Checked 속성 (PrintField attribute 소유자). |
| DESC | Desc 속성 (PrintField attribute 소유자). |
| ROW_SPAN | RowSpan 속성 (Table attribute 소유자). |
| COL_SPAN | ColSpan 속성 (Table attribute 소유자). |
| HEADERS | Headers 속성 (Table attribute 소유자). |
| 범위 | 범위 속성 (테이블 속성 소유자). |
| 요약 | 요약 속성 (테이블 속성 소유자). |

### 또 보기

* namespace [aspose.pdf.logicalstructure](/pdf/python-net/aspose.pdf.logicalstructure/)
* assembly [Aspose.PDF](/pdf/python-net/)

