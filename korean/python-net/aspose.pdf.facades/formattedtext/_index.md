---
title: "FormattedText"
second_title: "Aspose.PDF for Python via .NET API 참조"
description: "서식이 지정된 텍스트를 나타내는 클래스. 텍스트와 그 색상, 크기, 스타일에 대한 정보를 포함합니다."
type: docs
weight: 130
url: /ko/python-net/aspose.pdf.facades/formattedtext/
---

## FormattedText class

서식이 지정된 텍스트를 나타내는 클래스. 텍스트와 그 색상, 크기, 스타일에 대한 정보를 포함합니다.

FormattedText 유형은 다음 멤버를 노출합니다:
## 생성자
| 이름 | 설명 |
| :- | :- |
| FormattedText() | FormattedText를 초기화합니다. |
| FormattedText(text) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, font_color, font_style, encoding_type, embedded, text_size) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, font_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, color, text_font, text_encoding, embedded, text_size) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, back_color, text_font, encoding, embedded, text_size) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, back_color, font_name, text_encoding, embedded, font_size) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, back_color) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
| FormattedText(text, text_color, font_name, text_encoding, embedded, font_size) | FormattedText 클래스의 새 인스턴스를 초기화합니다. |
## 속성
| 이름 | 설명 |
| :- | :- |
| text_height | 텍스트의 높이를 가져옵니다. |
| text_width | 텍스트의 너비를 가져옵니다. |
## 메서드
| 이름 | 설명 |
| :- | :- |
| add_new_line_text(new_line_text) | FormattedText 객체에 새 줄을 추가하고 newLineText를 다음 줄의 텍스트로 설정합니다. |
| add_new_line_text(new_line_text, line_spacing) | FormattedText 객체에 새 줄을 추가하고 newLineText를 다음 줄의 텍스트로 설정합니다. |
| is_cjk() | 텍스트가 CJK(중국어, 일본어 또는 한국어)인지 확인합니다. |
| set_cjk_font_style() | CJK(중국어, 일본어 또는 한국어) 글꼴에 대해 FormattedText의 글꼴 스타일을 변경합니다. |

### 또 보기

* namespace [aspose.pdf.facades](/pdf/python-net/aspose.pdf.facades/)
* assembly [Aspose.PDF](/pdf/python-net/)

