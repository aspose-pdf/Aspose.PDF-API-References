---
title: "TiffDevice"
second_title: "Aspose.PDF for Python via .NET API 참조"
description: "이 클래스는 PDF 문서 페이지를 하나의 TIFF 이미지에 페이지별로 저장하도록 도와줍니다."
type: docs
weight: 150
url: /ko/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

이 클래스는 PDF 문서 페이지를 하나의 TIFF 이미지에 페이지별로 저장하도록 도와줍니다.

TiffDevice 유형은 다음 멤버를 노출합니다:
## 생성자
| 이름 | 설명 |
| :- | :- |
| TiffDevice(resolution) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(resolution, settings) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(resolution, settings, converter) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(settings) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(settings, converter) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice() | 기본 설정으로 [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) 클래스의 새 인스턴스를 초기화합니다. |
| TiffDevice(width, height, resolution, settings) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(width, height, resolution, settings, converter) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(page_size, resolution, settings) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(page_size, resolution, settings, converter) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(width, height, resolution) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(page_size, resolution) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(width, height, settings) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(width, height, settings, converter) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(page_size, settings, converter) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(page_size, settings) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(width, height) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
| TiffDevice(page_size) | TiffDevice 클래스의 새 인스턴스를 초기화합니다 |
## 속성
| 이름 | 설명 |
| :- | :- |
| rendering_options | 렌더링 옵션을 가져오거나 설정합니다. |
| form_presentation_mode | 폼 프레젠테이션 모드를 가져오거나 설정합니다. |
| settings | PDF를 TIFF 이미지로 매핑하기 위한 설정을 가져옵니다. |
| resolution | 이미지 해상도를 가져옵니다. |
| width | 이미지 출력 너비를 가져옵니다. |
| 높이 | 이미지 출력 높이를 가져옵니다. |
## 메서드
| 이름 | 설명 |
| :- | :- |
| process(document, from_page, to_page, output) | 특정 문서 페이지를 TIFF로 변환하고 출력 스트림에 저장합니다. |
| process(document, output) | 특정 문서 페이지를 TIFF로 변환하고 출력 스트림에 저장합니다. |
| process(document, output_file_name) | 특정 문서 페이지를 TIFF로 변환하고 출력 스트림에 저장합니다. |
| process(document, from_page, to_page, output_file_name) | 특정 문서 페이지를 TIFF로 변환하고 출력 스트림에 저장합니다. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | 입력 스트림에 대해 Bradley 이진화를 수행합니다. |

### 또 보기

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

