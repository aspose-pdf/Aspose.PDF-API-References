---
title: Class FormFieldOptions
second_title: Aspose.PDF for .NET API Reference
description: Aspose.Pdf.Plugins.FormFieldOptions 클래스. 필드 옵션을 나타냅니다. PdfFormFieldCreateOptions 및 PdfFormFillFieldOptions의 기본 클래스
type: docs
weight: 8710
url: /ko/net/aspose.pdf.plugins/formfieldoptions/
---
## FormFieldOptions 클래스

필드 옵션을 나타냅니다. PdfFormFieldCreateOptions 및 PdfFormFillFieldOptions의 기본 클래스입니다.

```csharp
public abstract class FormFieldOptions
```

## 속성

| 이름 | 설명 |
| --- | --- |
| [AlternateName](../../aspose.pdf.plugins/formfieldoptions/alternatename/) { get; set; } | 생성/수정된 필드의 AlternateName 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [Color](../../aspose.pdf.plugins/formfieldoptions/color/) { get; set; } | 생성/수정된 필드의 Color 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [Contents](../../aspose.pdf.plugins/formfieldoptions/contents/) { get; set; } | 생성/수정된 필드의 Contents 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [DefaultAppearance](../../aspose.pdf.plugins/formfieldoptions/defaultappearance/) { get; set; } | 생성/수정된 필드의 DefaultAppearance 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [Exportable](../../aspose.pdf.plugins/formfieldoptions/exportable/) { get; set; } | 생성/수정된 필드가 내보낼 수 있는지 여부를 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [FitIntoRectangle](../../aspose.pdf.plugins/formfieldoptions/fitintorectangle/) { get; set; } | 생성/수정된 필드가 사각형에 맞는지 여부를 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [Flags](../../aspose.pdf.plugins/formfieldoptions/flags/) { get; set; } | 생성/수정된 필드의 Flags 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [IsSharedField](../../aspose.pdf.plugins/formfieldoptions/issharedfield/) { get; set; } | 생성/수정된 필드가 공유 필드인지 여부를 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [MappingName](../../aspose.pdf.plugins/formfieldoptions/mappingname/) { get; set; } | 생성/수정된 필드의 MappingName 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [MaxFontSize](../../aspose.pdf.plugins/formfieldoptions/maxfontsize/) { get; set; } | 생성/수정된 필드의 MaxFontSize 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [MinFontSize](../../aspose.pdf.plugins/formfieldoptions/minfontsize/) { get; set; } | 생성/수정된 필드의 MinFontSize 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [Name](../../aspose.pdf.plugins/formfieldoptions/name/) { get; set; } | 생성/수정된 필드의 Name 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [PartialName](../../aspose.pdf.plugins/formfieldoptions/partialname/) { get; set; } | 생성/수정된 필드의 PartialName 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [ReadOnly](../../aspose.pdf.plugins/formfieldoptions/readonly/) { get; set; } | 생성/수정된 필드가 읽기 전용인지 여부를 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [Required](../../aspose.pdf.plugins/formfieldoptions/required/) { get; set; } | 생성/수정된 필드가 필수인지 여부를 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [TextHorizontalAlignment](../../aspose.pdf.plugins/formfieldoptions/texthorizontalalignment/) { get; set; } | 생성/수정된 필드의 TextHorizontalAlignment 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [UpdateAppearanceOnConvert](../../aspose.pdf.plugins/formfieldoptions/updateappearanceonconvert/) { get; set; } | 생성/수정된 필드가 변환 시 외관을 업데이트하는지 여부를 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [UseFontSubset](../../aspose.pdf.plugins/formfieldoptions/usefontsubset/) { get; set; } | 생성/수정된 필드가 글꼴 하위 집합을 사용하는지 여부를 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |
| [Value](../../aspose.pdf.plugins/formfieldoptions/value/) { get; set; } | 생성/수정된 필드의 Value 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |

## 필드

| 이름 | 설명 |
| --- | --- |
| [Highlighting](../../aspose.pdf.plugins/formfieldoptions/highlighting/) | 생성/수정된 필드의 Highlighting 속성을 결정하는 값을 가져오거나 설정합니다 (설정될 경우). |

### 참조

* 네임스페이스 [Aspose.Pdf.Plugins](../../aspose.pdf.plugins/)
* 어셈블리 [Aspose.PDF](../../)