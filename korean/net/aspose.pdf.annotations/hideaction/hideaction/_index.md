---
title: HideAction.HideAction
second_title: Aspose.PDF for .NET API Reference
description: HideAction 생성자. 지정된 주석에 대한 HideAction 클래스의 새 인스턴스를 초기화합니다.
type: docs
weight: 10
url: /ko/net/aspose.pdf.annotations/hideaction/hideaction/
---
## HideAction(Annotation) {#constructor}

지정된 주석에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(Annotation annotation)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| annotation | Annotation | 숨겨질 주석입니다. |

### 참조

* 클래스 [Annotation](../../annotation/)
* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)

---

## HideAction(Annotation, bool) {#constructor_1}

지정된 주석 및 불투명성 플래그에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(Annotation annotation, bool isHidden)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| annotation | Annotation | 숨기거나 표시할 주석입니다. |
| isHidden | Boolean | 주석을 숨길지(true) 또는 표시할지(false) 여부를 나타내는 플래그입니다. |

### 참조

* 클래스 [Annotation](../../annotation/)
* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)

---

## HideAction(string) {#constructor_4}

지정된 필드 이름에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(string fieldName)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| fieldName | String | 대화형 양식 필드의 전체 자격 필드 이름을 제공하는 텍스트 문자열입니다. |

### 참조

* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)

---

## HideAction(string, bool) {#constructor_5}

지정된 필드 이름 및 불투명성 플래그에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(string fieldName, bool isHidden)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| fieldName | String | 대화형 양식 필드의 전체 자격 필드 이름을 제공하는 텍스트 문자열입니다. |
| isHidden | Boolean | 필드를 숨길지(true) 또는 표시할지(false) 여부를 나타내는 플래그입니다. |

### 참조

* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)

---

## HideAction(Annotation[]) {#constructor_2}

지정된 주석에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(Annotation[] annotations)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| annotations | Annotation[] | 숨겨질 주석의 배열입니다. |

### 참조

* 클래스 [Annotation](../../annotation/)
* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)

---

## HideAction(Annotation[], bool) {#constructor_3}

지정된 주석 및 불투명성 플래그에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(Annotation[] annotations, bool isHidden)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| annotations | Annotation[] | 숨기거나 표시할 주석의 배열입니다. |
| isHidden | Boolean | 주석을 숨길지(true) 또는 표시할지(false) 여부를 나타내는 플래그입니다. |

### 참조

* 클래스 [Annotation](../../annotation/)
* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)

---

## HideAction(string[]) {#constructor_6}

지정된 필드 이름에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(string[] names)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| names | String[] | 대화형 양식 필드의 전체 자격 필드 이름을 제공하는 문자열 배열입니다. |

### 참조

* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)

---

## HideAction(string[], bool) {#constructor_7}

지정된 필드 이름 및 불투명성 플래그에 대한 [`HideAction`](../) 클래스의 새 인스턴스를 초기화합니다.

```csharp
public HideAction(string[] names, bool isHidden)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| names | String[] | 대화형 양식 필드의 전체 자격 필드 이름을 제공하는 문자열 배열입니다. |
| isHidden | Boolean | 필드를 숨길지(true) 또는 표시할지(false) 여부를 나타내는 플래그입니다. |

### 참조

* 클래스 [HideAction](../)
* 네임스페이스 [Aspose.Pdf.Annotations](../../../aspose.pdf.annotations/)
* 어셈블리 [Aspose.PDF](../../../)