---
title: "TiffDevice"
second_title: "Aspose.PDF per Python via .NET Riferimento API"
description: "Questa classe aiuta a salvare le pagine del documento PDF, una per una, in un'unica immagine TIFF."
type: docs
weight: 150
url: /it/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Questa classe aiuta a salvare le pagine del documento PDF, una per una, in un'unica immagine TIFF.

Il tipo TiffDevice espone i seguenti membri:
## Costruttori
| Nome | Descrizione |
| :- | :- |
| TiffDevice(resolution) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(resolution, settings) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(resolution, settings, converter) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(settings) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(settings, converter) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice() | Inizializza una nuova istanza della classe [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) con impostazioni predefinite. |
| TiffDevice(width, height, resolution, settings) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(page_size, resolution, settings) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(width, height, resolution) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(page_size, resolution) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(width, height, settings) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(width, height, settings, converter) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(page_size, settings, converter) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(page_size, settings) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(width, height) | Inizializza una nuova istanza della classe TiffDevice |
| TiffDevice(page_size) | Inizializza una nuova istanza della classe TiffDevice |
## Proprietà
| Nome | Descrizione |
| :- | :- |
| rendering_options | Ottiene o imposta le opzioni di rendering. |
| form_presentation_mode | Ottiene o imposta la modalità di presentazione del modulo. |
| impostazioni | Ottiene le impostazioni per mappare il pdf in un'immagine tiff. |
| resolution | Ottiene la risoluzione dell'immagine. |
| width | Ottiene la larghezza di output dell'immagine. |
| height | Ottiene l'altezza di output dell'immagine. |
## Metodi
| Nome | Descrizione |
| :- | :- |
| process(document, from_page, to_page, output) | Converte alcune pagine del documento in tiff e le salva nello stream di output. |
| process(document, output) | Converte alcune pagine del documento in tiff e le salva nello stream di output. |
| process(document, output_file_name) | Converte alcune pagine del documento in tiff e le salva nello stream di output. |
| process(document, from_page, to_page, output_file_name) | Converte alcune pagine del documento in tiff e le salva nello stream di output. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Esegue la binarizzazione Bradley per lo stream di input. |

### Vedi anche

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

