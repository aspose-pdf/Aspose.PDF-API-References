---
title: "DefaultMetadataProperties"
linktitle: "DefaultMetadataProperties"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Enumerazione delle proprietà XMP standard."
type: docs
weight: 100
url: /it/java/com.aspose.pdf.facades/defaultmetadataproperties/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.facades.DefaultMetadataProperties, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.facades.DefaultMetadataProperties, com.aspose.ms.System.Enum, com.aspose.pdf.facades.DefaultMetadataProperties

```
public final class DefaultMetadataProperties extends com.aspose.ms.System.Enum
```

Enumerazione delle proprietà XMP standard.

## Campi

| Campo | Descrizione |
| --- | --- |
| [Advisory](#Advisory) | xmp:Advisory property. Un array non ordinato che specifica le proprietà che sono state modificate al di fuori dell'applicazione di authoring. Ogni elemento dovrebbe contenere un singolo /// namespace e XPath separati da uno spazio ASCII. |
| [BaseURL](#BaseURL) | xmp:BaseURL property. L'URL di base per gli URL relativi nel contenuto del documento. Se questo documento contiene collegamenti Internet e tali collegamenti sono relativi, /// sono relativi a questo URL di base. Questa proprietà fornisce un modo standard per interpretare gli URL relativi incorporati dagli strumenti. /// Gli strumenti di authoring web dovrebbero impostare il valore in base alla loro concezione di dove gli URL saranno interpretati. |
| [CreateDate](#CreateDate) | xmp:CreateDate property. La data e l'ora in cui la risorsa è stata originariamente creata. |
| [CreatorTool](#CreatorTool) | xmp:CreatorTool property. Il nome del primo strumento noto utilizzato per creare la risorsa. |
| [Identifier](#Identifier) | xmp:Identifier property. Un array non ordinato di stringhe di testo che identificano in modo univoco la risorsa all'interno di un determinato contesto. |
| [MetadataDate](#MetadataDate) | xmp:MetadataDate property. La data e l'ora in cui i metadati di questa risorsa sono stati modificati l'ultima volta. |
| [ModifyDate](#ModifyDate) | xmp:ModifyDate property. La data e l'ora in cui la risorsa è stata modificata l'ultima volta. |
| [Nickname](#Nickname) | xmp:Nickname property. Un breve nome informale per la risorsa. |
| [Thumbnails](#Thumbnails) | xmp:Thumbnails property. Un array alternativo di immagini miniatura per un file, che può differire per caratteristiche come dimensione o codifica dell'immagine. |

### Advisory {#Advisory}
```
public static final int Advisory
```

xmp:Advisory property. Un array non ordinato che specifica le proprietà che sono state modificate al di fuori dell'applicazione di authoring. Ogni elemento dovrebbe contenere un singolo /// namespace e XPath separati da uno spazio ASCII.

### BaseURL {#BaseURL}
```
public static final int BaseURL
```

xmp:BaseURL property. L'URL di base per gli URL relativi nel contenuto del documento. Se questo documento contiene collegamenti Internet e tali collegamenti sono relativi, /// sono relativi a questo URL di base. Questa proprietà fornisce un modo standard per interpretare gli URL relativi incorporati dagli strumenti. /// Gli strumenti di authoring web dovrebbero impostare il valore in base alla loro concezione di dove gli URL saranno interpretati.

### CreateDate {#CreateDate}
```
public static final int CreateDate
```

xmp:CreateDate property. La data e l'ora in cui la risorsa è stata originariamente creata.

### CreatorTool {#CreatorTool}
```
public static final int CreatorTool
```

xmp:CreatorTool property. Il nome del primo strumento noto utilizzato per creare la risorsa.

### Identifier {#Identifier}
```
public static final int Identifier
```

xmp:Identifier property. Un array non ordinato di stringhe di testo che identificano in modo univoco la risorsa all'interno di un determinato contesto.

### MetadataDate {#MetadataDate}
```
public static final int MetadataDate
```

xmp:MetadataDate property. La data e l'ora in cui i metadati di questa risorsa sono stati modificati l'ultima volta.

### ModifyDate {#ModifyDate}
```
public static final int ModifyDate
```

xmp:ModifyDate property. La data e l'ora in cui la risorsa è stata modificata l'ultima volta.

### Nickname {#Nickname}
```
public static final int Nickname
```

xmp:Nickname property. Un breve nome informale per la risorsa.

### Thumbnails {#Thumbnails}
```
public static final int Thumbnails
```

xmp:Thumbnails property. Un array alternativo di immagini miniatura per un file, che può differire per caratteristiche come dimensione o codifica dell'immagine.
