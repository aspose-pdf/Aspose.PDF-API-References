---
title: "ImageMergeMode"
linktitle: "ImageMergeMode"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Rappresenta le modalità per unire le immagini."
type: docs
weight: 270
url: /it/java/com.aspose.pdf.facades/imagemergemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ImageMergeMode > com.aspose.pdf.facades.ImageMergeMode, java.lang.Enum < ImageMergeMode >, com.aspose.pdf.facades.ImageMergeMode

**All Implemented Interfaces:**
Serializable, Comparable < ImageMergeMode >

```
public enum ImageMergeMode extends Enum < ImageMergeMode >
```

Rappresenta le modalità per unire le immagini.

## Campi

| Campo | Descrizione |
| --- | --- |
| [Center](#Center) | Immagini allineate al centro. |
| [Horizontal](#Horizontal) | Immagini unite orizzontalmente. |
| [Vertical](#Vertical) | Immagini unite verticalmente. |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Restituisce la costante enum di questo tipo con il nome specificato. |
| [values](#values--) | Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate. |

### Center {#Center}
```
public static final ImageMergeMode Center
```

Immagini allineate al centro.

### Horizontal {#Horizontal}
```
public static final ImageMergeMode Horizontal
```

Immagini unite orizzontalmente.

### Vertical {#Vertical}
```
public static final ImageMergeMode Vertical
```

Immagini unite verticalmente.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Restituisce la costante enum di questo tipo con il nome specificato.

### values {#values--}
```
public static ImageMergeMode [] values()
```

Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate.

**Returns:**
un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate
