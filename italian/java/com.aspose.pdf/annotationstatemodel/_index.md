---
title: "AnnotationStateModel"
linktitle: "AnnotationStateModel"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Il modello di stato corrispondente allo stato dell'annotazione."
type: docs
weight: 120
url: /it/java/com.aspose.pdf/annotationstatemodel/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationStateModel > com.aspose.pdf.AnnotationStateModel, java.lang.Enum < AnnotationStateModel >, com.aspose.pdf.AnnotationStateModel

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationStateModel >

```
public enum AnnotationStateModel extends Enum < AnnotationStateModel >
```

Il modello di stato corrispondente allo stato dell'annotazione.

## Campi

| Campo | Descrizione |
| --- | --- |
| [Marked](#Marked) | L'annotazione è stata contrassegnata (o decontrassegnata) dall'utente. |
| [Review](#Review) | L'annotazione è stata revisionata (accettata, rifiutata, annullata, completata, nessuna) dall'utente. |
| [Undefined](#Undefined) | Modello di stato non definito. |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Restituisce la costante enum di questo tipo con il nome specificato. |
| [values](#values--) | Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate. |

### Marked {#Marked}
```
public static final AnnotationStateModel Marked
```

L'annotazione è stata contrassegnata (o decontrassegnata) dall'utente.

### Review {#Review}
```
public static final AnnotationStateModel Review
```

L'annotazione è stata revisionata (accettata, rifiutata, annullata, completata, nessuna) dall'utente.

### Undefined {#Undefined}
```
public static final AnnotationStateModel Undefined
```

Modello di stato non definito.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Restituisce la costante enum di questo tipo con il nome specificato.

### values {#values--}
```
public static AnnotationStateModel [] values()
```

Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate.

**Returns:**
un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate
