---
title: "AnnotationState"
linktitle: "AnnotationState"
second_title: "Riferimento API Aspose.PDF per Java"
description: "L'enumerazione degli stati a cui può essere impostata l'annotazione originale."
type: docs
weight: 110
url: /it/java/com.aspose.pdf/annotationstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationState > com.aspose.pdf.AnnotationState, java.lang.Enum < AnnotationState >, com.aspose.pdf.AnnotationState

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationState >

```
public enum AnnotationState extends Enum < AnnotationState >
```

L'enumerazione degli stati a cui può essere impostata l'annotazione originale.

## Campi

| Campo | Descrizione |
| --- | --- |
| [Accepted](#Accepted) | L'utente è d'accordo con la modifica. |
| [Cancelled](#Cancelled) | La modifica è stata annullata. |
| [Completed](#Completed) | La modifica è stata completata. |
| [Marked](#Marked) | L'annotazione è stata contrassegnata dall'utente. |
| [None](#None) | L'utente non ha indicato nulla sulla modifica. |
| [Rejected](#Rejected) | L'utente non è d'accordo con la modifica. |
| [Undefined](#Undefined) | Stato non definito. |
| [Unmarked](#Unmarked) | L'annotazione non è stata contrassegnata dall'utente. |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Restituisce la costante enum di questo tipo con il nome specificato. |
| [values](#values--) | Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate. |

### Accepted {#Accepted}
```
public static final AnnotationState Accepted
```

L'utente è d'accordo con la modifica.

### Cancelled {#Cancelled}
```
public static final AnnotationState Cancelled
```

La modifica è stata annullata.

### Completed {#Completed}
```
public static final AnnotationState Completed
```

La modifica è stata completata.

### Marked {#Marked}
```
public static final AnnotationState Marked
```

L'annotazione è stata contrassegnata dall'utente.

### None {#None}
```
public static final AnnotationState None
```

L'utente non ha indicato nulla sulla modifica.

### Rejected {#Rejected}
```
public static final AnnotationState Rejected
```

L'utente non è d'accordo con la modifica.

### Undefined {#Undefined}
```
public static final AnnotationState Undefined
```

Stato non definito.

### Unmarked {#Unmarked}
```
public static final AnnotationState Unmarked
```

L'annotazione non è stata contrassegnata dall'utente.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Restituisce la costante enum di questo tipo con il nome specificato.

### values {#values--}
```
public static AnnotationState [] values()
```

Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate.

**Returns:**
un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate
