---
title: "Document.RepairOptions"
linktitle: "Document.RepairOptions"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Rappresenta le opzioni per la riparazione di un documento PDF. Questa classe fornisce un modo per personalizzare il processo di riparazione di un documento PDF."
type: docs
weight: 1120
url: /it/java/com.aspose.pdf/document.repairoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.Document.RepairOptions

```
public static class Document.RepairOptions extends Object
```

Rappresenta le opzioni per la riparazione di un documento PDF. Questa classe fornisce un modo per personalizzare il processo di riparazione di un documento PDF.

## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [RepairOptions](#RepairOptions--) |  |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [isRestoreIndirectObjectGenerations](#isRestoreIndirectObjectGenerations--) | Ottiene o imposta un valore che indica se ripristinare i numeri di generazione errati nei riferimenti a oggetti indiretti durante il processo di riparazione. |
| [setRestoreIndirectObjectGenerations](#setRestoreIndirectObjectGenerations-boolean-) | Ottiene o imposta un valore che indica se ripristinare i numeri di generazione errati nei riferimenti a oggetti indiretti durante il processo di riparazione. |

### RepairOptions {#RepairOptions--}
```
public RepairOptions()
```



### isRestoreIndirectObjectGenerations {#isRestoreIndirectObjectGenerations--}
```
public boolean isRestoreIndirectObjectGenerations()
```

Ottiene o imposta un valore che indica se ripristinare i numeri di generazione errati nei riferimenti a oggetti indiretti durante il processo di riparazione.

**Returns:**
valore booleano

### setRestoreIndirectObjectGenerations {#setRestoreIndirectObjectGenerations-boolean-}
```
public void setRestoreIndirectObjectGenerations(boolean restoreIndirectObjectGenerations)
```

Ottiene o imposta un valore che indica se ripristinare i numeri di generazione errati nei riferimenti a oggetti indiretti durante il processo di riparazione.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| restoreIndirectObjectGenerations |  | valore booleano |
