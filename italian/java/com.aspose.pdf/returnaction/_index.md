---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Enum rappresenta un'azione del flusso di lavoro del programma nel caso di invocazione del metodo {@code IWarningCallback.Warning(WarningInfo)}."
type: docs
weight: 4240
url: /it/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

Enum rappresenta un'azione del flusso di lavoro del programma nel caso di invocazione del metodo {@code IWarningCallback.Warning(WarningInfo)}.

## Campi

| Campo | Descrizione |
| --- | --- |
| [Abort](#Abort) | Utilizzato per interrompere il flusso. |
| [Continue](#Continue) | Utilizzato per continuare il flusso. |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | Restituisce la costante enum di questo tipo con il nome specificato. |
| [values](#values--) | Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

Utilizzato per interrompere il flusso.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

Utilizzato per continuare il flusso.

### valueOf {#valueOf-java.lang.String-}
Restituisce la costante enum di questo tipo con il nome specificato.

### values {#values--}
```
public static ReturnAction [] values()
```

Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate.

**Returns:**
un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate
