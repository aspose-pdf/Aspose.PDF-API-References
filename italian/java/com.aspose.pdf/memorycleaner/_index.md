---
title: "MemoryCleaner"
linktitle: "MemoryCleaner"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Rappresenta la classe MemoryCleaner"
type: docs
weight: 3010
url: /it/java/com.aspose.pdf/memorycleaner/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MemoryCleaner

```
public class MemoryCleaner extends Object
```

Rappresenta la classe MemoryCleaner

## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [MemoryCleaner](#MemoryCleaner--) |  |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [clear](#clear--) | Cancella la memoria Heap dalle istanze statiche di Aspose.PDF (statiche e threadLocal) ed elimina tutti i file temporanei. |
| [clearAllTempFiles](#clearAllTempFiles--) | Cancella i file temporanei, creati da Aspose.PDF nella directory temporanea di sistema. |
| [clearCurrentThreadLocals](#clearCurrentThreadLocals--) | Cancella in modo sicuro la memoria Heap dalle istanze ThreadLocal per il thread corrente |
| [clearFontRegistry](#clearFontRegistry--) | Cancella le informazioni di FontRegistry dalla memoria Heap. |
| [clearKeepingFontSettings](#clearKeepingFontSettings--) | Cancella la memoria Heap dalle istanze statiche di Aspose.PDF (statiche e threadLocal) ed elimina tutti i file temporanei. |
| [clearLicense](#clearLicense--) | Cancella le informazioni di licenza dalla memoria Heap. |
| [clearStaticInstances](#clearStaticInstances--) | Cancella la memoria Heap dalle istanze statiche di Aspose.PDF. |

### MemoryCleaner {#MemoryCleaner--}
```
public MemoryCleaner()
```



### clear {#clear--}
```
public static void clear()
```

Cancella la memoria Heap dalle istanze statiche di Aspose.PDF (statiche e threadLocal) ed elimina tutti i file temporanei.

### clearAllTempFiles {#clearAllTempFiles--}
```
public static void clearAllTempFiles()
```

Cancella i file temporanei, creati da Aspose.PDF nella directory temporanea di sistema.

### clearCurrentThreadLocals {#clearCurrentThreadLocals--}
```
public static void clearCurrentThreadLocals()
```

Cancella in modo sicuro la memoria Heap dalle istanze ThreadLocal per il thread corrente

### clearFontRegistry {#clearFontRegistry--}
```
public static void clearFontRegistry()
```

Cancella le informazioni di FontRegistry dalla memoria Heap.

### clearKeepingFontSettings {#clearKeepingFontSettings--}
```
public static void clearKeepingFontSettings()
```

Cancella la memoria Heap dalle istanze statiche di Aspose.PDF (statiche e threadLocal) ed elimina tutti i file temporanei.

### clearLicense {#clearLicense--}
```
public static void clearLicense()
```

Cancella le informazioni di licenza dalla memoria Heap.

### clearStaticInstances {#clearStaticInstances--}
```
public static void clearStaticInstances()
```

Cancella la memoria Heap dalle istanze statiche di Aspose.PDF.
