---
title: "RenditionOperation"
linktitle: "RenditionOperation"
second_title: "Riferimento API Aspose.PDF per Java"
description: "L'operazione da eseguire quando l'azione viene attivata."
type: docs
weight: 4190
url: /it/java/com.aspose.pdf/renditionoperation/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.Enum, com.aspose.pdf.RenditionOperation

```
public final class RenditionOperation extends com.aspose.ms.System.Enum
```

L'operazione da eseguire quando l'azione viene attivata.

## Campi

| Campo | Descrizione |
| --- | --- |
| [Pause](#Pause) | Metti in pausa qualsiasi rendition in riproduzione in associazione con l'annotazione. |
| [PlayResume](#PlayResume) | Riproduci la rendition specificata, associandola all'annotazione. Se una rendition è già associata all'annotazione, riprendi la rendition se è in pausa. |
| [PlayStop](#PlayStop) | Se nessuna rendition è associata all'annotazione, riproduci la rendition specificata, associandola all'annotazione. Se una rendition è già associata all'annotazione, essa deve essere interrotta e la nuova rendition deve essere associata all'annotazione. |
| [Resume](#Resume) | Riprendi qualsiasi rendition in riproduzione in associazione con l'annotazione. |
| [Stop](#Stop) | Interrompi qualsiasi rendition in riproduzione in associazione con l'annotazione. |
| [Undefined](#Undefined) | Operazione non definita. |

### Pause {#Pause}
```
public static final int Pause
```

Metti in pausa qualsiasi rendition in riproduzione in associazione con l'annotazione.

### PlayResume {#PlayResume}
```
public static final int PlayResume
```

Riproduci la rendition specificata, associandola all'annotazione. Se una rendition è già associata all'annotazione, riprendi la rendition se è in pausa.

### PlayStop {#PlayStop}
```
public static final int PlayStop
```

Se nessuna rendition è associata all'annotazione, riproduci la rendition specificata, associandola all'annotazione. Se una rendition è già associata all'annotazione, essa deve essere interrotta e la nuova rendition deve essere associata all'annotazione.

### Resume {#Resume}
```
public static final int Resume
```

Riprendi qualsiasi rendition in riproduzione in associazione con l'annotazione.

### Stop {#Stop}
```
public static final int Stop
```

Interrompi qualsiasi rendition in riproduzione in associazione con l'annotazione.

### Undefined {#Undefined}
```
public static final int Undefined
```

Operazione non definita.
