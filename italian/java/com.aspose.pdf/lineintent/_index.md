---
title: "LineIntent"
linktitle: "LineIntent"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Elenca le intenzioni dell'annotazione di linea."
type: docs
weight: 2750
url: /it/java/com.aspose.pdf/lineintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LineIntent > com.aspose.pdf.LineIntent, java.lang.Enum < LineIntent >, com.aspose.pdf.LineIntent

**All Implemented Interfaces:**
Serializable, Comparable < LineIntent >

```
public enum LineIntent extends Enum < LineIntent >
```

Elenca le intenzioni dell'annotazione di linea.

## Campi

| Campo | Descrizione |
| --- | --- |
| [LineArrow](#LineArrow) | Indica che l'annotazione è destinata a funzionare come una freccia. |
| [LineDimension](#LineDimension) | Indica che l'annotazione è destinata a funzionare come una linea di dimensione. |
| [Undefined](#Undefined) | Stato non definito. |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Restituisce la costante enum di questo tipo con il nome specificato. |
| [values](#values--) | Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate. |

### LineArrow {#LineArrow}
```
public static final LineIntent LineArrow
```

Indica che l'annotazione è destinata a funzionare come una freccia.

### LineDimension {#LineDimension}
```
public static final LineIntent LineDimension
```

Indica che l'annotazione è destinata a funzionare come una linea di dimensione.

### Undefined {#Undefined}
```
public static final LineIntent Undefined
```

Stato non definito.

### getByValue {#getByValue-int-}
```
public static LineIntent getByValue(int value)
```



**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Restituisce la costante enum di questo tipo con il nome specificato.

### values {#values--}
```
public static LineIntent [] values()
```

Restituisce un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate.

**Returns:**
un array contenente le costanti di questo tipo enum, nell'ordine in cui sono dichiarate
