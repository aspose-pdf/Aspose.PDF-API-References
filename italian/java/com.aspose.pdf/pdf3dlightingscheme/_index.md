---
title: "PDF3DLightingScheme"
linktitle: "PDF3DLightingScheme"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Classe PDF3DLightingScheme."
type: docs
weight: 3620
url: /it/java/com.aspose.pdf/pdf3dlightingscheme/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DLightingScheme

```
public class PDF3DLightingScheme extends Object
```

Classe PDF3DLightingScheme.

## Campi

| Campo | Descrizione |
| --- | --- |
| [Artwork](#Artwork) | Lo schema di illuminazione "Artwork". |
| [Blue](#Blue) | Lo schema di illuminazione "Blue". |
| [CAD](#CAD) | Lo schema di illuminazione "Cad". |
| [Cube](#Cube) | Lo schema di illuminazione "Cube". |
| [Day](#Day) | Lo schema di illuminazione "Day". |
| [Hard](#Hard) | Lo schema di illuminazione "Hard". |
| [Headlamp](#Headlamp) | Lo schema di illuminazione "Headlamp". |
| [Night](#Night) | Lo schema di illuminazione "Night". |
| [None](#None) | Lo schema di illuminazione "None". |
| [Primary](#Primary) | Lo schema di illuminazione "Primary". |
| [Red](#Red) | Lo schema di illuminazione "Red". |
| [White](#White) | Lo schema di illuminazione "White". |

## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [PDF3DLightingScheme](#PDF3DLightingScheme-com.aspose.pdf.LightingSchemeType-) | Inizializza una nuova istanza della classe {@code PDF3DLightingScheme}. |
| [PDF3DLightingScheme](#PDF3DLightingScheme-java.lang.String-) | Inizializza una nuova istanza della classe {@code PDF3DLightingScheme}. |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getType](#getType--) | Ottiene il tipo di schema di illuminazione. |

### Artwork {#Artwork}
```
public static final PDF3DLightingScheme Artwork
```

Lo schema di illuminazione "Artwork".

### Blue {#Blue}
```
public static final PDF3DLightingScheme Blue
```

Lo schema di illuminazione "Blue".

### CAD {#CAD}
```
public static final PDF3DLightingScheme CAD
```

Lo schema di illuminazione "Cad".

### Cube {#Cube}
```
public static final PDF3DLightingScheme Cube
```

Lo schema di illuminazione "Cube".

### Day {#Day}
```
public static final PDF3DLightingScheme Day
```

Lo schema di illuminazione "Day".

### Hard {#Hard}
```
public static final PDF3DLightingScheme Hard
```

Lo schema di illuminazione "Hard".

### Headlamp {#Headlamp}
```
public static final PDF3DLightingScheme Headlamp
```

Lo schema di illuminazione "Headlamp".

### Night {#Night}
```
public static final PDF3DLightingScheme Night
```

Lo schema di illuminazione "Night".

### None {#None}
```
public static final PDF3DLightingScheme None
```

Lo schema di illuminazione "None".

### Primary {#Primary}
```
public static final PDF3DLightingScheme Primary
```

Lo schema di illuminazione "Primary".

### Red {#Red}
```
public static final PDF3DLightingScheme Red
```

Lo schema di illuminazione "Red".

### White {#White}
```
public static final PDF3DLightingScheme White
```

Lo schema di illuminazione "White".

### PDF3DLightingScheme {#PDF3DLightingScheme-com.aspose.pdf.LightingSchemeType-}
Inizializza una nuova istanza della classe {@code PDF3DLightingScheme}.

### PDF3DLightingScheme {#PDF3DLightingScheme-java.lang.String-}
Inizializza una nuova istanza della classe {@code PDF3DLightingScheme}.

### getType {#getType--}
```
public LightingSchemeType getType()
```

Ottiene il tipo di schema di illuminazione.

**Returns:**
valore int dell'elemento LightingSchemeType: Il tipo di schema di illuminazione. @see LightingSchemeType
