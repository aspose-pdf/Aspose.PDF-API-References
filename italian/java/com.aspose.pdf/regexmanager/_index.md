---
title: "RegexManager"
linktitle: "RegexManager"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Fornisce un wrapper per le operazioni di espressioni regolari con impostazioni di timeout configurabili."
type: docs
weight: 4130
url: /it/java/com.aspose.pdf/regexmanager/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.RegexManager

```
public class RegexManager extends Object
```

Fornisce un wrapper per le operazioni di espressioni regolari con impostazioni di timeout configurabili.

## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [RegexManager](#RegexManager--) |  |

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getMatchTimeout](#getMatchTimeout--) | Ottiene o imposta il timeout per le operazioni Regex in tutta la libreria. Il valore predefinito è 1000 ms. Valore: Un {@link double} che rappresenta la durata predefinita del timeout. |
| [setMatchTimeout](#setMatchTimeout-int-) | Ottiene o imposta il timeout per le operazioni Regex in tutta la libreria. Il valore predefinito è 1000 ms. Valore: |

### RegexManager {#RegexManager--}
```
public RegexManager()
```



### getMatchTimeout {#getMatchTimeout--}
```
public static int getMatchTimeout()
```

Ottiene o imposta il timeout per le operazioni Regex in tutta la libreria. Il valore predefinito è 1000 ms. Valore: Un {@link double} che rappresenta la durata predefinita del timeout.

**Returns:**
valore int

### setMatchTimeout {#setMatchTimeout-int-}
```
public static void setMatchTimeout(int value)
```

Ottiene o imposta il timeout per le operazioni Regex in tutta la libreria. Il valore predefinito è 1000 ms. Valore:

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore |  | Un valore che rappresenta la durata predefinita del timeout in millisecondi. |
