---
title: "WarningType"
linktitle: "WarningType"
second_title: "Riferimento API Aspose.PDF per Java"
description: "/ * Enum rappresenta il tipo di avviso. / * /"
type: docs
weight: 5490
url: /it/java/com.aspose.pdf/warningtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.Enum, com.aspose.pdf.WarningType

```
public final class WarningType extends com.aspose.ms.System.Enum
```

/ * Enum rappresenta il tipo di avviso. / * /

## Campi

| Campo | Descrizione |
| --- | --- |
| [CompatibilityIssue](#CompatibilityIssue) | Problema noto che impedirà l'apertura del documento da parte di alcuni agenti utente, o versioni precedenti degli agenti utente. |
| [DataLoss](#DataLoss) | Il testo/grafico/immagine o altri dati sono completamente mancanti sia dall'albero del documento dopo il caricamento, sia dal documento creato dopo il salvataggio. |
| [InvalidInputStreamType](#InvalidInputStreamType) | Tipo di flusso di input non valido |
| [MajorFormattingLoss](#MajorFormattingLoss) | Perdite di formattazione importanti rispetto al documento originale. Questo è per i casi in cui la perdita di formattazione è notevole ma i dati sono ancora presenti. |
| [MinorFormattingLoss](#MinorFormattingLoss) | Perdite di formattazione minori rispetto al documento originale. Questo è per perdite di fedeltà minori. |
| [SourceFileCorruption](#SourceFileCorruption) | Il file è corrotto. |
| [UnexpectedContent](#UnexpectedContent) | Il file contiene contenuto inaspettato. |

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

Problema noto che impedirà l'apertura del documento da parte di alcuni agenti utente, o versioni precedenti degli agenti utente.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

Il testo/grafico/immagine o altri dati sono completamente mancanti sia dall'albero del documento dopo il caricamento, sia dal documento creato dopo il salvataggio.

### InvalidInputStreamType {#InvalidInputStreamType}
```
public static final int InvalidInputStreamType
```

Tipo di flusso di input non valido

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

Perdite di formattazione importanti rispetto al documento originale. Questo è per i casi in cui la perdita di formattazione è notevole ma i dati sono ancora presenti.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

Perdite di formattazione minori rispetto al documento originale. Questo è per perdite di fedeltà minori.

### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

Il file è corrotto.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

Il file contiene contenuto inaspettato.
