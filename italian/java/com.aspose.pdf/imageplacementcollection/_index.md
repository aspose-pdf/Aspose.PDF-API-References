---
title: "ImagePlacementCollection"
linktitle: "ImagePlacementCollection"
second_title: "Riferimento API Aspose.PDF per Java"
description: "Rappresenta una collezione di posizionamenti di immagini."
type: docs
weight: 2350
url: /it/java/com.aspose.pdf/imageplacementcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ImagePlacementCollection

**All Implemented Interfaces:**
Iterable < ImagePlacement >

```
public final class ImagePlacementCollection extends Object implements Iterable < ImagePlacement >
```

Rappresenta una collezione di posizionamenti di immagini.

## Metodi

| Metodo | Descrizione |
| --- | --- |
| [add](#add-com.aspose.pdf.ImagePlacement-) | Aggiunge l'elemento text fragment all'indice specificato. |
| [clear](#clear--) | Cancella tutti gli elementi dalla collezione. |
| [contains](#contains-com.aspose.pdf.ImagePlacement-) | Determina se la collezione contiene un valore specifico. |
| [copyTo](#copyTo-com.aspose.pdf.ImagePlacement:A-int-) | Copia l'intera collezione in un Array monodimensionale compatibile, iniziando dall'indice specificato dell'array di destinazione. |
| [get_Item](#get_Item-int-) | Restituisce l'elemento frammento di testo all'indice specificato. |
| [getSyncRoot](#getSyncRoot--) | Restituisce un oggetto che può essere usato per sincronizzare l'accesso alla collezione. |
| [isReadOnly](#isReadOnly--) | Ottiene un valore che indica se la collezione è di sola lettura. |
| [isSynchronized](#isSynchronized--) | Restituisce un valore che indica se l'accesso alla collezione è sincronizzato (thread safe). |
| [iterator](#iterator--) | Restituisce un enumeratore per l'intera collezione. |
| [remove](#remove-com.aspose.pdf.ImagePlacement-) | Elimina l'elemento specificato dalla collezione |
| [size](#size--) | Restituisce il numero di elementi oggetto {@code ImagePlacement} effettivamente contenuti nella collezione. |

### add {#add-com.aspose.pdf.ImagePlacement-}
Aggiunge l'elemento text fragment all'indice specificato.

### clear {#clear--}
```
public void clear()
```

Cancella tutti gli elementi dalla collezione.

### contains {#contains-com.aspose.pdf.ImagePlacement-}
Determina se la collezione contiene un valore specifico.

### copyTo {#copyTo-com.aspose.pdf.ImagePlacement:A-int-}
Copia l'intera collezione in un Array monodimensionale compatibile, iniziando dall'indice specificato dell'array di destinazione.

### get_Item {#get_Item-int-}
```
public ImagePlacement get_Item(int index)
```

Restituisce l'elemento frammento di testo all'indice specificato.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| index |  | Indice del posizionamento immagine. |

**Returns:**
Oggetto ImagePlacement.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Restituisce un oggetto che può essere usato per sincronizzare l'accesso alla collezione.

**Returns:**
Oggetto per la sincronizzazione

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Ottiene un valore che indica se la collezione è di sola lettura.

**Returns:**
valore booleano

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Restituisce un valore che indica se l'accesso alla collezione è sincronizzato (thread safe).

**Returns:**
valore booleano

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator< ImagePlacement > iterator()
```

Restituisce un enumeratore per l'intera collezione.

**Returns:**
Oggetto enumeratore.

### remove {#remove-com.aspose.pdf.ImagePlacement-}
Elimina l'elemento specificato dalla collezione

### size {#size--}
```
public int size()
```

Restituisce il numero di elementi oggetto {@code ImagePlacement} effettivamente contenuti nella collezione.

**Returns:**
valore int
