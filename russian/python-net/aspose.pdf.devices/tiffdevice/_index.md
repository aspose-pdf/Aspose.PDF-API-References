---
title: "TiffDevice"
second_title: "Aspose.PDF for Python via .NET справочник API"
description: "Этот класс помогает сохранять страницы PDF‑документа по одной в одно TIFF‑изображение."
type: docs
weight: 150
url: /ru/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Этот класс помогает сохранять страницы PDF‑документа по одной в одно TIFF‑изображение.

Тип TiffDevice раскрывает следующие члены:
## Конструкторы
| Имя | Описание |
| :- | :- |
| TiffDevice(resolution) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(resolution, settings) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(resolution, settings, converter) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(settings) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(settings, converter) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice() | Инициализирует новый экземпляр класса [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) с настройками по умолчанию. |
| TiffDevice(width, height, resolution, settings) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(page_size, resolution, settings) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(width, height, resolution) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(page_size, resolution) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(width, height, settings) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(width, height, settings, converter) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(page_size, settings, converter) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(page_size, settings) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(width, height) | Инициализирует новый экземпляр класса TiffDevice |
| TiffDevice(page_size) | Инициализирует новый экземпляр класса TiffDevice |
## Свойства
| Имя | Описание |
| :- | :- |
| rendering_options | Получает или задает параметры рендеринга. |
| form_presentation_mode | Получает или задает режим представления формы. |
| настройки | Получает настройки для отображения pdf в tiff‑изображение. |
| resolution | Получает разрешение изображения. |
| ширина | Получает ширину выходного изображения. |
| высота | Получает высоту выходного изображения. |
## Методы
| Имя | Описание |
| :- | :- |
| process(document, from_page, to_page, output) | Преобразует определённые страницы документа в tiff и сохраняет их в выходном потоке. |
| process(document, output) | Преобразует определённые страницы документа в tiff и сохраняет их в выходном потоке. |
| process(document, output_file_name) | Преобразует определённые страницы документа в tiff и сохраняет их в выходном потоке. |
| process(document, from_page, to_page, output_file_name) | Преобразует определённые страницы документа в tiff и сохраняет их в выходном потоке. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Выполняет бинаризацию Брэдли для входного потока. |

### См. также

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

