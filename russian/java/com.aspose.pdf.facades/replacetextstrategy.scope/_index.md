---
title: "ReplaceTextStrategy.Scope"
linktitle: "ReplaceTextStrategy.Scope"
second_title: "Справочник API Aspose.PDF для Java"
description: "Область, в которой применяется операция замены текста, по умолчанию REPLACE_FIRST."
type: docs
weight: 670
url: /ru/java/com.aspose.pdf.facades/replacetextstrategy.scope/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReplaceTextStrategy.Scope > com.aspose.pdf.facades.ReplaceTextStrategy.Scope, java.lang.Enum < ReplaceTextStrategy.Scope >, com.aspose.pdf.facades.ReplaceTextStrategy.Scope

**All Implemented Interfaces:**
Serializable, Comparable < ReplaceTextStrategy.Scope >

```
public static enum ReplaceTextStrategy.Scope extends Enum < ReplaceTextStrategy.Scope >
```

Область, в которой применяется операция замены текста, по умолчанию REPLACE_FIRST.

## Поля

| Поле | Описание |
| --- | --- |
| [ReplaceAll](#ReplaceAll) | Заменить все вхождения текста на всех затронутых страницах. |
| [ReplaceFirst](#ReplaceFirst) | Заменить только первое вхождение текста на каждой из затронутых страниц. |

## Методы

| Метод | Описание |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Возвращает константу перечисления этого типа с указанным именем. |
| [values](#values--) | Возвращает массив, содержащий константы этого типа перечисления в порядке их объявления. |

### ReplaceAll {#ReplaceAll}
```
public static final ReplaceTextStrategy.Scope ReplaceAll
```

Заменить все вхождения текста на всех затронутых страницах.

### ReplaceFirst {#ReplaceFirst}
```
public static final ReplaceTextStrategy.Scope ReplaceFirst
```

Заменить только первое вхождение текста на каждой из затронутых страниц.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Возвращает константу перечисления этого типа с указанным именем.

### values {#values--}
```
public static ReplaceTextStrategy.Scope [] values()
```

Возвращает массив, содержащий константы этого типа перечисления в порядке их объявления.

**Returns:**
массив, содержащий константы этого типа перечисления в порядке их объявления
