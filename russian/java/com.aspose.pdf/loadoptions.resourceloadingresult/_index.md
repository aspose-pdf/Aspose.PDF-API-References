---
title: "LoadOptions.ResourceLoadingResult"
linktitle: "LoadOptions.ResourceLoadingResult"
second_title: "Справочник API Aspose.PDF для Java"
description: "Результат пользовательской загрузки ресурса"
type: docs
weight: 2820
url: /ru/java/com.aspose.pdf/loadoptions.resourceloadingresult/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.LoadOptions.ResourceLoadingResult

```
public static class LoadOptions.ResourceLoadingResult extends Object
```

Результат пользовательской загрузки ресурса

## Конструкторы

| Конструктор | Описание |
| --- | --- |
| [ResourceLoadingResult](#ResourceLoadingResult-byte:A-) | Создаёт экземпляр результата загрузки |

## Методы

| Метод | Описание |
| --- | --- |
| [getData](#getData--) | Бинарные данные, загруженные пользовательским загрузчиком, — их необходимо установить после загрузки |
| [getEncodingIfKnown](#getEncodingIfKnown--) | Иногда кодировка ресурса известна после или во время загрузки. В таком случае пользовательский код может передать конвертеру эту информацию через данный параметр. Вы можете оставить null в этом параметре, если кодировка неизвестна или не имеет значения. |
| [getExceptionOfLoadingIfAny](#getExceptionOfLoadingIfAny--) | Иногда невозможно загрузить запрошенный ресурс по какой‑то причине. Недоступность ресурса часто не приводит к сбою конвертации, и итоговый документ всё равно может быть создан (хотя, возможно, в несколько худшем качестве, без изображений и т.д.). Если во время загрузки возникло исключение, просто перехватите его и поместите в этот параметр — иногда эта информация полезна конвертеру при формировании результата. |
| [getMIMETypeIfKnown](#getMIMETypeIfKnown--) | Иногда информация о MIME‑типе загруженного ресурса полезна конвертеру. Вы можете указать MIME‑тип (если он известен после загрузки) в этом параметре. Пожалуйста, оставьте параметр равным null, когда MIME‑тип неизвестен или его не требуется указывать. |
| [isLoadingCancelled](#isLoadingCancelled--) | Иногда по некоторым причинам загрузка не должна происходить в пользовательском коде. В таком случае установите этот флаг в значение True. Тогда конвертер попытается использовать внутренний загрузчик ресурсов по умолчанию, чтобы получить результат (как это происходит, когда пользовательская стратегия не указана). |
| [setEncodingIfKnown](#setEncodingIfKnown-java.nio.charset.Charset-) | Иногда кодировка ресурса известна после или во время загрузки. В таком случае пользовательский код может передать конвертеру эту информацию через данный параметр. Вы можете оставить null в этом параметре, если кодировка неизвестна или не имеет значения. |
| [setExceptionOfLoadingIfAny](#setExceptionOfLoadingIfAny-com.aspose.ms.System.Exception-) | Иногда невозможно загрузить запрошенный ресурс по какой‑то причине. |
| [setLoadingCancelled](#setLoadingCancelled-boolean-) | Иногда по некоторым причинам загрузка не должна происходить в пользовательском коде. В таком случае установите этот флаг в значение True. Тогда конвертер попытается использовать внутренний загрузчик ресурсов по умолчанию, чтобы получить результат (как это происходит, когда пользовательская стратегия не указана). |
| [setMIMETypeIfKnown](#setMIMETypeIfKnown-java.lang.String-) | Иногда информация о MIME‑типе загруженного ресурса полезна конвертеру. Вы можете указать MIME‑тип (если он известен после загрузки) в этом параметре. Пожалуйста, оставьте параметр равным null, когда MIME‑тип неизвестен или его не требуется указывать. |

### ResourceLoadingResult {#ResourceLoadingResult-byte:A-}
```
public ResourceLoadingResult(byte[] data)
```

Создаёт экземпляр результата загрузки

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| данные |  | результат пользовательской загрузки должен всегда предоставляться, он может быть массивом нулевой длины, если получить какой‑либо результат невозможно |

### getData {#getData--}
```
public byte[] getData()
```

Бинарные данные, загруженные пользовательским загрузчиком, — их необходимо установить после загрузки

**Returns:**
массив байтовых значений

### getEncodingIfKnown {#getEncodingIfKnown--}
```
public Charset getEncodingIfKnown()
```

Иногда кодировка ресурса известна после или во время загрузки. В таком случае пользовательский код может передать конвертеру эту информацию через данный параметр. Вы можете оставить null в этом параметре, если кодировка неизвестна или не имеет значения.

**Returns:**
Экземпляр Charset

### getExceptionOfLoadingIfAny {#getExceptionOfLoadingIfAny--}
```
public com.aspose.ms.System.Exception getExceptionOfLoadingIfAny()
```

Иногда невозможно загрузить запрошенный ресурс по какой‑то причине. Недоступность ресурса часто не приводит к сбою конвертации, и итоговый документ всё равно может быть создан (хотя, возможно, в несколько худшем качестве, без изображений и т.д.). Если во время загрузки возникло исключение, просто перехватите его и поместите в этот параметр — иногда эта информация полезна конвертеру при формировании результата.

**Returns:**
Исключение

### getMIMETypeIfKnown {#getMIMETypeIfKnown--}
```
public String getMIMETypeIfKnown()
```

Иногда информация о MIME‑типе загруженного ресурса полезна конвертеру. Вы можете указать MIME‑тип (если он известен после загрузки) в этом параметре. Пожалуйста, оставьте параметр равным null, когда MIME‑тип неизвестен или его не требуется указывать.

**Returns:**
строковое значение

### isLoadingCancelled {#isLoadingCancelled--}
```
public boolean isLoadingCancelled()
```

Иногда по некоторым причинам загрузка не должна происходить в пользовательском коде. В таком случае установите этот флаг в значение True. Тогда конвертер попытается использовать внутренний загрузчик ресурсов по умолчанию, чтобы получить результат (как это происходит, когда пользовательская стратегия не указана).

**Returns:**
логическое значение

### setEncodingIfKnown {#setEncodingIfKnown-java.nio.charset.Charset-}
Иногда кодировка ресурса известна после или во время загрузки. В таком случае пользовательский код может передать конвертеру эту информацию через данный параметр. Вы можете оставить null в этом параметре, если кодировка неизвестна или не имеет значения.

### setExceptionOfLoadingIfAny {#setExceptionOfLoadingIfAny-com.aspose.ms.System.Exception-}
Иногда невозможно загрузить запрошенный ресурс по какой‑то причине.

### setLoadingCancelled {#setLoadingCancelled-boolean-}
```
public void setLoadingCancelled(boolean loadingCancelled)
```

Иногда по некоторым причинам загрузка не должна происходить в пользовательском коде. В таком случае установите этот флаг в значение True. Тогда конвертер попытается использовать внутренний загрузчик ресурсов по умолчанию, чтобы получить результат (как это происходит, когда пользовательская стратегия не указана).

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| loadingCancelled |  | логическое значение |

### setMIMETypeIfKnown {#setMIMETypeIfKnown-java.lang.String-}
Иногда информация о MIME‑типе загруженного ресурса полезна конвертеру. Вы можете указать MIME‑тип (если он известен после загрузки) в этом параметре. Пожалуйста, оставьте параметр равным null, когда MIME‑тип неизвестен или его не требуется указывать.
