---
title: "CollectionFieldSubtype"
linktitle: "CollectionFieldSubtype"
second_title: "Справочник API Aspose.PDF для Java"
description: "Представляет параметр подтипа поля в коллекции схем."
type: docs
weight: 630
url: /ru/java/com.aspose.pdf/collectionfieldsubtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.Enum, com.aspose.pdf.CollectionFieldSubtype

```
public final class CollectionFieldSubtype extends com.aspose.ms.System.Enum
```

Представляет параметр подтипа поля в коллекции схем.

## Поля

| Поле | Описание |
| --- | --- |
| [CompressedSize](#CompressedSize) | (PDF 2.0) Данные поля представляют длину встроенного файлового потока, определяемую записью Length в словаре встроенного файлового потока, и оба значения должны быть одинаковыми. |
| [CreationDate](#CreationDate) | Данные поля должны быть датой создания встроенного файлового потока, определяемой записью CreationDate во встроенном файле. |
| [D](#D) | Тип даты. Данные поля должны храниться как строка даты PDF. |
| [Desc](#Desc) | Данные поля должны быть именем файла встроенного файлового потока, определяемым записью UF в спецификации файла, если она присутствует; иначе — записью F в спецификации файла. |
| [F](#F) | Данные поля должны быть именем файла встроенного файлового потока, определяемым записью UF в спецификации файла, если она присутствует; иначе — записью F в спецификации файла. |
| [ModDate](#ModDate) | Данные поля должны быть датой изменения встроенного файлового потока, определяемой записью ModDate в словаре параметров встроенного файла. |
| [N](#N) | Тип числа. Данные поля должны храниться как число PDF. |
| [None](#None) | Подтип не определён. |
| [S](#S) | Тип текста. Данные поля должны храниться как текстовая строка PDF. |
| [Size](#Size) | Данные поля должны быть размером встроенного файла, определяемым записью Size в словаре параметров встроенного файла. |

### CompressedSize {#CompressedSize}
```
public static final int CompressedSize
```

(PDF 2.0) Данные поля представляют длину встроенного файлового потока, определяемую записью Length в словаре встроенного файлового потока, и оба значения должны быть одинаковыми.

### CreationDate {#CreationDate}
```
public static final int CreationDate
```

Данные поля должны быть датой создания встроенного файлового потока, определяемой записью CreationDate во встроенном файле.

### D {#D}
```
public static final int D
```

Тип даты. Данные поля должны храниться как строка даты PDF.

### Desc {#Desc}
```
public static final int Desc
```

Данные поля должны быть именем файла встроенного файлового потока, определяемым записью UF в спецификации файла, если она присутствует; иначе — записью F в спецификации файла.

### F {#F}
```
public static final int F
```

Данные поля должны быть именем файла встроенного файлового потока, определяемым записью UF в спецификации файла, если она присутствует; иначе — записью F в спецификации файла.

### ModDate {#ModDate}
```
public static final int ModDate
```

Данные поля должны быть датой изменения встроенного файлового потока, определяемой записью ModDate в словаре параметров встроенного файла.

### N {#N}
```
public static final int N
```

Тип числа. Данные поля должны храниться как число PDF.

### None {#None}
```
public static final int None
```

Подтип не определён.

### S {#S}
```
public static final int S
```

Тип текста. Данные поля должны храниться как текстовая строка PDF.

### Size {#Size}
```
public static final int Size
```

Данные поля должны быть размером встроенного файла, определяемым записью Size в словаре параметров встроенного файла.
