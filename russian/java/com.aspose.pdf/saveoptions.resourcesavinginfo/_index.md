---
title: "SaveOptions.ResourceSavingInfo"
linktitle: "SaveOptions.ResourceSavingInfo"
second_title: "Справочник API Aspose.PDF для Java"
description: "Этот класс представляет набор данных, связанных с сохранением внешнего ресурсного файла, которое происходит при конвертации PDF в другой формат (например, HTML)"
type: docs
weight: 4440
url: /ru/java/com.aspose.pdf/saveoptions.resourcesavinginfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.SaveOptions.ResourceSavingInfo

```
public static class SaveOptions.ResourceSavingInfo extends Object
```

Этот класс представляет набор данных, связанных с сохранением внешнего ресурсного файла, которое происходит при конвертации PDF в другой формат (например, HTML)

## Методы

| Метод | Описание |
| --- | --- |
| [getContentStream](#getContentStream--) | Устанавливается конвертером. Представляет двоичное содержимое сохранённого файла. |
| [getResourceType](#getResourceType--) | Устанавливается конвертером. Предполагаемое имя файла, которое передаётся от конвертера к коду пользовательского метода. Может использоваться в пользовательском коде для решения, как обрабатывать или куда сохранять этот файл. |
| [getSupposedFileName](#getSupposedFileName--) | Устанавливается конвертером. Предполагаемое имя файла, которое передаётся от конвертера к коду пользовательского метода. Может использоваться в пользовательском коде для решения, как обрабатывать или куда сохранять этот файл. |
| [isCustomProcessingCancelled](#isCustomProcessingCancelled--) | Этот флаг должен быть установлен в \"true\" в пользовательском коде, если по каким‑то причинам предлагаемый файл должен обрабатываться не пользовательским кодом, а кодом конвертера в стандартном для конвертера режиме. Таким образом, установка флага в true означает, что пользовательский код не обработал указанный файл, и конвертер должен обработать его сам (и при сохранении где‑то, и при назначении имени в ссылочном файле). |
| [setCustomProcessingCancelled](#setCustomProcessingCancelled-boolean-) | Этот флаг должен быть установлен в \"true\" в пользовательском коде, если по каким‑то причинам предлагаемый файл должен обрабатываться не пользовательским кодом, а кодом конвертера в стандартном для конвертера режиме. Таким образом, установка флага в true означает, что пользовательский код не обработал указанный файл, и конвертер должен обработать его сам (и при сохранении где‑то, и при назначении имени в ссылочном файле). |

### getContentStream {#getContentStream--}
```
public byte[] getContentStream()
```

Устанавливается конвертером. Представляет двоичное содержимое сохранённого файла.

**Returns:**
массив байтов

### getResourceType {#getResourceType--}
```
public SaveOptions.NodeLevelResourceType getResourceType()
```

Устанавливается конвертером. Предполагаемое имя файла, которое передаётся от конвертера к коду пользовательского метода. Может использоваться в пользовательском коде для решения, как обрабатывать или куда сохранять этот файл.

**Returns:**
Элемент NodeLevelResourceType @see NodeLevelResourceType

### getSupposedFileName {#getSupposedFileName--}
```
public String getSupposedFileName()
```

Устанавливается конвертером. Предполагаемое имя файла, которое передаётся от конвертера к коду пользовательского метода. Может использоваться в пользовательском коде для решения, как обрабатывать или куда сохранять этот файл.

**Returns:**
строковое значение

### isCustomProcessingCancelled {#isCustomProcessingCancelled--}
```
public boolean isCustomProcessingCancelled()
```

Этот флаг должен быть установлен в \"true\" в пользовательском коде, если по каким‑то причинам предлагаемый файл должен обрабатываться не пользовательским кодом, а кодом конвертера в стандартном для конвертера режиме. Таким образом, установка флага в true означает, что пользовательский код не обработал указанный файл, и конвертер должен обработать его сам (и при сохранении где‑то, и при назначении имени в ссылочном файле).

**Returns:**
логическое значение

### setCustomProcessingCancelled {#setCustomProcessingCancelled-boolean-}
```
public void setCustomProcessingCancelled(boolean customProcessingCancelled)
```

Этот флаг должен быть установлен в \"true\" в пользовательском коде, если по каким‑то причинам предлагаемый файл должен обрабатываться не пользовательским кодом, а кодом конвертера в стандартном для конвертера режиме. Таким образом, установка флага в true означает, что пользовательский код не обработал указанный файл, и конвертер должен обработать его сам (и при сохранении где‑то, и при назначении имени в ссылочном файле).

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| customProcessingCancelled |  | логическое значение |
