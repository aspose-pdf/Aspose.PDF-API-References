---
title: "SaveOptions.MarginPartStyle"
linktitle: "SaveOptions.MarginPartStyle"
second_title: "Справочник API Aspose.PDF для Java"
description: "Представляет информацию об одной части поля (верх, низ, левая сторона или правая сторона)"
type: docs
weight: 4420
url: /ru/java/com.aspose.pdf/saveoptions.marginpartstyle/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.SaveOptions.MarginPartStyle

```
public static class SaveOptions.MarginPartStyle extends Object
```

Представляет информацию об одной части поля (верх, низ, левая сторона или правая сторона)

## Конструкторы

| Конструктор | Описание |
| --- | --- |
| [MarginPartStyle](#MarginPartStyle-boolean-) | Создает экземпляр класса MarginPartStyle и инициализирует его значение в пунктах |
| [MarginPartStyle](#MarginPartStyle-int-) | Создает экземпляр класса MarginPartStyle и задает его значение в пунктах |

## Методы

| Метод | Описание |
| --- | --- |
| [getValueInPoints](#getValueInPoints--) | Представляет поле в пунктах. Должно быть число больше нуля. |
| [isAuto](#isAuto--) | Получает или задает значение, указывающее, является ли этот экземпляр автоматическим. Значение: {@code true}, если экземпляр автоматический; иначе {@code false}. |
| [setAuto](#setAuto-boolean-) | Получает или задает значение, указывающее, является ли этот экземпляр автоматическим. Значение: {@code true}, если экземпляр автоматический; иначе {@code false}. |
| [setValueInPoints](#setValueInPoints-int-) | Представляет поле в пунктах. Должно быть число больше нуля. |

### MarginPartStyle {#MarginPartStyle-boolean-}
```
public MarginPartStyle(boolean isAuto)
```

Создает экземпляр класса MarginPartStyle и инициализирует его значение в пунктах

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| isAuto |  | Установить margin auto |

### MarginPartStyle {#MarginPartStyle-int-}
```
public MarginPartStyle(int valueInPoints)
```

Создает экземпляр класса MarginPartStyle и задает его значение в пунктах

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| valueInPoints |  | Целочисленное значение в пунктах |

### getValueInPoints {#getValueInPoints--}
```
public final int getValueInPoints()
```

Представляет поле в пунктах. Должно быть число больше нуля.

**Returns:**
int значение

### isAuto {#isAuto--}
```
public final boolean isAuto()
```

Получает или задает значение, указывающее, является ли этот экземпляр автоматическим. Значение: {@code true}, если экземпляр автоматический; иначе {@code false}.

**Returns:**
логическое значение

### setAuto {#setAuto-boolean-}
```
public final void setAuto(boolean value)
```

Получает или задает значение, указывающее, является ли этот экземпляр автоматическим. Значение: {@code true}, если экземпляр автоматический; иначе {@code false}.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| значение |  | логическое значение |

### setValueInPoints {#setValueInPoints-int-}
```
public final void setValueInPoints(int value)
```

Представляет поле в пунктах. Должно быть число больше нуля.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| значение |  | int значение |
