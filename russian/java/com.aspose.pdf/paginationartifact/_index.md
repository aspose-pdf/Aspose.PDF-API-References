---
title: "PaginationArtifact"
linktitle: "PaginationArtifact"
second_title: "Справочник API Aspose.PDF для Java"
description: "Представляет абстрактный базовый класс для артефактов пагинации в документе."
type: docs
weight: 3460
url: /ru/java/com.aspose.pdf/paginationartifact/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.Artifact com.aspose.pdf.PaginationArtifact, com.aspose.pdf.Artifact, com.aspose.pdf.PaginationArtifact

**All Implemented Interfaces:**
com.aspose.ms.System.IDisposable, Closeable, AutoCloseable

```
public abstract class PaginationArtifact extends Artifact
```

Представляет абстрактный базовый класс для артефактов пагинации в документе.

## Методы

| Метод | Описание |
| --- | --- |
| [getEndPage](#getEndPage--) | Получает или задает номер конечной страницы для артефакта. Значение должно быть больше или равно 0. Если задается значение меньше 0, оно будет скорректировано до 0. Значение по умолчанию 0 означает отсутствие границ конечной страницы. |
| [getStartPage](#getStartPage--) | Получает или задает номер начальной страницы для артефакта. Значение должно быть больше или равно 1. Если задается значение меньше 1, оно будет скорректировано до 1. |
| [getSubset](#getSubset--) | Получает или задает подмножество страниц, к которым применяется артефакт (например, все страницы, четные страницы, нечетные страницы). |
| [setEndPage](#setEndPage-int-) | Получает или задает номер конечной страницы для артефакта. Значение должно быть больше или равно 0. Если задается значение меньше 0, оно будет скорректировано до 0. Значение по умолчанию 0 означает отсутствие границ конечной страницы. |
| [setStartPage](#setStartPage-int-) | Получает или задает номер начальной страницы для артефакта. Значение должно быть больше или равно 1. Если задается значение меньше 1, оно будет скорректировано до 1. |
| [setSubset](#setSubset-int-) | Получает или задает подмножество страниц, к которым применяется артефакт (например, все страницы, четные страницы, нечетные страницы). |

### getEndPage {#getEndPage--}
```
public final int getEndPage()
```

Получает или задает номер конечной страницы для артефакта. Значение должно быть больше или равно 0. Если задается значение меньше 0, оно будет скорректировано до 0. Значение по умолчанию 0 означает отсутствие границ конечной страницы.

**Returns:**
int значение

### getStartPage {#getStartPage--}
```
public final int getStartPage()
```

Получает или задает номер начальной страницы для артефакта. Значение должно быть больше или равно 1. Если задается значение меньше 1, оно будет скорректировано до 1.

**Returns:**
int значение

### getSubset {#getSubset--}
```
public final int getSubset()
```

Получает или задает подмножество страниц, к которым применяется артефакт (например, все страницы, четные страницы, нечетные страницы).

**Returns:**
int значение

### setEndPage {#setEndPage-int-}
```
public final void setEndPage(int value)
```

Получает или задает номер конечной страницы для артефакта. Значение должно быть больше или равно 0. Если задается значение меньше 0, оно будет скорректировано до 0. Значение по умолчанию 0 означает отсутствие границ конечной страницы.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| значение |  | int значение |

### setStartPage {#setStartPage-int-}
```
public final void setStartPage(int value)
```

Получает или задает номер начальной страницы для артефакта. Значение должно быть больше или равно 1. Если задается значение меньше 1, оно будет скорректировано до 1.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| значение |  | int значение |

### setSubset {#setSubset-int-}
```
public final void setSubset(int value)
```

Получает или задает подмножество страниц, к которым применяется артефакт (например, все страницы, четные страницы, нечетные страницы).

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| значение |  | int значение |
