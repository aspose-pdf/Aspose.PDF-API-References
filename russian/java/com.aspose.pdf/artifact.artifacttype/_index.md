---
title: "Artifact.ArtifactType"
linktitle: "Artifact.ArtifactType"
second_title: "Справочник API Aspose.PDF для Java"
description: "Перечисление возможных типов артефактов."
type: docs
weight: 210
url: /ru/java/com.aspose.pdf/artifact.artifacttype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < Artifact.ArtifactType > com.aspose.pdf.Artifact.ArtifactType, java.lang.Enum < Artifact.ArtifactType >, com.aspose.pdf.Artifact.ArtifactType

**All Implemented Interfaces:**
Serializable, Comparable < Artifact.ArtifactType >

```
public static enum Artifact.ArtifactType extends Enum < Artifact.ArtifactType >
```

Перечисление возможных типов артефактов.

## Поля

| Поле | Описание |
| --- | --- |
| [Background](#Background) | Фоновые артефакты. |
| [Layout](#Layout) | Артефакты макета. |
| [Page](#Page) | Артефакты страницы. |
| [Pagination](#Pagination) | Артефакты пагинации. |
| [Undefined](#Undefined) | Тип артефакта не определён или неизвестен. |

## Методы

| Метод | Описание |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Возвращает константу перечисления этого типа с указанным именем. |
| [values](#values--) | Возвращает массив, содержащий константы этого типа перечисления в порядке их объявления. |

### Background {#Background}
```
public static final Artifact.ArtifactType Background
```

Фоновые артефакты.

### Layout {#Layout}
```
public static final Artifact.ArtifactType Layout
```

Артефакты макета.

### Page {#Page}
```
public static final Artifact.ArtifactType Page
```

Артефакты страницы.

### Pagination {#Pagination}
```
public static final Artifact.ArtifactType Pagination
```

Артефакты пагинации.

### Undefined {#Undefined}
```
public static final Artifact.ArtifactType Undefined
```

Тип артефакта не определён или неизвестен.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Возвращает константу перечисления этого типа с указанным именем.

### values {#values--}
```
public static Artifact.ArtifactType [] values()
```

Возвращает массив, содержащий константы этого типа перечисления в порядке их объявления.

**Returns:**
массив, содержащий константы этого типа перечисления в порядке их объявления
