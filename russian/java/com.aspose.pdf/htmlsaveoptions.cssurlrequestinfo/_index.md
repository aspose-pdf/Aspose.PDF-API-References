---
title: "HtmlSaveOptions.CssUrlRequestInfo"
linktitle: "HtmlSaveOptions.CssUrlRequestInfo"
second_title: "Справочник API Aspose.PDF для Java"
description: "Представляет набор данных, связанных с запросом от конвертера к пользовательскому коду, направленным на получение желаемого URL (или шаблона URL) CSS‑файла."
type: docs
weight: 2040
url: /ru/java/com.aspose.pdf/htmlsaveoptions.cssurlrequestinfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.HtmlSaveOptions.CssUrlRequestInfo

```
public static class HtmlSaveOptions.CssUrlRequestInfo extends Object
```

Представляет набор данных, связанных с запросом от конвертера к пользовательскому коду, направленным на получение желаемого URL (или шаблона URL) CSS‑файла.

## Конструкторы

| Конструктор | Описание |
| --- | --- |
| [CssUrlRequestInfo](#CssUrlRequestInfo--) | Создаёт экземпляр CssUrlRequestInfo. |

## Методы

| Метод | Описание |
| --- | --- |
| [isCustomProcessingCancelled](#isCustomProcessingCancelled--) | Должно устанавливаться пользовательским кодом, если он не может или не должен определять URL, который будет использоваться в сгенерированном HTML для ссылки на этот CSS. Если значение 'true', то CSS‑файл будет сохранён стандартным способом в стандартном месте. |
| [setCustomProcessingCancelled](#setCustomProcessingCancelled-boolean-) | Должно устанавливаться пользовательским кодом, если он не может или не должен определять URL, который будет использоваться в сгенерированном HTML для ссылки на этот CSS. Если значение 'true', то CSS‑файл будет сохранён стандартным способом в стандартном месте. |

### CssUrlRequestInfo {#CssUrlRequestInfo--}
```
public CssUrlRequestInfo()
```

Создаёт экземпляр CssUrlRequestInfo.

### isCustomProcessingCancelled {#isCustomProcessingCancelled--}
```
public boolean isCustomProcessingCancelled()
```

Должно устанавливаться пользовательским кодом, если он не может или не должен определять URL, который будет использоваться в сгенерированном HTML для ссылки на этот CSS. Если значение 'true', то CSS‑файл будет сохранён стандартным способом в стандартном месте.

**Returns:**
логическое значение

### setCustomProcessingCancelled {#setCustomProcessingCancelled-boolean-}
```
public void setCustomProcessingCancelled(boolean customProcessingCancelled)
```

Должно устанавливаться пользовательским кодом, если он не может или не должен определять URL, который будет использоваться в сгенерированном HTML для ссылки на этот CSS. Если значение 'true', то CSS‑файл будет сохранён стандартным способом в стандартном месте.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| customProcessingCancelled |  | логическое значение |
