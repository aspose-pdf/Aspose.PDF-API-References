---
title: "CollectionField"
linktitle: "CollectionField"
second_title: "Справочник API Aspose.PDF для Java"
description: "Представляет класс поля схемы коллекции документов."
type: docs
weight: 620
url: /ru/java/com.aspose.pdf/collectionfield/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.CollectionField

```
public class CollectionField extends Object
```

Представляет класс поля схемы коллекции документов.

## Методы

| Метод | Описание |
| --- | --- |
| [getE](#getE--) | Возвращает флаг, указывающий, должен ли интерактивный PDF‑процессор предоставлять поддержку редактирования значения поля. Значение по умолчанию: false. |
| [getFiledType](#getFiledType--) | Возвращает тип значения поля в схеме коллекции. Это поле описывает тип значения, соответствующий {@code Subtype}({@link #getSubtype}/{@link #setSubtype(int)}). |
| [getN](#getN--) | Возвращает текстовое имя поля, которое будет отображено пользователю интерактивным PDF‑процессором. |
| [getO](#getO--) | Возвращает относительный порядок имени поля в пользовательском интерфейсе. Поля будут отсортированы интерактивным PDF‑процессором в порядке возрастания. |
| [getSubtype](#getSubtype--) | Возвращает подтип значения поля в схеме коллекции. Подтип поля коллекции или связанного с файлом поля, описываемый этим словарём. Эта запись определяет тип данных, который будет храниться в поле. |
| [getV](#getV--) | Возвращает начальную видимость поля в пользовательском интерфейсе. Значение по умолчанию: true. |

### getE {#getE--}
```
public final boolean getE()
```

Возвращает флаг, указывающий, должен ли интерактивный PDF‑процессор предоставлять поддержку редактирования значения поля. Значение по умолчанию: false.

**Returns:**
логическое значение

### getFiledType {#getFiledType--}
```
public final int getFiledType()
```

Возвращает тип значения поля в схеме коллекции. Это поле описывает тип значения, соответствующий {@code Subtype}({@link #getSubtype}/{@link #setSubtype(int)}).

**Returns:**
FieldValueType element

### getN {#getN--}
```
public final String getN()
```

Возвращает текстовое имя поля, которое будет отображено пользователю интерактивным PDF‑процессором.

**Returns:**
строковое значение

### getO {#getO--}
```
public final Integer [] getO()
```

Возвращает относительный порядок имени поля в пользовательском интерфейсе. Поля будут отсортированы интерактивным PDF‑процессором в порядке возрастания.

**Returns:**
массив Integer

### getSubtype {#getSubtype--}
```
public final int getSubtype()
```

Возвращает подтип значения поля в схеме коллекции. Подтип поля коллекции или связанного с файлом поля, описываемый этим словарём. Эта запись определяет тип данных, который будет храниться в поле.

**Returns:**
CollectionFieldSubtype element

### getV {#getV--}
```
public final boolean getV()
```

Возвращает начальную видимость поля в пользовательском интерфейсе. Значение по умолчанию: true.

**Returns:**
логическое значение
