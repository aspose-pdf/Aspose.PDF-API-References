---
title: "System::Threading::Interlocked класс"
linktitle: "Interlocked"
second_title: "Справочник API Aspose.PDF для C++"
description: "Класс System::Threading::Interlocked. Предоставляет API для потокобезопасных операций. Это статический тип без сервисов экземпляра. Вы никогда не должны создавать его экземпляры каким-либо способом в C++."
type: docs
weight: 600
url: /ru/cpp/system.threading/interlocked/
---
## Interlocked class


Предоставляет API для потокобезопасных операций. Это статический тип без сервисов экземпляра. Вы никогда не должны создавать его экземпляры каким-либо способом.

```cpp
class Interlocked
```

## Методы

| Метод | Описание |
| --- | --- |
| static [Add](./add/)(int32_t\&, int32_t) | Увеличивает значение атомарно. |
| static [Add](./add/)(int64_t\&, int64_t) | Увеличивает значение атомарно. |
| static [CompareExchange](./compareexchange/)(T\&, T, T) | Сравнивает и меняет значение переменной: проверяет, равно ли переменная определённому значению, и сохраняет новое значение только если текущее значение соответствует ожидаемому. |
| static [CompareExchange](./compareexchange/)(T\&, T, T) | Сравнивает и меняет значение переменной: проверяет, равно ли переменная определённому значению, и сохраняет новое значение только если текущее значение соответствует ожидаемому. Не реализовано. |
| static [CompareExchange](./compareexchange/)(int32_t\&, int32_t, int32_t, bool\&) | Сравнивает и меняет значение переменной: проверяет, равно ли переменная определённому значению, и сохраняет новое значение только если текущее значение соответствует ожидаемому. |
| static [Decrement](./decrement/)(int32_t\&) | Уменьшает значение атомарно. |
| static [Decrement](./decrement/)(int64_t\&) | Уменьшает значение атомарно. |
| static [Exchange](./exchange/)(T\&, T) | Обменивает значение переменной: сохраняет новое значение и возвращает значение переменной, которое было непосредственно перед сохранением. |
| static [Exchange](./exchange/)(T\&, T) | Обменивает значение переменной: сохраняет новое значение и возвращает значение переменной, которое было непосредственно перед сохранением. Не реализовано. |
| static [ExchangeAdd](./exchangeadd/)(int32_t\&, int32_t) | Увеличивает значение атомарно с помощью процедуры exchange-add. |
| static [ExchangeAdd](./exchangeadd/)(int64_t\&, int64_t) | Увеличивает значение атомарно с помощью процедуры exchange-add. |
| static [Increment](./increment/)(int32_t\&) | Инкрементирует значение атомарно. |
| static [Increment](./increment/)(int64_t\&) | Инкрементирует значение атомарно. |
| static [Read](./read/)(int64_t\&) | Возвращает 64‑битное значение, загруженное атомарной операцией. |
## См. также

* Namespace [System::Threading](../)
* Library [Aspose.PDF for C++](../../)
