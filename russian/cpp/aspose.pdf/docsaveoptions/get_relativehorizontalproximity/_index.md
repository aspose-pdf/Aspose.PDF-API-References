---
title: "Aspose::Pdf::DocSaveOptions::get_RelativeHorizontalProximity метод"
linktitle: "get_RelativeHorizontalProximity"
second_title: "Справочник API Aspose.PDF для C++"
description: "Aspose::Pdf::DocSaveOptions::get_RelativeHorizontalProximity метод. В PDF слова могут быть внутренне представлены операторами, которые выводят слова, печатая их буквы или слоги независимо. Поэтому для обнаружения слов иногда необходимо выявлять группы независимых символов, которые на самом деле являются словами. Эта настройка определяет ширину пробела между текстовыми элементами (буквами, слогами), который следует рассматривать как расстояние между словами при распознавании слов в исходном PDF. (наличие пустого пространства как минимум этой ширины между буквами означает, что текстовые элементы относятся к разным словам). Нормируется по размеру шрифта — 1.0 означает 100 % предполагаемого размера шрифта слова. ВНИМАНИЕ! Используется только в случаях, когда исходный PDF содержит специфические редкостно используемые шрифты, для которых оптимальное значение нельзя вычислить из шрифта. Таким образом, в подавляющем большинстве случаев этот параметр не меняет результат в документе в C++."
type: docs
weight: 1200
url: /ru/cpp/aspose.pdf/docsaveoptions/get_relativehorizontalproximity/
---
## DocSaveOptions::get_RelativeHorizontalProximity method


В [Pdf](../../) слова могут быть внутренне представлены операторами, которые выводят слова, печатая их буквы или слоги независимо. Поэтому для обнаружения слов иногда необходимо определять группы независимых символов, которые на самом деле являются словами. Эта настройка определяет ширину пробела между текстовыми элементами (буквами, слогами), который следует рассматривать как расстояние между словами при распознавании слов в исходном PDF. (наличие пустого пространства как минимум этой ширины между буквами означает, что текстовые элементы относятся к разным словам). Нормируется относительно размера шрифта — 1.0 означает 100 % предполагаемого размера шрифта слова. ВНИМАНИЕ! Используется только в случаях, когда исходный PDF содержит специфические редкостно используемые шрифты, для которых оптимальное значение нельзя вычислить из шрифта. Таким образом, в подавляющем большинстве случаев этот параметр не меняет результат документа.

```cpp
float Aspose::Pdf::DocSaveOptions::get_RelativeHorizontalProximity() const
```

## См. также

* Class [DocSaveOptions](../)
* Namespace [Aspose::Pdf](../../)
* Library [Aspose.PDF for C++](../../../)
