---
title: "Класс System::Console"
linktitle: "Console"
second_title: "Справочник API Aspose.PDF для C++"
description: "Класс System::Console. Предоставляет методы вывода данных в стандартный поток вывода. Это статический тип без сервисов экземпляра. Вы никогда не должны создавать его экземпляры каким-либо способом в C++."
type: docs
weight: 1500
url: /ru/cpp/system/console/
---
## Console class


Предоставляет методы вывода данных в стандартный поток вывода. Это статический тип без сервисов экземпляров. Вы никогда не должны создавать его экземпляры никаким способом.

```cpp
class Console
```

## Методы

| Метод | Описание |
| --- | --- |
| static [Beep](./beep/)() | НЕ РЕАЛИЗОВАНО. |
| static [get_Error](./get_error/)() | Возвращает shared pointer, указывающий на объект, представляющий стандартный поток ошибок. |
| static [get_In](./get_in/)() | Возвращает shared pointer, указывающий на объект, представляющий стандартный поток ввода. |
| static [get_Out](./get_out/)() | Возвращает shared pointer, указывающий на объект, представляющий стандартный поток вывода. |
| static [Mute](./mute/)(bool) | Отключает или включает стандартный поток вывода. |
| static [ReadKey](./readkey/)() | НЕ РЕАЛИЗОВАНО. |
| static [set_Title](./set_title/)(const String\&) | Устанавливает заголовок окна консоли. |
| static [SetError](./seterror/)(const SharedPtr\<System::IO::TextWriter\>\&) | Назначает указанный объект свойству Error класса. |
| static [SetIn](./setin/)(const SharedPtr\<System::IO::TextReader\>\&) | Устанавливает свойство In в указанный объект TextReader. |
| static [SetOut](./setout/)(const SharedPtr\<System::IO::TextWriter\>\&) | Назначает указанный объект свойству Out класса. |
| static [Write](./write/)(const SharedPtr\<T\>\&) | Выводит строковое представление указанного объекта в стандартный поток вывода. |
| static [Write](./write/)(bool) | Выводит строковое представление значения bool в стандартный поток вывода. |
| static [Write](./write/)(char_t) | Выводит указанное значение символа в стандартный поток вывода. |
| static [Write](./write/)(const ArrayPtr\<char_t\>\&) | Выводит строковое представление указанного массива символов в стандартный поток вывода. |
| static [Write](./write/)(const Decimal\&) | Выводит строковое представление значения [Decimal](../decimal/) в стандартный поток вывода. |
| static [Write](./write/)(double) | Выводит строковое представление значения двойной точности с плавающей запятой в стандартный поток вывода. |
| static [Write](./write/)(float) | Выводит строковое представление значения одинарной точности с плавающей запятой в стандартный поток вывода. |
| static [Write](./write/)(int32_t) | Выводит строковое представление значения 32-битного целого числа в стандартный поток вывода. |
| static [Write](./write/)(int64_t) | Выводит строковое представление значения 64-битного целого числа в стандартный поток вывода. |
| static [Write](./write/)(const String\&) | Выводит указанный строковый объект в стандартный поток вывода. |
| static [Write](./write/)(const char_t *) | Выводит указанную C-строку в стандартный поток вывода. |
| static [Write](./write/)(const TypeInfo\&) | Выводит строковое представление значения [TypeInfo](../typeinfo/) в стандартный поток вывода. |
| static [Write](./write/)(uint32_t) | Выводит строковое представление значения беззнакового 32-битного целого числа в стандартный поток вывода. |
| static [Write](./write/)(uint64_t) | Выводит строковое представление значения беззнакового 64-битного целого числа в стандартный поток вывода. |
| static [Write](./write/)(const ArrayPtr\<char_t\>\&, int32_t, int32_t) | Выводит строковое представление указанного диапазона указанного массива символов в стандартный поток вывода. |
| static [Write](./write/)(const String\&, Args\&&...) | Выводит строковое представление указанных аргументов, отформатированных согласно указанному формату, в стандартный поток вывода. |
| static [Write](./write/)(const char *) |  |
| static [WriteLine](./writeline/)() | Выводит текущий разделитель строк в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const SharedPtr\<T\>\&) | Выводит строковое представление указанного объекта, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(bool) | Выводит строковое представление значения bool, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(char_t) | Выводит указанное значение символа, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const ArrayPtr\<char_t\>\&) | Выводит строковое представление указанного массива символов, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const Decimal\&) | Выводит строковое представление значения [Decimal](../decimal/), за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(double) | Выводит строковое представление значения двойной точности с плавающей запятой, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(float) | Выводит строковое представление значения одинарной точности с плавающей запятой, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(int32_t) | Выводит строковое представление значения 32-битного целого числа, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(int64_t) | Выводит строковое представление значения 64-битного целого числа, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const String\&) | Выводит указанный объект строки, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const char_t *) | Выводит указанную C-строку, за которой следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const TypeInfo\&) | Выводит строковое представление значения [TypeInfo](../typeinfo/), за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(uint32_t) | Выводит строковое представление значения беззнакового 32‑битного целого числа, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(uint64_t) | Выводит строковое представление значения беззнакового 64‑битного целого числа, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const ArrayPtr\<char_t\>\&, int, int) | Выводит строковое представление указанного диапазона указанного массива символов, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const Exception\&) | Выводит строковое представление указанного объекта [Exception](../exception/), за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const String\&, Args\&&...) | Выводит строковое представление указанных аргументов, отформатированных согласно указанному формату, за которым следует текущий разделитель строк, в стандартный поток вывода. |
| static [WriteLine](./writeline/)(const char *) |  |
## Примечания



```cpp
#include "system/console.h"
#include <array>

int main()
{
  using namespace System;

  // Выведите приветственное сообщение.
  Console::WriteLine(u"Hello, world!");

  // Создайте экземпляр класса 'std::array'.
  std::array<int, 5> arr = {1, 2, 3, 4, 5};

  // Выведите элементы массива.
  for (auto el: arr)
  {
    Console::Write(u"{0} ", el);
  }
  Console::WriteLine();

  return 0;
}
/*
This code example produces the following output:
Hello, world!
1 2 3 4 5
*/
```

## См. также

* Namespace [System](../)
* Library [Aspose.PDF for C++](../../)
