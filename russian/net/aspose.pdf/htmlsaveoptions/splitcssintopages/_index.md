---
title: "HtmlSaveOptions.SplitCssIntoPages"
second_title: "Справочник API Aspose.PDF для .NET"
description: "Свойство HtmlSaveOptions. Когда выбран режим мультистраничности, т.е. SplitIntoPages равно true, этот атрибут определяет, следует ли создавать отдельный CSS‑файл для каждой результирующей HTML‑страницы. По умолчанию атрибут имеет значение false, поэтому будет создан один большой общий CSS для всех созданных страниц. Суммарный размер всех CSS, генерируемых в этом режиме (по одному CSS на страницу), обычно гораздо больше, чем размер одного большого CSS‑файла, потому что в первом случае классы CSS дублируются в нескольких файлах для каждой страницы. Поэтому эта настройка целесообразна только тогда, когда вам требуется дальнейшая обработка каждой HTML‑страницы независимо, и размер CSS каждой отдельной страницы является критически важным."
type: docs
weight: 190
url: /ru/net/aspose.pdf/htmlsaveoptions/splitcssintopages/
---
## HtmlSaveOptions.SplitCssIntoPages property

Когда выбран режим мультистраничности (т.е. 'SplitIntoPages' = 'true'), этот атрибут определяет, следует ли создавать отдельный CSS‑файл для каждой результирующей HTML‑страницы. По умолчанию атрибут имеет значение false, поэтому создаётся один большой общий CSS для всех страниц. Суммарный размер всех CSS‑файлов, генерируемых в этом режиме (по одному CSS на страницу), обычно значительно превышает размер одного большого CSS‑файла, потому что в первом случае классы CSS дублируются в нескольких файлах для каждой страницы. Поэтому данную настройку следует использовать только тогда, когда вам необходимо последующее независимое обработка каждой HTML‑страницы, и размер CSS отдельной страницы является критическим фактором.

```csharp
public bool SplitCssIntoPages { get; set; }
```

### См. также

* class [HtmlSaveOptions](../)
* namespace [Aspose.Pdf](../../../aspose.pdf/)
* assembly [Aspose.PDF](../../../)


