---
title: "Document.Optimize"
second_title: "Справочник API Aspose.PDF для .NET"
description: "Document method. Линеаризовать документ, чтобы  открыть первую страницу как можно быстрее  отобразить следующую страницу или перейти по ссылке на следующую страницу как можно быстрее  отображать страницу постепенно по мере поступления, когда данные для страницы передаются по медленному каналу, отображать сначала наиболее полезные данные  позволять взаимодействие пользователя, например переход по ссылке, ещё до того, как вся страница будет получена и отображена. Вызов этого метода фактически не сохраняет документ. Напротив, документ лишь подготавливается к оптимизированной структуре; затем вызовите Save, чтобы получить оптимизированный документ."
type: docs
weight: 770
url: /ru/net/aspose.pdf/document/optimize/
---
## Document.Optimize method

Линеаризовать документ, чтобы - открыть первую страницу как можно быстрее; - отобразить следующую страницу или перейти по ссылке на следующую страницу как можно быстрее; - отображать страницу постепенно по мере поступления данных, когда данные для страницы передаются по медленному каналу (отображать наиболее полезные данные в первую очередь); - позволить взаимодействие пользователя, например переход по ссылке, выполняться даже до того, как вся страница будет получена и отображена. Вызов этого метода фактически не сохраняет документ. Напротив, документ только подготавливается к оптимизированной структуре, затем вызовите Save, чтобы получить оптимизированный документ.

```csharp
public void Optimize()
```

### Примеры

Следующий пример показывает, как оптимизировать PDF‑документ для веба.

```csharp
[C#]
	// Путь к вашему файлу PDF.
	string pdfFilePath = "YOUR_PDF_FILE_PATH";

	// Открыть документ
	using (Document pdfDocument = new Document(pdfFilePath))
	{

	// Оптимизировать для веба
	pdfDocument.Optimize();

	// Сохранить выходной документ
	pdfDocument.Save(pdfFilePath);
	}
```

```csharp
[VB.NET]

    ' The path to your PDF File.
    Dim pdfFilePath As String = "YOUR_PDF_FILE_PATH"
	
    ' Open document
    Using pdfDocument As Document = New Document(pdfFilePath)

        ' Optimize for web
        pdfDocument.Optimize()

        ' Save output document
        pdfDocument.Save(pdfFilePath)
    End Using
```

### См. также

* class [Document](../)
* namespace [Aspose.Pdf](../../../aspose.pdf/)
* assembly [Aspose.PDF](../../../)


