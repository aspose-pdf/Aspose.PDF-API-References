---
title: "AttributeKey"
second_title: "Aspose.PDF för Python via .NET API‑referens"
description: "Representerar standardattributnycklar."
type: docs
weight: 40
url: /sv/python-net/aspose.pdf.logicalstructure/attributekey/
---

## AttributeKey class

Representerar standardattributnycklar.

Typen AttributeKey exponerar följande medlemmar:
## Egenskaper
| Namn | Beskrivning |
| :- | :- |
| key | Hämta attributnyckel. |
| owner | Hämta attributägare. |
| PLACEMENT | Placeringsegenskap (Layout-attributägare). |
| WRITING_MODE | WritingMode-egenskap (Layout-attributägare). |
| BACKGROUND_COLOR | BackgroundColor-egenskap (Layout-attributägare). |
| BORDER_COLOR | BorderColor-egenskap (Layout-attributägare). |
| BORDER_STYLE | BorderStyle-attribut (Layout-attributägare). |
| BORDER_THICKNESS | BorderThickness-attribut (Layout-attributägare). |
| PADDING | Padding-attribut (Layout-attributägare). |
| COLOR | Color-attribut (Layout-attributägare). |
| SPACE_BEFORE | SpaceBefore-attribut (Layout-attributägare). |
| SPACE_AFTER | SpaceAfter-attribut (Layout-attributägare). |
| START_INDENT | StartIndent-attribut (Layout-attributägare). |
| END_INDENT | EndIndent-attribut (Layout-attributägare). |
| TEXT_INDENT | TextIndent-attribut (Layout-attributägare). |
| TEXT_ALIGN | TextAlign-attribut (Layout-attributägare). |
| B_BOX | BBox-attribut (Layout-attributägare). |
| WIDTH | Width-attribut (Layout-attributägare). |
| HEIGHT | Höjd attribut (Layout attribut ägare). |
| BLOCK_ALIGN | BlockAlign attribut (Layout attribut ägare). |
| INLINE_ALIGN | InlineAlign attribut (Layout attribut ägare). |
| T_BORDER_STYLE | TBorderStyle attribut (Layout attribut ägare). |
| T_PADDING | TPadding attribut (Layout attribut ägare). |
| BASELINE_SHIFT | BaselineShift attribut (Layout attribut ägare). |
| LINE_HEIGHT | LineHeight attribut (Layout attribut ägare). |
| TEXT_DECORATION_COLOR | TextDecorationColor attribut (Layout attribut ägare). |
| TEXT_DECORATION_THICKNESS | TextDecorationThickness attribut (Layout attribut ägare). |
| TEXT_DECORATION_TYPE | TextDecorationType attribut (Layout attribut ägare). |
| RUBY_ALIGN | RubyAlign attribut (Layout attribut ägare). |
| RUBY_POSITION | RubyPosition attribut (Layout attribut ägare). |
| GLYPH_ORIENTATION_VERTICAL | GlyphOrientationVertical attribut (Layout attribut ägare). |
| COLUMN_COUNT | ColumnCount-attribut (Layout attributägare). |
| COLUMN_GAP | ColumnGap-attribut (Layout attributägare). |
| COLUMN_WIDTHS | ColumnWidths-attribut (Layout attributägare). |
| LIST_NUMBERING | ListNumbering-attribut (List attributägare). |
| ROLE | Role-attribut (PrintField attributägare). |
| CHECKED | Checked-attribut (PrintField attributägare). |
| DESC | Desc-attribut (PrintField attributägare). |
| ROW_SPAN | RowSpan-attribut (Table attributägare). |
| COL_SPAN | ColSpan-attribut (Table attributägare). |
| HEADERS | Headers-attribut (Table attributägare). |
| SCOPE | Scope-attribut (Table attributägare). |
| SUMMARY | Summary-attribut (Table attributägare). |

### Se även

* namespace [aspose.pdf.logicalstructure](/pdf/python-net/aspose.pdf.logicalstructure/)
* assembly [Aspose.PDF](/pdf/python-net/)

