---
title: "TiffDevice"
second_title: "Aspose.PDF för Python via .NET API‑referens"
description: "Denna klass hjälper till att spara pdf-dokument sida för sida i en enda tiff-bild."
type: docs
weight: 150
url: /sv/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Denna klass hjälper till att spara pdf-dokument sida för sida i en enda tiff-bild.

TiffDevice-typen visar följande medlemmar:
## Konstruktörer
| Namn | Beskrivning |
| :- | :- |
| TiffDevice(resolution) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(resolution, settings) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(resolution, settings, converter) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(settings) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(settings, converter) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice() | Initierar en ny instans av [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) klassen med standardinställningar. |
| TiffDevice(width, height, resolution, settings) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(width, height, resolution, settings, converter) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(page_size, resolution, settings) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(page_size, resolution, settings, converter) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(width, height, resolution) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(page_size, resolution) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(width, height, settings) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(width, height, settings, converter) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(page_size, settings, converter) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(page_size, settings) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(width, height) | Initierar en ny instans av TiffDevice-klassen |
| TiffDevice(page_size) | Initierar en ny instans av TiffDevice-klassen |
## Egenskaper
| Namn | Beskrivning |
| :- | :- |
| rendering_options | Hämtar eller anger renderingsalternativ. |
| form_presentation_mode | Hämtar eller anger formulärets presentationsläge. |
| settings | Hämtar inställningar för att mappa pdf till tiff-bild. |
| resolution | Hämtar bildens upplösning. |
| bredd | Hämtar bildens utskriftsbredd. |
| höjd | Hämtar bildens utskriftshöjd. |
## Metoder
| Namn | Beskrivning |
| :- | :- |
| process(document, from_page, to_page, output) | Konverterar vissa dokumentsidor till tiff och sparar dem i utdataströmmen. |
| process(document, output) | Konverterar vissa dokumentsidor till tiff och sparar dem i utdataströmmen. |
| process(document, output_file_name) | Konverterar vissa dokumentsidor till tiff och sparar dem i utdataströmmen. |
| process(document, from_page, to_page, output_file_name) | Konverterar vissa dokumentsidor till tiff och sparar dem i utdataströmmen. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Utför Bradley-binarisering för inmatningsströmmen. |

### Se även

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

