---
title: "SubmitFormFlag"
linktitle: "SubmitFormFlag"
second_title: "Aspose.PDF för Java API-referens"
description: "Enumeration av möjliga submit‑formulärflaggor."
type: docs
weight: 730
url: /sv/java/com.aspose.pdf.facades/submitformflag/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < SubmitFormFlag > com.aspose.pdf.facades.SubmitFormFlag, java.lang.Enum < SubmitFormFlag >, com.aspose.pdf.facades.SubmitFormFlag

**All Implemented Interfaces:**
Serializable, Comparable < SubmitFormFlag >

```
public enum SubmitFormFlag extends Enum < SubmitFormFlag >
```

Enumeration av möjliga submit‑formulärflaggor.

## Fält

| Fält | Beskrivning |
| --- | --- |
| [Fdf](#Fdf) | Data kommer att returneras i FDF-format. |
| [FdfWithComments](#FdfWithComments) | Data kommer att returneras i FDF-format med kommentarer. |
| [Html](#Html) | Data kommer att returneras i HTML-format. |
| [Pdf](#Pdf) | Hela PDF-filen kommer att skickas in. |
| [Xfdf](#Xfdf) | Data kommer att returneras i XFDF-format. |
| [XfdfWithComments](#XfdfWithComments) | Data kommer att returneras i XFDF-format med kommentarer. |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Returnerar enum‑konstanten av denna typ med det angivna namnet. |
| [values](#values--) | Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras. |

### Fdf {#Fdf}
```
public static final SubmitFormFlag Fdf
```

Data kommer att returneras i FDF-format.

### FdfWithComments {#FdfWithComments}
```
public static final SubmitFormFlag FdfWithComments
```

Data kommer att returneras i FDF-format med kommentarer.

### Html {#Html}
```
public static final SubmitFormFlag Html
```

Data kommer att returneras i HTML-format.

### Pdf {#Pdf}
```
public static final SubmitFormFlag Pdf
```

Hela PDF-filen kommer att skickas in.

### Xfdf {#Xfdf}
```
public static final SubmitFormFlag Xfdf
```

Data kommer att returneras i XFDF-format.

### XfdfWithComments {#XfdfWithComments}
```
public static final SubmitFormFlag XfdfWithComments
```

Data kommer att returneras i XFDF-format med kommentarer.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Returnerar enum‑konstanten av denna typ med det angivna namnet.

### values {#values--}
```
public static SubmitFormFlag [] values()
```

Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras.

**Returns:**
en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras
