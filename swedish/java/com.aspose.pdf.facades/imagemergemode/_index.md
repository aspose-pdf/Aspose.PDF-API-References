---
title: "ImageMergeMode"
linktitle: "ImageMergeMode"
second_title: "Aspose.PDF för Java API-referens"
description: "Representerar lägen för sammanslagning av bilder."
type: docs
weight: 270
url: /sv/java/com.aspose.pdf.facades/imagemergemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ImageMergeMode > com.aspose.pdf.facades.ImageMergeMode, java.lang.Enum < ImageMergeMode >, com.aspose.pdf.facades.ImageMergeMode

**All Implemented Interfaces:**
Serializable, Comparable < ImageMergeMode >

```
public enum ImageMergeMode extends Enum < ImageMergeMode >
```

Representerar lägen för sammanslagning av bilder.

## Fält

| Fält | Beskrivning |
| --- | --- |
| [Center](#Center) | Bilder centrerade. |
| [Horizontal](#Horizontal) | Bilder sammanslagna horisontellt. |
| [Vertical](#Vertical) | Bilder sammanslagna vertikalt. |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Returnerar enum‑konstanten av denna typ med det angivna namnet. |
| [values](#values--) | Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras. |

### Center {#Center}
```
public static final ImageMergeMode Center
```

Bilder centrerade.

### Horizontal {#Horizontal}
```
public static final ImageMergeMode Horizontal
```

Bilder sammanslagna horisontellt.

### Vertical {#Vertical}
```
public static final ImageMergeMode Vertical
```

Bilder sammanslagna vertikalt.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Returnerar enum‑konstanten av denna typ med det angivna namnet.

### values {#values--}
```
public static ImageMergeMode [] values()
```

Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras.

**Returns:**
en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras
