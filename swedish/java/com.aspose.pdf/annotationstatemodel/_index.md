---
title: "AnnotationStateModel"
linktitle: "AnnotationStateModel"
second_title: "Aspose.PDF för Java API-referens"
description: "Tillståndsmodellen som motsvarar annoteringens tillstånd."
type: docs
weight: 120
url: /sv/java/com.aspose.pdf/annotationstatemodel/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationStateModel > com.aspose.pdf.AnnotationStateModel, java.lang.Enum < AnnotationStateModel >, com.aspose.pdf.AnnotationStateModel

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationStateModel >

```
public enum AnnotationStateModel extends Enum < AnnotationStateModel >
```

Tillståndsmodellen som motsvarar annoteringens tillstånd.

## Fält

| Fält | Beskrivning |
| --- | --- |
| [Marked](#Marked) | Anmärkningen har markerats (eller avmarkerats) av användaren. |
| [Review](#Review) | Anmärkningen har granskats (accepterad, avvisad, avbruten, slutförd, ingen) av användaren. |
| [Undefined](#Undefined) | Ingen definierad tillståndsmodell. |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Returnerar enum‑konstanten av denna typ med det angivna namnet. |
| [values](#values--) | Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras. |

### Marked {#Marked}
```
public static final AnnotationStateModel Marked
```

Anmärkningen har markerats (eller avmarkerats) av användaren.

### Review {#Review}
```
public static final AnnotationStateModel Review
```

Anmärkningen har granskats (accepterad, avvisad, avbruten, slutförd, ingen) av användaren.

### Undefined {#Undefined}
```
public static final AnnotationStateModel Undefined
```

Ingen definierad tillståndsmodell.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Returnerar enum‑konstanten av denna typ med det angivna namnet.

### values {#values--}
```
public static AnnotationStateModel [] values()
```

Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras.

**Returns:**
en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras
