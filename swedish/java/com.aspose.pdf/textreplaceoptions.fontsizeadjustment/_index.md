---
title: "TextReplaceOptions.FontSizeAdjustment"
linktitle: "TextReplaceOptions.FontSizeAdjustment"
second_title: "Aspose.PDF för Java API-referens"
description: "Specificerar en policy för hur textens teckensnittsstorlek ska justeras för att passa inom ett omgivande område."
type: docs
weight: 5260
url: /sv/java/com.aspose.pdf/textreplaceoptions.fontsizeadjustment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextReplaceOptions.FontSizeAdjustment > com.aspose.pdf.TextReplaceOptions.FontSizeAdjustment, java.lang.Enum < TextReplaceOptions.FontSizeAdjustment >, com.aspose.pdf.TextReplaceOptions.FontSizeAdjustment

**All Implemented Interfaces:**
Serializable, Comparable < TextReplaceOptions.FontSizeAdjustment >

```
public static enum TextReplaceOptions.FontSizeAdjustment extends Enum < TextReplaceOptions.FontSizeAdjustment >
```

Specificerar en policy för hur textens teckensnittsstorlek ska justeras för att passa inom ett omgivande område.

## Fält

| Fält | Beskrivning |
| --- | --- |
| [None](#None) | Teckenstorleken ändras inte. |
| [ScaleToFill](#ScaleToFill) | Teckenstorleken justeras (både krympning och förstoring) för att få texten att fylla rektangelns gränser så mycket som möjligt. |
| [ShrinkToFit](#ShrinkToFit) | Teckenstorleken minskas om texten är för stor för att passa inom gränserna. |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Returnerar enum‑konstanten av denna typ med det angivna namnet. |
| [values](#values--) | Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras. |

### None {#None}
```
public static final TextReplaceOptions.FontSizeAdjustment None
```

Teckenstorleken ändras inte.

### ScaleToFill {#ScaleToFill}
```
public static final TextReplaceOptions.FontSizeAdjustment ScaleToFill
```

Teckenstorleken justeras (både krympning och förstoring) för att få texten att fylla rektangelns gränser så mycket som möjligt.

### ShrinkToFit {#ShrinkToFit}
```
public static final TextReplaceOptions.FontSizeAdjustment ShrinkToFit
```

Teckenstorleken minskas om texten är för stor för att passa inom gränserna.

### getByValue {#getByValue-int-}
```
public static TextReplaceOptions.FontSizeAdjustment getByValue(int value)
```



### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Returnerar enum‑konstanten av denna typ med det angivna namnet.

### values {#values--}
```
public static TextReplaceOptions.FontSizeAdjustment [] values()
```

Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras.

**Returns:**
en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras
