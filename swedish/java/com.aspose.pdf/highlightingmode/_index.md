---
title: "HighlightingMode"
linktitle: "HighlightingMode"
second_title: "Aspose.PDF för Java API-referens"
description: "Enumererar annoteringens markeringsläge, den visuella effekt som ska användas när musknappen trycks ned eller hålls ned inne i dess aktiva område."
type: docs
weight: 1920
url: /sv/java/com.aspose.pdf/highlightingmode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < HighlightingMode > com.aspose.pdf.HighlightingMode, java.lang.Enum < HighlightingMode >, com.aspose.pdf.HighlightingMode

**All Implemented Interfaces:**
Serialiserbar, Jämförbar < HighlightingMode >

```
public enum HighlightingMode extends Enum < HighlightingMode >
```

Enumererar annoteringens markeringsläge, den visuella effekt som ska användas när musknappen trycks ned eller hålls ned inne i dess aktiva område.

## Fält

| Fält | Beskrivning |
| --- | --- |
| [Invert](#Invert) | Vänd på innehållet i annoteringsrektangeln. |
| [None](#None) | Ingen markering. |
| [Outline](#Outline) | Vänd på annoteringens kant. |
| [Push](#Push) | Visa annoteringens nedre utseende, om något. |
| [Toggle](#Toggle) | Samma som Push (vilket föredras). |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Returnerar enum‑konstanten av denna typ med det angivna namnet. |
| [values](#values--) | Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras. |

### Invert {#Invert}
```
public static final HighlightingMode Invert
```

Vänd på innehållet i annoteringsrektangeln.

### None {#None}
```
public static final HighlightingMode None
```

Ingen markering.

### Outline {#Outline}
```
public static final HighlightingMode Outline
```

Vänd på annoteringens kant.

### Push {#Push}
```
public static final HighlightingMode Push
```

Visa annoteringens nedre utseende, om något.

### Toggle {#Toggle}
```
public static final HighlightingMode Toggle
```

Samma som Push (vilket föredras).

### getByValue {#getByValue-int-}
```
public static HighlightingMode getByValue(int value)
```



**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Returnerar enum‑konstanten av denna typ med det angivna namnet.

### values {#values--}
```
public static HighlightingMode [] values()
```

Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras.

**Returns:**
en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras
