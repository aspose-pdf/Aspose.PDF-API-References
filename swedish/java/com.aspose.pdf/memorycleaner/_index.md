---
title: "MemoryCleaner"
linktitle: "MemoryCleaner"
second_title: "Aspose.PDF för Java API-referens"
description: "Representerar MemoryCleaner-klassen."
type: docs
weight: 3010
url: /sv/java/com.aspose.pdf/memorycleaner/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MemoryCleaner

```
public class MemoryCleaner extends Object
```

Representerar MemoryCleaner-klassen.

## Konstruktörer

| Konstruktör | Beskrivning |
| --- | --- |
| [MemoryCleaner](#MemoryCleaner--) |  |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [clear](#clear--) | Rensar heap-minne från statiska Aspose.PDF-instansier (statisk och threadLocal) och tar bort alla temporära filer. |
| [clearAllTempFiles](#clearAllTempFiles--) | Rensar temporära filer, skapade av Aspose.PDF i systemets temporära katalog. |
| [clearCurrentThreadLocals](#clearCurrentThreadLocals--) | Rensar säkert Heap-minnet från ThreadLocal-instansers för den aktuella tråden |
| [clearFontRegistry](#clearFontRegistry--) | Rensar FontRegistry-information från Heap-minnet. |
| [clearKeepingFontSettings](#clearKeepingFontSettings--) | Rensar heap-minne från statiska Aspose.PDF-instansier (statisk och threadLocal) och tar bort alla temporära filer. |
| [clearLicense](#clearLicense--) | Rensar licensinformation från Heap-minnet. |
| [clearStaticInstances](#clearStaticInstances--) | Rensar Heap-minnet från statiska Aspose.PDF-instansers. |

### MemoryCleaner {#MemoryCleaner--}
```
public MemoryCleaner()
```



### clear {#clear--}
```
public static void clear()
```

Rensar heap-minne från statiska Aspose.PDF-instansier (statisk och threadLocal) och tar bort alla temporära filer.

### clearAllTempFiles {#clearAllTempFiles--}
```
public static void clearAllTempFiles()
```

Rensar temporära filer, skapade av Aspose.PDF i systemets temporära katalog.

### clearCurrentThreadLocals {#clearCurrentThreadLocals--}
```
public static void clearCurrentThreadLocals()
```

Rensar säkert Heap-minnet från ThreadLocal-instansers för den aktuella tråden

### clearFontRegistry {#clearFontRegistry--}
```
public static void clearFontRegistry()
```

Rensar FontRegistry-information från Heap-minnet.

### clearKeepingFontSettings {#clearKeepingFontSettings--}
```
public static void clearKeepingFontSettings()
```

Rensar heap-minne från statiska Aspose.PDF-instansier (statisk och threadLocal) och tar bort alla temporära filer.

### clearLicense {#clearLicense--}
```
public static void clearLicense()
```

Rensar licensinformation från Heap-minnet.

### clearStaticInstances {#clearStaticInstances--}
```
public static void clearStaticInstances()
```

Rensar Heap-minnet från statiska Aspose.PDF-instansers.
