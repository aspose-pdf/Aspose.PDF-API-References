---
title: "TextEditOptions.NoCharacterAction"
linktitle: "TextEditOptions.NoCharacterAction"
second_title: "Aspose.PDF för Java API-referens"
description: "Åtgärd att utföra om teckensnittet inte innehåller det erforderliga tecknet"
type: docs
weight: 5010
url: /sv/java/com.aspose.pdf/texteditoptions.nocharacteraction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextEditOptions.NoCharacterAction > com.aspose.pdf.TextEditOptions.NoCharacterAction, java.lang.Enum < TextEditOptions.NoCharacterAction >, com.aspose.pdf.TextEditOptions.NoCharacterAction

**All Implemented Interfaces:**
Serializable, Comparable < TextEditOptions.NoCharacterAction >

```
public static enum TextEditOptions.NoCharacterAction extends Enum < TextEditOptions.NoCharacterAction >
```

Åtgärd att utföra om teckensnittet inte innehåller det erforderliga tecknet

## Fält

| Fält | Beskrivning |
| --- | --- |
| [ReplaceAnyway](#ReplaceAnyway) | Ersätt text ändå utan teckensnittssubstitution |
| [ReplaceFonts](#ReplaceFonts) | Ersätter teckensnitt vid behov för att säkerställa att alla tecken i texten kan visas. |
| [ThrowException](#ThrowException) | Kasta undantag |
| [UseCustomReplacementFont](#UseCustomReplacementFont) | Ersätt teckensnittet med det definierade ersättningsteckensnittet |
| [UseStandardFont](#UseStandardFont) | Ersätt teckensnittet med standardteckensnitt som innehåller det erforderliga tecknet |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Returnerar enum‑konstanten av denna typ med det angivna namnet. |
| [values](#values--) | Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras. |

### ReplaceAnyway {#ReplaceAnyway}
```
public static final TextEditOptions.NoCharacterAction ReplaceAnyway
```

Ersätt text ändå utan teckensnittssubstitution

### ReplaceFonts {#ReplaceFonts}
```
public static final TextEditOptions.NoCharacterAction ReplaceFonts
```

Ersätter teckensnitt vid behov för att säkerställa att alla tecken i texten kan visas.

### ThrowException {#ThrowException}
```
public static final TextEditOptions.NoCharacterAction ThrowException
```

Kasta undantag

### UseCustomReplacementFont {#UseCustomReplacementFont}
```
public static final TextEditOptions.NoCharacterAction UseCustomReplacementFont
```

Ersätt teckensnittet med det definierade ersättningsteckensnittet

### UseStandardFont {#UseStandardFont}
```
public static final TextEditOptions.NoCharacterAction UseStandardFont
```

Ersätt teckensnittet med standardteckensnitt som innehåller det erforderliga tecknet

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Returnerar enum‑konstanten av denna typ med det angivna namnet.

### values {#values--}
```
public static TextEditOptions.NoCharacterAction [] values()
```

Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras.

**Returns:**
en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras
