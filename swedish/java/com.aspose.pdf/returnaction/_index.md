---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Aspose.PDF för Java API-referens"
description: "Enum representerade en programarbetsflödesåtgärd vid anrop av {@code IWarningCallback.Warning(WarningInfo)}-metoden."
type: docs
weight: 4240
url: /sv/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

Enum representerade en programarbetsflödesåtgärd vid anrop av {@code IWarningCallback.Warning(WarningInfo)}-metoden.

## Fält

| Fält | Beskrivning |
| --- | --- |
| [Abort](#Abort) | Används för att avbryta flödet. |
| [Continue](#Continue) | Används för att fortsätta flödet. |

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | Returnerar enum‑konstanten av denna typ med det angivna namnet. |
| [values](#values--) | Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

Används för att avbryta flödet.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

Används för att fortsätta flödet.

### valueOf {#valueOf-java.lang.String-}
Returnerar enum‑konstanten av denna typ med det angivna namnet.

### values {#values--}
```
public static ReturnAction [] values()
```

Returnerar en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras.

**Returns:**
en array som innehåller konstanterna för denna enum‑typ, i den ordning de deklareras
