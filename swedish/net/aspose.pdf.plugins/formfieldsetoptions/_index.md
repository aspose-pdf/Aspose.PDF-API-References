---
title: Class FormFieldSetOptions
second_title: Aspose.PDF for .NET API Reference
description: Aspose.Pdf.Plugins.FormFieldSetOptions klass. Representerar alternativ för att ställa in egenskaper i fält
type: docs
weight: 8720
url: /sv/net/aspose.pdf.plugins/formfieldsetoptions/
---
## FormFieldSetOptions klass

Representerar alternativ för att ställa in egenskaper i fält.

```csharp
public class FormFieldSetOptions : FormFieldOptions
```

## Konstruktörer

| Namn | Beskrivning |
| --- | --- |
| [FormFieldSetOptions](formfieldsetoptions/)() | Standardkonstruktorn. |

## Egenskaper

| Namn | Beskrivning |
| --- | --- |
| [AlternateName](../../aspose.pdf.plugins/formfieldoptions/alternatename/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen AlternateName för skapat/ändrat fält (om det kommer att ställas in). |
| [Color](../../aspose.pdf.plugins/formfieldoptions/color/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen Color för skapat/ändrat fält (om det kommer att ställas in). |
| [Contents](../../aspose.pdf.plugins/formfieldoptions/contents/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen Contents för skapat/ändrat fält (om det kommer att ställas in). |
| [DefaultAppearance](../../aspose.pdf.plugins/formfieldoptions/defaultappearance/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen DefaultAppearance för skapat/ändrat fält (om det kommer att ställas in). |
| [Exportable](../../aspose.pdf.plugins/formfieldoptions/exportable/) { get; set; } | Hämtar/ställer in värdet för att bestämma om skapat/ändrat fält är exportabelt eller inte (om det kommer att ställas in). |
| [FitIntoRectangle](../../aspose.pdf.plugins/formfieldoptions/fitintorectangle/) { get; set; } | Hämtar/ställer in värdet för att bestämma om skapat/ändrat fält passar in i rektangeln eller inte (om det kommer att ställas in). |
| [Flags](../../aspose.pdf.plugins/formfieldoptions/flags/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen Flags för skapat/ändrat fält (om det kommer att ställas in). |
| [IsSharedField](../../aspose.pdf.plugins/formfieldoptions/issharedfield/) { get; set; } | Hämtar/ställer in värdet för att bestämma om skapat/ändrat fält är ett delat fält eller inte (om det kommer att ställas in). |
| [MappingName](../../aspose.pdf.plugins/formfieldoptions/mappingname/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen MappingName för skapat/ändrat fält (om det kommer att ställas in). |
| [MaxFontSize](../../aspose.pdf.plugins/formfieldoptions/maxfontsize/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen MaxFontSize för skapat/ändrat fält (om det kommer att ställas in). |
| [MinFontSize](../../aspose.pdf.plugins/formfieldoptions/minfontsize/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen MinFontSize för skapat/ändrat fält (om det kommer att ställas in). |
| [Name](../../aspose.pdf.plugins/formfieldoptions/name/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen Name för skapat/ändrat fält (om det kommer att ställas in). |
| [PartialName](../../aspose.pdf.plugins/formfieldoptions/partialname/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen PartialName för skapat/ändrat fält (om det kommer att ställas in). |
| [ReadOnly](../../aspose.pdf.plugins/formfieldoptions/readonly/) { get; set; } | Hämtar/ställer in värdet för att bestämma om skapat/ändrat fält är skrivskyddat eller inte (om det kommer att ställas in). |
| [Rect](../../aspose.pdf.plugins/formfieldsetoptions/rect/) { get; set; } | Rektangel som kan ställas in på fältet/fälten. |
| [Required](../../aspose.pdf.plugins/formfieldoptions/required/) { get; set; } | Hämtar/ställer in värdet för att bestämma om skapat/ändrat fält är obligatoriskt eller inte (om det kommer att ställas in). |
| [TextHorizontalAlignment](../../aspose.pdf.plugins/formfieldoptions/texthorizontalalignment/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen TextHorizontalAlignment för skapat/ändrat fält (om det kommer att ställas in). |
| [UpdateAppearanceOnConvert](../../aspose.pdf.plugins/formfieldoptions/updateappearanceonconvert/) { get; set; } | Hämtar/ställer in värdet för att bestämma om skapat/ändrat fält uppdaterar utseendet vid konvertering eller inte (om det kommer att ställas in). |
| [UseFontSubset](../../aspose.pdf.plugins/formfieldoptions/usefontsubset/) { get; set; } | Hämtar/ställer in värdet för att bestämma om skapat/ändrat fält använder teckensnittssubset eller inte (om det kommer att ställas in). |
| [Value](../../aspose.pdf.plugins/formfieldoptions/value/) { get; set; } | Hämtar/ställer in värdet för att bestämma egenskapen Value för skapat/ändrat fält (om det kommer att ställas in). |

## Fält

| Namn | Beskrivning |
| --- | --- |
| [Highlighting](../../aspose.pdf.plugins/formfieldoptions/highlighting/) | Hämtar/ställer in värdet för att bestämma egenskapen Highlighting för skapat/ändrat fält (om det kommer att ställas in). |

### Se Även

* klass [FormFieldOptions](../formfieldoptions/)
* namnrymd [Aspose.Pdf.Plugins](../../aspose.pdf.plugins/)
* sammansättning [Aspose.PDF](../../)