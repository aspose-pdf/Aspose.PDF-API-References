---
title: Class FormComboBoxFieldSetOptions
second_title: Aspose.PDF for .NET API Reference
description: Aspose.Pdf.Plugins.FormComboBoxFieldSetOptions klass. Representerar alternativ för att ställa in egenskaper i ComboBoxField av FormEditor-plugin
type: docs
weight: 8610
url: /sv/net/aspose.pdf.plugins/formcomboboxfieldsetoptions/
---
## FormComboBoxFieldSetOptions klass

Representerar alternativ för att ställa in egenskaper i ComboBoxField av [`FormEditor`](../formeditor/) plugin.

```csharp
public class FormComboBoxFieldSetOptions : FormFieldSetOptions
```

## Konstruktörer

| Namn | Beskrivning |
| --- | --- |
| [FormComboBoxFieldSetOptions](formcomboboxfieldsetoptions/)() | Standardkonstruktorn. |

## Egenskaper

| Namn | Beskrivning |
| --- | --- |
| [AlternateName](../../aspose.pdf.plugins/formfieldoptions/alternatename/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen AlternateName för det skapade/ändrade fältet (om det kommer att ställas in). |
| [Color](../../aspose.pdf.plugins/formfieldoptions/color/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Color för det skapade/ändrade fältet (om det kommer att ställas in). |
| [Contents](../../aspose.pdf.plugins/formfieldoptions/contents/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Contents för det skapade/ändrade fältet (om det kommer att ställas in). |
| [DefaultAppearance](../../aspose.pdf.plugins/formfieldoptions/defaultappearance/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen DefaultAppearance för det skapade/ändrade fältet (om det kommer att ställas in). |
| [Editable](../../aspose.pdf.plugins/formcomboboxfieldsetoptions/editable/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Editable för det ändrade fältet (om det kommer att ställas in). |
| [Exportable](../../aspose.pdf.plugins/formfieldoptions/exportable/) { get; set; } | Hämtar/anger värdet för att bestämma om det skapade/ändrade fältet är exportabelt eller inte (om det kommer att ställas in). |
| [FitIntoRectangle](../../aspose.pdf.plugins/formfieldoptions/fitintorectangle/) { get; set; } | Hämtar/anger värdet för att bestämma om det skapade/ändrade fältet passar in i en rektangel eller inte (om det kommer att ställas in). |
| [Flags](../../aspose.pdf.plugins/formfieldoptions/flags/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Flags för det skapade/ändrade fältet (om det kommer att ställas in). |
| [IsSharedField](../../aspose.pdf.plugins/formfieldoptions/issharedfield/) { get; set; } | Hämtar/anger värdet för att bestämma om det skapade/ändrade fältet är ett delat fält eller inte (om det kommer att ställas in). |
| [MappingName](../../aspose.pdf.plugins/formfieldoptions/mappingname/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen MappingName för det skapade/ändrade fältet (om det kommer att ställas in). |
| [MaxFontSize](../../aspose.pdf.plugins/formfieldoptions/maxfontsize/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen MaxFontSize för det skapade/ändrade fältet (om det kommer att ställas in). |
| [MinFontSize](../../aspose.pdf.plugins/formfieldoptions/minfontsize/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen MinFontSize för det skapade/ändrade fältet (om det kommer att ställas in). |
| [Name](../../aspose.pdf.plugins/formfieldoptions/name/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Name för det skapade/ändrade fältet (om det kommer att ställas in). |
| [Options](../../aspose.pdf.plugins/formcomboboxfieldsetoptions/options/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Options för det ändrade fältet (om det kommer att ställas in). |
| [PartialName](../../aspose.pdf.plugins/formfieldoptions/partialname/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen PartialName för det skapade/ändrade fältet (om det kommer att ställas in). |
| [ReadOnly](../../aspose.pdf.plugins/formfieldoptions/readonly/) { get; set; } | Hämtar/anger värdet för att bestämma om det skapade/ändrade fältet är skrivskyddat eller inte (om det kommer att ställas in). |
| [Rect](../../aspose.pdf.plugins/formfieldsetoptions/rect/) { get; set; } | Rektangel som kan ställas in på fältet/fälten. |
| [Required](../../aspose.pdf.plugins/formfieldoptions/required/) { get; set; } | Hämtar/anger värdet för att bestämma om det skapade/ändrade fältet är obligatoriskt eller inte (om det kommer att ställas in). |
| [Selected](../../aspose.pdf.plugins/formcomboboxfieldsetoptions/selected/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Selected för det ändrade fältet (om det kommer att ställas in). |
| [TextHorizontalAlignment](../../aspose.pdf.plugins/formfieldoptions/texthorizontalalignment/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen TextHorizontalAlignment för det skapade/ändrade fältet (om det kommer att ställas in). |
| [UpdateAppearanceOnConvert](../../aspose.pdf.plugins/formfieldoptions/updateappearanceonconvert/) { get; set; } | Hämtar/anger värdet för att bestämma om det skapade/ändrade fältet uppdaterar utseendet vid konvertering eller inte (om det kommer att ställas in). |
| [UseFontSubset](../../aspose.pdf.plugins/formfieldoptions/usefontsubset/) { get; set; } | Hämtar/anger värdet för att bestämma om det skapade/ändrade fältet använder teckensnittssubset eller inte (om det kommer att ställas in). |
| [Value](../../aspose.pdf.plugins/formfieldoptions/value/) { get; set; } | Hämtar/anger värdet för att bestämma egenskapen Value för det skapade/ändrade fältet (om det kommer att ställas in). |

## Fält

| Namn | Beskrivning |
| --- | --- |
| [Highlighting](../../aspose.pdf.plugins/formfieldoptions/highlighting/) | Hämtar/anger värdet för att bestämma egenskapen Highlighting för det skapade/ändrade fältet (om det kommer att ställas in). |

### Se Även

* klass [FormFieldSetOptions](../formfieldsetoptions/)
* namnrum [Aspose.Pdf.Plugins](../../aspose.pdf.plugins/)
* samling [Aspose.PDF](../../)