---
title: "AnnotationSelector"
second_title: "Aspose.PDF for Python via .NET API 参考"
description: "此类用于使用 Visitor 模板思想选择注释。"
type: docs
weight: 50
url: /zh/python-net/aspose.pdf.annotations/annotationselector/
---

## AnnotationSelector class

此类用于使用 Visitor 模板思想选择注释。

AnnotationSelector 类型公开以下成员：
## 构造函数
| 名称 | 描述 |
| :- | :- |
| AnnotationSelector() | 初始化 AnnotationSelector 类的新实例。 |
| AnnotationSelector(annotation) | 初始化 AnnotationSelector 类的新实例 |
## 属性
| 名称 | 描述 |
| :- | :- |
| selected | 已选择对象的列表。 |
## 方法
| 名称 | 描述 |
| :- | :- |
| 访问(link) | 如果 AnnotationSelector 使用 LinkAnnotation 对象初始化，则选择链接注释。 |
| 访问(attachment) | 如果 AnnotationSelector 使用 FileAttachmentAnnotation 对象初始化，则选择附件注释。 |
| 访问(text) | 如果 AnnotationSelector 使用 TextAnnotation 对象初始化，则选择文本注释。 |
| visit(redact) | 如果 AnnotationSelector 使用 RedactAnnotation 对象初始化，则选择编辑注释。 |
| 访问(freetext) | 如果 AnnotationSelector 使用 FreeTextAnnotation 对象初始化，则选择自由文本注释。 |
| 访问(highlight) | 如果 AnnotationSelector 使用 FreeTextAnnotation 对象初始化，则选择附件注释。 |
| 访问(underline) | 如果 AnnotationSelector 使用 UnderlineAnnotation 对象初始化，则选择下划线注释。 |
| 访问(strike_out) | 如果 AnnotationSelector 使用 StrikeOutAnnotation 对象初始化，则选择删除线注释。 |
| 访问(squiggly) | 如果 AnnotationSelector 使用 SquigglyAnnotation 对象初始化，则选择波浪线注释。 |
| 访问(popup) | 如果 AnnotationSelector 使用 PopupAnnotation 对象初始化，则选择弹出注释。 |
| 访问(line) | 如果 AnnotationSelector 使用 UnderlineAnnotation 对象初始化，则选择下划线注释。 |
| 访问(circle) | 如果 AnnotationSelector 使用 CircleAnnotation 对象初始化，则选择圆形注释。 |
| 访问(square) | 如果 AnnotationSelector 使用 SquareAnnotation 对象初始化，则选择方形注释。 |
| 访问(ink) | 如果 AnnotationSelector 使用 LinkAnnotation 对象初始化，则选择链接注释。 |
| 访问(polyline) | 如果 AnnotationSelector 使用 PolylineAnnotation 对象初始化，则选择折线注释。 |
| visit(polygon) | 如果 AnnotationSelector 使用 PolygonAnnotation 对象初始化，则选择多边形注释。 |
| visit(caret) | 如果 AnnotationSelector 使用 CaretAnnotation 对象初始化，则选择插入符号注释。 |
| visit(stamp) | 如果 AnnotationSelector 使用 StampAnnotation 对象初始化，则选择印章注释。 |
| visit(widget) | 如果 AnnotationSelector 使用 WidgetAnnotation 对象初始化，则选择小部件注释。 |
| visit(watermark) | 如果 AnnotationSelector 使用 WatermarkAnnotation 对象初始化，则选择水印注释。 |
| visit(movie) | 如果 AnnotationSelector 使用 MovieAnnotation 对象初始化，则选择电影注释。 |
| visit(rich_media) | 如果 AnnotationSelector 使用 RichMedia 注释对象初始化，则选择电影注释。 |
| visit(screen) | 如果 AnnotationSelector 使用 ScreenAnnotation 对象初始化，则选择屏幕注释。 |
| visit(pdf_3d) | 如果 AnnotationSelector 使用 PDF3DAnnotation 对象初始化，则选择 PDF3D 注释。 |

### 另请参阅

* namespace [aspose.pdf.annotations](/pdf/python-net/aspose.pdf.annotations/)
* assembly [Aspose.PDF](/pdf/python-net/)

