---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Aspose.PDF for Java API 参考"
description: "枚举表示在调用 {@code IWarningCallback.Warning(WarningInfo)} 方法时的程序工作流操作。"
type: docs
weight: 4240
url: /zh/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

枚举表示在调用 {@code IWarningCallback.Warning(WarningInfo)} 方法时的程序工作流操作。

## 字段

| 字段 | 描述 |
| --- | --- |
| [Abort](#Abort) | 用于中止流程。 |
| [Continue](#Continue) | 用于继续流程。 |

## 方法

| 方法 | 描述 |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | 返回此类型中具有指定名称的枚举常量。 |
| [values](#values--) | 返回一个数组，包含此枚举类型的常量，按声明顺序排列。 |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

用于中止流程。

### Continue {#Continue}
```
public static final ReturnAction Continue
```

用于继续流程。

### valueOf {#valueOf-java.lang.String-}
返回此类型中具有指定名称的枚举常量。

### values {#values--}
```
public static ReturnAction [] values()
```

返回一个数组，包含此枚举类型的常量，按声明顺序排列。

**Returns:**
一个数组，包含此枚举类型的常量，按声明顺序排列
