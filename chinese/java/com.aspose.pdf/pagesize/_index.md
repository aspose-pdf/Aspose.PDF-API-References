---
title: "PageSize"
linktitle: "PageSize"
second_title: "Aspose.PDF for Java API 参考"
description: "表示 PDF 文档中页面尺寸的类。"
type: docs
weight: 3450
url: /zh/java/com.aspose.pdf/pagesize/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PageSize

```
public final class PageSize extends Object
```

表示 PDF 文档中页面尺寸的类。

## 字段

| 字段 | 描述 |
| --- | --- |
| [LEAVE_INTACT](#LEAVE_INTACT) | 仅供内部使用 |

## 构造函数

| 构造函数 | 描述 |
| --- | --- |
| [PageSize](#PageSize-float-float-) | PageSize 的构造函数。 |

## 方法

| 方法 | 描述 |
| --- | --- |
| [getA0](#getA0--) | A0 尺寸（1189x840 mm）。 |
| [getA1](#getA1--) | A1 尺寸（840x594 mm）。 |
| [getA2](#getA2--) | A2 尺寸（594x420 mm）。 |
| [getA3](#getA3--) | A3 尺寸（420x297 mm）。 |
| [getA4](#getA4--) | A4 尺寸（297x210 mm）。 |
| [getA5](#getA5--) | A5 尺寸（210x148 mm）。 |
| [getA6](#getA6--) | A6 尺寸 (148x105 mm). |
| [getB5](#getB5--) | B5 尺寸 (250x176 mm). |
| [getHeight](#getHeight--) | 获取页面高度。 |
| [getP11x17](#getP11x17--) | 11x17 英寸格式. |
| [getPageLedger](#getPageLedger--) | Ledger 尺寸 (432x279 mm). |
| [getPageLegal](#getPageLegal--) | Legal 尺寸 (356x216 mm). |
| [getPageLetter](#getPageLetter--) | Letter 尺寸 (279x216 mm). |
| [getWidth](#getWidth--) | 获取页面宽度。 |
| [isLandscape](#isLandscape--) | 获取页面方向。若为横向返回 true，若为纵向返回 false。 |
| [setHeight](#setHeight-float-) | 设置页面高度。 |
| [setLandscape](#setLandscape-boolean-) | 设置页面方向。若为横向返回 true，若为纵向返回 false。 |
| [setWidth](#setWidth-float-) | 设置页面宽度。 |

### LEAVE_INTACT {#LEAVE_INTACT}
```
public static final float LEAVE_INTACT
```

仅供内部使用

### PageSize {#PageSize-float-float-}
```
public PageSize(float x, float y)
```

PageSize 的构造函数。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| x |  | 页面宽度。 |
| y |  | 页面高度。 |

### getA0 {#getA0--}
```
public static PageSize getA0()
```

A0 尺寸（1189x840 mm）。

**Returns:**
PageSize 对象

### getA1 {#getA1--}
```
public static PageSize getA1()
```

A1 尺寸（840x594 mm）。

**Returns:**
PageSize 对象

### getA2 {#getA2--}
```
public static PageSize getA2()
```

A2 尺寸（594x420 mm）。

**Returns:**
PageSize 对象

### getA3 {#getA3--}
```
public static PageSize getA3()
```

A3 尺寸（420x297 mm）。

**Returns:**
PageSize 对象

### getA4 {#getA4--}
```
public static PageSize getA4()
```

A4 尺寸（297x210 mm）。

**Returns:**
PageSize 对象

### getA5 {#getA5--}
```
public static PageSize getA5()
```

A5 尺寸（210x148 mm）。

**Returns:**
PageSize 对象

### getA6 {#getA6--}
```
public static PageSize getA6()
```

A6 尺寸 (148x105 mm).

**Returns:**
PageSize 对象

### getB5 {#getB5--}
```
public static PageSize getB5()
```

B5 尺寸 (250x176 mm).

**Returns:**
PageSize 对象

### getHeight {#getHeight--}
```
public float getHeight()
```

获取页面高度。

**Returns:**
页面高度。

### getP11x17 {#getP11x17--}
```
public static PageSize getP11x17()
```

11x17 英寸格式.

**Returns:**
PageSize 对象

### getPageLedger {#getPageLedger--}
```
public static PageSize getPageLedger()
```

Ledger 尺寸 (432x279 mm).

**Returns:**
PageSize 对象

### getPageLegal {#getPageLegal--}
```
public static PageSize getPageLegal()
```

Legal 尺寸 (356x216 mm).

**Returns:**
PageSize 对象

### getPageLetter {#getPageLetter--}
```
public static PageSize getPageLetter()
```

Letter 尺寸 (279x216 mm).

**Returns:**
PageSize 对象

### getWidth {#getWidth--}
```
public float getWidth()
```

获取页面宽度。

**Returns:**
页面宽度。

### isLandscape {#isLandscape--}
```
public boolean isLandscape()
```

获取页面方向。若为横向返回 true，若为纵向返回 false。

**Returns:**
布尔值

### setHeight {#setHeight-float-}
```
public void setHeight(float value)
```

设置页面高度。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 值 |  | 页面高度。 |

### setLandscape {#setLandscape-boolean-}
```
public void setLandscape(boolean value)
```

设置页面方向。若为横向返回 true，若为纵向返回 false。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 值 |  | 布尔值 |

### setWidth {#setWidth-float-}
```
public void setWidth(float value)
```

设置页面宽度。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 值 |  | 页面宽度。 |
