---
title: "ExplicitDestinationType"
linktitle: "ExplicitDestinationType"
second_title: "Aspose.PDF for Java API 参考"
description: "枚举显式目标的类型。"
type: docs
weight: 1290
url: /zh/java/com.aspose.pdf/explicitdestinationtype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ExplicitDestinationType > com.aspose.pdf.ExplicitDestinationType, java.lang.Enum < ExplicitDestinationType >, com.aspose.pdf.ExplicitDestinationType

**All Implemented Interfaces:**
Serializable, Comparable < ExplicitDestinationType >

```
public enum ExplicitDestinationType extends Enum < ExplicitDestinationType >
```

枚举显式目标的类型。

## 字段

| 字段 | 描述 |
| --- | --- |
| [Fit](#Fit) | 显示页面，使其内容放大到恰好在窗口中水平和垂直方向上完整适配整个页面。 |
| [FitB](#FitB) | 显示页面，使其内容放大到恰好在窗口中水平和垂直方向上完整适配其边界框。 |
| [FitBH](#FitBH) | 显示页面，将垂直坐标 top 放置在窗口的上边缘，并将页面内容放大到恰好在窗口中完整适配其边界框的整个宽度。 |
| [FitBV](#FitBV) | 显示页面，将水平坐标 left 放置在窗口的左边缘，并将页面内容放大到恰好在窗口中完整适配其边界框的整个高度。 |
| [FitH](#FitH) | 显示页面，将垂直坐标 top 放置在窗口的上边缘，并将页面内容放大到恰好在窗口中完整适配页面的整个宽度。 |
| [FitR](#FitR) | 显示页面，将其内容放大到恰好在窗口中水平和垂直方向上完整适配由 left、bottom、right 和 top 指定的矩形。 |
| [FitV](#FitV) | 显示页面，将水平坐标 left 放置在窗口的左边缘，并将页面内容放大到恰好在窗口中完整适配页面的整个高度。 |
| [XYZ](#XYZ) | 显示页面，将坐标 (left, top) 放置在窗口的左上角，并按 zoom 系数放大页面内容。 |

## 方法

| 方法 | 描述 |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 返回此类型中具有指定名称的枚举常量。 |
| [values](#values--) | 返回一个数组，包含此枚举类型的常量，按声明顺序排列。 |

### Fit {#Fit}
```
public static final ExplicitDestinationType Fit
```

显示页面，使其内容放大到恰好在窗口中水平和垂直方向上完整适配整个页面。

### FitB {#FitB}
```
public static final ExplicitDestinationType FitB
```

显示页面，使其内容放大到恰好在窗口中水平和垂直方向上完整适配其边界框。

### FitBH {#FitBH}
```
public static final ExplicitDestinationType FitBH
```

显示页面，将垂直坐标 top 放置在窗口的上边缘，并将页面内容放大到恰好在窗口中完整适配其边界框的整个宽度。

### FitBV {#FitBV}
```
public static final ExplicitDestinationType FitBV
```

显示页面，将水平坐标 left 放置在窗口的左边缘，并将页面内容放大到恰好在窗口中完整适配其边界框的整个高度。

### FitH {#FitH}
```
public static final ExplicitDestinationType FitH
```

显示页面，将垂直坐标 top 放置在窗口的上边缘，并将页面内容放大到恰好在窗口中完整适配页面的整个宽度。

### FitR {#FitR}
```
public static final ExplicitDestinationType FitR
```

显示页面，将其内容放大到恰好在窗口中水平和垂直方向上完整适配由 left、bottom、right 和 top 指定的矩形。

### FitV {#FitV}
```
public static final ExplicitDestinationType FitV
```

显示页面，将水平坐标 left 放置在窗口的左边缘，并将页面内容放大到恰好在窗口中完整适配页面的整个高度。

### XYZ {#XYZ}
```
public static final ExplicitDestinationType XYZ
```

显示页面，将坐标 (left, top) 放置在窗口的左上角，并按 zoom 系数放大页面内容。

### getByValue {#getByValue-int-}
```
public static ExplicitDestinationType getByValue(int value)
```



**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 值 |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
返回此类型中具有指定名称的枚举常量。

### values {#values--}
```
public static ExplicitDestinationType [] values()
```

返回一个数组，包含此枚举类型的常量，按声明顺序排列。

**Returns:**
一个数组，包含此枚举类型的常量，按声明顺序排列
