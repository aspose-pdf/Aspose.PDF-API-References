---
title: "ParagraphAbsorberOptions"
linktitle: "ParagraphAbsorberOptions"
second_title: "Aspose.PDF for Java API 参考"
description: "表示 {@link ParagraphAbsorber} 的选项。"
type: docs
weight: 3480
url: /zh/java/com.aspose.pdf/paragraphabsorberoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ParagraphAbsorberOptions

```
public final class ParagraphAbsorberOptions extends Object
```

表示 {@link ParagraphAbsorber} 的选项。

## 构造函数

| 构造函数 | 描述 |
| --- | --- |
| [ParagraphAbsorberOptions](#ParagraphAbsorberOptions--) | 初始化 {@link ParagraphAbsorberOptions} 对象的新实例。 |

## 方法

| 方法 | 描述 |
| --- | --- |
| [getSearchRectangle](#getSearchRectangle--) | 获取或设置段落搜索矩形。 |
| [getSectionUnbreakingHorizontalOverride](#getSectionUnbreakingHorizontalOverride--) | 获取或设置在水平分段断点中不被视为实际水平分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。 |
| [getSectionUnbreakingVerticalOverride](#getSectionUnbreakingVerticalOverride--) | 获取或设置在垂直分段断点中不被视为实际垂直分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。 |
| [setSearchRectangle](#setSearchRectangle-com.aspose.pdf.Rectangle-) | 获取或设置段落搜索矩形。 |
| [setSectionUnbreakingHorizontalOverride](#setSectionUnbreakingHorizontalOverride-double-) | 获取或设置在水平分段断点中不被视为实际水平分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。 |
| [setSectionUnbreakingVerticalOverride](#setSectionUnbreakingVerticalOverride-double-) | 获取或设置在垂直分段断点中不被视为实际垂直分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。 |

### ParagraphAbsorberOptions {#ParagraphAbsorberOptions--}
```
public ParagraphAbsorberOptions()
```

初始化 {@link ParagraphAbsorberOptions} 对象的新实例。

### getSearchRectangle {#getSearchRectangle--}
```
public final Rectangle getSearchRectangle()
```

获取或设置段落搜索矩形。

**Returns:**
Rectangle 实例

### getSectionUnbreakingHorizontalOverride {#getSectionUnbreakingHorizontalOverride--}
```
public final double getSectionUnbreakingHorizontalOverride()
```

获取或设置在水平分段断点中不被视为实际水平分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。

**Returns:**
double 值

### getSectionUnbreakingVerticalOverride {#getSectionUnbreakingVerticalOverride--}
```
public final double getSectionUnbreakingVerticalOverride()
```

获取或设置在垂直分段断点中不被视为实际垂直分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。

**Returns:**
double 值

### setSearchRectangle {#setSearchRectangle-com.aspose.pdf.Rectangle-}
获取或设置段落搜索矩形。

### setSectionUnbreakingHorizontalOverride {#setSectionUnbreakingHorizontalOverride-double-}
```
public final void setSectionUnbreakingHorizontalOverride(double value)
```

获取或设置在水平分段断点中不被视为实际水平分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 值 |  | double 值 |

### setSectionUnbreakingVerticalOverride {#setSectionUnbreakingVerticalOverride-double-}
```
public final void setSectionUnbreakingVerticalOverride(double value)
```

获取或设置在垂直分段断点中不被视为实际垂直分段的“零填充级别”区域的最大长度（以页面宽度/高度的比例表示）。默认值为 0.005。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 值 |  | double 值 |
