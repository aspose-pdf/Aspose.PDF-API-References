---
title: "PDF3DView"
linktitle: "PDF3DView"
second_title: "Aspose.PDF for Java API 参考"
description: "类 PDF3DView。"
type: docs
weight: 3650
url: /zh/java/com.aspose.pdf/pdf3dview/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DView

```
public class PDF3DView extends Object
```

类 PDF3DView。

## 构造函数

| 构造函数 | 描述 |
| --- | --- |
| [PDF3DView](#PDF3DView-com.aspose.pdf.IDocument-com.aspose.pdf.Matrix3D-double-java.lang.String-) | 初始化 {@code PDF3DView} 类的新实例。 |
| [PDF3DView](#PDF3DView-com.aspose.pdf.IDocument-com.aspose.pdf.PDF3DView-java.lang.String-) | 初始化 {@code PDF3DView} 类的新实例。 |

## 方法

| 方法 | 描述 |
| --- | --- |
| [getBackGroundColor](#getBackGroundColor--) | 获取或设置视图背景的颜色。 |
| [getCameraOrbit](#getCameraOrbit--) | 获取或设置视图的相机轨道。 |
| [getCameraPosition](#getCameraPosition--) | 获取或设置视图的相机位置。 |
| [getCrossSectionsArray](#getCrossSectionsArray--) | 获取视图的横截面数组。 |
| [getLightingScheme](#getLightingScheme--) | 获取或设置视图的照明方案。 |
| [getRenderMode](#getRenderMode--) | 获取或设置视图的渲染模式。 |
| [getViewName](#getViewName--) | 获取或设置视图的名称。 |
| [setBackGroundColor](#setBackGroundColor-com.aspose.pdf.Color-) | 获取或设置视图的背景颜色。 |
| [setCameraOrbit](#setCameraOrbit-double-) | 获取或设置视图的相机轨道。 |
| [setCameraPosition](#setCameraPosition-com.aspose.pdf.Matrix3D-) | 获取或设置视图的相机位置。 |
| [setLightingScheme](#setLightingScheme-com.aspose.pdf.PDF3DLightingScheme-) | 获取或设置视图的照明方案。 |
| [setRenderMode](#setRenderMode-com.aspose.pdf.PDF3DRenderMode-) | 获取或设置视图的渲染模式。 |
| [setViewName](#setViewName-java.lang.String-) | 获取或设置视图的名称。 |

### PDF3DView {#PDF3DView-com.aspose.pdf.IDocument-com.aspose.pdf.Matrix3D-double-java.lang.String-}
初始化 {@code PDF3DView} 类的新实例。

### PDF3DView {#PDF3DView-com.aspose.pdf.IDocument-com.aspose.pdf.PDF3DView-java.lang.String-}
初始化 {@code PDF3DView} 类的新实例。

### getBackGroundColor {#getBackGroundColor--}
```
public Color getBackGroundColor()
```

获取或设置视图背景的颜色。

**Returns:**
颜色值：视图背景的颜色。

### getCameraOrbit {#getCameraOrbit--}
```
public double getCameraOrbit()
```

获取或设置视图的相机轨道。

**Returns:**
double 值：视图的相机轨道。

### getCameraPosition {#getCameraPosition--}
```
public Matrix3D getCameraPosition()
```

获取或设置视图的相机位置。

**Returns:**
Matrix3D 对象：视图的相机位置。

### getCrossSectionsArray {#getCrossSectionsArray--}
```
public PDF3DCrossSectionArray getCrossSectionsArray()
```

获取视图的横截面数组。

**Returns:**
PDF3DCrossSectionArray 值：视图的横截面数组。

### getLightingScheme {#getLightingScheme--}
```
public PDF3DLightingScheme getLightingScheme()
```

获取或设置视图的照明方案。

**Returns:**
PDF3DLightingScheme 对象：视图的照明方案。

### getRenderMode {#getRenderMode--}
```
public PDF3DRenderMode getRenderMode()
```

获取或设置视图的渲染模式。

**Returns:**
PDF3DRenderMode 值：视图的渲染模式。

### getViewName {#getViewName--}
```
public String getViewName()
```

获取或设置视图的名称。

**Returns:**
String 对象 - 视图的名称。

### setBackGroundColor {#setBackGroundColor-com.aspose.pdf.Color-}
获取或设置视图的背景颜色。

### setCameraOrbit {#setCameraOrbit-double-}
```
public void setCameraOrbit(double value)
```

获取或设置视图的相机轨道。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 值 |  | ：视图的相机轨道。 |

### setCameraPosition {#setCameraPosition-com.aspose.pdf.Matrix3D-}
获取或设置视图的相机位置。

### setLightingScheme {#setLightingScheme-com.aspose.pdf.PDF3DLightingScheme-}
获取或设置视图的照明方案。

### setRenderMode {#setRenderMode-com.aspose.pdf.PDF3DRenderMode-}
获取或设置视图的渲染模式。

### setViewName {#setViewName-java.lang.String-}
获取或设置视图的名称。
