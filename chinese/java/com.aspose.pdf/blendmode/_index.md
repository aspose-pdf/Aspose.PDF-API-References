---
title: "BlendMode"
linktitle: "BlendMode"
second_title: "Aspose.PDF for Java API 参考"
description: "混合模式枚举。"
type: docs
weight: 330
url: /zh/java/com.aspose.pdf/blendmode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < BlendMode > com.aspose.pdf.BlendMode, java.lang.Enum < BlendMode >, com.aspose.pdf.BlendMode

**All Implemented Interfaces:**
Serializable, Comparable < BlendMode >

```
public enum BlendMode extends Enum < BlendMode >
```

混合模式枚举。

## 字段

| 字段 | 描述 |
| --- | --- |
| [Color](#Color) | 颜色混合模式。 |
| [ColorBurn](#ColorBurn) | ColorBurn 混合模式。 |
| [ColorDodge](#ColorDodge) | ColorDodge 混合模式。 |
| [Compatible](#Compatible) | 兼容混合模式。 |
| [Darken](#Darken) | 变暗混合模式。 |
| [Difference](#Difference) | 差值混合模式。 |
| [Exclusion](#Exclusion) | 排除混合模式。 |
| [HardLight](#HardLight) | 强光混合模式。 |
| [Hue](#Hue) | 色相混合模式。 |
| [Lighten](#Lighten) | 变亮混合模式。 |
| [Luminosity](#Luminosity) | 亮度混合模式。 |
| [Multiply](#Multiply) | 正片叠底混合模式。 |
| [Normal](#Normal) | 正常混合模式。 |
| [Overlay](#Overlay) | 叠加混合模式。 |
| [Saturation](#Saturation) | 饱和度混合模式。 |
| [Screen](#Screen) | 滤色混合模式。 |
| [SoftLight](#SoftLight) | 柔光混合模式。 |

## 方法

| 方法 | 描述 |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 返回此类型中具有指定名称的枚举常量。 |
| [values](#values--) | 返回一个数组，包含此枚举类型的常量，按声明顺序排列。 |

### Color {#Color}
```
public static final BlendMode Color
```

颜色混合模式。

### ColorBurn {#ColorBurn}
```
public static final BlendMode ColorBurn
```

ColorBurn 混合模式。

### ColorDodge {#ColorDodge}
```
public static final BlendMode ColorDodge
```

ColorDodge 混合模式。

### Compatible {#Compatible}
```
public static final BlendMode Compatible
```

兼容混合模式。

### Darken {#Darken}
```
public static final BlendMode Darken
```

变暗混合模式。

### Difference {#Difference}
```
public static final BlendMode Difference
```

差值混合模式。

### Exclusion {#Exclusion}
```
public static final BlendMode Exclusion
```

排除混合模式。

### HardLight {#HardLight}
```
public static final BlendMode HardLight
```

强光混合模式。

### Hue {#Hue}
```
public static final BlendMode Hue
```

色相混合模式。

### Lighten {#Lighten}
```
public static final BlendMode Lighten
```

变亮混合模式。

### Luminosity {#Luminosity}
```
public static final BlendMode Luminosity
```

亮度混合模式。

### Multiply {#Multiply}
```
public static final BlendMode Multiply
```

正片叠底混合模式。

### Normal {#Normal}
```
public static final BlendMode Normal
```

正常混合模式。

### Overlay {#Overlay}
```
public static final BlendMode Overlay
```

叠加混合模式。

### Saturation {#Saturation}
```
public static final BlendMode Saturation
```

饱和度混合模式。

### Screen {#Screen}
```
public static final BlendMode Screen
```

滤色混合模式。

### SoftLight {#SoftLight}
```
public static final BlendMode SoftLight
```

柔光混合模式。

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
返回此类型中具有指定名称的枚举常量。

### values {#values--}
```
public static BlendMode [] values()
```

返回一个数组，包含此枚举类型的常量，按声明顺序排列。

**Returns:**
一个数组，包含此枚举类型的常量，按声明顺序排列
