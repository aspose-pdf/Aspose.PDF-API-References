---
title: "LoadFormat"
linktitle: "LoadFormat"
second_title: "Aspose.PDF for Java API 参考"
description: "指定加载格式。"
type: docs
weight: 2780
url: /zh/java/com.aspose.pdf/loadformat/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LoadFormat > com.aspose.pdf.LoadFormat, java.lang.Enum < LoadFormat >, com.aspose.pdf.LoadFormat

**All Implemented Interfaces:**
Serializable, Comparable < LoadFormat >

```
public enum LoadFormat extends Enum < LoadFormat >
```

指定加载格式。

## 字段

| 字段 | 描述 |
| --- | --- |
| [APS](#APS) | 表示以 APS 格式加载文档。 |
| [CDR](#CDR) | 表示以 CDR 格式加载文档。 |
| [CGM](#CGM) | 表示以 CGM 格式加载文档 |
| [DJVU](#DJVU) | 表示以 Djvu 格式加载文档。 |
| [EPUB](#EPUB) | 表示以 EPUB 格式加载文档（电子书的特殊格式） |
| [HTML](#HTML) | 表示以 HTML 格式加载文档 |
| [MD](#MD) | 表示文档以 MD 格式（Markdown）加载。 |
| [MHT](#MHT) | 表示以 MHT 格式加载文档（即打包的 HTML 格式） |
| [OFD](#OFD) | 表示以 OFD 格式加载文档。 |
| [PCL](#PCL) | 表示以 PCL 格式加载文档 |
| [PDFXML](#PDFXML) | 内部 PDF 文档结构采用 XML 格式。 |
| [PS](#PS) | 表示以 PS 格式加载文档（PostScript 文档的格式） |
| [RTF](#RTF) | 表示以 RTF 格式加载文档。 |
| [SVG](#SVG) | 表示以 SVG 格式加载文档——Latex 文本编辑器的格式 |
| [TEX](#TEX) | 表示以 TEX 格式加载文档——Latex 文本编辑器的格式 |
| [TXT](#TXT) | 表示文档以 TXT 格式加载。 |
| [XML](#XML) | 表示以 XML 格式加载文档（表示 PDF 文档逻辑结构的特殊 XML） |
| [XPS](#XPS) | 表示以 XPS 格式加载文档 |
| [XSLFO](#XSLFO) | 表示以 XSLFO 格式加载文档 |

## 方法

| 方法 | 描述 |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 返回此类型中具有指定名称的枚举常量。 |
| [values](#values--) | 返回一个数组，包含此枚举类型的常量，按声明顺序排列。 |

### APS {#APS}
```
public static final LoadFormat APS
```

表示以 APS 格式加载文档。

### CDR {#CDR}
```
public static final LoadFormat CDR
```

表示以 CDR 格式加载文档。

### CGM {#CGM}
```
public static final LoadFormat CGM
```

表示以 CGM 格式加载文档

### DJVU {#DJVU}
```
public static final LoadFormat DJVU
```

表示以 Djvu 格式加载文档。

### EPUB {#EPUB}
```
public static final LoadFormat EPUB
```

表示以 EPUB 格式加载文档（电子书的特殊格式）

### HTML {#HTML}
```
public static final LoadFormat HTML
```

表示以 HTML 格式加载文档

### MD {#MD}
```
public static final LoadFormat MD
```

表示文档以 MD 格式（Markdown）加载。

### MHT {#MHT}
```
public static final LoadFormat MHT
```

表示以 MHT 格式加载文档（即打包的 HTML 格式）

### OFD {#OFD}
```
public static final LoadFormat OFD
```

表示以 OFD 格式加载文档。

### PCL {#PCL}
```
public static final LoadFormat PCL
```

表示以 PCL 格式加载文档

### PDFXML {#PDFXML}
```
public static final LoadFormat PDFXML
```

内部 PDF 文档结构采用 XML 格式。

### PS {#PS}
```
public static final LoadFormat PS
```

表示以 PS 格式加载文档（PostScript 文档的格式）

### RTF {#RTF}
```
public static final LoadFormat RTF
```

表示以 RTF 格式加载文档。

### SVG {#SVG}
```
public static final LoadFormat SVG
```

表示以 SVG 格式加载文档——Latex 文本编辑器的格式

### TEX {#TEX}
```
public static final LoadFormat TEX
```

表示以 TEX 格式加载文档——Latex 文本编辑器的格式

### TXT {#TXT}
```
public static final LoadFormat TXT
```

表示文档以 TXT 格式加载。

### XML {#XML}
```
public static final LoadFormat XML
```

表示以 XML 格式加载文档（表示 PDF 文档逻辑结构的特殊 XML）

### XPS {#XPS}
```
public static final LoadFormat XPS
```

表示以 XPS 格式加载文档

### XSLFO {#XSLFO}
```
public static final LoadFormat XSLFO
```

表示以 XSLFO 格式加载文档

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
返回此类型中具有指定名称的枚举常量。

### values {#values--}
```
public static LoadFormat [] values()
```

返回一个数组，包含此枚举类型的常量，按声明顺序排列。

**Returns:**
一个数组，包含此枚举类型的常量，按声明顺序排列
