---
title: "CollectionFieldSubtype"
linktitle: "CollectionFieldSubtype"
second_title: "Aspose.PDF for Java API 参考"
description: "表示模式集合中字段的子类型参数。"
type: docs
weight: 630
url: /zh/java/com.aspose.pdf/collectionfieldsubtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.Enum, com.aspose.pdf.CollectionFieldSubtype

```
public final class CollectionFieldSubtype extends com.aspose.ms.System.Enum
```

表示模式集合中字段的子类型参数。

## 字段

| 字段 | 描述 |
| --- | --- |
| [CompressedSize](#CompressedSize) | (PDF 2.0) 字段数据是嵌入文件流的长度，由嵌入文件流字典中的 Length 条目标识，并且这两个值应当相同。 |
| [CreationDate](#CreationDate) | 字段数据应为嵌入文件流的创建日期，由嵌入文件中的 CreationDate 条目标识 |
| [D](#D) | 日期类型。字段数据应存储为 PDF 日期字符串。 |
| [Desc](#Desc) | 字段数据应为嵌入文件流的文件名，由文件规范的 UF 条目标识（如果存在）；否则由文件规范的 F 条目标识 |
| [F](#F) | 字段数据应为嵌入文件流的文件名，由文件规范的 UF 条目标识（如果存在）；否则由文件规范的 F 条目标识 |
| [ModDate](#ModDate) | 字段数据应为嵌入文件流的修改日期，由嵌入文件参数字典中的 ModDate 条目标识。 |
| [N](#N) | 数字类型。字段数据应存储为 PDF 数字。 |
| [None](#None) | 子类型未定义。 |
| [S](#S) | 文本类型。字段数据应存储为 PDF 文本字符串。 |
| [Size](#Size) | 字段数据应为嵌入文件的大小，由嵌入文件参数字典中的 Size 条目标识 |

### CompressedSize {#CompressedSize}
```
public static final int CompressedSize
```

(PDF 2.0) 字段数据是嵌入文件流的长度，由嵌入文件流字典中的 Length 条目标识，并且这两个值应当相同。

### CreationDate {#CreationDate}
```
public static final int CreationDate
```

字段数据应为嵌入文件流的创建日期，由嵌入文件中的 CreationDate 条目标识

### D {#D}
```
public static final int D
```

日期类型。字段数据应存储为 PDF 日期字符串。

### Desc {#Desc}
```
public static final int Desc
```

字段数据应为嵌入文件流的文件名，由文件规范的 UF 条目标识（如果存在）；否则由文件规范的 F 条目标识

### F {#F}
```
public static final int F
```

字段数据应为嵌入文件流的文件名，由文件规范的 UF 条目标识（如果存在）；否则由文件规范的 F 条目标识

### ModDate {#ModDate}
```
public static final int ModDate
```

字段数据应为嵌入文件流的修改日期，由嵌入文件参数字典中的 ModDate 条目标识。

### N {#N}
```
public static final int N
```

数字类型。字段数据应存储为 PDF 数字。

### None {#None}
```
public static final int None
```

子类型未定义。

### S {#S}
```
public static final int S
```

文本类型。字段数据应存储为 PDF 文本字符串。

### Size {#Size}
```
public static final int Size
```

字段数据应为嵌入文件的大小，由嵌入文件参数字典中的 Size 条目标识
