---
title: "CrashReportOptions"
linktitle: "CrashReportOptions"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "クラッシュレポート生成のオプションです。"
type: docs
weight: 50
url: /ja/java/com.aspose.pdf.exceptions/crashreportoptions/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.exceptions.CrashReportOptions

```
public class CrashReportOptions extends Object
```

クラッシュレポート生成のオプションです。

## コンストラクタ

| コンストラクタ | 説明 |
| --- | --- |
| [CrashReportOptions](#CrashReportOptions-com.aspose.ms.System.Exception-) | デフォルトパラメーターで CrashReportOptions を作成します。 |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getApplicationTitle](#getApplicationTitle--) | 例外が発生したライブラリの名前。 |
| [getCrashReportDirectory](#getCrashReportDirectory--) | クラッシュレポートの出力ディレクトリです。デフォルトでは現在のディレクトリに設定されます。 |
| [getCrashReportFilename](#getCrashReportFilename--) | クラッシュレポートのファイル名です。デフォルトでは、CrashReport_date_ticks.html 形式で自動生成されます。 |
| [getCrashReportPath](#getCrashReportPath--) | クラッシュレポートファイルのフルパス |
| [getCustomMessage](#getCustomMessage--) | レポートに含めるカスタムメッセージです。変数の値や送信したいその他の詳細情報などが考えられます。 |
| [getException](#getException--) | クラッシュレポートの基になる例外 |
| [getLibraryVersion](#getLibraryVersion--) | 使用されているライブラリのバージョン。 |
| [setCrashReportDirectory](#setCrashReportDirectory-java.lang.String-) | クラッシュレポートの出力ディレクトリです。デフォルトでは現在のディレクトリに設定されます。 |
| [setCrashReportFilename](#setCrashReportFilename-java.lang.String-) | クラッシュレポートのファイル名です。デフォルトでは、CrashReport_date_ticks.html 形式で自動生成されます。 |
| [setCustomMessage](#setCustomMessage-java.lang.String-) | レポートに含めるカスタムメッセージです。変数の値や送信したいその他の詳細情報などが考えられます。 |

### CrashReportOptions {#CrashReportOptions-com.aspose.ms.System.Exception-}
デフォルトパラメーターで CrashReportOptions を作成します。

### getApplicationTitle {#getApplicationTitle--}
```
public final String getApplicationTitle()
```

例外が発生したライブラリの名前。

**Returns:**
文字列値

### getCrashReportDirectory {#getCrashReportDirectory--}
```
public final String getCrashReportDirectory()
```

クラッシュレポートの出力ディレクトリです。デフォルトでは現在のディレクトリに設定されます。

**Returns:**
文字列値

### getCrashReportFilename {#getCrashReportFilename--}
```
public final String getCrashReportFilename()
```

クラッシュレポートのファイル名です。デフォルトでは、CrashReport_date_ticks.html 形式で自動生成されます。

**Returns:**
文字列値

### getCrashReportPath {#getCrashReportPath--}
```
public final String getCrashReportPath()
```

クラッシュレポートファイルのフルパス

**Returns:**
文字列値

### getCustomMessage {#getCustomMessage--}
```
public final String getCustomMessage()
```

レポートに含めるカスタムメッセージです。変数の値や送信したいその他の詳細情報などが考えられます。

**Returns:**
文字列値

### getException {#getException--}
```
public final com.aspose.ms.System.Exception getException()
```

クラッシュレポートの基になる例外

**Returns:**
例外インスタンス

### getLibraryVersion {#getLibraryVersion--}
```
public final String getLibraryVersion()
```

使用されているライブラリのバージョン。

**Returns:**
文字列値

### setCrashReportDirectory {#setCrashReportDirectory-java.lang.String-}
クラッシュレポートの出力ディレクトリです。デフォルトでは現在のディレクトリに設定されます。

### setCrashReportFilename {#setCrashReportFilename-java.lang.String-}
クラッシュレポートのファイル名です。デフォルトでは、CrashReport_date_ticks.html 形式で自動生成されます。

### setCustomMessage {#setCustomMessage-java.lang.String-}
レポートに含めるカスタムメッセージです。変数の値や送信したいその他の詳細情報などが考えられます。
