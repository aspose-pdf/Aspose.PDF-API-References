---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "この列挙は {@code IWarningCallback.Warning(WarningInfo)} メソッドを呼び出す場合のプログラムワークフローアクションを表します。"
type: docs
weight: 4240
url: /ja/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

この列挙は {@code IWarningCallback.Warning(WarningInfo)} メソッドを呼び出す場合のプログラムワークフローアクションを表します。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [Abort](#Abort) | フローの中止に使用されます。 |
| [Continue](#Continue) | フローの継続に使用されます。 |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | 指定された名前でこの型の列挙定数を返します。 |
| [values](#values--) | 宣言された順序でこの列挙型の定数を含む配列を返します。 |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

フローの中止に使用されます。

### Continue {#Continue}
```
public static final ReturnAction Continue
```

フローの継続に使用されます。

### valueOf {#valueOf-java.lang.String-}
指定された名前でこの型の列挙定数を返します。

### values {#values--}
```
public static ReturnAction [] values()
```

宣言された順序でこの列挙型の定数を含む配列を返します。

**Returns:**
宣言された順序でこの列挙型の定数を含む配列
