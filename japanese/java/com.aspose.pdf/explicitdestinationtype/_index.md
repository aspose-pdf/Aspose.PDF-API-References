---
title: "ExplicitDestinationType"
linktitle: "ExplicitDestinationType"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "明示的なデスティネーションのタイプを列挙します。"
type: docs
weight: 1290
url: /ja/java/com.aspose.pdf/explicitdestinationtype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ExplicitDestinationType > com.aspose.pdf.ExplicitDestinationType, java.lang.Enum < ExplicitDestinationType >, com.aspose.pdf.ExplicitDestinationType

**All Implemented Interfaces:**
Serializable, Comparable < ExplicitDestinationType >

```
public enum ExplicitDestinationType extends Enum < ExplicitDestinationType >
```

明示的なデスティネーションのタイプを列挙します。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [Fit](#Fit) | ページを表示し、内容をウィンドウ内にページ全体が水平・垂直の両方で収まるようにちょうど拡大します。 |
| [FitB](#FitB) | ページを表示し、内容をウィンドウ内にバウンディングボックスが水平・垂直の両方で完全に収まるようにちょうど拡大します。 |
| [FitBH](#FitBH) | ページを表示し、垂直座標 top をウィンドウの上端に配置し、内容をバウンディングボックスの幅全体がウィンドウ内に収まるようにちょうど拡大します。 |
| [FitBV](#FitBV) | ページを表示し、水平座標 left をウィンドウの左端に配置し、内容をバウンディングボックスの高さ全体がウィンドウ内に収まるようにちょうど拡大します。 |
| [FitH](#FitH) | ページを表示し、垂直座標 top をウィンドウの上端に配置し、内容をページの幅全体がウィンドウ内に収まるようにちょうど拡大します。 |
| [FitR](#FitR) | ページを表示し、内容を左、下、右、topentirely の座標で指定された矩形がウィンドウ内に水平・垂直の両方で完全に収まるようにちょうど拡大します。 |
| [FitV](#FitV) | ページを表示し、水平座標 left をウィンドウの左端に配置し、内容をページの高さ全体がウィンドウ内に収まるようにちょうど拡大します。 |
| [XYZ](#XYZ) | ページを表示し、座標 (left, top) をウィンドウの左上隅に配置し、内容をズーム係数で拡大します。 |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 指定された名前でこの型の列挙定数を返します。 |
| [values](#values--) | 宣言された順序でこの列挙型の定数を含む配列を返します。 |

### Fit {#Fit}
```
public static final ExplicitDestinationType Fit
```

ページを表示し、内容をウィンドウ内にページ全体が水平・垂直の両方で収まるようにちょうど拡大します。

### FitB {#FitB}
```
public static final ExplicitDestinationType FitB
```

ページを表示し、内容をウィンドウ内にバウンディングボックスが水平・垂直の両方で完全に収まるようにちょうど拡大します。

### FitBH {#FitBH}
```
public static final ExplicitDestinationType FitBH
```

ページを表示し、垂直座標 top をウィンドウの上端に配置し、内容をバウンディングボックスの幅全体がウィンドウ内に収まるようにちょうど拡大します。

### FitBV {#FitBV}
```
public static final ExplicitDestinationType FitBV
```

ページを表示し、水平座標 left をウィンドウの左端に配置し、内容をバウンディングボックスの高さ全体がウィンドウ内に収まるようにちょうど拡大します。

### FitH {#FitH}
```
public static final ExplicitDestinationType FitH
```

ページを表示し、垂直座標 top をウィンドウの上端に配置し、内容をページの幅全体がウィンドウ内に収まるようにちょうど拡大します。

### FitR {#FitR}
```
public static final ExplicitDestinationType FitR
```

ページを表示し、内容を左、下、右、topentirely の座標で指定された矩形がウィンドウ内に水平・垂直の両方で完全に収まるようにちょうど拡大します。

### FitV {#FitV}
```
public static final ExplicitDestinationType FitV
```

ページを表示し、水平座標 left をウィンドウの左端に配置し、内容をページの高さ全体がウィンドウ内に収まるようにちょうど拡大します。

### XYZ {#XYZ}
```
public static final ExplicitDestinationType XYZ
```

ページを表示し、座標 (left, top) をウィンドウの左上隅に配置し、内容をズーム係数で拡大します。

### getByValue {#getByValue-int-}
```
public static ExplicitDestinationType getByValue(int value)
```



**Parameters:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| 値 |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
指定された名前でこの型の列挙定数を返します。

### values {#values--}
```
public static ExplicitDestinationType [] values()
```

宣言された順序でこの列挙型の定数を含む配列を返します。

**Returns:**
宣言された順序でこの列挙型の定数を含む配列
