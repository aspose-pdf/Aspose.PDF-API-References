---
title: "BlendMode"
linktitle: "BlendMode"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "ブレンドモードの列挙です。"
type: docs
weight: 330
url: /ja/java/com.aspose.pdf/blendmode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < BlendMode > com.aspose.pdf.BlendMode, java.lang.Enum < BlendMode >, com.aspose.pdf.BlendMode

**All Implemented Interfaces:**
Serializable, Comparable < BlendMode >

```
public enum BlendMode extends Enum < BlendMode >
```

ブレンドモードの列挙です。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [Color](#Color) | カラー ブレンドモード。 |
| [ColorBurn](#ColorBurn) | カラー バーン ブレンドモード。 |
| [ColorDodge](#ColorDodge) | カラー ドッジ ブレンドモード。 |
| [Compatible](#Compatible) | 互換ブレンドモード。 |
| [Darken](#Darken) | 暗くするブレンドモード。 |
| [Difference](#Difference) | 差分ブレンドモード。 |
| [Exclusion](#Exclusion) | 除外ブレンドモード。 |
| [HardLight](#HardLight) | ハードライトブレンドモード。 |
| [Hue](#Hue) | 色相ブレンドモード。 |
| [Lighten](#Lighten) | 明るくするブレンドモード。 |
| [Luminosity](#Luminosity) | 輝度ブレンドモード。 |
| [Multiply](#Multiply) | 乗算ブレンドモード。 |
| [Normal](#Normal) | 通常ブレンドモード。 |
| [Overlay](#Overlay) | オーバーレイブレンドモード。 |
| [Saturation](#Saturation) | 彩度ブレンドモード。 |
| [Screen](#Screen) | スクリーンブレンドモード。 |
| [SoftLight](#SoftLight) | ソフトライトブレンドモード。 |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 指定された名前でこの型の列挙定数を返します。 |
| [values](#values--) | 宣言された順序でこの列挙型の定数を含む配列を返します。 |

### Color {#Color}
```
public static final BlendMode Color
```

カラー ブレンドモード。

### ColorBurn {#ColorBurn}
```
public static final BlendMode ColorBurn
```

カラー バーン ブレンドモード。

### ColorDodge {#ColorDodge}
```
public static final BlendMode ColorDodge
```

カラー ドッジ ブレンドモード。

### Compatible {#Compatible}
```
public static final BlendMode Compatible
```

互換ブレンドモード。

### Darken {#Darken}
```
public static final BlendMode Darken
```

暗くするブレンドモード。

### Difference {#Difference}
```
public static final BlendMode Difference
```

差分ブレンドモード。

### Exclusion {#Exclusion}
```
public static final BlendMode Exclusion
```

除外ブレンドモード。

### HardLight {#HardLight}
```
public static final BlendMode HardLight
```

ハードライトブレンドモード。

### Hue {#Hue}
```
public static final BlendMode Hue
```

色相ブレンドモード。

### Lighten {#Lighten}
```
public static final BlendMode Lighten
```

明るくするブレンドモード。

### Luminosity {#Luminosity}
```
public static final BlendMode Luminosity
```

輝度ブレンドモード。

### Multiply {#Multiply}
```
public static final BlendMode Multiply
```

乗算ブレンドモード。

### Normal {#Normal}
```
public static final BlendMode Normal
```

通常ブレンドモード。

### Overlay {#Overlay}
```
public static final BlendMode Overlay
```

オーバーレイブレンドモード。

### Saturation {#Saturation}
```
public static final BlendMode Saturation
```

彩度ブレンドモード。

### Screen {#Screen}
```
public static final BlendMode Screen
```

スクリーンブレンドモード。

### SoftLight {#SoftLight}
```
public static final BlendMode SoftLight
```

ソフトライトブレンドモード。

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
指定された名前でこの型の列挙定数を返します。

### values {#values--}
```
public static BlendMode [] values()
```

宣言された順序でこの列挙型の定数を含む配列を返します。

**Returns:**
宣言された順序でこの列挙型の定数を含む配列
