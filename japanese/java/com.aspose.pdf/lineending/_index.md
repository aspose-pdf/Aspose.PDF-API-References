---
title: "LineEnding"
linktitle: "LineEnding"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "線を描画する際に使用されるラインエンドスタイルを列挙します。"
type: docs
weight: 2720
url: /ja/java/com.aspose.pdf/lineending/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LineEnding > com.aspose.pdf.LineEnding, java.lang.Enum < LineEnding >, com.aspose.pdf.LineEnding

**All Implemented Interfaces:**
Serializable, Comparable < LineEnding >

```
public enum LineEnding extends Enum < LineEnding >
```

線を描画する際に使用されるラインエンドスタイルを列挙します。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [Butt](#Butt) | 線自体に対して垂直な端点に短い線です。 |
| [Circle](#Circle) | 注釈の内部色で塗りつぶされた円（存在する場合）。 |
| [ClosedArrow](#ClosedArrow) | OpenArrow スタイルのように鋭角で交わる2本の短い線と、3本目の線で接続されて三角形の閉じた矢じりを形成し、注釈の内部色で塗りつぶされたもの。 |
| [Diamond](#Diamond) | 注釈の内部色で塗りつぶされたダイヤモンド形（存在する場合）。 |
| [None](#None) | 線の終端なし。 |
| [OpenArrow](#OpenArrow) | 鋭角で交わる2本の短い線で開いた矢じりを形成します。 |
| [RClosedArrow](#RClosedArrow) | ClosedArrow の逆方向にある三角形の閉じた矢じりです。 |
| [ROpenArrow](#ROpenArrow) | OpenArrow の逆方向にある2本の短い線です。 |
| [Slash](#Slash) | 線自体に対して垂直から約30度時計回りに位置する端点に短い線があります。 |
| [Square](#Square) | 注釈の内部色で塗りつぶされた正方形（存在する場合）。 |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 指定された名前でこの型の列挙定数を返します。 |
| [values](#values--) | 宣言された順序でこの列挙型の定数を含む配列を返します。 |

### Butt {#Butt}
```
public static final LineEnding Butt
```

線自体に対して垂直な端点に短い線です。

### Circle {#Circle}
```
public static final LineEnding Circle
```

注釈の内部色で塗りつぶされた円（存在する場合）。

### ClosedArrow {#ClosedArrow}
```
public static final LineEnding ClosedArrow
```

OpenArrow スタイルのように鋭角で交わる2本の短い線と、3本目の線で接続されて三角形の閉じた矢じりを形成し、注釈の内部色で塗りつぶされたもの。

### Diamond {#Diamond}
```
public static final LineEnding Diamond
```

注釈の内部色で塗りつぶされたダイヤモンド形（存在する場合）。

### None {#None}
```
public static final LineEnding None
```

線の終端なし。

### OpenArrow {#OpenArrow}
```
public static final LineEnding OpenArrow
```

鋭角で交わる2本の短い線で開いた矢じりを形成します。

### RClosedArrow {#RClosedArrow}
```
public static final LineEnding RClosedArrow
```

ClosedArrow の逆方向にある三角形の閉じた矢じりです。

### ROpenArrow {#ROpenArrow}
```
public static final LineEnding ROpenArrow
```

OpenArrow の逆方向にある2本の短い線です。

### Slash {#Slash}
```
public static final LineEnding Slash
```

線自体に対して垂直から約30度時計回りに位置する端点に短い線があります。

### Square {#Square}
```
public static final LineEnding Square
```

注釈の内部色で塗りつぶされた正方形（存在する場合）。

### getByValue {#getByValue-int-}
```
public static LineEnding getByValue(int value)
```



**Parameters:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| 値 |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
指定された名前でこの型の列挙定数を返します。

### values {#values--}
```
public static LineEnding [] values()
```

宣言された順序でこの列挙型の定数を含む配列を返します。

**Returns:**
宣言された順序でこの列挙型の定数を含む配列
