---
title: "CollectionFieldSubtype"
linktitle: "CollectionFieldSubtype"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "スキーマコレクション内のフィールドのサブタイプパラメータを表します。"
type: docs
weight: 630
url: /ja/java/com.aspose.pdf/collectionfieldsubtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.Enum, com.aspose.pdf.CollectionFieldSubtype

```
public final class CollectionFieldSubtype extends com.aspose.ms.System.Enum
```

スキーマコレクション内のフィールドのサブタイプパラメータを表します。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [CompressedSize](#CompressedSize) | (PDF 2.0) フィールドデータは埋め込みファイルストリームの長さであり、埋め込みファイルストリーム辞書の Length エントリで識別され、2 つの値は同一でなければなりません。 |
| [CreationDate](#CreationDate) | フィールドデータは埋め込みファイルストリームの作成日であり、埋め込みファイルの CreationDate エントリで識別されます。 |
| [D](#D) | 日付型です。フィールドデータは PDF 日付文字列として保存されます。 |
| [Desc](#Desc) | フィールドデータは埋め込みファイルストリームのファイル名であり、ファイル仕様の UF エントリで識別されます（存在する場合）。存在しない場合はファイル仕様の F エントリで識別されます。 |
| [F](#F) | フィールドデータは埋め込みファイルストリームのファイル名であり、ファイル仕様の UF エントリで識別されます（存在する場合）。存在しない場合はファイル仕様の F エントリで識別されます。 |
| [ModDate](#ModDate) | フィールドデータは埋め込みファイルストリームの更新日であり、埋め込みファイルパラメータ辞書の ModDate エントリで識別されます。 |
| [N](#N) | 数値型です。フィールドデータは PDF 数値として保存されます。 |
| [None](#None) | サブタイプは定義されていません。 |
| [S](#S) | テキスト型です。フィールドデータは PDF テキスト文字列として保存されます。 |
| [Size](#Size) | フィールドデータは埋め込みファイルのサイズであり、埋め込みファイルパラメータ辞書の Size エントリで識別されます。 |

### CompressedSize {#CompressedSize}
```
public static final int CompressedSize
```

(PDF 2.0) フィールドデータは埋め込みファイルストリームの長さであり、埋め込みファイルストリーム辞書の Length エントリで識別され、2 つの値は同一でなければなりません。

### CreationDate {#CreationDate}
```
public static final int CreationDate
```

フィールドデータは埋め込みファイルストリームの作成日であり、埋め込みファイルの CreationDate エントリで識別されます。

### D {#D}
```
public static final int D
```

日付型です。フィールドデータは PDF 日付文字列として保存されます。

### Desc {#Desc}
```
public static final int Desc
```

フィールドデータは埋め込みファイルストリームのファイル名であり、ファイル仕様の UF エントリで識別されます（存在する場合）。存在しない場合はファイル仕様の F エントリで識別されます。

### F {#F}
```
public static final int F
```

フィールドデータは埋め込みファイルストリームのファイル名であり、ファイル仕様の UF エントリで識別されます（存在する場合）。存在しない場合はファイル仕様の F エントリで識別されます。

### ModDate {#ModDate}
```
public static final int ModDate
```

フィールドデータは埋め込みファイルストリームの更新日であり、埋め込みファイルパラメータ辞書の ModDate エントリで識別されます。

### N {#N}
```
public static final int N
```

数値型です。フィールドデータは PDF 数値として保存されます。

### None {#None}
```
public static final int None
```

サブタイプは定義されていません。

### S {#S}
```
public static final int S
```

テキスト型です。フィールドデータは PDF テキスト文字列として保存されます。

### Size {#Size}
```
public static final int Size
```

フィールドデータは埋め込みファイルのサイズであり、埋め込みファイルパラメータ辞書の Size エントリで識別されます。
