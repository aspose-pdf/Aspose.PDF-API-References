---
title: "LoadFormat"
linktitle: "LoadFormat"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "ロード形式を指定します。"
type: docs
weight: 2780
url: /ja/java/com.aspose.pdf/loadformat/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LoadFormat > com.aspose.pdf.LoadFormat, java.lang.Enum < LoadFormat >, com.aspose.pdf.LoadFormat

**All Implemented Interfaces:**
Serializable, Comparable < LoadFormat >

```
public enum LoadFormat extends Enum < LoadFormat >
```

ロード形式を指定します。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [APS](#APS) | APS 形式のドキュメントを読み込むことを意味します。 |
| [CDR](#CDR) | CDR 形式のドキュメントを読み込むことを意味します。 |
| [CGM](#CGM) | CGM 形式のドキュメントを読み込むことを意味します。 |
| [DJVU](#DJVU) | Djvu 形式のドキュメントを読み込むことを意味します。 |
| [EPUB](#EPUB) | EPUB 形式（電子書籍の特殊形式）のドキュメントを読み込むことを意味します。 |
| [HTML](#HTML) | HTML 形式のドキュメントを読み込むことを意味します。 |
| [MD](#MD) | MD 形式（Markdown）のドキュメントを読み込むことを意味します。 |
| [MHT](#MHT) | MHT 形式（圧縮された HTML 形式）のドキュメントを読み込むことを意味します。 |
| [OFD](#OFD) | OFD 形式のドキュメントを読み込むことを意味します。 |
| [PCL](#PCL) | PCL 形式のドキュメントを読み込むことを意味します。 |
| [PDFXML](#PDFXML) | XML 形式の内部 PDF ドキュメント構造です。 |
| [PS](#PS) | PS 形式（PostScript ドキュメントの形式）のドキュメントを読み込むことを意味します。 |
| [RTF](#RTF) | RTF 形式のドキュメントを読み込むことを意味します。 |
| [SVG](#SVG) | SVG 形式（LaTeX テキストエディタの形式）のドキュメントを読み込むことを意味します。 |
| [TEX](#TEX) | TEX 形式（LaTeX テキストエディタの形式）のドキュメントを読み込むことを意味します。 |
| [TXT](#TXT) | TXT 形式のドキュメントを読み込むことを意味します。 |
| [XML](#XML) | XML 形式（PDF ドキュメントの論理構造を表す特殊な XML）のドキュメントを読み込むことを意味します。 |
| [XPS](#XPS) | XPS 形式のドキュメントの読み込みを意味します |
| [XSLFO](#XSLFO) | XSLFO 形式のドキュメントの読み込みを意味します |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 指定された名前でこの型の列挙定数を返します。 |
| [values](#values--) | 宣言された順序でこの列挙型の定数を含む配列を返します。 |

### APS {#APS}
```
public static final LoadFormat APS
```

APS 形式のドキュメントを読み込むことを意味します。

### CDR {#CDR}
```
public static final LoadFormat CDR
```

CDR 形式のドキュメントを読み込むことを意味します。

### CGM {#CGM}
```
public static final LoadFormat CGM
```

CGM 形式のドキュメントを読み込むことを意味します。

### DJVU {#DJVU}
```
public static final LoadFormat DJVU
```

Djvu 形式のドキュメントを読み込むことを意味します。

### EPUB {#EPUB}
```
public static final LoadFormat EPUB
```

EPUB 形式（電子書籍の特殊形式）のドキュメントを読み込むことを意味します。

### HTML {#HTML}
```
public static final LoadFormat HTML
```

HTML 形式のドキュメントを読み込むことを意味します。

### MD {#MD}
```
public static final LoadFormat MD
```

MD 形式（Markdown）のドキュメントを読み込むことを意味します。

### MHT {#MHT}
```
public static final LoadFormat MHT
```

MHT 形式（圧縮された HTML 形式）のドキュメントを読み込むことを意味します。

### OFD {#OFD}
```
public static final LoadFormat OFD
```

OFD 形式のドキュメントを読み込むことを意味します。

### PCL {#PCL}
```
public static final LoadFormat PCL
```

PCL 形式のドキュメントを読み込むことを意味します。

### PDFXML {#PDFXML}
```
public static final LoadFormat PDFXML
```

XML 形式の内部 PDF ドキュメント構造です。

### PS {#PS}
```
public static final LoadFormat PS
```

PS 形式（PostScript ドキュメントの形式）のドキュメントを読み込むことを意味します。

### RTF {#RTF}
```
public static final LoadFormat RTF
```

RTF 形式のドキュメントを読み込むことを意味します。

### SVG {#SVG}
```
public static final LoadFormat SVG
```

SVG 形式（LaTeX テキストエディタの形式）のドキュメントを読み込むことを意味します。

### TEX {#TEX}
```
public static final LoadFormat TEX
```

TEX 形式（LaTeX テキストエディタの形式）のドキュメントを読み込むことを意味します。

### TXT {#TXT}
```
public static final LoadFormat TXT
```

TXT 形式のドキュメントを読み込むことを意味します。

### XML {#XML}
```
public static final LoadFormat XML
```

XML 形式（PDF ドキュメントの論理構造を表す特殊な XML）のドキュメントを読み込むことを意味します。

### XPS {#XPS}
```
public static final LoadFormat XPS
```

XPS 形式のドキュメントの読み込みを意味します

### XSLFO {#XSLFO}
```
public static final LoadFormat XSLFO
```

XSLFO 形式のドキュメントの読み込みを意味します

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
指定された名前でこの型の列挙定数を返します。

### values {#values--}
```
public static LoadFormat [] values()
```

宣言された順序でこの列挙型の定数を含む配列を返します。

**Returns:**
宣言された順序でこの列挙型の定数を含む配列
