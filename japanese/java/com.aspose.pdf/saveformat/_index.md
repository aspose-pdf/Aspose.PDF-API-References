---
title: "SaveFormat"
linktitle: "SaveFormat"
second_title: "Java 用 Aspose.PDF API リファレンス"
description: "形式を指定します"
type: docs
weight: 4360
url: /ja/java/com.aspose.pdf/saveformat/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < SaveFormat > com.aspose.pdf.SaveFormat, java.lang.Enum < SaveFormat >, com.aspose.pdf.SaveFormat

**All Implemented Interfaces:**
Serializable, Comparable < SaveFormat >

```
public enum SaveFormat extends Enum < SaveFormat >
```

形式を指定します

## フィールド

| フィールド | 説明 |
| --- | --- |
| [Aps](#Aps) | APS XML ファイルとして保存しています。 |
| [Doc](#Doc) | DOC 形式で保存することを意味します |
| [DocX](#DocX) | DOCX 形式で保存することを意味します |
| [Eps](#Eps) | Encapsulated PostScript 形式で保存することを意味します。 |
| [Epub](#Epub) | EPUB 形式（電子書籍の特別な形式）で保存することを意味します |
| [Excel](#Excel) | MsExcel 形式で保存することを意味します |
| [Html](#Html) | HTML 形式で保存することを意味します |
| [Markdown](#Markdown) | Markdown 形式で保存することを意味します。 |
| [MobiXml](#MobiXml) | MobiXML 形式（電子書籍の特別な形式）で保存することを意味します |
| [None](#None) | 非推奨です。 |
| [Pdf](#Pdf) | 形式を変更せずに保存することを意味します、すなわち。 |
| [PdfXml](#PdfXml) | XML 形式の内部 PDF ドキュメント構造 |
| [Pptx](#Pptx) | MHT（Web アーカイブ）で保存することを意味します /// |
| [Ps](#Ps) | PostScript 形式で保存することを意味します。 |
| [Svg](#Svg) | SVG 形式で保存することを意味します |
| [TeX](#TeX) | TEX 形式で保存することを意味します（すなわち）。 |
| [Xml](#Xml) | XML 形式で保存することを意味します |
| [Xps](#Xps) | XPS 形式で保存することを意味します |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | 指定された名前でこの型の列挙定数を返します。 |
| [values](#values--) | 宣言された順序でこの列挙型の定数を含む配列を返します。 |

### Aps {#Aps}
```
public static final SaveFormat Aps
```

APS XML ファイルとして保存しています。

### Doc {#Doc}
```
public static final SaveFormat Doc
```

DOC 形式で保存することを意味します

### DocX {#DocX}
```
public static final SaveFormat DocX
```

DOCX 形式で保存することを意味します

### Eps {#Eps}
```
public static final SaveFormat Eps
```

Encapsulated PostScript 形式で保存することを意味します。

### Epub {#Epub}
```
public static final SaveFormat Epub
```

EPUB 形式（電子書籍の特別な形式）で保存することを意味します

### Excel {#Excel}
```
public static final SaveFormat Excel
```

MsExcel 形式で保存することを意味します

### Html {#Html}
```
public static final SaveFormat Html
```

HTML 形式で保存することを意味します

### Markdown {#Markdown}
```
public static final SaveFormat Markdown
```

Markdown 形式で保存することを意味します。

### MobiXml {#MobiXml}
```
public static final SaveFormat MobiXml
```

MobiXML 形式（電子書籍の特別な形式）で保存することを意味します

### None {#None}
```
@Deprecated public static final SaveFormat None
```

非推奨です。

### Pdf {#Pdf}
```
public static final SaveFormat Pdf
```

形式を変更せずに保存することを意味します、すなわち。

### PdfXml {#PdfXml}
```
public static final SaveFormat PdfXml
```

XML 形式の内部 PDF ドキュメント構造

### Pptx {#Pptx}
```
public static final SaveFormat Pptx
```

MHT（Web アーカイブ）で保存することを意味します ///

### Ps {#Ps}
```
public static final SaveFormat Ps
```

PostScript 形式で保存することを意味します。

### Svg {#Svg}
```
public static final SaveFormat Svg
```

SVG 形式で保存することを意味します

### TeX {#TeX}
```
public static final SaveFormat TeX
```

TEX 形式で保存することを意味します（すなわち）。

### Xml {#Xml}
```
public static final SaveFormat Xml
```

XML 形式で保存することを意味します

### Xps {#Xps}
```
public static final SaveFormat Xps
```

XPS 形式で保存することを意味します

### getByValue {#getByValue-int-}
```
public static SaveFormat getByValue(int value)
```



**Parameters:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| 値 |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
指定された名前でこの型の列挙定数を返します。

### values {#values--}
```
public static SaveFormat [] values()
```

宣言された順序でこの列挙型の定数を含む配列を返します。

**Returns:**
宣言された順序でこの列挙型の定数を含む配列
