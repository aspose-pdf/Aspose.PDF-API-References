---
title: "TiffDevice"
second_title: "Referencia de API de Aspose.PDF para Python vía .NET"
description: "Esta clase ayuda a guardar el documento pdf página por página en una única imagen tiff."
type: docs
weight: 150
url: /es/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Esta clase ayuda a guardar el documento pdf página por página en una única imagen tiff.

El tipo TiffDevice expone los siguientes miembros:
## Constructores
| Nombre | Descripción |
| :- | :- |
| TiffDevice(resolution) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(resolution, settings) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(resolution, settings, converter) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(settings) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(settings, converter) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice() | Inicializa una nueva instancia de la clase [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) con la configuración predeterminada. |
| TiffDevice(width, height, resolution, settings) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(width, height, resolution, settings, converter) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(page_size, resolution, settings) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(page_size, resolution, settings, converter) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(width, height, resolution) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(page_size, resolution) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(width, height, settings) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(width, height, settings, converter) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(page_size, settings, converter) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(page_size, settings) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(width, height) | Inicializa una nueva instancia de la clase TiffDevice |
| TiffDevice(page_size) | Inicializa una nueva instancia de la clase TiffDevice |
## Propiedades
| Nombre | Descripción |
| :- | :- |
| rendering_options | Obtiene o establece las opciones de renderizado. |
| form_presentation_mode | Obtiene o establece el modo de presentación del formulario. |
| configuraciones | Obtiene configuraciones para mapear pdf a imagen tiff. |
| resolution | Obtiene la resolución de la imagen. |
| ancho | Obtiene el ancho de salida de la imagen. |
| alto | Obtiene la altura de salida de la imagen. |
## Métodos
| Nombre | Descripción |
| :- | :- |
| process(document, from_page, to_page, output) | Convierte ciertas páginas del documento a tiff y las guarda en el flujo de salida. |
| process(document, output) | Convierte ciertas páginas del documento a tiff y las guarda en el flujo de salida. |
| process(document, output_file_name) | Convierte ciertas páginas del documento a tiff y las guarda en el flujo de salida. |
| process(document, from_page, to_page, output_file_name) | Convierte ciertas páginas del documento a tiff y las guarda en el flujo de salida. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Realiza binarización Bradley para el flujo de entrada. |

### Ver también

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

