---
title: "FormattedText"
second_title: "Referencia de API de Aspose.PDF para Python vía .NET"
description: "Clase que representa texto formateado. Contiene información sobre el texto y su color, tamaño y estilo."
type: docs
weight: 130
url: /es/python-net/aspose.pdf.facades/formattedtext/
---

## FormattedText class

Clase que representa texto formateado. Contiene información sobre el texto y su color, tamaño y estilo.

El tipo FormattedText expone los siguientes miembros:
## Constructores
| Nombre | Descripción |
| :- | :- |
| FormattedText() | Inicializa FormattedText. |
| FormattedText(text) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, font_color, font_style, encoding_type, embedded, text_size) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, font_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, color, text_font, text_encoding, embedded, text_size) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, back_color, text_font, encoding, embedded, text_size) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, back_color, font_name, text_encoding, embedded, font_size) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, back_color) | Inicializa una nueva instancia de la clase FormattedText |
| FormattedText(text, text_color, font_name, text_encoding, embedded, font_size) | Inicializa una nueva instancia de la clase FormattedText |
## Propiedades
| Nombre | Descripción |
| :- | :- |
| text_height | Obtiene la altura del texto. |
| text_width | Obtiene el ancho del texto. |
## Métodos
| Nombre | Descripción |
| :- | :- |
| add_new_line_text(new_line_text) | Añade una nueva línea al objeto FormattedText y establece newLineText al texto de la siguiente línea. |
| add_new_line_text(new_line_text, line_spacing) | Añade una nueva línea al objeto FormattedText y establece newLineText al texto de la siguiente línea. |
| is_cjk() | Comprueba si el texto es CJK (chino, japonés o coreano). |
| set_cjk_font_style() | Cambia el estilo de fuente de FormattedText para la fuente CJK (chino, japonés o coreano). |

### Ver también

* namespace [aspose.pdf.facades](/pdf/python-net/aspose.pdf.facades/)
* assembly [Aspose.PDF](/pdf/python-net/)

