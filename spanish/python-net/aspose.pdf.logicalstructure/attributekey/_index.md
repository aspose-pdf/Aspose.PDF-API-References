---
title: "AttributeKey"
second_title: "Referencia de API de Aspose.PDF para Python vía .NET"
description: "Representa claves de atributos estándar."
type: docs
weight: 40
url: /es/python-net/aspose.pdf.logicalstructure/attributekey/
---

## AttributeKey class

Representa claves de atributos estándar.

El tipo AttributeKey expone los siguientes miembros:
## Propiedades
| Nombre | Descripción |
| :- | :- |
| key | Obtener la clave de atributo. |
| owner | Obtener el propietario del atributo. |
| PLACEMENT | Atributo Placement (propietario del atributo Layout). |
| WRITING_MODE | Atributo WritingMode (propietario del atributo Layout). |
| BACKGROUND_COLOR | Atributo BackgroundColor (propietario del atributo Layout). |
| BORDER_COLOR | Atributo BorderColor (propietario del atributo Layout). |
| BORDER_STYLE | Atributo BorderStyle (propietario del atributo Layout). |
| BORDER_THICKNESS | Atributo BorderThickness (propietario del atributo Layout). |
| PADDING | Atributo Padding (propietario del atributo Layout). |
| COLOR | Atributo Color (propietario del atributo Layout). |
| SPACE_BEFORE | Atributo SpaceBefore (propietario del atributo Layout). |
| SPACE_AFTER | Atributo SpaceAfter (propietario del atributo Layout). |
| START_INDENT | Atributo StartIndent (propietario del atributo Layout). |
| END_INDENT | Atributo EndIndent (propietario del atributo Layout). |
| TEXT_INDENT | Atributo TextIndent (propietario del atributo Layout). |
| TEXT_ALIGN | Atributo TextAlign (propietario del atributo Layout). |
| B_BOX | Atributo BBox (propietario del atributo Layout). |
| WIDTH | Atributo Width (propietario del atributo Layout). |
| HEIGHT | Atributo Height (propietario del atributo Layout). |
| BLOCK_ALIGN | Atributo BlockAlign (propietario del atributo Layout). |
| INLINE_ALIGN | Atributo InlineAlign (propietario del atributo Layout). |
| T_BORDER_STYLE | Atributo TBorderStyle (propietario del atributo Layout). |
| T_PADDING | Atributo TPadding (propietario del atributo Layout). |
| BASELINE_SHIFT | Atributo BaselineShift (propietario del atributo Layout). |
| LINE_HEIGHT | Atributo LineHeight (propietario del atributo Layout). |
| TEXT_DECORATION_COLOR | Atributo TextDecorationColor (propietario del atributo Layout). |
| TEXT_DECORATION_THICKNESS | Atributo TextDecorationThickness (propietario del atributo Layout). |
| TEXT_DECORATION_TYPE | Atributo TextDecorationType (propietario del atributo Layout). |
| RUBY_ALIGN | Atributo RubyAlign (propietario del atributo Layout). |
| RUBY_POSITION | Atributo RubyPosition (propietario del atributo Layout). |
| GLYPH_ORIENTATION_VERTICAL | Atributo GlyphOrientationVertical (propietario del atributo Layout). |
| COLUMN_COUNT | Atributo ColumnCount (propietario del atributo Layout). |
| COLUMN_GAP | Atributo ColumnGap (propietario del atributo Layout). |
| COLUMN_WIDTHS | Atributo ColumnWidths (propietario del atributo Layout). |
| LIST_NUMBERING | Atributo ListNumbering (propietario del atributo List). |
| ROLE | Atributo Role (propietario del atributo PrintField). |
| CHECKED | Atributo Checked (propietario del atributo PrintField). |
| DESC | Atributo Desc (propietario del atributo PrintField). |
| ROW_SPAN | Atributo RowSpan (propietario del atributo Table). |
| COL_SPAN | Atributo ColSpan (propietario del atributo Table). |
| HEADERS | Atributo Headers (propietario del atributo Table). |
| SCOPE | Atributo Scope (propietario del atributo Table). |
| SUMMARY | Atributo Summary (propietario del atributo Table). |

### Ver también

* namespace [aspose.pdf.logicalstructure](/pdf/python-net/aspose.pdf.logicalstructure/)
* assembly [Aspose.PDF](/pdf/python-net/)

