---
title: "Espacio de nombres System::Net"
linktitle: "System::Net"
second_title: "Referencia de API de Aspose.PDF para C++"
description: "Cómo usar el espacio de nombres System::Net en C++."
type: docs
weight: 4900
url: /es/cpp/system.net/
---



## Clases

| Clase | Descripción |
| --- | --- |
| [Cookie](./cookie/) | Representa una cookie HTTP. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [CookieCollection](./cookiecollection/) | Representa una lista de cookies ordenadas. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [CookieComparer](./cookiecomparer/) | Se utiliza para comparar instancias de la clase [Cookie](./cookie/). Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [CookieContainer](./cookiecontainer/) | Proporciona un contenedor para las instancias de la clase CookieCollection. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [CookieParser](./cookieparser/) | Se utiliza para analizar una cabecera de cookie y crear una instancia de la clase [Cookie](./cookie/). Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [CredentialCache](./credentialcache/) | Proporciona el almacenamiento de credenciales. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [Dns](./dns/) | Proporciona métodos para trabajar con DNS. |
| [DnsEndPoint](./dnsendpoint/) | Contiene información utilizada por la aplicación para conectarse al servicio. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [EndPoint](./endpoint/) | La clase abstracta contiene una dirección de red. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [FileWebRequest](./filewebrequest/) | Proporciona la implementación de la clase abstracta [WebRequest](./webrequest/) para trabajar con el sistema de archivos. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [FileWebResponse](./filewebresponse/) | Proporciona la implementación de la clase abstracta [WebResponse](./webresponse/) para trabajar con el sistema de archivos. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [FtpWebRequest](./ftpwebrequest/) | Implementa un cliente del Protocolo de Transferencia de Archivos (FTP). Clase ficticia que permite enlazar el código traducido con referencias a [FtpWebRequest](./ftpwebrequest/), pero no lo ejecuta. No contiene miembros implementados correctamente. |
| [FtpWebResponse](./ftpwebresponse/) | Clase ficticia que permite enlazar el código traducido con referencias a [FtpWebResponse](./ftpwebresponse/), pero no lo ejecuta. No contiene miembros implementados correctamente. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [HeaderVariantInfo](./headervariantinfo/) | Se utiliza para coincidir el nombre de la cookie y la especificación. |
| [HttpKnownHeaderNames](./httpknownheadernames/) | Contiene los valores de cadena de los nombres de encabezado conocidos. |
| [HttpRequestHeaderExtensions](./httprequestheaderextensions/) | Contiene métodos utilitarios para trabajar con los valores de enumeración de [HttpRequestHeader](./httprequestheader/). |
| [HttpResponseHeaderExtensions](./httpresponseheaderextensions/) | Contiene métodos utilitarios para trabajar con los valores de enumeración de [HttpResponseHeader](./httpresponseheader/). |
| [HttpStatusDescription](./httpstatusdescription/) | Contiene los métodos utilitarios para obtener la representación en cadena del estado HTTP. |
| [HttpVersion](./httpversion/) | Contiene versiones de HTTP. |
| [HttpWebRequest](./httpwebrequest/) | Representa la solicitud web HTTP. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [HttpWebResponse](./httpwebresponse/) | Representa la respuesta web HTTP. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [ICredentials](./icredentials/) | Proporciona la interfaz de autenticación. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [ICredentialsByHost](./icredentialsbyhost/) | Proporciona la interfaz de autenticación para obtener credenciales para un host, puerto y tipo de autenticación. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [IPAddress](./ipaddress/) | Representa la dirección IP. Los objetos de esta clase solo deben asignarse usando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que resultará en errores de tiempo de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [IPEndPoint](./ipendpoint/) | Representa un punto final de red que contiene una dirección IP y un puerto. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [IPHostEntry](./iphostentry/) | Representa información sobre una dirección de host de internet. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [IWebProxy](./iwebproxy/) | Esta interfaz se utiliza para la implementación del acceso proxy a la clase [WebRequest](./webrequest/). Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [IWebRequestCreate](./iwebrequestcreate/) | Proporciona los métodos para crear instancias de la clase [WebRequest](./webrequest/). Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [NetworkCredential](./networkcredential/) | Proporciona credenciales para los esquemas de autenticación basados en contraseña. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [PathList](./pathlist/) | Representa la lista de instancias de la clase [CookieCollection](./cookiecollection/). Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [ServicePoint](./servicepoint/) | Proporciona la gestión de conexiones HTTP. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [ServicePointManager](./servicepointmanager/) | Gestiona las etapas del ciclo de vida (creación, mantenimiento y eliminación) de las instancias de la clase [ServicePoint](./servicepoint/). Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [SocketAddress](./socketaddress/) | Se utiliza para almacenar información serializada sobre las instancias de la clase [EndPoint](./endpoint/). Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [UriScheme](./urischeme/) | Representa el esquema para URI. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [WebClient](./webclient/) | [WebClient](./webclient/) proporciona métodos comunes para enviar y recibir datos. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [WebHeaderCollection](./webheadercollection/) | Representa la colección de los encabezados del protocolo. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [WebProxy](./webproxy/) | Representa un servidor proxy web HTTP. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [WebRequest](./webrequest/) | Representa una solicitud web. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
| [WebRequestMethods](./webrequestmethods/) | Representa las constantes de cadena de las solicitudes web. |
| [WebResponse](./webresponse/) | Representa una respuesta web. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a funciones como argumento. |
## Enums

| Enumeración | Descripción |
| --- | --- |
| [AuthenticationSchemes](./authenticationschemes/) | Enumera los esquemas de autenticación. |
| [CookieVariant](./cookievariant/) | Enumera las especificaciones de cookies. |
| [DecompressionMethods](./decompressionmethods/) | Enumera el algoritmo de compresión-descompresión. |
| [HttpRequestHeader](./httprequestheader/) | Enumera los encabezados de solicitud. |
| [HttpResponseHeader](./httpresponseheader/) | Enumera los encabezados de respuesta HTTP. |
| [HttpStatusCode](./httpstatuscode/) |  |
| [SecurityProtocolType](./securityprotocoltype/) | Enumera los tipos de protocolo de seguridad. |
| [TransportType](./transporttype/) | Define el tipo de transporte permitido para el socket. |
| [WebExceptionStatus](./webexceptionstatus/) | Enumera los códigos de estado de la clase [WebException](./webexception/). |
| [WebHeaderCollectionType](./webheadercollectiontype/) | Enumera los tipos de la colección de encabezados del protocolo. |
## Typedefs

| Typedef | Descripción |
| --- | --- |
| [BindIPEndPoint](./bindipendpoint/) |  |
| [CookieException](./cookieexception/) |  |
| [WebException](./webexception/) |  |
