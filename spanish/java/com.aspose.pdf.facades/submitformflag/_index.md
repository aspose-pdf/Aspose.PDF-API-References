---
title: "SubmitFormFlag"
linktitle: "SubmitFormFlag"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Enumeración de las posibles banderas de envío de formulario."
type: docs
weight: 730
url: /es/java/com.aspose.pdf.facades/submitformflag/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < SubmitFormFlag > com.aspose.pdf.facades.SubmitFormFlag, java.lang.Enum < SubmitFormFlag >, com.aspose.pdf.facades.SubmitFormFlag

**All Implemented Interfaces:**
Serializable, Comparable < SubmitFormFlag >

```
public enum SubmitFormFlag extends Enum < SubmitFormFlag >
```

Enumeración de las posibles banderas de envío de formulario.

## Campos

| Campo | Descripción |
| --- | --- |
| [Fdf](#Fdf) | Los datos se devolverán en formato FDF. |
| [FdfWithComments](#FdfWithComments) | Los datos se devolverán en formato FDF con comentarios. |
| [Html](#Html) | Los datos se devolverán en formato HTML. |
| [Pdf](#Pdf) | Se enviará el archivo PDF completo. |
| [Xfdf](#Xfdf) | Los datos se devolverán en formato XFDF. |
| [XfdfWithComments](#XfdfWithComments) | Los datos se devolverán en formato XFDF con comentarios. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Fdf {#Fdf}
```
public static final SubmitFormFlag Fdf
```

Los datos se devolverán en formato FDF.

### FdfWithComments {#FdfWithComments}
```
public static final SubmitFormFlag FdfWithComments
```

Los datos se devolverán en formato FDF con comentarios.

### Html {#Html}
```
public static final SubmitFormFlag Html
```

Los datos se devolverán en formato HTML.

### Pdf {#Pdf}
```
public static final SubmitFormFlag Pdf
```

Se enviará el archivo PDF completo.

### Xfdf {#Xfdf}
```
public static final SubmitFormFlag Xfdf
```

Los datos se devolverán en formato XFDF.

### XfdfWithComments {#XfdfWithComments}
```
public static final SubmitFormFlag XfdfWithComments
```

Los datos se devolverán en formato XFDF con comentarios.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static SubmitFormFlag [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
