---
title: "BlendingColorSpace"
linktitle: "BlendingColorSpace"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Clase que representa el espacio de color de mezcla."
type: docs
weight: 50
url: /es/java/com.aspose.pdf.facades/blendingcolorspace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < BlendingColorSpace > com.aspose.pdf.facades.BlendingColorSpace, java.lang.Enum < BlendingColorSpace >, com.aspose.pdf.facades.BlendingColorSpace

**All Implemented Interfaces:**
Serializable, Comparable < BlendingColorSpace >

```
public enum BlendingColorSpace extends Enum < BlendingColorSpace >
```

Clase que representa el espacio de color de mezcla.

## Campos

| Campo | Descripción |
| --- | --- |
| [Auto](#Auto) | El espacio de color de mezcla se determina automáticamente. |
| [DeviceCMYK](#DeviceCMYK) | El espacio de color de mezcla está configurado a DeviceCMYK. |
| [DeviceRGB](#DeviceRGB) | El espacio de color de mezcla está configurado a DeviceRGB. |
| [DontChange](#DontChange) | El espacio de color de mezcla no se cambia. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Auto {#Auto}
```
public static final BlendingColorSpace Auto
```

El espacio de color de mezcla se determina automáticamente.

### DeviceCMYK {#DeviceCMYK}
```
public static final BlendingColorSpace DeviceCMYK
```

El espacio de color de mezcla está configurado a DeviceCMYK.

### DeviceRGB {#DeviceRGB}
```
public static final BlendingColorSpace DeviceRGB
```

El espacio de color de mezcla está configurado a DeviceRGB.

### DontChange {#DontChange}
```
public static final BlendingColorSpace DontChange
```

El espacio de color de mezcla no se cambia.

### getByValue {#getByValue-int-}
```
public static BlendingColorSpace getByValue(int value)
```



**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static BlendingColorSpace [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
