---
title: "KeySize"
linktitle: "KeySize"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Define diferentes tamaños de clave que pueden usarse para encriptar documentos PDF."
type: docs
weight: 340
url: /es/java/com.aspose.pdf.facades/keysize/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < KeySize > com.aspose.pdf.facades.KeySize, java.lang.Enum < KeySize >, com.aspose.pdf.facades.KeySize

**All Implemented Interfaces:**
Serializable, Comparable < KeySize >

```
public enum KeySize extends Enum < KeySize >
```

Define diferentes tamaños de clave que pueden usarse para encriptar documentos PDF.

## Campos

| Campo | Descripción |
| --- | --- |
| [x128](#x128) | Clave de 128 bits. |
| [x256](#x256) | Clave de 256 bits. |
| [x40](#x40) | Clave de 40 bits. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### x128 {#x128}
```
public static final KeySize x128
```

Clave de 128 bits.

### x256 {#x256}
```
public static final KeySize x256
```

Clave de 256 bits.

### x40 {#x40}
```
public static final KeySize x40
```

Clave de 40 bits.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static KeySize [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
