---
title: "ImageMergeMode"
linktitle: "ImageMergeMode"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Representa modos para combinar imágenes."
type: docs
weight: 270
url: /es/java/com.aspose.pdf.facades/imagemergemode/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ImageMergeMode > com.aspose.pdf.facades.ImageMergeMode, java.lang.Enum < ImageMergeMode >, com.aspose.pdf.facades.ImageMergeMode

**All Implemented Interfaces:**
Serializable, Comparable < ImageMergeMode >

```
public enum ImageMergeMode extends Enum < ImageMergeMode >
```

Representa modos para combinar imágenes.

## Campos

| Campo | Descripción |
| --- | --- |
| [Center](#Center) | Imágenes alineadas al centro. |
| [Horizontal](#Horizontal) | Imágenes combinadas horizontalmente. |
| [Vertical](#Vertical) | Imágenes combinadas verticalmente. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Center {#Center}
```
public static final ImageMergeMode Center
```

Imágenes alineadas al centro.

### Horizontal {#Horizontal}
```
public static final ImageMergeMode Horizontal
```

Imágenes combinadas horizontalmente.

### Vertical {#Vertical}
```
public static final ImageMergeMode Vertical
```

Imágenes combinadas verticalmente.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static ImageMergeMode [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
