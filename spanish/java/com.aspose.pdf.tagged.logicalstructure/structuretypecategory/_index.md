---
title: "StructureTypeCategory"
linktitle: "StructureTypeCategory"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Representa categorías de tipos de estructura estándar."
type: docs
weight: 120
url: /es/java/com.aspose.pdf.tagged.logicalstructure/structuretypecategory/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.logicalstructure.StructureTypeCategory

```
public final class StructureTypeCategory extends Object
```

Representa categorías de tipos de estructura estándar.

## Campos

| Campo | Descripción |
| --- | --- |
| [BLSEs](#BLSEs) | Los elementos estructurales a nivel de bloque (BLSEs) describen la disposición general del contenido en la página, avanzando en la dirección de progresión de bloque. |
| [GroupingElements](#GroupingElements) | Los elementos de agrupación agrupan otros elementos en secuencias o jerarquías, pero no contienen contenido directamente y no tienen efecto directo en la disposición. |
| [IllustrationElements](#IllustrationElements) | Los elementos de ilustración son secuencias compactas de contenido, en el orden del contenido de la página, que se consideran objetos unitarios respecto a la disposición de la página. Una ilustración puede tratarse como un BLSE o un ILSE. |
| [ILSEs](#ILSEs) | Los elementos estructurales a nivel en línea (ILSEs) describen la disposición del contenido dentro de un BLSE, avanzando en la dirección de progresión en línea. |

## Métodos

| Método | Descripción |
| --- | --- |
| [to_StructureTypeCategory](#to_StructureTypeCategory-java.lang.String-) | Realiza una conversión explícita de {@link String} a {@link StructureTypeCategory}. |
| [toString](#toString--) | Devuelve una cadena que representa el objeto actual. |

### BLSEs {#BLSEs}
```
public static final StructureTypeCategory BLSEs
```

Los elementos estructurales a nivel de bloque (BLSEs) describen la disposición general del contenido en la página, avanzando en la dirección de progresión de bloque.

### GroupingElements {#GroupingElements}
```
public static final StructureTypeCategory GroupingElements
```

Los elementos de agrupación agrupan otros elementos en secuencias o jerarquías, pero no contienen contenido directamente y no tienen efecto directo en la disposición.

### IllustrationElements {#IllustrationElements}
```
public static final StructureTypeCategory IllustrationElements
```

Los elementos de ilustración son secuencias compactas de contenido, en el orden del contenido de la página, que se consideran objetos unitarios respecto a la disposición de la página. Una ilustración puede tratarse como un BLSE o un ILSE.

### ILSEs {#ILSEs}
```
public static final StructureTypeCategory ILSEs
```

Los elementos estructurales a nivel en línea (ILSEs) describen la disposición del contenido dentro de un BLSE, avanzando en la dirección de progresión en línea.

### to_StructureTypeCategory {#to_StructureTypeCategory-java.lang.String-}
Realiza una conversión explícita de {@link String} a {@link StructureTypeCategory}.

### toString {#toString--}
```
public String toString()
```

Devuelve una cadena que representa el objeto actual.

**Returns:**
Cadena que representa el objeto actual.
