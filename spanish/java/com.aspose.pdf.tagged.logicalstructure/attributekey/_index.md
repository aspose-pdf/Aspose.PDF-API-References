---
title: "AttributeKey"
linktitle: "AttributeKey"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Representa claves de atributos estándar."
type: docs
weight: 10
url: /es/java/com.aspose.pdf.tagged.logicalstructure/attributekey/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.tagged.logicalstructure.AttributeKey

```
public final class AttributeKey extends Object
```

Representa claves de atributos estándar.

## Campos

| Campo | Descripción |
| --- | --- |
| [BackgroundColor](#BackgroundColor) | Atributo BackgroundColor (propietario del atributo Layout). |
| [BaselineShift](#BaselineShift) | Atributo BaselineShift (propietario del atributo Layout). |
| [BBox](#BBox) | Atributo BBox (propietario del atributo Layout). |
| [BlockAlign](#BlockAlign) | Atributo BlockAlign (propietario del atributo Layout). |
| [BorderColor](#BorderColor) | Atributo BorderColor (propietario del atributo Layout). |
| [BorderStyle](#BorderStyle) | Atributo BorderStyle (propietario del atributo Layout). |
| [BorderThickness](#BorderThickness) | Atributo BorderThickness (propietario del atributo Layout). |
| [Checked](#Checked) | Atributo Checked (propietario del atributo PrintField). |
| [Color](#Color) | Atributo Color (propietario del atributo Layout). |
| [ColSpan](#ColSpan) | Atributo ColSpan (propietario del atributo Table). |
| [ColumnCount](#ColumnCount) | Atributo ColumnCount (propietario del atributo Layout). |
| [ColumnGap](#ColumnGap) | Atributo ColumnGap (propietario del atributo Layout). |
| [ColumnWidths](#ColumnWidths) | Atributo ColumnWidths (propietario del atributo Layout). |
| [Desc](#Desc) | Atributo Desc (propietario del atributo PrintField). |
| [EndIndent](#EndIndent) | Atributo EndIndent (propietario del atributo Layout). |
| [GlyphOrientationVertical](#GlyphOrientationVertical) | Atributo GlyphOrientationVertical (propietario del atributo Layout). |
| [Headers](#Headers) | Atributo Headers (propietario del atributo Table). |
| [Height](#Height) | Atributo Height (propietario del atributo Layout). |
| [InlineAlign](#InlineAlign) | Atributo InlineAlign (propietario del atributo Layout). |
| [LineHeight](#LineHeight) | Atributo LineHeight (propietario del atributo Layout). |
| [ListNumbering](#ListNumbering) | Atributo ListNumbering (propietario del atributo List). |
| [Padding](#Padding) | Atributo Padding (propietario del atributo Layout). |
| [Placement](#Placement) | Atributo Placement (propietario del atributo Layout). |
| [Role](#Role) | Atributo Role (propietario del atributo PrintField). |
| [RowSpan](#RowSpan) | Atributo RowSpan (propietario del atributo Table). |
| [RubyAlign](#RubyAlign) | Atributo RubyAlign (propietario del atributo Layout). |
| [RubyPosition](#RubyPosition) | Atributo RubyPosition (propietario del atributo Layout). |
| [Scope](#Scope) | Atributo Scope (propietario del atributo Table). |
| [SpaceAfter](#SpaceAfter) | Atributo SpaceAfter (propietario del atributo Layout). |
| [SpaceBefore](#SpaceBefore) | Atributo SpaceBefore (propietario del atributo Layout). |
| [StartIndent](#StartIndent) | Atributo StartIndent (propietario del atributo Layout). |
| [Summary](#Summary) | Atributo Summary (propietario del atributo Table). |
| [TBorderStyle](#TBorderStyle) | Atributo TBorderStyle (propietario del atributo Layout). |
| [TextAlign](#TextAlign) | Atributo TextAlign (propietario del atributo Layout). |
| [TextDecorationColor](#TextDecorationColor) | Atributo TextDecorationColor (propietario del atributo Layout). |
| [TextDecorationThickness](#TextDecorationThickness) | Atributo TextDecorationThickness (propietario del atributo Layout). |
| [TextDecorationType](#TextDecorationType) | Atributo TextDecorationType (propietario del atributo Layout). |
| [TextIndent](#TextIndent) | Atributo TextIndent (propietario del atributo Layout). |
| [TPadding](#TPadding) | Atributo TPadding (propietario del atributo Layout). |
| [Width](#Width) | Atributo Width (propietario del atributo Layout). |
| [WritingMode](#WritingMode) | Atributo WritingMode (propietario del atributo Layout). |

## Métodos

| Método | Descripción |
| --- | --- |
| [getKey](#getKey--) | Obtener clave de atributo. Valor: clave de atributo. |
| [getOwner](#getOwner--) | Obtener propietario del atributo. Valor: Propietario del atributo. |
| [hashCode](#hashCode--) |  |
| [to_AttributeKey](#to_AttributeKey-java.lang.String-) | Realiza una conversión explícita de {@link String} a {@link AttributeKey}. |
| [toString](#toString--) | Devuelve una cadena que representa el objeto actual. |

### BackgroundColor {#BackgroundColor}
```
public static final AttributeKey BackgroundColor
```

Atributo BackgroundColor (propietario del atributo Layout).

### BaselineShift {#BaselineShift}
```
public static final AttributeKey BaselineShift
```

Atributo BaselineShift (propietario del atributo Layout).

### BBox {#BBox}
```
public static final AttributeKey BBox
```

Atributo BBox (propietario del atributo Layout).

### BlockAlign {#BlockAlign}
```
public static final AttributeKey BlockAlign
```

Atributo BlockAlign (propietario del atributo Layout).

### BorderColor {#BorderColor}
```
public static final AttributeKey BorderColor
```

Atributo BorderColor (propietario del atributo Layout).

### BorderStyle {#BorderStyle}
```
public static final AttributeKey BorderStyle
```

Atributo BorderStyle (propietario del atributo Layout).

### BorderThickness {#BorderThickness}
```
public static final AttributeKey BorderThickness
```

Atributo BorderThickness (propietario del atributo Layout).

### Checked {#Checked}
```
public static final AttributeKey Checked
```

Atributo Checked (propietario del atributo PrintField).

### Color {#Color}
```
public static final AttributeKey Color
```

Atributo Color (propietario del atributo Layout).

### ColSpan {#ColSpan}
```
public static final AttributeKey ColSpan
```

Atributo ColSpan (propietario del atributo Table).

### ColumnCount {#ColumnCount}
```
public static final AttributeKey ColumnCount
```

Atributo ColumnCount (propietario del atributo Layout).

### ColumnGap {#ColumnGap}
```
public static final AttributeKey ColumnGap
```

Atributo ColumnGap (propietario del atributo Layout).

### ColumnWidths {#ColumnWidths}
```
public static final AttributeKey ColumnWidths
```

Atributo ColumnWidths (propietario del atributo Layout).

### Desc {#Desc}
```
public static final AttributeKey Desc
```

Atributo Desc (propietario del atributo PrintField).

### EndIndent {#EndIndent}
```
public static final AttributeKey EndIndent
```

Atributo EndIndent (propietario del atributo Layout).

### GlyphOrientationVertical {#GlyphOrientationVertical}
```
public static final AttributeKey GlyphOrientationVertical
```

Atributo GlyphOrientationVertical (propietario del atributo Layout).

### Headers {#Headers}
```
public static final AttributeKey Headers
```

Atributo Headers (propietario del atributo Table).

### Height {#Height}
```
public static final AttributeKey Height
```

Atributo Height (propietario del atributo Layout).

### InlineAlign {#InlineAlign}
```
public static final AttributeKey InlineAlign
```

Atributo InlineAlign (propietario del atributo Layout).

### LineHeight {#LineHeight}
```
public static final AttributeKey LineHeight
```

Atributo LineHeight (propietario del atributo Layout).

### ListNumbering {#ListNumbering}
```
public static final AttributeKey ListNumbering
```

Atributo ListNumbering (propietario del atributo List).

### Padding {#Padding}
```
public static final AttributeKey Padding
```

Atributo Padding (propietario del atributo Layout).

### Placement {#Placement}
```
public static final AttributeKey Placement
```

Atributo Placement (propietario del atributo Layout).

### Role {#Role}
```
public static final AttributeKey Role
```

Atributo Role (propietario del atributo PrintField).

### RowSpan {#RowSpan}
```
public static final AttributeKey RowSpan
```

Atributo RowSpan (propietario del atributo Table).

### RubyAlign {#RubyAlign}
```
public static final AttributeKey RubyAlign
```

Atributo RubyAlign (propietario del atributo Layout).

### RubyPosition {#RubyPosition}
```
public static final AttributeKey RubyPosition
```

Atributo RubyPosition (propietario del atributo Layout).

### Scope {#Scope}
```
public static final AttributeKey Scope
```

Atributo Scope (propietario del atributo Table).

### SpaceAfter {#SpaceAfter}
```
public static final AttributeKey SpaceAfter
```

Atributo SpaceAfter (propietario del atributo Layout).

### SpaceBefore {#SpaceBefore}
```
public static final AttributeKey SpaceBefore
```

Atributo SpaceBefore (propietario del atributo Layout).

### StartIndent {#StartIndent}
```
public static final AttributeKey StartIndent
```

Atributo StartIndent (propietario del atributo Layout).

### Summary {#Summary}
```
public static final AttributeKey Summary
```

Atributo Summary (propietario del atributo Table).

### TBorderStyle {#TBorderStyle}
```
public static final AttributeKey TBorderStyle
```

Atributo TBorderStyle (propietario del atributo Layout).

### TextAlign {#TextAlign}
```
public static final AttributeKey TextAlign
```

Atributo TextAlign (propietario del atributo Layout).

### TextDecorationColor {#TextDecorationColor}
```
public static final AttributeKey TextDecorationColor
```

Atributo TextDecorationColor (propietario del atributo Layout).

### TextDecorationThickness {#TextDecorationThickness}
```
public static final AttributeKey TextDecorationThickness
```

Atributo TextDecorationThickness (propietario del atributo Layout).

### TextDecorationType {#TextDecorationType}
```
public static final AttributeKey TextDecorationType
```

Atributo TextDecorationType (propietario del atributo Layout).

### TextIndent {#TextIndent}
```
public static final AttributeKey TextIndent
```

Atributo TextIndent (propietario del atributo Layout).

### TPadding {#TPadding}
```
public static final AttributeKey TPadding
```

Atributo TPadding (propietario del atributo Layout).

### Width {#Width}
```
public static final AttributeKey Width
```

Atributo Width (propietario del atributo Layout).

### WritingMode {#WritingMode}
```
public static final AttributeKey WritingMode
```

Atributo WritingMode (propietario del atributo Layout).

### getKey {#getKey--}
```
public final String getKey()
```

Obtener clave de atributo. Valor: clave de atributo.

**Returns:**
valor String

### getOwner {#getOwner--}
```
public final AttributeOwnerStandard getOwner()
```

Obtener propietario del atributo. Valor: Propietario del atributo.

**Returns:**
Instancia de AttributeKey

### hashCode {#hashCode--}
```
public int hashCode()
```



### to_AttributeKey {#to_AttributeKey-java.lang.String-}
Realiza una conversión explícita de {@link String} a {@link AttributeKey}.

### toString {#toString--}
```
public String toString()
```

Devuelve una cadena que representa el objeto actual.

**Returns:**
Cadena que representa el objeto actual.
