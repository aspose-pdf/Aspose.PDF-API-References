---
title: "FreeTextIntent"
linktitle: "FreeTextIntent"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Enumera las intenciones de la anotación de texto libre."
type: docs
weight: 1800
url: /es/java/com.aspose.pdf/freetextintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < FreeTextIntent > com.aspose.pdf.FreeTextIntent, java.lang.Enum < FreeTextIntent >, com.aspose.pdf.FreeTextIntent

**All Implemented Interfaces:**
Serializable, Comparable < FreeTextIntent >

```
public enum FreeTextIntent extends Enum < FreeTextIntent >
```

Enumera las intenciones de la anotación de texto libre.

## Campos

| Campo | Descripción |
| --- | --- |
| [FreeTextCallout](#FreeTextCallout) | Significa que la anotación está destinada a funcionar como una llamada. |
| [FreeTextTypeWriter](#FreeTextTypeWriter) | Significa que la anotación está destinada a funcionar como un objeto de clic-para-escribir o máquina de escribir. |
| [Undefined](#Undefined) | Estado no definido. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### FreeTextCallout {#FreeTextCallout}
```
public static final FreeTextIntent FreeTextCallout
```

Significa que la anotación está destinada a funcionar como una llamada.

### FreeTextTypeWriter {#FreeTextTypeWriter}
```
public static final FreeTextIntent FreeTextTypeWriter
```

Significa que la anotación está destinada a funcionar como un objeto de clic-para-escribir o máquina de escribir.

### Undefined {#Undefined}
```
public static final FreeTextIntent Undefined
```

Estado no definido.

### getByValue {#getByValue-int-}
```
public static FreeTextIntent getByValue(int value)
```



**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static FreeTextIntent [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
