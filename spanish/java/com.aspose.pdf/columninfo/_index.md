---
title: "ColumnInfo"
linktitle: "ColumnInfo"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Esta clase representa la información de una columna."
type: docs
weight: 730
url: /es/java/com.aspose.pdf/columninfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ColumnInfo

```
public final class ColumnInfo extends Object
```

Esta clase representa la información de una columna.

## Constructores

| Constructor | Descripción |
| --- | --- |
| [ColumnInfo](#ColumnInfo--) | Inicializa una nueva instancia de la clase ColumnInfo. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getColumnCount](#getColumnCount--) | Obtiene un valor entero que indica el número de columnas. |
| [getColumnSpacing](#getColumnSpacing--) | <p> Obtiene o establece una cadena que contiene el espaciado entre columnas. El valor de cada espaciado debe estar separado por un espacio. La unidad predeterminada es punto, pero cm y inch también son compatibles. Por ejemplo,"120 2.5cm 1.5inch". </p><hr> <p> Si esta propiedad no se establece, se utilizará el valor predeterminado 0 para cada espaciado. </p> |
| [getColumnWidths](#getColumnWidths--) | Obtiene o establece una cadena que contiene el ancho de las columnas. El valor de cada columna debe estar separado por un espacio. La unidad predeterminada es punto, pero cm, inch y porcentaje del ancho disponible también son compatibles. Por ejemplo,"120 2.5cm 1.5inch" |
| [setColumnCount](#setColumnCount-int-) | Establece un valor entero que indica el número de columnas. |
| [setColumnSpacing](#setColumnSpacing-java.lang.String-) | <p> Obtiene o establece una cadena que contiene el espaciado entre columnas. El valor de cada espaciado debe estar separado por un espacio. La unidad predeterminada es punto, pero cm y inch también son compatibles. Por ejemplo,"120 2.5cm 1.5inch". </p><hr> <p> Si esta propiedad no se establece, se utilizará el valor predeterminado 0 para cada espaciado. </p> |
| [setColumnWidths](#setColumnWidths-java.lang.String-) | Obtiene o establece una cadena que contiene el ancho de las columnas. El valor de cada columna debe estar separado por un espacio. La unidad predeterminada es punto, pero cm, inch y porcentaje del ancho disponible también son compatibles. Por ejemplo,"120 2.5cm 1.5inch" |

### ColumnInfo {#ColumnInfo--}
```
public ColumnInfo()
```

Inicializa una nueva instancia de la clase ColumnInfo.

### getColumnCount {#getColumnCount--}
```
public int getColumnCount()
```

Obtiene un valor entero que indica el número de columnas.

**Returns:**
recuento de columnas

### getColumnSpacing {#getColumnSpacing--}
```
public String getColumnSpacing()
```

<p> Obtiene o establece una cadena que contiene el espaciado entre columnas. El valor de cada espaciado debe estar separado por un espacio. La unidad predeterminada es punto, pero cm y inch también son compatibles. Por ejemplo,"120 2.5cm 1.5inch". </p><hr> <p> Si esta propiedad no se establece, se utilizará el valor predeterminado 0 para cada espaciado. </p>

**Returns:**
valor String

### getColumnWidths {#getColumnWidths--}
```
public String getColumnWidths()
```

Obtiene o establece una cadena que contiene el ancho de las columnas. El valor de cada columna debe estar separado por un espacio. La unidad predeterminada es punto, pero cm, inch y porcentaje del ancho disponible también son compatibles. Por ejemplo,"120 2.5cm 1.5inch"

**Returns:**
valor String

### setColumnCount {#setColumnCount-int-}
```
public void setColumnCount(int value)
```

Establece un valor entero que indica el número de columnas.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  | recuento de columnas |

### setColumnSpacing {#setColumnSpacing-java.lang.String-}
<p> Obtiene o establece una cadena que contiene el espaciado entre columnas. El valor de cada espaciado debe estar separado por un espacio. La unidad predeterminada es punto, pero cm y inch también son compatibles. Por ejemplo,"120 2.5cm 1.5inch". </p><hr> <p> Si esta propiedad no se establece, se utilizará el valor predeterminado 0 para cada espaciado. </p>

### setColumnWidths {#setColumnWidths-java.lang.String-}
Obtiene o establece una cadena que contiene el ancho de las columnas. El valor de cada columna debe estar separado por un espacio. La unidad predeterminada es punto, pero cm, inch y porcentaje del ancho disponible también son compatibles. Por ejemplo,"120 2.5cm 1.5inch"
