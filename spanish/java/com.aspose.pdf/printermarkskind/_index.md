---
title: "PrinterMarksKind"
linktitle: "PrinterMarksKind"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Especifica los tipos de marcas de impresora que se agregarán a un documento. Esta enumeración tiene un atributo {@link FlagsAttribute} que permite una combinación bit a bit de sus valores de miembros."
type: docs
weight: 4020
url: /es/java/com.aspose.pdf/printermarkskind/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.PrinterMarksKind, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.PrinterMarksKind, com.aspose.ms.System.Enum, com.aspose.pdf.PrinterMarksKind

```
public final class PrinterMarksKind extends com.aspose.ms.System.Enum
```

Especifica los tipos de marcas de impresora que se agregarán a un documento. Esta enumeración tiene un atributo {@link FlagsAttribute} que permite una combinación bit a bit de sus valores de miembros.

## Campos

| Campo | Descripción |
| --- | --- |
| [All](#All) | Especifica que se deben agregar todos los tipos de marcas de impresora. |
| [BleedMarks](#BleedMarks) | Especifica que se deben agregar marcas de sangrado. |
| [ColorBars](#ColorBars) | Especifica que se deben agregar barras de color. |
| [None](#None) | Especifica que no se deben agregar marcas de impresora. |
| [PageInformation](#PageInformation) | Especifica que se debe agregar información de página. |
| [RegistrationMarks](#RegistrationMarks) | Especifica que se deben agregar marcas de registro. |
| [TrimMarks](#TrimMarks) | Especifica que se deben agregar marcas de recorte. |

### All {#All}
```
public static final int All
```

Especifica que se deben agregar todos los tipos de marcas de impresora.

### BleedMarks {#BleedMarks}
```
public static final int BleedMarks
```

Especifica que se deben agregar marcas de sangrado.

### ColorBars {#ColorBars}
```
public static final int ColorBars
```

Especifica que se deben agregar barras de color.

### None {#None}
```
public static final int None
```

Especifica que no se deben agregar marcas de impresora.

### PageInformation {#PageInformation}
```
public static final int PageInformation
```

Especifica que se debe agregar información de página.

### RegistrationMarks {#RegistrationMarks}
```
public static final int RegistrationMarks
```

Especifica que se deben agregar marcas de registro.

### TrimMarks {#TrimMarks}
```
public static final int TrimMarks
```

Especifica que se deben agregar marcas de recorte.
