---
title: "Justificación"
linktitle: "Justificación"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Enumera las formas de justificación (quadding) que se usarán al mostrar el texto de la anotación."
type: docs
weight: 2580
url: /es/java/com.aspose.pdf/justification/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < Justification > com.aspose.pdf.Justification, java.lang.Enum <Justification >, com.aspose.pdf.Justification

**All Implemented Interfaces:**
Serializable, Comparable < Justification >

```
public enum Justification extends Enum < Justification >
```

Enumera las formas de justificación (quadding) que se usarán al mostrar el texto de la anotación.

## Campos

| Campo | Descripción |
| --- | --- |
| [Center](#Center) | Justificación centrada. |
| [Left](#Left) | Justificación a la izquierda. |
| [Right](#Right) | Justificación a la derecha. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Center {#Center}
```
public static final Justification Center
```

Justificación centrada.

### Left {#Left}
```
public static final Justification Left
```

Justificación a la izquierda.

### Right {#Right}
```
public static final Justification Right
```

Justificación a la derecha.

### getByValue {#getByValue-int-}
```
public static Justification getByValue(int value)
```



**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static Justification [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
