---
title: "HorizontalAlignment"
linktitle: "HorizontalAlignment"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Describe la alineación horizontal."
type: docs
weight: 1930
url: /es/java/com.aspose.pdf/horizontalalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < HorizontalAlignment > com.aspose.pdf.HorizontalAlignment, java.lang.Enum < HorizontalAlignment >, com.aspose.pdf.HorizontalAlignment

**All Implemented Interfaces:**
Serializable, Comparable < HorizontalAlignment >

```
public enum HorizontalAlignment extends Enum < HorizontalAlignment >
```

Describe la alineación horizontal.

## Campos

| Campo | Descripción |
| --- | --- |
| [Center](#Center) | Alineación centrada. |
| [FullJustify](#FullJustify) | Similar al alineamiento 'Justify', excepto que la última línea solo se alineará a la izquierda en modo 'Justify', mientras que en modo 'FullJustify' todas las líneas estarán alineadas a la izquierda y a la derecha. |
| [Justify](#Justify) | Alineación Justify. |
| [Left](#Left) | Alinear a la izquierda. |
| [None](#None) | Sin alineación. |
| [Right](#Right) | Alinear a la derecha. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) | Obtener número de |
| [valueOf](#valueOf-int-) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Center {#Center}
```
public static final HorizontalAlignment Center
```

Alineación centrada.

### FullJustify {#FullJustify}
```
public static final HorizontalAlignment FullJustify
```

Similar al alineamiento 'Justify', excepto que la última línea solo se alineará a la izquierda en modo 'Justify', mientras que en modo 'FullJustify' todas las líneas estarán alineadas a la izquierda y a la derecha.

### Justify {#Justify}
```
public static final HorizontalAlignment Justify
```

Alineación Justify.

### Left {#Left}
```
public static final HorizontalAlignment Left
```

Alinear a la izquierda.

### None {#None}
```
public static final HorizontalAlignment None
```

Sin alineación.

### Right {#Right}
```
public static final HorizontalAlignment Right
```

Alinear a la derecha.

### getValue {#getValue--}
```
public int getValue()
```

Obtener número de

**Returns:**
valor int del elemento HorizontalAlignment

### valueOf {#valueOf-int-}
```
public static HorizontalAlignment valueOf(int alignmentType)
```



**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| alignmentType |  |  |

### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static HorizontalAlignment [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
