---
title: "AnnotationState"
linktitle: "AnnotationState"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "La enumeración de estados a los que se puede establecer la anotación original."
type: docs
weight: 110
url: /es/java/com.aspose.pdf/annotationstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationState > com.aspose.pdf.AnnotationState, java.lang.Enum < AnnotationState >, com.aspose.pdf.AnnotationState

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationState >

```
public enum AnnotationState extends Enum < AnnotationState >
```

La enumeración de estados a los que se puede establecer la anotación original.

## Campos

| Campo | Descripción |
| --- | --- |
| [Accepted](#Accepted) | El usuario está de acuerdo con el cambio. |
| [Cancelled](#Cancelled) | El cambio ha sido cancelado. |
| [Completed](#Completed) | El cambio ha sido completado. |
| [Marked](#Marked) | La anotación ha sido marcada por el usuario. |
| [None](#None) | El usuario no ha indicado nada sobre el cambio. |
| [Rejected](#Rejected) | El usuario no está de acuerdo con el cambio. |
| [Undefined](#Undefined) | Estado no definido. |
| [Unmarked](#Unmarked) | La anotación no ha sido marcada por el usuario. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Accepted {#Accepted}
```
public static final AnnotationState Accepted
```

El usuario está de acuerdo con el cambio.

### Cancelled {#Cancelled}
```
public static final AnnotationState Cancelled
```

El cambio ha sido cancelado.

### Completed {#Completed}
```
public static final AnnotationState Completed
```

El cambio ha sido completado.

### Marked {#Marked}
```
public static final AnnotationState Marked
```

La anotación ha sido marcada por el usuario.

### None {#None}
```
public static final AnnotationState None
```

El usuario no ha indicado nada sobre el cambio.

### Rejected {#Rejected}
```
public static final AnnotationState Rejected
```

El usuario no está de acuerdo con el cambio.

### Undefined {#Undefined}
```
public static final AnnotationState Undefined
```

Estado no definido.

### Unmarked {#Unmarked}
```
public static final AnnotationState Unmarked
```

La anotación no ha sido marcada por el usuario.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static AnnotationState [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
