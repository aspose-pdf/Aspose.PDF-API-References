---
title: "LineIntent"
linktitle: "LineIntent"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Enumera los propósitos de la anotación de línea."
type: docs
weight: 2750
url: /es/java/com.aspose.pdf/lineintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < LineIntent > com.aspose.pdf.LineIntent, java.lang.Enum < LineIntent >, com.aspose.pdf.LineIntent

**All Implemented Interfaces:**
Serializable, Comparable < LineIntent >

```
public enum LineIntent extends Enum < LineIntent >
```

Enumera los propósitos de la anotación de línea.

## Campos

| Campo | Descripción |
| --- | --- |
| [LineArrow](#LineArrow) | Indica que la anotación está destinada a funcionar como una flecha. |
| [LineDimension](#LineDimension) | Indica que la anotación está destinada a funcionar como una línea de dimensión. |
| [Undefined](#Undefined) | Estado indefinido. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### LineArrow {#LineArrow}
```
public static final LineIntent LineArrow
```

Indica que la anotación está destinada a funcionar como una flecha.

### LineDimension {#LineDimension}
```
public static final LineIntent LineDimension
```

Indica que la anotación está destinada a funcionar como una línea de dimensión.

### Undefined {#Undefined}
```
public static final LineIntent Undefined
```

Estado indefinido.

### getByValue {#getByValue-int-}
```
public static LineIntent getByValue(int value)
```



**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static LineIntent [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
