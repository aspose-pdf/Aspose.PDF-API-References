---
title: "HtmlSaveOptions.CssUrlRequestInfo"
linktitle: "HtmlSaveOptions.CssUrlRequestInfo"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Representa un conjunto de datos relacionados con la solicitud del conversor al código personalizado destinada a obtener la URL deseada (o plantilla de URL) del CSS en cuestión"
type: docs
weight: 2040
url: /es/java/com.aspose.pdf/htmlsaveoptions.cssurlrequestinfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.HtmlSaveOptions.CssUrlRequestInfo

```
public static class HtmlSaveOptions.CssUrlRequestInfo extends Object
```

Representa un conjunto de datos relacionados con la solicitud del conversor al código personalizado destinada a obtener la URL deseada (o plantilla de URL) del CSS en cuestión

## Constructores

| Constructor | Descripción |
| --- | --- |
| [CssUrlRequestInfo](#CssUrlRequestInfo--) | Crea una instancia de CssUrlRequestInfo |

## Métodos

| Método | Descripción |
| --- | --- |
| [isCustomProcessingCancelled](#isCustomProcessingCancelled--) | Debe establecerse mediante código personalizado si no puede o no debe definir la URL que se utilizará en el HTML generado para referenciar ese CSS. Si es 'true', el archivo CSS se guardará de forma estándar en el lugar estándar. |
| [setCustomProcessingCancelled](#setCustomProcessingCancelled-boolean-) | Debe establecerse mediante código personalizado si no puede o no debe definir la URL que se utilizará en el HTML generado para referenciar ese CSS. Si es 'true', el archivo CSS se guardará de forma estándar en el lugar estándar. |

### CssUrlRequestInfo {#CssUrlRequestInfo--}
```
public CssUrlRequestInfo()
```

Crea una instancia de CssUrlRequestInfo

### isCustomProcessingCancelled {#isCustomProcessingCancelled--}
```
public boolean isCustomProcessingCancelled()
```

Debe establecerse mediante código personalizado si no puede o no debe definir la URL que se utilizará en el HTML generado para referenciar ese CSS. Si es 'true', el archivo CSS se guardará de forma estándar en el lugar estándar.

**Returns:**
valor booleano

### setCustomProcessingCancelled {#setCustomProcessingCancelled-boolean-}
```
public void setCustomProcessingCancelled(boolean customProcessingCancelled)
```

Debe establecerse mediante código personalizado si no puede o no debe definir la URL que se utilizará en el HTML generado para referenciar ese CSS. Si es 'true', el archivo CSS se guardará de forma estándar en el lugar estándar.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| customProcessingCancelled |  | valor booleano |
