---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Enum representa una acción del flujo de trabajo del programa en caso de invocar el método {@code IWarningCallback.Warning(WarningInfo)}."
type: docs
weight: 4240
url: /es/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

Enum representa una acción del flujo de trabajo del programa en caso de invocar el método {@code IWarningCallback.Warning(WarningInfo)}.

## Campos

| Campo | Descripción |
| --- | --- |
| [Abort](#Abort) | Utilizado para abortar el flujo. |
| [Continue](#Continue) | Utilizado para continuar el flujo. |

## Métodos

| Método | Descripción |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

Utilizado para abortar el flujo.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

Utilizado para continuar el flujo.

### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static ReturnAction [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
