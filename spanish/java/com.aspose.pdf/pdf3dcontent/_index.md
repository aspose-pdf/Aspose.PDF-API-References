---
title: "PDF3DContent"
linktitle: "PDF3DContent"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Clase PDF3DContent."
type: docs
weight: 3580
url: /es/java/com.aspose.pdf/pdf3dcontent/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DContent

```
public class PDF3DContent extends Object
```

Clase PDF3DContent.

## Constructores

| Constructor | Descripción |
| --- | --- |
| [PDF3DContent](#PDF3DContent--) | Inicializa una nueva instancia de la clase {@code PDF3DContent}. |
| [PDF3DContent](#PDF3DContent-java.lang.String-) | Inicializa una nueva instancia de la clase {@code PDF3DContent}. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getAsByteArray](#getAsByteArray--) | Obtiene el contenido 3D como matriz de bytes. |
| [getAsStream](#getAsStream--) | Obtiene el contenido 3D como flujo. |
| [getExtension](#getExtension--) | Obtiene la extensión . |
| [load](#load-java.lang.String-) | Carga el contenido 3D con el nombre de archivo especificado. |
| [loadAsPRC](#loadAsPRC-byte:A-) | Carga el contenido 3D desde una matriz de bytes en formato PRC. |
| [loadAsPRC](#loadAsPRC-java.io.InputStream-) | Carga el contenido 3D desde un flujo en formato PRC. |
| [loadAsPRC](#loadAsPRC-java.lang.String-) | Carga el contenido 3D con el nombre de archivo especificado en formato PRC. |
| [loadAsU3D](#loadAsU3D-byte:A-) | Carga el contenido 3D desde una matriz de bytes en formato U3D. |
| [loadAsU3D](#loadAsU3D-java.io.InputStream-) | Carga el contenido 3D desde un flujo en formato U3D. |
| [loadAsU3D](#loadAsU3D-java.lang.String-) | Carga el contenido 3D con el nombre de archivo especificado en formato U3D. |
| [saveToFile](#saveToFile-java.lang.String-) | Guarda el contenido 3D en un archivo. |

### PDF3DContent {#PDF3DContent--}
```
public PDF3DContent()
```

Inicializa una nueva instancia de la clase {@code PDF3DContent}.

### PDF3DContent {#PDF3DContent-java.lang.String-}
Inicializa una nueva instancia de la clase {@code PDF3DContent}.

### getAsByteArray {#getAsByteArray--}
```
public byte[] getAsByteArray()
```

Obtiene el contenido 3D como matriz de bytes.

**Returns:**
System.Byte[].

### getAsStream {#getAsStream--}
```
public InputStream getAsStream()
```

Obtiene el contenido 3D como flujo.

**Returns:**
Flujo.

### getExtension {#getExtension--}
```
public String getExtension()
```

Obtiene la extensión .

**Returns:**
Objeto String: La extensión.

### load {#load-java.lang.String-}
Carga el contenido 3D con el nombre de archivo especificado.

### loadAsPRC {#loadAsPRC-byte:A-}
```
public void loadAsPRC(byte[] stream)
```

Carga el contenido 3D desde una matriz de bytes en formato PRC.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| flujo |  | El flujo. |

### loadAsPRC {#loadAsPRC-java.io.InputStream-}
Carga el contenido 3D desde un flujo en formato PRC.

### loadAsPRC {#loadAsPRC-java.lang.String-}
Carga el contenido 3D con el nombre de archivo especificado en formato PRC.

### loadAsU3D {#loadAsU3D-byte:A-}
```
public void loadAsU3D(byte[] stream)
```

Carga el contenido 3D desde una matriz de bytes en formato U3D.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| flujo |  | El flujo. |

### loadAsU3D {#loadAsU3D-java.io.InputStream-}
Carga el contenido 3D desde un flujo en formato U3D.

### loadAsU3D {#loadAsU3D-java.lang.String-}
Carga el contenido 3D con el nombre de archivo especificado en formato U3D.

### saveToFile {#saveToFile-java.lang.String-}
Guarda el contenido 3D en un archivo.
