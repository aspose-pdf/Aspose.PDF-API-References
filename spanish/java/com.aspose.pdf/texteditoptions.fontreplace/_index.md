---
title: "TextEditOptions.FontReplace"
linktitle: "TextEditOptions.FontReplace"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Comportamiento de sustitución de fuentes."
type: docs
weight: 4990
url: /es/java/com.aspose.pdf/texteditoptions.fontreplace/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextEditOptions.FontReplace > com.aspose.pdf.TextEditOptions.FontReplace, java.lang.Enum < TextEditOptions.FontReplace >, com.aspose.pdf.TextEditOptions.FontReplace

**All Implemented Interfaces:**
Serializable, Comparable < TextEditOptions.FontReplace >

```
public static enum TextEditOptions.FontReplace extends Enum < TextEditOptions.FontReplace >
```

Comportamiento de sustitución de fuentes.

## Campos

| Campo | Descripción |
| --- | --- |
| [Default](#Default) | No se realizaron cambios adicionales durante la sustitución de fuentes. |
| [RemoveUnusedFonts](#RemoveUnusedFonts) | Las fuentes que queden sin usar durante la sustitución de fuentes se eliminarán del documento resultante. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Default {#Default}
```
public static final TextEditOptions.FontReplace Default
```

No se realizaron cambios adicionales durante la sustitución de fuentes.

### RemoveUnusedFonts {#RemoveUnusedFonts}
```
public static final TextEditOptions.FontReplace RemoveUnusedFonts
```

Las fuentes que queden sin usar durante la sustitución de fuentes se eliminarán del documento resultante.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static TextEditOptions.FontReplace [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
