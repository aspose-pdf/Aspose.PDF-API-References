---
title: "RenditionOperation"
linktitle: "RenditionOperation"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "La operación a realizar cuando se activa la acción."
type: docs
weight: 4190
url: /es/java/com.aspose.pdf/renditionoperation/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.RenditionOperation, com.aspose.ms.System.Enum, com.aspose.pdf.RenditionOperation

```
public final class RenditionOperation extends com.aspose.ms.System.Enum
```

La operación a realizar cuando se activa la acción.

## Campos

| Campo | Descripción |
| --- | --- |
| [Pause](#Pause) | Pausa cualquier representación que se esté reproduciendo en asociación con la anotación. |
| [PlayResume](#PlayResume) | Reproduce la representación especificada, asociándola con la anotación. Si una representación ya está asociada con la anotación, reanuda la representación si está pausada. |
| [PlayStop](#PlayStop) | Si no hay ninguna representación asociada a la anotación, reproduce la representación especificada, asociándola con la anotación. Si ya existe una representación asociada a la anotación, se detendrá, y la nueva representación se asociará con la anotación. |
| [Resume](#Resume) | Reanuda cualquier representación que se esté reproduciendo en asociación con la anotación. |
| [Stop](#Stop) | Detiene cualquier representación que se esté reproduciendo en asociación con la anotación. |
| [Undefined](#Undefined) | Operación no definida. |

### Pause {#Pause}
```
public static final int Pause
```

Pausa cualquier representación que se esté reproduciendo en asociación con la anotación.

### PlayResume {#PlayResume}
```
public static final int PlayResume
```

Reproduce la representación especificada, asociándola con la anotación. Si una representación ya está asociada con la anotación, reanuda la representación si está pausada.

### PlayStop {#PlayStop}
```
public static final int PlayStop
```

Si no hay ninguna representación asociada a la anotación, reproduce la representación especificada, asociándola con la anotación. Si ya existe una representación asociada a la anotación, se detendrá, y la nueva representación se asociará con la anotación.

### Resume {#Resume}
```
public static final int Resume
```

Reanuda cualquier representación que se esté reproduciendo en asociación con la anotación.

### Stop {#Stop}
```
public static final int Stop
```

Detiene cualquier representación que se esté reproduciendo en asociación con la anotación.

### Undefined {#Undefined}
```
public static final int Undefined
```

Operación no definida.
