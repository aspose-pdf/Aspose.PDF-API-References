---
title: "Measure"
linktitle: "Measure"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Clase que describe el sistema de coordenadas Measure."
type: docs
weight: 2930
url: /es/java/com.aspose.pdf/measure/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.Measure

```
public class Measure extends Object
```

Clase que describe el sistema de coordenadas Measure.

## Constructores

| Constructor | Descripción |
| --- | --- |
| [Measure](#Measure-com.aspose.pdf.Annotation-) | Crea un objeto Measure para anotaciones de medida. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getAngleFormat](#getAngleFormat--) | Una matriz de formato numérico para la medición de ángulos. |
| [getAreaFormat](#getAreaFormat--) | Una matriz de formato numérico para la medición de áreas. |
| [getDistanceFormat](#getDistanceFormat--) | Una matriz de formato numérico para la medición de distancia en cualquier dirección. |
| [getOrigin](#getOrigin--) | Punto que debe especificar el origen del sistema de coordenadas de medida en coordenadas del espacio de usuario predeterminado. |
| [getScaleRatio](#getScaleRatio--) | Una cadena de texto que expresa la proporción de escala del dibujo. |
| [getSlopeFormat](#getSlopeFormat--) | Una matriz de formato numérico para la medición de la pendiente de una línea. |
| [getXFormat](#getXFormat--) | Una matriz de formato numérico para la medición del cambio a lo largo del eje x y, si Y no está presente, también a lo largo del eje y. |
| [getXYFactor](#getXYFactor--) | Un factor que debe usarse para convertir las unidades más grandes a lo largo del eje y a las unidades más grandes a lo largo del eje x. |
| [getYFormat](#getYFormat--) | Una matriz de formato numérico para la medición del cambio a lo largo del eje y. |
| [setAngleFormat](#setAngleFormat-com.aspose.pdf.Measure.NumberFormatList-) | Una matriz de formato numérico para la medición de ángulos. |
| [setAreaFormat](#setAreaFormat-com.aspose.pdf.Measure.NumberFormatList-) | Una matriz de formato numérico para la medición de áreas. |
| [setDistanceFormat](#setDistanceFormat-com.aspose.pdf.Measure.NumberFormatList-) | Una matriz de formato numérico para la medición de distancia en cualquier dirección. |
| [setOrigin](#setOrigin-com.aspose.pdf.Point-) | Punto que debe especificar el origen del sistema de coordenadas de medida en coordenadas del espacio de usuario predeterminado. |
| [setScaleRatio](#setScaleRatio-java.lang.String-) |  |
| [setSlopeFormat](#setSlopeFormat-com.aspose.pdf.Measure.NumberFormatList-) | Una matriz de formato numérico para la medición de la pendiente de una línea. |
| [setXFormat](#setXFormat-com.aspose.pdf.Measure.NumberFormatList-) | Una matriz de formato numérico para la medición del cambio a lo largo del eje x y, si Y no está presente, también a lo largo del eje y. |
| [setXYFactor](#setXYFactor-double-) | Un factor que debe usarse para convertir las unidades más grandes a lo largo del eje y a las unidades más grandes a lo largo del eje x. |
| [setYFormat](#setYFormat-com.aspose.pdf.Measure.NumberFormatList-) | Una matriz de formato numérico para la medición del cambio a lo largo del eje y. |

### Measure {#Measure-com.aspose.pdf.Annotation-}
Crea un objeto Measure para anotaciones de medida.

### getAngleFormat {#getAngleFormat--}
```
public Measure.NumberFormatList getAngleFormat()
```

Una matriz de formato numérico para la medición de ángulos.

**Returns:**
Valor NumberFormatList

### getAreaFormat {#getAreaFormat--}
```
public Measure.NumberFormatList getAreaFormat()
```

Una matriz de formato numérico para la medición de áreas.

**Returns:**
Valor NumberFormatList

### getDistanceFormat {#getDistanceFormat--}
```
public Measure.NumberFormatList getDistanceFormat()
```

Una matriz de formato numérico para la medición de distancia en cualquier dirección.

**Returns:**
Valor NumberFormatList

### getOrigin {#getOrigin--}
```
public Point getOrigin()
```

Punto que debe especificar el origen del sistema de coordenadas de medida en coordenadas del espacio de usuario predeterminado.

**Returns:**
Objeto Point

### getScaleRatio {#getScaleRatio--}
```
public String getScaleRatio()
```

Una cadena de texto que expresa la proporción de escala del dibujo.

**Returns:**
objeto string

### getSlopeFormat {#getSlopeFormat--}
```
public Measure.NumberFormatList getSlopeFormat()
```

Una matriz de formato numérico para la medición de la pendiente de una línea.

**Returns:**
Valor NumberFormatList

### getXFormat {#getXFormat--}
```
public Measure.NumberFormatList getXFormat()
```

Una matriz de formato numérico para la medición del cambio a lo largo del eje x y, si Y no está presente, también a lo largo del eje y.

**Returns:**
Valor NumberFormatList

### getXYFactor {#getXYFactor--}
```
public double getXYFactor()
```

Un factor que debe usarse para convertir las unidades más grandes a lo largo del eje y a las unidades más grandes a lo largo del eje x.

**Returns:**
valor double

### getYFormat {#getYFormat--}
```
public Measure.NumberFormatList getYFormat()
```

Una matriz de formato numérico para la medición del cambio a lo largo del eje y.

**Returns:**
Valor NumberFormatList

### setAngleFormat {#setAngleFormat-com.aspose.pdf.Measure.NumberFormatList-}
Una matriz de formato numérico para la medición de ángulos.

### setAreaFormat {#setAreaFormat-com.aspose.pdf.Measure.NumberFormatList-}
Una matriz de formato numérico para la medición de áreas.

### setDistanceFormat {#setDistanceFormat-com.aspose.pdf.Measure.NumberFormatList-}
Una matriz de formato numérico para la medición de distancia en cualquier dirección.

### setOrigin {#setOrigin-com.aspose.pdf.Point-}
Punto que debe especificar el origen del sistema de coordenadas de medida en coordenadas del espacio de usuario predeterminado.

### setScaleRatio {#setScaleRatio-java.lang.String-}


### setSlopeFormat {#setSlopeFormat-com.aspose.pdf.Measure.NumberFormatList-}
Una matriz de formato numérico para la medición de la pendiente de una línea.

### setXFormat {#setXFormat-com.aspose.pdf.Measure.NumberFormatList-}
Una matriz de formato numérico para la medición del cambio a lo largo del eje x y, si Y no está presente, también a lo largo del eje y.

### setXYFactor {#setXYFactor-double-}
```
public void setXYFactor(double value)
```

Un factor que debe usarse para convertir las unidades más grandes a lo largo del eje y a las unidades más grandes a lo largo del eje x.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  | valor double |

### setYFormat {#setYFormat-com.aspose.pdf.Measure.NumberFormatList-}
Una matriz de formato numérico para la medición del cambio a lo largo del eje y.
