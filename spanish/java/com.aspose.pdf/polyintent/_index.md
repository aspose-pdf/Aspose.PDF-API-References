---
title: "PolyIntent"
linktitle: "PolyIntent"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Enumera los propósitos de la anotación de polígono o polilínea."
type: docs
weight: 3910
url: /es/java/com.aspose.pdf/polyintent/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < PolyIntent > com.aspose.pdf.PolyIntent, java.lang.Enum < PolyIntent >, com.aspose.pdf.PolyIntent

**All Implemented Interfaces:**
Serializable, Comparable < PolyIntent >

```
public enum PolyIntent extends Enum < PolyIntent >
```

Enumera los propósitos de la anotación de polígono o polilínea.

## Campos

| Campo | Descripción |
| --- | --- |
| [PolygonCloud](#PolygonCloud) | Significa que la anotación está destinada a funcionar como un objeto de nube. |
| [PolygonDimension](#PolygonDimension) | Indica que la anotación de polígono está destinada a funcionar como una dimensión. |
| [PolyLineDimension](#PolyLineDimension) | Indica que la anotación de polilínea está destinada a funcionar como una dimensión. |
| [Undefined](#Undefined) | Estado indefinido. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### PolygonCloud {#PolygonCloud}
```
public static final PolyIntent PolygonCloud
```

Significa que la anotación está destinada a funcionar como un objeto de nube.

### PolygonDimension {#PolygonDimension}
```
public static final PolyIntent PolygonDimension
```

Indica que la anotación de polígono está destinada a funcionar como una dimensión.

### PolyLineDimension {#PolyLineDimension}
```
public static final PolyIntent PolyLineDimension
```

Indica que la anotación de polilínea está destinada a funcionar como una dimensión.

### Undefined {#Undefined}
```
public static final PolyIntent Undefined
```

Estado indefinido.

### getByValue {#getByValue-int-}
```
public static PolyIntent getByValue(int value)
```



**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static PolyIntent [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
