---
title: "WarningType"
linktitle: "WarningType"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "/ * Enum que representa el tipo de advertencia. / * /"
type: docs
weight: 5490
url: /es/java/com.aspose.pdf/warningtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.WarningType, com.aspose.ms.System.Enum, com.aspose.pdf.WarningType

```
public final class WarningType extends com.aspose.ms.System.Enum
```

/ * Enum que representa el tipo de advertencia. / * /

## Campos

| Campo | Descripción |
| --- | --- |
| [CompatibilityIssue](#CompatibilityIssue) | Problema conocido que impedirá que el documento sea abierto por ciertos agentes de usuario, o versiones anteriores de agentes de usuario. |
| [DataLoss](#DataLoss) | El texto/gráfica/imagen u otros datos faltan completamente del árbol del documento después de cargar, o del documento creado después de guardar. |
| [InvalidInputStreamType](#InvalidInputStreamType) | Tipo de flujo de entrada no válido |
| [MajorFormattingLoss](#MajorFormattingLoss) | Pérdidas importantes de formato comparado con el documento original. Esto es para casos en los que la pérdida de formato es sustancial pero los datos aún están presentes. |
| [MinorFormattingLoss](#MinorFormattingLoss) | Pérdidas menores de formato comparado con el documento original. Esto es para pérdidas menores de fidelidad. |
| [SourceFileCorruption](#SourceFileCorruption) | El archivo está corrupto. |
| [UnexpectedContent](#UnexpectedContent) | El archivo tiene contenido inesperado. |

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

Problema conocido que impedirá que el documento sea abierto por ciertos agentes de usuario, o versiones anteriores de agentes de usuario.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

El texto/gráfica/imagen u otros datos faltan completamente del árbol del documento después de cargar, o del documento creado después de guardar.

### InvalidInputStreamType {#InvalidInputStreamType}
```
public static final int InvalidInputStreamType
```

Tipo de flujo de entrada no válido

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

Pérdidas importantes de formato comparado con el documento original. Esto es para casos en los que la pérdida de formato es sustancial pero los datos aún están presentes.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

Pérdidas menores de formato comparado con el documento original. Esto es para pérdidas menores de fidelidad.

### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

El archivo está corrupto.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

El archivo tiene contenido inesperado.
