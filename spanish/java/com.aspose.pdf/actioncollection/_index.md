---
title: "ActionCollection"
linktitle: "ActionCollection"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Colección de acciones"
type: docs
weight: 40
url: /es/java/com.aspose.pdf/actioncollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ActionCollection

**All Implemented Interfaces:**
Iterable < PdfAction >

```
public final class ActionCollection extends Object implements Iterable < PdfAction >
```

Colección de acciones

## Métodos

| Método | Descripción |
| --- | --- |
| [add](#add-com.aspose.pdf.PdfAction-) | Agrega una nueva acción a la colección. |
| [clear](#clear--) | Borra la colección. |
| [contains](#contains-com.aspose.pdf.PdfAction-) | Aún no soportado. Devuelve true si el elemento dado está presente en la colección. |
| [copyTo](#copyTo-com.aspose.pdf.PdfAction:A-int-) | Copia la matriz de acciones en la colección. |
| [delete](#delete--) | Elimina todas las acciones. |
| [delete](#delete-int-) | Elimina la acción de la colección por índice. |
| [get_Item](#get_Item-int-) | Obtiene la acción por su índice. |
| [getSyncRoot](#getSyncRoot--) | Obtiene el objeto de sincronización. |
| [isReadOnly](#isReadOnly--) | Devuelve true si la colección es de solo lectura. |
| [isSynchronized](#isSynchronized--) | Devuelve true si el objeto está sincronizado. |
| [iterator](#iterator--) | / * / * Devuelve enumerador para la colección. / * / * / * |
| [remove](#remove-com.aspose.pdf.PdfAction-) | * Aún no soportado. Elimina el elemento de la colección. |
| [size](#size--) | Recuento de acciones en la colección. |

### add {#add-com.aspose.pdf.PdfAction-}
Agrega una nueva acción a la colección.

### clear {#clear--}
```
public void clear()
```

Borra la colección.

### contains {#contains-com.aspose.pdf.PdfAction-}
Aún no soportado. Devuelve true si el elemento dado está presente en la colección.

### copyTo {#copyTo-com.aspose.pdf.PdfAction:A-int-}
Copia la matriz de acciones en la colección.

### delete {#delete--}
```
public void delete()
```

Elimina todas las acciones.

### delete {#delete-int-}
```
public void delete(int index)
```

Elimina la acción de la colección por índice.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| index |  | Índice de la acción a eliminar. |

### get_Item {#get_Item-int-}
```
public PdfAction get_Item(int index)
```

Obtiene la acción por su índice.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| index |  | Índice de la acción. |

**Returns:**
Acción recuperada.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Obtiene el objeto de sincronización.

**Returns:**
Valor Object

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Devuelve true si la colección es de solo lectura.

**Returns:**
valor booleano

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Devuelve true si el objeto está sincronizado.

**Returns:**
valor booleano

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator< PdfAction > iterator()
```

/ * / * Devuelve enumerador para la colección. / * / * / *

**Returns:**
Enumerador de colección. /

### remove {#remove-com.aspose.pdf.PdfAction-}
* Not supported yet. Removes item from collection.

### size {#size--}
```
public int size()
```

Recuento de acciones en la colección.

**Returns:**
valor int
