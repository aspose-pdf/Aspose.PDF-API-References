---
title: "MemoryCleaner"
linktitle: "MemoryCleaner"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Representa la clase MemoryCleaner"
type: docs
weight: 3010
url: /es/java/com.aspose.pdf/memorycleaner/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MemoryCleaner

```
public class MemoryCleaner extends Object
```

Representa la clase MemoryCleaner

## Constructores

| Constructor | Descripción |
| --- | --- |
| [MemoryCleaner](#MemoryCleaner--) |  |

## Métodos

| Método | Descripción |
| --- | --- |
| [clear](#clear--) | Limpia la memoria Heap de instancias estáticas de Aspose.PDF (estáticas y threadLocal) y elimina todos los archivos temporales. |
| [clearAllTempFiles](#clearAllTempFiles--) | Elimina los archivos temporales creados por Aspose.PDF en el directorio temporal del sistema. |
| [clearCurrentThreadLocals](#clearCurrentThreadLocals--) | Limpia de forma segura la memoria Heap de instancias ThreadLocal para el hilo actual. |
| [clearFontRegistry](#clearFontRegistry--) | Elimina la información de FontRegistry de la memoria Heap. |
| [clearKeepingFontSettings](#clearKeepingFontSettings--) | Limpia la memoria Heap de instancias estáticas de Aspose.PDF (estáticas y threadLocal) y elimina todos los archivos temporales. |
| [clearLicense](#clearLicense--) | Elimina la información de licencia de la memoria Heap. |
| [clearStaticInstances](#clearStaticInstances--) | Elimina la memoria Heap de instancias estáticas de Aspose.PDF. |

### MemoryCleaner {#MemoryCleaner--}
```
public MemoryCleaner()
```



### clear {#clear--}
```
public static void clear()
```

Limpia la memoria Heap de instancias estáticas de Aspose.PDF (estáticas y threadLocal) y elimina todos los archivos temporales.

### clearAllTempFiles {#clearAllTempFiles--}
```
public static void clearAllTempFiles()
```

Elimina los archivos temporales creados por Aspose.PDF en el directorio temporal del sistema.

### clearCurrentThreadLocals {#clearCurrentThreadLocals--}
```
public static void clearCurrentThreadLocals()
```

Limpia de forma segura la memoria Heap de instancias ThreadLocal para el hilo actual.

### clearFontRegistry {#clearFontRegistry--}
```
public static void clearFontRegistry()
```

Elimina la información de FontRegistry de la memoria Heap.

### clearKeepingFontSettings {#clearKeepingFontSettings--}
```
public static void clearKeepingFontSettings()
```

Limpia la memoria Heap de instancias estáticas de Aspose.PDF (estáticas y threadLocal) y elimina todos los archivos temporales.

### clearLicense {#clearLicense--}
```
public static void clearLicense()
```

Elimina la información de licencia de la memoria Heap.

### clearStaticInstances {#clearStaticInstances--}
```
public static void clearStaticInstances()
```

Elimina la memoria Heap de instancias estáticas de Aspose.PDF.
