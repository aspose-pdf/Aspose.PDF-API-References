---
title: "Artifact.ArtifactType"
linktitle: "Artifact.ArtifactType"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Enumeración de tipos posibles de artefactos."
type: docs
weight: 210
url: /es/java/com.aspose.pdf/artifact.artifacttype/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < Artifact.ArtifactType > com.aspose.pdf.Artifact.ArtifactType, java.lang.Enum < Artifact.ArtifactType >, com.aspose.pdf.Artifact.ArtifactType

**All Implemented Interfaces:**
Serializable, Comparable < Artifact.ArtifactType >

```
public static enum Artifact.ArtifactType extends Enum < Artifact.ArtifactType >
```

Enumeración de tipos posibles de artefactos.

## Campos

| Campo | Descripción |
| --- | --- |
| [Background](#Background) | Artefactos de fondo. |
| [Layout](#Layout) | Artefactos de diseño. |
| [Page](#Page) | Artefactos de página. |
| [Pagination](#Pagination) | Artefactos de paginación. |
| [Undefined](#Undefined) | El tipo de artefacto no está definido o es desconocido. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Background {#Background}
```
public static final Artifact.ArtifactType Background
```

Artefactos de fondo.

### Layout {#Layout}
```
public static final Artifact.ArtifactType Layout
```

Artefactos de diseño.

### Page {#Page}
```
public static final Artifact.ArtifactType Page
```

Artefactos de página.

### Pagination {#Pagination}
```
public static final Artifact.ArtifactType Pagination
```

Artefactos de paginación.

### Undefined {#Undefined}
```
public static final Artifact.ArtifactType Undefined
```

El tipo de artefacto no está definido o es desconocido.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static Artifact.ArtifactType [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
