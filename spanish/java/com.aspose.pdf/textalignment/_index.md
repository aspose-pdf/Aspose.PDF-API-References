---
title: "TextAlignment"
linktitle: "TextAlignment"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Alineación del texto en la anotación."
type: docs
weight: 4910
url: /es/java/com.aspose.pdf/textalignment/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextAlignment > com.aspose.pdf.TextAlignment, java.lang.Enum < TextAlignment >, com.aspose.pdf.TextAlignment

**All Implemented Interfaces:**
Serializable, Comparable < TextAlignment >

```
public enum TextAlignment extends Enum < TextAlignment >
```

Alineación del texto en la anotación.

## Campos

| Campo | Descripción |
| --- | --- |
| [Center](#Center) | El texto está centrado. |
| [Left](#Left) | El texto está alineado a la izquierda. |
| [Right](#Right) | El texto está alineado a la derecha. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### Center {#Center}
```
public static final TextAlignment Center
```

El texto está centrado.

### Left {#Left}
```
public static final TextAlignment Left
```

El texto está alineado a la izquierda.

### Right {#Right}
```
public static final TextAlignment Right
```

El texto está alineado a la derecha.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static TextAlignment [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
