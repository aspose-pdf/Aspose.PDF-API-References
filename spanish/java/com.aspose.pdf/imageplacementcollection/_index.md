---
title: "ImagePlacementCollection"
linktitle: "ImagePlacementCollection"
second_title: "Referencia de la API de Aspose.PDF para Java"
description: "Representa una colección de colocaciones de imágenes"
type: docs
weight: 2350
url: /es/java/com.aspose.pdf/imageplacementcollection/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ImagePlacementCollection

**All Implemented Interfaces:**
Iterable < ImagePlacement >

```
public final class ImagePlacementCollection extends Object implements Iterable < ImagePlacement >
```

Representa una colección de colocaciones de imágenes

## Métodos

| Método | Descripción |
| --- | --- |
| [add](#add-com.aspose.pdf.ImagePlacement-) | Agrega el elemento TextFragment en el índice especificado. |
| [clear](#clear--) | Elimina todos los elementos de la colección. |
| [contains](#contains-com.aspose.pdf.ImagePlacement-) | Determina si la colección contiene un valor específico. |
| [copyTo](#copyTo-com.aspose.pdf.ImagePlacement:A-int-) | Copia toda la colección a una matriz unidimensional compatible, comenzando en el índice especificado de la matriz de destino. |
| [get_Item](#get_Item-int-) | Obtiene el elemento de fragmento de texto en el índice especificado. |
| [getSyncRoot](#getSyncRoot--) | Obtiene un objeto que puede usarse para sincronizar el acceso a la colección. |
| [isReadOnly](#isReadOnly--) | Obtiene un valor que indica si la colección es de solo lectura. |
| [isSynchronized](#isSynchronized--) | Obtiene un valor que indica si el acceso a la colección está sincronizado (seguro para subprocesos). |
| [iterator](#iterator--) | Devuelve un enumerador para toda la colección. |
| [remove](#remove-com.aspose.pdf.ImagePlacement-) | Elimina el elemento especificado de la colección |
| [size](#size--) | Obtiene el número de elementos de objeto {@code ImagePlacement} realmente contenidos en la colección. |

### add {#add-com.aspose.pdf.ImagePlacement-}
Agrega el elemento TextFragment en el índice especificado.

### clear {#clear--}
```
public void clear()
```

Elimina todos los elementos de la colección.

### contains {#contains-com.aspose.pdf.ImagePlacement-}
Determina si la colección contiene un valor específico.

### copyTo {#copyTo-com.aspose.pdf.ImagePlacement:A-int-}
Copia toda la colección a una matriz unidimensional compatible, comenzando en el índice especificado de la matriz de destino.

### get_Item {#get_Item-int-}
```
public ImagePlacement get_Item(int index)
```

Obtiene el elemento de fragmento de texto en el índice especificado.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| index |  | Índice de la ubicación de la imagen. |

**Returns:**
Objeto ImagePlacement.

### getSyncRoot {#getSyncRoot--}
```
public Object getSyncRoot()
```

Obtiene un objeto que puede usarse para sincronizar el acceso a la colección.

**Returns:**
Objeto para sincronización

### isReadOnly {#isReadOnly--}
```
public boolean isReadOnly()
```

Obtiene un valor que indica si la colección es de solo lectura.

**Returns:**
valor booleano

### isSynchronized {#isSynchronized--}
```
public boolean isSynchronized()
```

Obtiene un valor que indica si el acceso a la colección está sincronizado (seguro para subprocesos).

**Returns:**
valor booleano

### iterator {#iterator--}
```
public com.aspose.ms.System.Collections.IEnumerator< ImagePlacement > iterator()
```

Devuelve un enumerador para toda la colección.

**Returns:**
Objeto enumerador.

### remove {#remove-com.aspose.pdf.ImagePlacement-}
Elimina el elemento especificado de la colección

### size {#size--}
```
public int size()
```

Obtiene el número de elementos de objeto {@code ImagePlacement} realmente contenidos en la colección.

**Returns:**
valor int
