---
title: "CoordinateOrigin"
linktitle: "CoordinateOrigin"
second_title: "Referencia de la API de Aspose.PDF para Java"
description:
type: docs
weight: 840
url: /es/java/com.aspose.pdf/coordinateorigin/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < CoordinateOrigin > com.aspose.pdf.CoordinateOrigin, java.lang.Enum < CoordinateOrigin >, com.aspose.pdf.CoordinateOrigin

**All Implemented Interfaces:**
Serializable, Comparable < CoordinateOrigin >

```
public enum CoordinateOrigin extends Enum < CoordinateOrigin >
```



## Campos

| Campo | Descripción |
| --- | --- |
| [BaseLine](#BaseLine) | La coordenada más baja del texto debe tratarse como la línea base. |
| [Descender](#Descender) | La coordenada más baja del texto debe tratarse como la línea descendente. |

## Métodos

| Método | Descripción |
| --- | --- |
| [getByValue](#getByValue-int-) |  |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Devuelve la constante enum de este tipo con el nombre especificado. |
| [values](#values--) | Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran. |

### BaseLine {#BaseLine}
```
public static final CoordinateOrigin BaseLine
```

La coordenada más baja del texto debe tratarse como la línea base.

### Descender {#Descender}
```
public static final CoordinateOrigin Descender
```

La coordenada más baja del texto debe tratarse como la línea descendente.

### getByValue {#getByValue-int-}
```
public static CoordinateOrigin getByValue(int value)
```



**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor |  |  |

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Devuelve la constante enum de este tipo con el nombre especificado.

### values {#values--}
```
public static CoordinateOrigin [] values()
```

Devuelve una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran.

**Returns:**
una matriz que contiene las constantes de este tipo enum, en el orden en que se declaran
