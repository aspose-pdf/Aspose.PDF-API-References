---
title: CollectionFieldSubtype
second_title: Aspose.PDF for Java API Reference
description: Represents the subtype parameter of a field in a sceme collection.
type: docs
weight: 630
url: /java/com.aspose.pdf/collectionfieldsubtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum> com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.ValueType<com.aspose.ms.System.Enum>, com.aspose.ms.System.Enum com.aspose.pdf.CollectionFieldSubtype, com.aspose.ms.System.Enum, com.aspose.pdf.CollectionFieldSubtype

```
public final class CollectionFieldSubtype extends com.aspose.ms.System.Enum
```

Represents the subtype parameter of a field in a sceme collection.

## Fields

| Field | Description |
| --- | --- |
| [CompressedSize](#CompressedSize) | (PDF 2.0) The field data is the length of the embedded file stream, as identified by the Length entry in the embedded file stream dictionary, and the two values shall be identical. |
| [CreationDate](#CreationDate) | The field data shall be the creation date of the embedded file stream, as identified by the CreationDate entry in the embedded file |
| [D](#D) | A date type. The field data shall be stored as a PDF date string. |
| [Desc](#Desc) | The field data shall be the file name of the embedded file stream, as identified by the UF entry of the file specification, if present; otherwise by the F entry of the file specification |
| [F](#F) | The field data shall be the file name of the embedded file stream, as identified by the UF entry of the file specification, if present; otherwise by the F entry of the file specification |
| [ModDate](#ModDate) | The field data shall be the modification date of the embedded file stream, as identified by the ModDate entry in the embedded file parameter dictionary. |
| [N](#N) | A number type. The field data shall be stored as a PDF number. |
| [None](#None) | The subtype is not defined. |
| [S](#S) | A text type. The field data shall be stored as a PDF text string. |
| [Size](#Size) | The field data shall be the size of the embedded file, as identified by the Size entry in the embedded file parameter dictionary |

### CompressedSize {#CompressedSize}
```
public static final int CompressedSize
```

(PDF 2.0) The field data is the length of the embedded file stream, as identified by the Length entry in the embedded file stream dictionary, and the two values shall be identical.

### CreationDate {#CreationDate}
```
public static final int CreationDate
```

The field data shall be the creation date of the embedded file stream, as identified by the CreationDate entry in the embedded file

### D {#D}
```
public static final int D
```

A date type. The field data shall be stored as a PDF date string.

### Desc {#Desc}
```
public static final int Desc
```

The field data shall be the file name of the embedded file stream, as identified by the UF entry of the file specification, if present; otherwise by the F entry of the file specification

### F {#F}
```
public static final int F
```

The field data shall be the file name of the embedded file stream, as identified by the UF entry of the file specification, if present; otherwise by the F entry of the file specification

### ModDate {#ModDate}
```
public static final int ModDate
```

The field data shall be the modification date of the embedded file stream, as identified by the ModDate entry in the embedded file parameter dictionary.

### N {#N}
```
public static final int N
```

A number type. The field data shall be stored as a PDF number.

### None {#None}
```
public static final int None
```

The subtype is not defined.

### S {#S}
```
public static final int S
```

A text type. The field data shall be stored as a PDF text string.

### Size {#Size}
```
public static final int Size
```

The field data shall be the size of the embedded file, as identified by the Size entry in the embedded file parameter dictionary
