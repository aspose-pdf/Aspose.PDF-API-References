---
title: "ColumnInfo"
linktitle: "ColumnInfo"
second_title: "Aspose.PDF for Java API Referansı"
description: "Bu sınıf bir sütunun bilgilerini temsil eder."
type: docs
weight: 730
url: /tr/java/com.aspose.pdf/columninfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.ColumnInfo

```
public final class ColumnInfo extends Object
```

Bu sınıf bir sütunun bilgilerini temsil eder.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [ColumnInfo](#ColumnInfo--) | ColumnInfo sınıfının yeni bir örneğini başlatır. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getColumnCount](#getColumnCount--) | Sütun sayısını gösteren int değerini alır. |
| [getColumnSpacing](#getColumnSpacing--) | <p> Alır veya ayarlar, sütunlar arasındaki boşlukları içeren bir dize. Her boşluğun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm ve inç de desteklenir. Örneğin,\"120 2.5cm 1.5inch\". </p><hr> <p> Bu özellik ayarlanmamışsa, her boşluk için varsayılan değer 0 kullanılacaktır. </p> |
| [getColumnWidths](#getColumnWidths--) | Alır veya ayarlar, sütun genişliklerini içeren bir dize. Her sütunun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm, inç ve kullanılabilir genişliğin yüzdesi de desteklenir. Örneğin,\"120 2.5cm 1.5inch\" |
| [setColumnCount](#setColumnCount-int-) | Sütun sayısını gösteren int değerini ayarlar. |
| [setColumnSpacing](#setColumnSpacing-java.lang.String-) | <p> Alır veya ayarlar, sütunlar arasındaki boşlukları içeren bir dize. Her boşluğun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm ve inç de desteklenir. Örneğin,\"120 2.5cm 1.5inch\". </p><hr> <p> Bu özellik ayarlanmamışsa, her boşluk için varsayılan değer 0 kullanılacaktır. </p> |
| [setColumnWidths](#setColumnWidths-java.lang.String-) | Alır veya ayarlar, sütun genişliklerini içeren bir dize. Her sütunun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm, inç ve kullanılabilir genişliğin yüzdesi de desteklenir. Örneğin,\"120 2.5cm 1.5inch\" |

### ColumnInfo {#ColumnInfo--}
```
public ColumnInfo()
```

ColumnInfo sınıfının yeni bir örneğini başlatır.

### getColumnCount {#getColumnCount--}
```
public int getColumnCount()
```

Sütun sayısını gösteren int değerini alır.

**Returns:**
sütun sayısı

### getColumnSpacing {#getColumnSpacing--}
```
public String getColumnSpacing()
```

<p> Alır veya ayarlar, sütunlar arasındaki boşlukları içeren bir dize. Her boşluğun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm ve inç de desteklenir. Örneğin,\"120 2.5cm 1.5inch\". </p><hr> <p> Bu özellik ayarlanmamışsa, her boşluk için varsayılan değer 0 kullanılacaktır. </p>

**Returns:**
String değeri

### getColumnWidths {#getColumnWidths--}
```
public String getColumnWidths()
```

Alır veya ayarlar, sütun genişliklerini içeren bir dize. Her sütunun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm, inç ve kullanılabilir genişliğin yüzdesi de desteklenir. Örneğin,\"120 2.5cm 1.5inch\"

**Returns:**
String değeri

### setColumnCount {#setColumnCount-int-}
```
public void setColumnCount(int value)
```

Sütun sayısını gösteren int değerini ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | sütun sayısı |

### setColumnSpacing {#setColumnSpacing-java.lang.String-}
<p> Alır veya ayarlar, sütunlar arasındaki boşlukları içeren bir dize. Her boşluğun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm ve inç de desteklenir. Örneğin,\"120 2.5cm 1.5inch\". </p><hr> <p> Bu özellik ayarlanmamışsa, her boşluk için varsayılan değer 0 kullanılacaktır. </p>

### setColumnWidths {#setColumnWidths-java.lang.String-}
Alır veya ayarlar, sütun genişliklerini içeren bir dize. Her sütunun değeri boşlukla ayrılmalıdır. Varsayılan birim puandır, ancak cm, inç ve kullanılabilir genişliğin yüzdesi de desteklenir. Örneğin,\"120 2.5cm 1.5inch\"
