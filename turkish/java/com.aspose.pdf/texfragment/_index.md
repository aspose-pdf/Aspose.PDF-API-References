---
title: "TeXFragment"
linktitle: "TeXFragment"
second_title: "Aspose.PDF for Java API Referansı"
description: "LaTeX parçacığını temsil eder."
type: docs
weight: 4860
url: /tr/java/com.aspose.pdf/texfragment/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.BaseParagraph com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.BaseParagraph, com.aspose.pdf.FormattedFragment com.aspose.pdf.TeXFragment, com.aspose.pdf.FormattedFragment, com.aspose.pdf.TeXFragment

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public class TeXFragment extends FormattedFragment
```

LaTeX parçacığını temsil eder.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [TeXFragment](#TeXFragment-java.lang.String-) | HtmlFragment sınıfının yeni bir örneğini başlatır. |
| [TeXFragment](#TeXFragment-java.lang.String-boolean-) | HtmlFragment sınıfının yeni bir örneğini başlatır. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [deepClone](#deepClone--) | Parçayı klonlar. |
| [getLatexLoadOptionsOfInstance](#getLatexLoadOptionsOfInstance--) | Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak TeXLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır. |
| [getTeXLoadOptionsOfInstance](#getTeXLoadOptionsOfInstance--) | Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak TeXLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır. |
| [setLatexLoadOptionsOfInstance](#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak TeXLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır. |
| [setTeXLoadOptionsOfInstance](#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-) | Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak LatexLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır. |

### TeXFragment {#TeXFragment-java.lang.String-}
HtmlFragment sınıfının yeni bir örneğini başlatır.

### TeXFragment {#TeXFragment-java.lang.String-boolean-}
HtmlFragment sınıfının yeni bir örneğini başlatır.

### deepClone {#deepClone--}
```
public Object deepClone()
```

Parçayı klonlar.

**Returns:**
Klonlanmış parça.

### getLatexLoadOptionsOfInstance {#getLatexLoadOptionsOfInstance--}
```
@Deprecated public final TeXLoadOptions getLatexLoadOptionsOfInstance()
```

Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak TeXLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır.

**Returns:**
TeXLoadOptions örneği @deprecated Lütfen bunun yerine TeXLoadOptionsOfInstance kullanın.

### getTeXLoadOptionsOfInstance {#getTeXLoadOptionsOfInstance--}
```
public TeXLoadOptions getTeXLoadOptionsOfInstance()
```

Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak TeXLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır.

**Returns:**
TeXLoadOptions örneği

### setLatexLoadOptionsOfInstance {#setLatexLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak TeXLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır.

### setTeXLoadOptionsOfInstance {#setTeXLoadOptionsOfInstance-com.aspose.pdf.TeXLoadOptions-}
Bu sınıfın bu örneğine LaTeX'in yüklenmesi (ve işlenmesi) için kullanılacak LatexLoadOptions değerini alır veya ayarlar. LaTeX'in bu ya da o örnek için içe aktarımında belirli bir ayar kullanılmasının gerektiği durumlarda (ör. bu ya da o örnek, içe aktarılan LaTeX için belirli bir BasePath kullanmalı veya dış kaynakların belirli bir yükleyicisini kullanmalı) lütfen bunu kullanın. Parametre varsayılan (null) ise, standart LaTeX yükleme seçenekleri kullanılacaktır.
