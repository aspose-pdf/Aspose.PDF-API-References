---
title: "RegexManager"
linktitle: "RegexManager"
second_title: "Aspose.PDF for Java API Referansı"
description: "Yapılandırılabilir zaman aşımı ayarlarıyla düzenli ifade işlemleri için bir sarmalayıcı sağlar."
type: docs
weight: 4130
url: /tr/java/com.aspose.pdf/regexmanager/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.RegexManager

```
public class RegexManager extends Object
```

Yapılandırılabilir zaman aşımı ayarlarıyla düzenli ifade işlemleri için bir sarmalayıcı sağlar.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [RegexManager](#RegexManager--) |  |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getMatchTimeout](#getMatchTimeout--) | Kütüphane genelinde Regex işlemleri için zaman aşımını alır veya ayarlar. Varsayılan değer 1000 ms'dir. Değer: Varsayılan zaman aşımı süresini temsil eden bir {@link double}. |
| [setMatchTimeout](#setMatchTimeout-int-) | Kütüphane genelinde Regex işlemleri için zaman aşımını alır veya ayarlar. Varsayılan değer 1000 ms'dir. Değer: |

### RegexManager {#RegexManager--}
```
public RegexManager()
```



### getMatchTimeout {#getMatchTimeout--}
```
public static int getMatchTimeout()
```

Kütüphane genelinde Regex işlemleri için zaman aşımını alır veya ayarlar. Varsayılan değer 1000 ms'dir. Değer: Varsayılan zaman aşımı süresini temsil eden bir {@link double}.

**Returns:**
int değer

### setMatchTimeout {#setMatchTimeout-int-}
```
public static void setMatchTimeout(int value)
```

Kütüphane genelinde Regex işlemleri için zaman aşımını alır veya ayarlar. Varsayılan değer 1000 ms'dir. Değer:

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | Milisaniye cinsinden varsayılan zaman aşımı süresini temsil eden bir A. |
