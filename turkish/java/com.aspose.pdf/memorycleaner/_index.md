---
title: "MemoryCleaner"
linktitle: "MemoryCleaner"
second_title: "Aspose.PDF for Java API Referansı"
description: "MemoryCleaner sınıfını temsil eder"
type: docs
weight: 3010
url: /tr/java/com.aspose.pdf/memorycleaner/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MemoryCleaner

```
public class MemoryCleaner extends Object
```

MemoryCleaner sınıfını temsil eder

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [MemoryCleaner](#MemoryCleaner--) |  |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [clear](#clear--) | Statik Aspose.PDF örneklerinden (statik ve threadLocal) Heap belleğini temizler ve tüm geçici dosyaları siler. |
| [clearAllTempFiles](#clearAllTempFiles--) | Sistem geçici dizininde Aspose.PDF tarafından oluşturulan geçici dosyaları temizler. |
| [clearCurrentThreadLocals](#clearCurrentThreadLocals--) | Geçerli iş parçacığı için ThreadLocal örneklerinden Heap belleğini güvenli bir şekilde temizler |
| [clearFontRegistry](#clearFontRegistry--) | Heap belleğinden FontRegistry bilgilerini temizler. |
| [clearKeepingFontSettings](#clearKeepingFontSettings--) | Statik Aspose.PDF örneklerinden (statik ve threadLocal) Heap belleğini temizler ve tüm geçici dosyaları siler. |
| [clearLicense](#clearLicense--) | Heap belleğinden lisans bilgilerini temizler. |
| [clearStaticInstances](#clearStaticInstances--) | Statik Aspose.PDF örneklerinden Heap belleğini temizler. |

### MemoryCleaner {#MemoryCleaner--}
```
public MemoryCleaner()
```



### clear {#clear--}
```
public static void clear()
```

Statik Aspose.PDF örneklerinden (statik ve threadLocal) Heap belleğini temizler ve tüm geçici dosyaları siler.

### clearAllTempFiles {#clearAllTempFiles--}
```
public static void clearAllTempFiles()
```

Sistem geçici dizininde Aspose.PDF tarafından oluşturulan geçici dosyaları temizler.

### clearCurrentThreadLocals {#clearCurrentThreadLocals--}
```
public static void clearCurrentThreadLocals()
```

Geçerli iş parçacığı için ThreadLocal örneklerinden Heap belleğini güvenli bir şekilde temizler

### clearFontRegistry {#clearFontRegistry--}
```
public static void clearFontRegistry()
```

Heap belleğinden FontRegistry bilgilerini temizler.

### clearKeepingFontSettings {#clearKeepingFontSettings--}
```
public static void clearKeepingFontSettings()
```

Statik Aspose.PDF örneklerinden (statik ve threadLocal) Heap belleğini temizler ve tüm geçici dosyaları siler.

### clearLicense {#clearLicense--}
```
public static void clearLicense()
```

Heap belleğinden lisans bilgilerini temizler.

### clearStaticInstances {#clearStaticInstances--}
```
public static void clearStaticInstances()
```

Statik Aspose.PDF örneklerinden Heap belleğini temizler.
