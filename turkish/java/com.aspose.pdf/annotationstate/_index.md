---
title: "AnnotationState"
linktitle: "AnnotationState"
second_title: "Aspose.PDF for Java API Referansı"
description: "Orijinal ek açıklamanın ayarlanabileceği durumların sıralaması."
type: docs
weight: 110
url: /tr/java/com.aspose.pdf/annotationstate/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < AnnotationState > com.aspose.pdf.AnnotationState, java.lang.Enum < AnnotationState >, com.aspose.pdf.AnnotationState

**All Implemented Interfaces:**
Serializable, Comparable < AnnotationState >

```
public enum AnnotationState extends Enum < AnnotationState >
```

Orijinal ek açıklamanın ayarlanabileceği durumların sıralaması.

## Alanlar

| Alan | Açıklama |
| --- | --- |
| [Accepted](#Accepted) | Kullanıcı değişikliği kabul ediyor. |
| [Cancelled](#Cancelled) | Değişiklik iptal edildi. |
| [Completed](#Completed) | Değişiklik tamamlandı. |
| [Marked](#Marked) | Ek açıklama kullanıcı tarafından işaretlendi. |
| [None](#None) | Kullanıcı değişiklik hakkında hiçbir şey belirtmedi. |
| [Rejected](#Rejected) | Kullanıcı değişikliği kabul etmiyor. |
| [Undefined](#Undefined) | Tanımsız durum. |
| [Unmarked](#Unmarked) | Ek açıklama kullanıcı tarafından işaretlenmedi. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Bu tipin belirtilen adla enum sabitini döndürür. |
| [values](#values--) | Bu enum tipinin sabitlerini içeren bir dizi döndürür, tanımlandıkları sırada. |

### Accepted {#Accepted}
```
public static final AnnotationState Accepted
```

Kullanıcı değişikliği kabul ediyor.

### Cancelled {#Cancelled}
```
public static final AnnotationState Cancelled
```

Değişiklik iptal edildi.

### Completed {#Completed}
```
public static final AnnotationState Completed
```

Değişiklik tamamlandı.

### Marked {#Marked}
```
public static final AnnotationState Marked
```

Ek açıklama kullanıcı tarafından işaretlendi.

### None {#None}
```
public static final AnnotationState None
```

Kullanıcı değişiklik hakkında hiçbir şey belirtmedi.

### Rejected {#Rejected}
```
public static final AnnotationState Rejected
```

Kullanıcı değişikliği kabul etmiyor.

### Undefined {#Undefined}
```
public static final AnnotationState Undefined
```

Tanımsız durum.

### Unmarked {#Unmarked}
```
public static final AnnotationState Unmarked
```

Ek açıklama kullanıcı tarafından işaretlenmedi.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Bu tipin belirtilen adla enum sabitini döndürür.

### values {#values--}
```
public static AnnotationState [] values()
```

Bu enum tipinin sabitlerini içeren bir dizi döndürür, tanımlandıkları sırada.

**Returns:**
Bu enum tipinin sabitlerini içeren bir dizi, tanımlandıkları sırada
