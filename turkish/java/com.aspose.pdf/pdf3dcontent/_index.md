---
title: "PDF3DContent"
linktitle: "PDF3DContent"
second_title: "Aspose.PDF for Java API Referansı"
description: "PDF3DContent sınıfı."
type: docs
weight: 3580
url: /tr/java/com.aspose.pdf/pdf3dcontent/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DContent

```
public class PDF3DContent extends Object
```

PDF3DContent sınıfı.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [PDF3DContent](#PDF3DContent--) | Yeni bir {@code PDF3DContent} sınıfı örneği başlatır. |
| [PDF3DContent](#PDF3DContent-java.lang.String-) | Yeni bir {@code PDF3DContent} sınıfı örneği başlatır. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getAsByteArray](#getAsByteArray--) | 3D içeriği bayt dizisi olarak alır. |
| [getAsStream](#getAsStream--) | 3D içeriği akış olarak alır. |
| [getExtension](#getExtension--) | Uzantıyı alır . |
| [load](#load-java.lang.String-) | Belirtilen dosya adıyla 3D içeriği yükler. |
| [loadAsPRC](#loadAsPRC-byte:A-) | Bayt dizisinden PRC formatında 3D içeriği yükler. |
| [loadAsPRC](#loadAsPRC-java.io.InputStream-) | Akıştan PRC formatında 3D içeriği yükler. |
| [loadAsPRC](#loadAsPRC-java.lang.String-) | Belirtilen dosya adıyla PRC formatında 3D içeriği yükler. |
| [loadAsU3D](#loadAsU3D-byte:A-) | Bayt dizisinden U3D formatında 3D içeriği yükler. |
| [loadAsU3D](#loadAsU3D-java.io.InputStream-) | Akıştan U3D formatında 3D içeriği yükler. |
| [loadAsU3D](#loadAsU3D-java.lang.String-) | Belirtilen dosya adıyla U3D formatında 3D içeriği yükler. |
| [saveToFile](#saveToFile-java.lang.String-) | 3D içeriği dosyaya kaydeder. |

### PDF3DContent {#PDF3DContent--}
```
public PDF3DContent()
```

Yeni bir {@code PDF3DContent} sınıfı örneği başlatır.

### PDF3DContent {#PDF3DContent-java.lang.String-}
Yeni bir {@code PDF3DContent} sınıfı örneği başlatır.

### getAsByteArray {#getAsByteArray--}
```
public byte[] getAsByteArray()
```

3D içeriği bayt dizisi olarak alır.

**Returns:**
System.Byte[].

### getAsStream {#getAsStream--}
```
public InputStream getAsStream()
```

3D içeriği akış olarak alır.

**Returns:**
Stream.

### getExtension {#getExtension--}
```
public String getExtension()
```

Uzantıyı alır .

**Returns:**
Dize nesnesi: Uzantı.

### load {#load-java.lang.String-}
Belirtilen dosya adıyla 3D içeriği yükler.

### loadAsPRC {#loadAsPRC-byte:A-}
```
public void loadAsPRC(byte[] stream)
```

Bayt dizisinden PRC formatında 3D içeriği yükler.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| stream |  | Akış. |

### loadAsPRC {#loadAsPRC-java.io.InputStream-}
Akıştan PRC formatında 3D içeriği yükler.

### loadAsPRC {#loadAsPRC-java.lang.String-}
Belirtilen dosya adıyla PRC formatında 3D içeriği yükler.

### loadAsU3D {#loadAsU3D-byte:A-}
```
public void loadAsU3D(byte[] stream)
```

Bayt dizisinden U3D formatında 3D içeriği yükler.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| stream |  | Akış. |

### loadAsU3D {#loadAsU3D-java.io.InputStream-}
Akıştan U3D formatında 3D içeriği yükler.

### loadAsU3D {#loadAsU3D-java.lang.String-}
Belirtilen dosya adıyla U3D formatında 3D içeriği yükler.

### saveToFile {#saveToFile-java.lang.String-}
3D içeriği dosyaya kaydeder.
