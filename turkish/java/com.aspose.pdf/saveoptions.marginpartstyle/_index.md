---
title: "SaveOptions.MarginPartStyle"
linktitle: "SaveOptions.MarginPartStyle"
second_title: "Aspose.PDF for Java API Referansı"
description: "Kenar boşluğunun bir parçasının (üst, alt, sol yan veya sağ yan) bilgisini temsil eder."
type: docs
weight: 4420
url: /tr/java/com.aspose.pdf/saveoptions.marginpartstyle/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.SaveOptions.MarginPartStyle

```
public static class SaveOptions.MarginPartStyle extends Object
```

Kenar boşluğunun bir parçasının (üst, alt, sol yan veya sağ yan) bilgisini temsil eder.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [MarginPartStyle](#MarginPartStyle-boolean-) | MarginPartStyle sınıfının bir örneğini oluşturur ve değerini puan cinsinden başlatır. |
| [MarginPartStyle](#MarginPartStyle-int-) | MarginPartStyle sınıfının bir örneğini oluşturur ve değerini puan cinsinden ayarlar. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getValueInPoints](#getValueInPoints--) | Marjı puan cinsinden temsil eder. Sıfırdan büyük bir sayı olmalıdır. |
| [isAuto](#isAuto--) | Bu örneğin otomatik olup olmadığını gösteren bir değeri alır veya ayarlar. Değer: {@code true} ise bu örnek otomatik; aksi takdirde {@code false}. |
| [setAuto](#setAuto-boolean-) | Bu örneğin otomatik olup olmadığını gösteren bir değeri alır veya ayarlar. Değer: {@code true} ise bu örnek otomatik; aksi takdirde {@code false}. |
| [setValueInPoints](#setValueInPoints-int-) | Marjı puan cinsinden temsil eder. Sıfırdan büyük bir sayı olmalıdır. |

### MarginPartStyle {#MarginPartStyle-boolean-}
```
public MarginPartStyle(boolean isAuto)
```

MarginPartStyle sınıfının bir örneğini oluşturur ve değerini puan cinsinden başlatır.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| isAuto |  | Marjı otomatik işaretle |

### MarginPartStyle {#MarginPartStyle-int-}
```
public MarginPartStyle(int valueInPoints)
```

MarginPartStyle sınıfının bir örneğini oluşturur ve değerini puan cinsinden ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| valueInPoints |  | Puan cinsinden tam sayı değeri |

### getValueInPoints {#getValueInPoints--}
```
public final int getValueInPoints()
```

Marjı puan cinsinden temsil eder. Sıfırdan büyük bir sayı olmalıdır.

**Returns:**
int değer

### isAuto {#isAuto--}
```
public final boolean isAuto()
```

Bu örneğin otomatik olup olmadığını gösteren bir değeri alır veya ayarlar. Değer: {@code true} ise bu örnek otomatik; aksi takdirde {@code false}.

**Returns:**
boolean değer

### setAuto {#setAuto-boolean-}
```
public final void setAuto(boolean value)
```

Bu örneğin otomatik olup olmadığını gösteren bir değeri alır veya ayarlar. Değer: {@code true} ise bu örnek otomatik; aksi takdirde {@code false}.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | boolean değer |

### setValueInPoints {#setValueInPoints-int-}
```
public final void setValueInPoints(int value)
```

Marjı puan cinsinden temsil eder. Sıfırdan büyük bir sayı olmalıdır.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | int değer |
