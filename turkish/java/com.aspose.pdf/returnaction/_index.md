---
title: "ReturnAction"
linktitle: "ReturnAction"
second_title: "Aspose.PDF for Java API Referansı"
description: "Enum, {@code IWarningCallback.Warning(WarningInfo)} yöntemi çağrıldığında bir program iş akışı eylemini temsil eder."
type: docs
weight: 4240
url: /tr/java/com.aspose.pdf/returnaction/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < ReturnAction > com.aspose.pdf.ReturnAction, java.lang.Enum < ReturnAction >, com.aspose.pdf.ReturnAction

**All Implemented Interfaces:**
Serializable, Comparable < ReturnAction >

```
public enum ReturnAction extends Enum < ReturnAction >
```

Enum, {@code IWarningCallback.Warning(WarningInfo)} yöntemi çağrıldığında bir program iş akışı eylemini temsil eder.

## Alanlar

| Alan | Açıklama |
| --- | --- |
| [Abort](#Abort) | İptal akışı için kullanılır. |
| [Continue](#Continue) | Devam akışı için kullanılır. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [valueOf](#valueOf-java.lang.String-) | Bu tipin belirtilen adla enum sabitini döndürür. |
| [values](#values--) | Bu enum tipinin sabitlerini içeren bir dizi döndürür, tanımlandıkları sırada. |

### Abort {#Abort}
```
public static final ReturnAction Abort
```

İptal akışı için kullanılır.

### Continue {#Continue}
```
public static final ReturnAction Continue
```

Devam akışı için kullanılır.

### valueOf {#valueOf-java.lang.String-}
Bu tipin belirtilen adla enum sabitini döndürür.

### values {#values--}
```
public static ReturnAction [] values()
```

Bu enum tipinin sabitlerini içeren bir dizi döndürür, tanımlandıkları sırada.

**Returns:**
Bu enum tipinin sabitlerini içeren bir dizi, tanımlandıkları sırada
