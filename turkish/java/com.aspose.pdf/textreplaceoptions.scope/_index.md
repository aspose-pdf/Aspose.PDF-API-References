---
title: "TextReplaceOptions.Scope"
linktitle: "TextReplaceOptions.Scope"
second_title: "Aspose.PDF for Java API Referansı"
description: "Metin değiştirme işleminin uygulandığı kapsam, varsayılan olarak REPLACE_FIRST'tir. Bu eski seçenek uyumluluk için tutulmuştur. PdfContentEditor'ı etkiler ve başka bir şeye etkisi yoktur."
type: docs
weight: 5280
url: /tr/java/com.aspose.pdf/textreplaceoptions.scope/
---
**Inheritance:**
java.lang.Object, java.lang.Enum < TextReplaceOptions.Scope > com.aspose.pdf.TextReplaceOptions.Scope, java.lang.Enum < TextReplaceOptions.Scope >, com.aspose.pdf.TextReplaceOptions.Scope

**All Implemented Interfaces:**
Serializable, Comparable < TextReplaceOptions.Scope >

```
public static enum TextReplaceOptions.Scope extends Enum < TextReplaceOptions.Scope >
```

Metin değiştirme işleminin uygulandığı kapsam REPLACE_FIRST varsayılan olarak. Bu eski seçenek uyumluluk için tutulmuştur. PdfContentEditor'i etkiler ve TextFragmentAbsorber'ı etkilemez.

## Alanlar

| Alan | Açıklama |
| --- | --- |
| [REPLACE_ALL](#REPLACE_ALL) | Etkilenen tüm sayfalardaki tüm metin oluşumlarını değiştir. |
| [REPLACE_FIRST](#REPLACE_FIRST) | Etkilenen her sayfada yalnızca metnin ilk oluşumunu değiştir. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getValue](#getValue--) |  |
| [valueOf](#valueOf-java.lang.String-) | Bu tipin belirtilen adla enum sabitini döndürür. |
| [values](#values--) | Bu enum tipinin sabitlerini içeren bir dizi döndürür, tanımlandıkları sırada. |

### REPLACE_ALL {#REPLACE_ALL}
```
public static final TextReplaceOptions.Scope REPLACE_ALL
```

Etkilenen tüm sayfalardaki tüm metin oluşumlarını değiştir.

### REPLACE_FIRST {#REPLACE_FIRST}
```
public static final TextReplaceOptions.Scope REPLACE_FIRST
```

Etkilenen her sayfada yalnızca metnin ilk oluşumunu değiştir.

### getValue {#getValue--}
```
public int getValue()
```



### valueOf {#valueOf-java.lang.String-}
Bu tipin belirtilen adla enum sabitini döndürür.

### values {#values--}
```
public static TextReplaceOptions.Scope [] values()
```

Bu enum tipinin sabitlerini içeren bir dizi döndürür, tanımlandıkları sırada.

**Returns:**
Bu enum tipinin sabitlerini içeren bir dizi, tanımlandıkları sırada
