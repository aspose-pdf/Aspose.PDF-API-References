---
title: "PDF3DLightingScheme"
linktitle: "PDF3DLightingScheme"
second_title: "Aspose.PDF for Java API Referansı"
description: "PDF3DLightingScheme sınıfı."
type: docs
weight: 3620
url: /tr/java/com.aspose.pdf/pdf3dlightingscheme/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.PDF3DLightingScheme

```
public class PDF3DLightingScheme extends Object
```

PDF3DLightingScheme sınıfı.

## Alanlar

| Alan | Açıklama |
| --- | --- |
| [Artwork](#Artwork) | "Artwork" aydınlatma şeması. |
| [Blue](#Blue) | "Blue" aydınlatma şeması. |
| [CAD](#CAD) | "Cad" aydınlatma şeması. |
| [Cube](#Cube) | "Cube" aydınlatma şeması. |
| [Day](#Day) | "Day" aydınlatma şeması. |
| [Hard](#Hard) | Bu "Hard" aydınlatma şeması. |
| [Headlamp](#Headlamp) | Bu "Headlamp" aydınlatma şeması. |
| [Night](#Night) | Bu "Night" aydınlatma şeması. |
| [None](#None) | Bu "None" aydınlatma şeması. |
| [Primary](#Primary) | Bu "Primary" aydınlatma şeması. |
| [Red](#Red) | Bu "Red" aydınlatma şeması. |
| [White](#White) | Bu "White" aydınlatma şeması. |

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [PDF3DLightingScheme](#PDF3DLightingScheme-com.aspose.pdf.LightingSchemeType-) | Yeni bir {@code PDF3DLightingScheme} sınıfının örneğini başlatır. |
| [PDF3DLightingScheme](#PDF3DLightingScheme-java.lang.String-) | Yeni bir {@code PDF3DLightingScheme} sınıfının örneğini başlatır. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getType](#getType--) | Aydınlatma şeması türünü alır. |

### Artwork {#Artwork}
```
public static final PDF3DLightingScheme Artwork
```

"Artwork" aydınlatma şeması.

### Blue {#Blue}
```
public static final PDF3DLightingScheme Blue
```

"Blue" aydınlatma şeması.

### CAD {#CAD}
```
public static final PDF3DLightingScheme CAD
```

"Cad" aydınlatma şeması.

### Cube {#Cube}
```
public static final PDF3DLightingScheme Cube
```

"Cube" aydınlatma şeması.

### Day {#Day}
```
public static final PDF3DLightingScheme Day
```

"Day" aydınlatma şeması.

### Hard {#Hard}
```
public static final PDF3DLightingScheme Hard
```

Bu "Hard" aydınlatma şeması.

### Headlamp {#Headlamp}
```
public static final PDF3DLightingScheme Headlamp
```

Bu "Headlamp" aydınlatma şeması.

### Night {#Night}
```
public static final PDF3DLightingScheme Night
```

Bu "Night" aydınlatma şeması.

### None {#None}
```
public static final PDF3DLightingScheme None
```

Bu "None" aydınlatma şeması.

### Primary {#Primary}
```
public static final PDF3DLightingScheme Primary
```

Bu "Primary" aydınlatma şeması.

### Red {#Red}
```
public static final PDF3DLightingScheme Red
```

Bu "Red" aydınlatma şeması.

### White {#White}
```
public static final PDF3DLightingScheme White
```

Bu "White" aydınlatma şeması.

### PDF3DLightingScheme {#PDF3DLightingScheme-com.aspose.pdf.LightingSchemeType-}
Yeni bir {@code PDF3DLightingScheme} sınıfının örneğini başlatır.

### PDF3DLightingScheme {#PDF3DLightingScheme-java.lang.String-}
Yeni bir {@code PDF3DLightingScheme} sınıfının örneğini başlatır.

### getType {#getType--}
```
public LightingSchemeType getType()
```

Aydınlatma şeması türünü alır.

**Returns:**
LightingSchemeType öğesinin int değeri: Aydınlatma şeması türü. @see LightingSchemeType
