---
title: "MarginInfo"
linktitle: "MarginInfo"
second_title: "Aspose.PDF for Java API Referansı"
description: "Bu sınıf, farklı nesneler için bir kenar boşluğunu temsil eder."
type: docs
weight: 2860
url: /tr/java/com.aspose.pdf/margininfo/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.MarginInfo

**All Implemented Interfaces:**
com.aspose.ms.System.ICloneable

```
public final class MarginInfo extends Object implements com.aspose.ms.System.ICloneable
```

Bu sınıf, farklı nesneler için bir kenar boşluğunu temsil eder.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [MarginInfo](#MarginInfo--) | Yeni bir {@code MarginInfo} sınıfı örneği başlatır. |
| [MarginInfo](#MarginInfo-double-double-double-double-) | Rectangle yapıcısı. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [deepClone](#deepClone--) | Yeni bir {@code MarginInfo} nesnesini klonlar. |
| [getBottom](#getBottom--) | Alt kenar boşluğunu gösteren bir float değer alır. |
| [getLeft](#getLeft--) | Sol kenar boşluğunu gösteren bir float değer alır. |
| [getRight](#getRight--) | Sağ kenar boşluğunu gösteren bir float değer alır. |
| [getTop](#getTop--) | Üst kenar boşluğunu gösteren bir float değer alır. |
| [setBottom](#setBottom-double-) | Alt kenar boşluğunu gösteren bir float değer ayarlar. |
| [setLeft](#setLeft-double-) | Sol kenar boşluğunu gösteren bir float değer ayarlar. |
| [setRight](#setRight-double-) | Sağ kenar boşluğunu gösteren bir float değer ayarlar. |
| [setTop](#setTop-double-) | Üst kenar boşluğunu gösteren bir float değer ayarlar. |

### MarginInfo {#MarginInfo--}
```
public MarginInfo()
```

Yeni bir {@code MarginInfo} sınıfı örneği başlatır.

### MarginInfo {#MarginInfo-double-double-double-double-}
```
public MarginInfo(double left, double bottom, double right, double top)
```

Rectangle yapıcısı.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| sol |  | Sol kenar boşluğu. |
| alt |  | Alt kenar boşluğu |
| sağ |  | Sağ kenar boşluğu. |
| üst |  | Üst kenar boşluğu. |

### deepClone {#deepClone--}
```
public Object deepClone()
```

Yeni bir {@code MarginInfo} nesnesini klonlar.

**Returns:**
Yeni nesne.

### getBottom {#getBottom--}
```
public double getBottom()
```

Alt kenar boşluğunu gösteren bir float değer alır.

**Returns:**
double değer

### getLeft {#getLeft--}
```
public double getLeft()
```

Sol kenar boşluğunu gösteren bir float değer alır.

**Returns:**
double değer

### getRight {#getRight--}
```
public double getRight()
```

Sağ kenar boşluğunu gösteren bir float değer alır.

**Returns:**
double değer

### getTop {#getTop--}
```
public double getTop()
```

Üst kenar boşluğunu gösteren bir float değer alır.

**Returns:**
double değer

### setBottom {#setBottom-double-}
```
public void setBottom(double value)
```

Alt kenar boşluğunu gösteren bir float değer ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | double değer |

### setLeft {#setLeft-double-}
```
public void setLeft(double value)
```

Sol kenar boşluğunu gösteren bir float değer ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | double değer |

### setRight {#setRight-double-}
```
public void setRight(double value)
```

Sağ kenar boşluğunu gösteren bir float değer ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | double değer |

### setTop {#setTop-double-}
```
public void setTop(double value)
```

Üst kenar boşluğunu gösteren bir float değer ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | double değer |
