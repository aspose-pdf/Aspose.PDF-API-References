---
title: "PrintPaperSource"
linktitle: "PrintPaperSource"
second_title: "Aspose.PDF for Java API Referansı"
description: "Yazıcının kağıt aldığı kağıt tepsisini belirtir."
type: docs
weight: 120
url: /tr/java/com.aspose.pdf.printing/printpapersource/
---
**Inheritance:**
java.lang.Object, com.aspose.pdf.printing.PrintPaperSource

```
public class PrintPaperSource extends Object
```

Yazıcının kağıt aldığı kağıt tepsisini belirtir.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [PrintPaperSource](#PrintPaperSource--) | PaperSource sınıfının yeni bir örneğini başlatır. |
| [PrintPaperSource](#PrintPaperSource-java.lang.String-int-) | PaperSource sınıfının yeni bir örneğini başlatır. |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getKind](#getKind--) | Kağıt kaynağını alır. |
| [getRawKind](#getRawKind--) | PaperSourceKind değerlerinden birini veya özel bir değeri temsil eden tam sayıyı alır veya ayarlar. |
| [getSourceName](#getSourceName--) | Kağıt kaynağının adını alır veya ayarlar. |
| [setRawKind](#setRawKind-int-) | PaperSourceKind değerlerinden birini veya özel bir değeri temsil eden tam sayıyı alır veya ayarlar. |
| [setSourceName](#setSourceName-java.lang.String-) | Kağıt kaynağının adını alır veya ayarlar. |
| [toString](#toString--) | Altyapı. PrintPaperSource hakkında bilgi sağlar, dize biçiminde. |

### PrintPaperSource {#PrintPaperSource--}
```
public PrintPaperSource()
```

PaperSource sınıfının yeni bir örneğini başlatır.

### PrintPaperSource {#PrintPaperSource-java.lang.String-int-}
PaperSource sınıfının yeni bir örneğini başlatır.

### getKind {#getKind--}
```
public int getKind()
```

Kağıt kaynağını alır.

**Returns:**
int değer

### getRawKind {#getRawKind--}
```
public int getRawKind()
```

PaperSourceKind değerlerinden birini veya özel bir değeri temsil eden tam sayıyı alır veya ayarlar.

**Returns:**
int değer

### getSourceName {#getSourceName--}
```
public String getSourceName()
```

Kağıt kaynağının adını alır veya ayarlar.

**Returns:**
String değeri

### setRawKind {#setRawKind-int-}
```
public void setRawKind(int value)
```

PaperSourceKind değerlerinden birini veya özel bir değeri temsil eden tam sayıyı alır veya ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer |  | int değer |

### setSourceName {#setSourceName-java.lang.String-}
Kağıt kaynağının adını alır veya ayarlar.

### toString {#toString--}
```
public String toString()
```

Altyapı. PrintPaperSource hakkında bilgi sağlar, dize biçiminde.

**Returns:**
String değeri
