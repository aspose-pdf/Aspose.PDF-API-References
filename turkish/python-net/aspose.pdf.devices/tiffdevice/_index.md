---
title: "TiffDevice"
second_title: "Aspose.PDF Python için .NET API Referansı"
description: "Bu sınıf, PDF belge sayfalarını tek bir tiff görüntüsüne sayfa sayfa kaydetmeye yardımcı olur."
type: docs
weight: 150
url: /tr/python-net/aspose.pdf.devices/tiffdevice/
---

## TiffDevice class

Bu sınıf, PDF belge sayfalarını tek bir tiff görüntüsüne sayfa sayfa kaydetmeye yardımcı olur.

TiffDevice türü aşağıdaki üyeleri gösterir:
## Yapıcılar
| Ad | Açıklama |
| :- | :- |
| TiffDevice(resolution) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(resolution, settings) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(resolution, settings, converter) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(settings) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(settings, converter) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice() | Varsayılan ayarlarla [TiffDevice](/pdf/python-net/aspose.pdf.devices/tiffdevice/) sınıfının yeni bir örneğini başlatır. |
| TiffDevice(width, height, resolution, settings) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(width, height, resolution, settings, converter) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(page_size, resolution, settings) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(page_size, resolution, settings, converter) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(width, height, resolution) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(page_size, resolution) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(width, height, settings) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(width, height, settings, converter) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(page_size, settings, converter) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(page_size, settings) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(width, height) | TiffDevice sınıfının yeni bir örneğini başlatır |
| TiffDevice(page_size) | TiffDevice sınıfının yeni bir örneğini başlatır |
## Özellikler
| Ad | Açıklama |
| :- | :- |
| rendering_options | Renderleme seçeneklerini alır veya ayarlar. |
| form_presentation_mode | Form sunum modunu alır veya ayarlar. |
| settings | pdf'yi tiff görüntüsüne eşlemek için ayarları alır. |
| resolution | Görüntü çözünürlüğünü alır. |
| genişlik | Görüntü çıkış genişliğini alır. |
| yükseklik | Görüntü çıkış yüksekliğini alır. |
## Yöntemler
| Ad | Açıklama |
| :- | :- |
| process(document, from_page, to_page, output) | Belirli belge sayfalarını tiff'e dönüştürür ve çıktı akışına kaydeder. |
| process(document, output) | Belirli belge sayfalarını tiff'e dönüştürür ve çıktı akışına kaydeder. |
| process(document, output_file_name) | Belirli belge sayfalarını tiff'e dönüştürür ve çıktı akışına kaydeder. |
| process(document, from_page, to_page, output_file_name) | Belirli belge sayfalarını tiff'e dönüştürür ve çıktı akışına kaydeder. |
| binarize_bradley(input_image_stream, output_image_stream, threshold) | Girdi akışı için Bradley ikileştirmesi yapar. |

### Ayrıca Bakınız

* namespace [aspose.pdf.devices](/pdf/python-net/aspose.pdf.devices/)
* assembly [Aspose.PDF](/pdf/python-net/)

