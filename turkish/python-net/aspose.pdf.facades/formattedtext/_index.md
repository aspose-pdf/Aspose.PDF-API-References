---
title: "FormattedText"
second_title: "Aspose.PDF Python için .NET API Referansı"
description: "Biçimlendirilmiş metni temsil eden sınıf. Metin ve onun rengi, boyutu, stili hakkında bilgi içerir."
type: docs
weight: 130
url: /tr/python-net/aspose.pdf.facades/formattedtext/
---

## FormattedText class

Biçimlendirilmiş metni temsil eden sınıf. Metin ve onun rengi, boyutu, stili hakkında bilgi içerir.

FormattedText türü aşağıdaki üyeleri sunar:
## Yapıcılar
| Ad | Açıklama |
| :- | :- |
| FormattedText() | FormattedText'i başlatır. |
| FormattedText(text) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, font_color, font_style, encoding_type, embedded, text_size) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, font_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, color, text_font, text_encoding, embedded, text_size) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, back_color, text_font, encoding, embedded, text_size) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, back_color, text_font, text_encoding, embedded, text_size, line_spacing) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, back_color, font_name, text_encoding, embedded, font_size) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, back_color) | FormattedText sınıfının yeni bir örneğini başlatır |
| FormattedText(text, text_color, font_name, text_encoding, embedded, font_size) | FormattedText sınıfının yeni bir örneğini başlatır |
## Özellikler
| Ad | Açıklama |
| :- | :- |
| text_height | Metnin yüksekliğini alır. |
| text_width | Metnin genişliğini alır. |
## Yöntemler
| Ad | Açıklama |
| :- | :- |
| add_new_line_text(new_line_text) | FormattedText nesnesine yeni bir satır ekler ve newLineText'i bir sonraki satırın metnine ayarlar. |
| add_new_line_text(new_line_text, line_spacing) | FormattedText nesnesine yeni bir satır ekler ve newLineText'i bir sonraki satırın metnine ayarlar. |
| is_cjk() | Metnin CJK (Çince, Japonca veya Korece) olup olmadığını kontrol eder. |
| set_cjk_font_style() | CJK (Çince, Japonca, veya Korece) fontu için FormattedText yazı tipi stilini değiştirir. |

### Ayrıca Bakınız

* namespace [aspose.pdf.facades](/pdf/python-net/aspose.pdf.facades/)
* assembly [Aspose.PDF](/pdf/python-net/)

